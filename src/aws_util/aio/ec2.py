"""Native async EC2 utilities using :mod:`aws_util.aio._engine`."""

from __future__ import annotations

import asyncio
import base64
from typing import Any

from aws_util.aio._engine import async_client
from aws_util.ec2 import (
    AcceptAddressTransferResult,
    AcceptCapacityReservationBillingOwnershipResult,
    AcceptReservedInstancesExchangeQuoteResult,
    AcceptTransitGatewayMulticastDomainAssociationsResult,
    AcceptTransitGatewayPeeringAttachmentResult,
    AcceptTransitGatewayVpcAttachmentResult,
    AcceptVpcEndpointConnectionsResult,
    AcceptVpcPeeringConnectionResult,
    AdvertiseByoipCidrResult,
    AllocateAddressResult,
    AllocateHostsResult,
    AllocateIpamPoolCidrResult,
    ApplySecurityGroupsToClientVpnTargetNetworkResult,
    AssignIpv6AddressesResult,
    AssignPrivateIpAddressesResult,
    AssignPrivateNatGatewayAddressResult,
    AssociateAddressResult,
    AssociateCapacityReservationBillingOwnerResult,
    AssociateClientVpnTargetNetworkResult,
    AssociateEnclaveCertificateIamRoleResult,
    AssociateIamInstanceProfileResult,
    AssociateInstanceEventWindowResult,
    AssociateIpamByoasnResult,
    AssociateIpamResourceDiscoveryResult,
    AssociateNatGatewayAddressResult,
    AssociateRouteServerResult,
    AssociateRouteTableResult,
    AssociateSecurityGroupVpcResult,
    AssociateSubnetCidrBlockResult,
    AssociateTransitGatewayMulticastDomainResult,
    AssociateTransitGatewayPolicyTableResult,
    AssociateTransitGatewayRouteTableResult,
    AssociateTrunkInterfaceResult,
    AssociateVpcCidrBlockResult,
    AttachClassicLinkVpcResult,
    AttachNetworkInterfaceResult,
    AttachVerifiedAccessTrustProviderResult,
    AttachVolumeResult,
    AttachVpnGatewayResult,
    AuthorizeClientVpnIngressResult,
    AuthorizeSecurityGroupEgressResult,
    AuthorizeSecurityGroupIngressResult,
    BundleInstanceResult,
    CancelBundleTaskResult,
    CancelCapacityReservationFleetsResult,
    CancelCapacityReservationResult,
    CancelDeclarativePoliciesReportResult,
    CancelImageLaunchPermissionResult,
    CancelImportTaskResult,
    CancelReservedInstancesListingResult,
    CancelSpotFleetRequestsResult,
    CancelSpotInstanceRequestsResult,
    ConfirmProductInstanceResult,
    CopyFpgaImageResult,
    CopyImageResult,
    CopySnapshotResult,
    CopyVolumesResult,
    CreateCapacityManagerDataExportResult,
    CreateCapacityReservationBySplittingResult,
    CreateCapacityReservationFleetResult,
    CreateCapacityReservationResult,
    CreateCarrierGatewayResult,
    CreateClientVpnEndpointResult,
    CreateClientVpnRouteResult,
    CreateCoipCidrResult,
    CreateCoipPoolResult,
    CreateCustomerGatewayResult,
    CreateDefaultSubnetResult,
    CreateDefaultVpcResult,
    CreateDelegateMacVolumeOwnershipTaskResult,
    CreateDhcpOptionsResult,
    CreateEgressOnlyInternetGatewayResult,
    CreateFleetResult,
    CreateFlowLogsResult,
    CreateFpgaImageResult,
    CreateImageUsageReportResult,
    CreateInstanceConnectEndpointResult,
    CreateInstanceEventWindowResult,
    CreateInstanceExportTaskResult,
    CreateInternetGatewayResult,
    CreateIpamExternalResourceVerificationTokenResult,
    CreateIpamPoolResult,
    CreateIpamPrefixListResolverResult,
    CreateIpamPrefixListResolverTargetResult,
    CreateIpamResourceDiscoveryResult,
    CreateIpamResult,
    CreateIpamScopeResult,
    CreateKeyPairResult,
    CreateLaunchTemplateResult,
    CreateLaunchTemplateVersionResult,
    CreateLocalGatewayRouteResult,
    CreateLocalGatewayRouteTableResult,
    CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult,
    CreateLocalGatewayRouteTableVpcAssociationResult,
    CreateLocalGatewayVirtualInterfaceGroupResult,
    CreateLocalGatewayVirtualInterfaceResult,
    CreateMacSystemIntegrityProtectionModificationTaskResult,
    CreateManagedPrefixListResult,
    CreateNatGatewayResult,
    CreateNetworkAclResult,
    CreateNetworkInsightsAccessScopeResult,
    CreateNetworkInsightsPathResult,
    CreateNetworkInterfacePermissionResult,
    CreateNetworkInterfaceResult,
    CreatePlacementGroupResult,
    CreatePublicIpv4PoolResult,
    CreateReplaceRootVolumeTaskResult,
    CreateReservedInstancesListingResult,
    CreateRestoreImageTaskResult,
    CreateRouteResult,
    CreateRouteServerEndpointResult,
    CreateRouteServerPeerResult,
    CreateRouteServerResult,
    CreateRouteTableResult,
    CreateSecurityGroupResult,
    CreateSnapshotResult,
    CreateSnapshotsResult,
    CreateSpotDatafeedSubscriptionResult,
    CreateStoreImageTaskResult,
    CreateSubnetCidrReservationResult,
    CreateSubnetResult,
    CreateTrafficMirrorFilterResult,
    CreateTrafficMirrorFilterRuleResult,
    CreateTrafficMirrorSessionResult,
    CreateTrafficMirrorTargetResult,
    CreateTransitGatewayConnectPeerResult,
    CreateTransitGatewayConnectResult,
    CreateTransitGatewayMulticastDomainResult,
    CreateTransitGatewayPeeringAttachmentResult,
    CreateTransitGatewayPolicyTableResult,
    CreateTransitGatewayPrefixListReferenceResult,
    CreateTransitGatewayResult,
    CreateTransitGatewayRouteResult,
    CreateTransitGatewayRouteTableAnnouncementResult,
    CreateTransitGatewayRouteTableResult,
    CreateTransitGatewayVpcAttachmentResult,
    CreateVerifiedAccessEndpointResult,
    CreateVerifiedAccessGroupResult,
    CreateVerifiedAccessInstanceResult,
    CreateVerifiedAccessTrustProviderResult,
    CreateVolumeResult,
    CreateVpcBlockPublicAccessExclusionResult,
    CreateVpcEndpointConnectionNotificationResult,
    CreateVpcEndpointResult,
    CreateVpcEndpointServiceConfigurationResult,
    CreateVpcPeeringConnectionResult,
    CreateVpcResult,
    CreateVpnConnectionResult,
    CreateVpnGatewayResult,
    DeleteCapacityManagerDataExportResult,
    DeleteCarrierGatewayResult,
    DeleteClientVpnEndpointResult,
    DeleteClientVpnRouteResult,
    DeleteCoipCidrResult,
    DeleteCoipPoolResult,
    DeleteEgressOnlyInternetGatewayResult,
    DeleteFleetsResult,
    DeleteFlowLogsResult,
    DeleteFpgaImageResult,
    DeleteImageUsageReportResult,
    DeleteInstanceConnectEndpointResult,
    DeleteInstanceEventWindowResult,
    DeleteIpamExternalResourceVerificationTokenResult,
    DeleteIpamPoolResult,
    DeleteIpamPrefixListResolverResult,
    DeleteIpamPrefixListResolverTargetResult,
    DeleteIpamResourceDiscoveryResult,
    DeleteIpamResult,
    DeleteIpamScopeResult,
    DeleteKeyPairResult,
    DeleteLaunchTemplateResult,
    DeleteLaunchTemplateVersionsResult,
    DeleteLocalGatewayRouteResult,
    DeleteLocalGatewayRouteTableResult,
    DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult,
    DeleteLocalGatewayRouteTableVpcAssociationResult,
    DeleteLocalGatewayVirtualInterfaceGroupResult,
    DeleteLocalGatewayVirtualInterfaceResult,
    DeleteManagedPrefixListResult,
    DeleteNatGatewayResult,
    DeleteNetworkInsightsAccessScopeAnalysisResult,
    DeleteNetworkInsightsAccessScopeResult,
    DeleteNetworkInsightsAnalysisResult,
    DeleteNetworkInsightsPathResult,
    DeleteNetworkInterfacePermissionResult,
    DeletePublicIpv4PoolResult,
    DeleteQueuedReservedInstancesResult,
    DeleteRouteServerEndpointResult,
    DeleteRouteServerPeerResult,
    DeleteRouteServerResult,
    DeleteSecurityGroupResult,
    DeleteSubnetCidrReservationResult,
    DeleteTrafficMirrorFilterResult,
    DeleteTrafficMirrorFilterRuleResult,
    DeleteTrafficMirrorSessionResult,
    DeleteTrafficMirrorTargetResult,
    DeleteTransitGatewayConnectPeerResult,
    DeleteTransitGatewayConnectResult,
    DeleteTransitGatewayMulticastDomainResult,
    DeleteTransitGatewayPeeringAttachmentResult,
    DeleteTransitGatewayPolicyTableResult,
    DeleteTransitGatewayPrefixListReferenceResult,
    DeleteTransitGatewayResult,
    DeleteTransitGatewayRouteResult,
    DeleteTransitGatewayRouteTableAnnouncementResult,
    DeleteTransitGatewayRouteTableResult,
    DeleteTransitGatewayVpcAttachmentResult,
    DeleteVerifiedAccessEndpointResult,
    DeleteVerifiedAccessGroupResult,
    DeleteVerifiedAccessInstanceResult,
    DeleteVerifiedAccessTrustProviderResult,
    DeleteVpcBlockPublicAccessExclusionResult,
    DeleteVpcEndpointConnectionNotificationsResult,
    DeleteVpcEndpointServiceConfigurationsResult,
    DeleteVpcEndpointsResult,
    DeleteVpcPeeringConnectionResult,
    DeprovisionByoipCidrResult,
    DeprovisionIpamByoasnResult,
    DeprovisionIpamPoolCidrResult,
    DeprovisionPublicIpv4PoolCidrResult,
    DeregisterImageResult,
    DeregisterInstanceEventNotificationAttributesResult,
    DeregisterTransitGatewayMulticastGroupMembersResult,
    DeregisterTransitGatewayMulticastGroupSourcesResult,
    DescribeAccountAttributesResult,
    DescribeAddressesAttributeResult,
    DescribeAddressesResult,
    DescribeAddressTransfersResult,
    DescribeAggregateIdFormatResult,
    DescribeAvailabilityZonesResult,
    DescribeAwsNetworkPerformanceMetricSubscriptionsResult,
    DescribeBundleTasksResult,
    DescribeByoipCidrsResult,
    DescribeCapacityBlockExtensionHistoryResult,
    DescribeCapacityBlockExtensionOfferingsResult,
    DescribeCapacityBlockOfferingsResult,
    DescribeCapacityBlocksResult,
    DescribeCapacityBlockStatusResult,
    DescribeCapacityManagerDataExportsResult,
    DescribeCapacityReservationBillingRequestsResult,
    DescribeCapacityReservationFleetsResult,
    DescribeCapacityReservationsResult,
    DescribeCapacityReservationTopologyResult,
    DescribeCarrierGatewaysResult,
    DescribeClassicLinkInstancesResult,
    DescribeClientVpnAuthorizationRulesResult,
    DescribeClientVpnConnectionsResult,
    DescribeClientVpnEndpointsResult,
    DescribeClientVpnRoutesResult,
    DescribeClientVpnTargetNetworksResult,
    DescribeCoipPoolsResult,
    DescribeConversionTasksResult,
    DescribeCustomerGatewaysResult,
    DescribeDeclarativePoliciesReportsResult,
    DescribeDhcpOptionsResult,
    DescribeEgressOnlyInternetGatewaysResult,
    DescribeElasticGpusResult,
    DescribeExportImageTasksResult,
    DescribeExportTasksResult,
    DescribeFastLaunchImagesResult,
    DescribeFastSnapshotRestoresResult,
    DescribeFleetHistoryResult,
    DescribeFleetInstancesResult,
    DescribeFleetsResult,
    DescribeFlowLogsResult,
    DescribeFpgaImageAttributeResult,
    DescribeFpgaImagesResult,
    DescribeHostReservationOfferingsResult,
    DescribeHostReservationsResult,
    DescribeHostsResult,
    DescribeIamInstanceProfileAssociationsResult,
    DescribeIdentityIdFormatResult,
    DescribeIdFormatResult,
    DescribeImageAttributeResult,
    DescribeImageReferencesResult,
    DescribeImageUsageReportEntriesResult,
    DescribeImageUsageReportsResult,
    DescribeImportImageTasksResult,
    DescribeImportSnapshotTasksResult,
    DescribeInstanceAttributeResult,
    DescribeInstanceConnectEndpointsResult,
    DescribeInstanceCreditSpecificationsResult,
    DescribeInstanceEventNotificationAttributesResult,
    DescribeInstanceEventWindowsResult,
    DescribeInstanceImageMetadataResult,
    DescribeInstanceStatusResult,
    DescribeInstanceTopologyResult,
    DescribeInstanceTypeOfferingsResult,
    DescribeInstanceTypesResult,
    DescribeInternetGatewaysResult,
    DescribeIpamByoasnResult,
    DescribeIpamExternalResourceVerificationTokensResult,
    DescribeIpamPoolsResult,
    DescribeIpamPrefixListResolversResult,
    DescribeIpamPrefixListResolverTargetsResult,
    DescribeIpamResourceDiscoveriesResult,
    DescribeIpamResourceDiscoveryAssociationsResult,
    DescribeIpamScopesResult,
    DescribeIpamsResult,
    DescribeIpv6PoolsResult,
    DescribeKeyPairsResult,
    DescribeLaunchTemplatesResult,
    DescribeLaunchTemplateVersionsResult,
    DescribeLocalGatewayRouteTablesResult,
    DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult,
    DescribeLocalGatewayRouteTableVpcAssociationsResult,
    DescribeLocalGatewaysResult,
    DescribeLocalGatewayVirtualInterfaceGroupsResult,
    DescribeLocalGatewayVirtualInterfacesResult,
    DescribeLockedSnapshotsResult,
    DescribeMacHostsResult,
    DescribeMacModificationTasksResult,
    DescribeManagedPrefixListsResult,
    DescribeMovingAddressesResult,
    DescribeNatGatewaysResult,
    DescribeNetworkAclsResult,
    DescribeNetworkInsightsAccessScopeAnalysesResult,
    DescribeNetworkInsightsAccessScopesResult,
    DescribeNetworkInsightsAnalysesResult,
    DescribeNetworkInsightsPathsResult,
    DescribeNetworkInterfaceAttributeResult,
    DescribeNetworkInterfacePermissionsResult,
    DescribeNetworkInterfacesResult,
    DescribeOutpostLagsResult,
    DescribePlacementGroupsResult,
    DescribePrefixListsResult,
    DescribePrincipalIdFormatResult,
    DescribePublicIpv4PoolsResult,
    DescribeRegionsResult,
    DescribeReplaceRootVolumeTasksResult,
    DescribeReservedInstancesListingsResult,
    DescribeReservedInstancesModificationsResult,
    DescribeReservedInstancesOfferingsResult,
    DescribeReservedInstancesResult,
    DescribeRouteServerEndpointsResult,
    DescribeRouteServerPeersResult,
    DescribeRouteServersResult,
    DescribeRouteTablesResult,
    DescribeScheduledInstanceAvailabilityResult,
    DescribeScheduledInstancesResult,
    DescribeSecurityGroupReferencesResult,
    DescribeSecurityGroupRulesResult,
    DescribeSecurityGroupVpcAssociationsResult,
    DescribeServiceLinkVirtualInterfacesResult,
    DescribeSnapshotAttributeResult,
    DescribeSnapshotsResult,
    DescribeSnapshotTierStatusResult,
    DescribeSpotDatafeedSubscriptionResult,
    DescribeSpotFleetInstancesResult,
    DescribeSpotFleetRequestHistoryResult,
    DescribeSpotFleetRequestsResult,
    DescribeSpotInstanceRequestsResult,
    DescribeSpotPriceHistoryResult,
    DescribeStaleSecurityGroupsResult,
    DescribeStoreImageTasksResult,
    DescribeSubnetsResult,
    DescribeTagsResult,
    DescribeTrafficMirrorFilterRulesResult,
    DescribeTrafficMirrorFiltersResult,
    DescribeTrafficMirrorSessionsResult,
    DescribeTrafficMirrorTargetsResult,
    DescribeTransitGatewayAttachmentsResult,
    DescribeTransitGatewayConnectPeersResult,
    DescribeTransitGatewayConnectsResult,
    DescribeTransitGatewayMulticastDomainsResult,
    DescribeTransitGatewayPeeringAttachmentsResult,
    DescribeTransitGatewayPolicyTablesResult,
    DescribeTransitGatewayRouteTableAnnouncementsResult,
    DescribeTransitGatewayRouteTablesResult,
    DescribeTransitGatewaysResult,
    DescribeTransitGatewayVpcAttachmentsResult,
    DescribeTrunkInterfaceAssociationsResult,
    DescribeVerifiedAccessEndpointsResult,
    DescribeVerifiedAccessGroupsResult,
    DescribeVerifiedAccessInstanceLoggingConfigurationsResult,
    DescribeVerifiedAccessInstancesResult,
    DescribeVerifiedAccessTrustProvidersResult,
    DescribeVolumeAttributeResult,
    DescribeVolumesModificationsResult,
    DescribeVolumesResult,
    DescribeVolumeStatusResult,
    DescribeVpcAttributeResult,
    DescribeVpcBlockPublicAccessExclusionsResult,
    DescribeVpcBlockPublicAccessOptionsResult,
    DescribeVpcClassicLinkDnsSupportResult,
    DescribeVpcClassicLinkResult,
    DescribeVpcEndpointAssociationsResult,
    DescribeVpcEndpointConnectionNotificationsResult,
    DescribeVpcEndpointConnectionsResult,
    DescribeVpcEndpointServiceConfigurationsResult,
    DescribeVpcEndpointServicePermissionsResult,
    DescribeVpcEndpointServicesResult,
    DescribeVpcEndpointsResult,
    DescribeVpcPeeringConnectionsResult,
    DescribeVpcsResult,
    DescribeVpnConnectionsResult,
    DescribeVpnGatewaysResult,
    DetachClassicLinkVpcResult,
    DetachVerifiedAccessTrustProviderResult,
    DetachVolumeResult,
    DisableAddressTransferResult,
    DisableAllowedImagesSettingsResult,
    DisableAwsNetworkPerformanceMetricSubscriptionResult,
    DisableCapacityManagerResult,
    DisableEbsEncryptionByDefaultResult,
    DisableFastLaunchResult,
    DisableFastSnapshotRestoresResult,
    DisableImageBlockPublicAccessResult,
    DisableImageDeprecationResult,
    DisableImageDeregistrationProtectionResult,
    DisableImageResult,
    DisableIpamOrganizationAdminAccountResult,
    DisableRouteServerPropagationResult,
    DisableSerialConsoleAccessResult,
    DisableSnapshotBlockPublicAccessResult,
    DisableTransitGatewayRouteTablePropagationResult,
    DisableVpcClassicLinkDnsSupportResult,
    DisableVpcClassicLinkResult,
    DisassociateCapacityReservationBillingOwnerResult,
    DisassociateClientVpnTargetNetworkResult,
    DisassociateEnclaveCertificateIamRoleResult,
    DisassociateIamInstanceProfileResult,
    DisassociateInstanceEventWindowResult,
    DisassociateIpamByoasnResult,
    DisassociateIpamResourceDiscoveryResult,
    DisassociateNatGatewayAddressResult,
    DisassociateRouteServerResult,
    DisassociateSecurityGroupVpcResult,
    DisassociateSubnetCidrBlockResult,
    DisassociateTransitGatewayMulticastDomainResult,
    DisassociateTransitGatewayPolicyTableResult,
    DisassociateTransitGatewayRouteTableResult,
    DisassociateTrunkInterfaceResult,
    DisassociateVpcCidrBlockResult,
    EC2Image,
    EC2Instance,
    EnableAddressTransferResult,
    EnableAllowedImagesSettingsResult,
    EnableAwsNetworkPerformanceMetricSubscriptionResult,
    EnableCapacityManagerResult,
    EnableEbsEncryptionByDefaultResult,
    EnableFastLaunchResult,
    EnableFastSnapshotRestoresResult,
    EnableImageBlockPublicAccessResult,
    EnableImageDeprecationResult,
    EnableImageDeregistrationProtectionResult,
    EnableImageResult,
    EnableIpamOrganizationAdminAccountResult,
    EnableReachabilityAnalyzerOrganizationSharingResult,
    EnableRouteServerPropagationResult,
    EnableSerialConsoleAccessResult,
    EnableSnapshotBlockPublicAccessResult,
    EnableTransitGatewayRouteTablePropagationResult,
    EnableVpcClassicLinkDnsSupportResult,
    EnableVpcClassicLinkResult,
    ExportClientVpnClientCertificateRevocationListResult,
    ExportClientVpnClientConfigurationResult,
    ExportImageResult,
    ExportTransitGatewayRoutesResult,
    ExportVerifiedAccessInstanceClientConfigurationResult,
    GetActiveVpnTunnelStatusResult,
    GetAllowedImagesSettingsResult,
    GetAssociatedEnclaveCertificateIamRolesResult,
    GetAssociatedIpv6PoolCidrsResult,
    GetAwsNetworkPerformanceDataResult,
    GetCapacityManagerAttributesResult,
    GetCapacityManagerMetricDataResult,
    GetCapacityManagerMetricDimensionsResult,
    GetCapacityReservationUsageResult,
    GetCoipPoolUsageResult,
    GetConsoleOutputResult,
    GetConsoleScreenshotResult,
    GetDeclarativePoliciesReportSummaryResult,
    GetDefaultCreditSpecificationResult,
    GetEbsDefaultKmsKeyIdResult,
    GetEbsEncryptionByDefaultResult,
    GetFlowLogsIntegrationTemplateResult,
    GetGroupsForCapacityReservationResult,
    GetHostReservationPurchasePreviewResult,
    GetImageAncestryResult,
    GetImageBlockPublicAccessStateResult,
    GetInstanceMetadataDefaultsResult,
    GetInstanceTpmEkPubResult,
    GetInstanceTypesFromInstanceRequirementsResult,
    GetInstanceUefiDataResult,
    GetIpamAddressHistoryResult,
    GetIpamDiscoveredAccountsResult,
    GetIpamDiscoveredPublicAddressesResult,
    GetIpamDiscoveredResourceCidrsResult,
    GetIpamPoolAllocationsResult,
    GetIpamPoolCidrsResult,
    GetIpamPrefixListResolverRulesResult,
    GetIpamPrefixListResolverVersionEntriesResult,
    GetIpamPrefixListResolverVersionsResult,
    GetIpamResourceCidrsResult,
    GetLaunchTemplateDataResult,
    GetManagedPrefixListAssociationsResult,
    GetManagedPrefixListEntriesResult,
    GetNetworkInsightsAccessScopeAnalysisFindingsResult,
    GetNetworkInsightsAccessScopeContentResult,
    GetPasswordDataResult,
    GetReservedInstancesExchangeQuoteResult,
    GetRouteServerAssociationsResult,
    GetRouteServerPropagationsResult,
    GetRouteServerRoutingDatabaseResult,
    GetSecurityGroupsForVpcResult,
    GetSerialConsoleAccessStatusResult,
    GetSnapshotBlockPublicAccessStateResult,
    GetSpotPlacementScoresResult,
    GetSubnetCidrReservationsResult,
    GetTransitGatewayAttachmentPropagationsResult,
    GetTransitGatewayMulticastDomainAssociationsResult,
    GetTransitGatewayPolicyTableAssociationsResult,
    GetTransitGatewayPolicyTableEntriesResult,
    GetTransitGatewayPrefixListReferencesResult,
    GetTransitGatewayRouteTableAssociationsResult,
    GetTransitGatewayRouteTablePropagationsResult,
    GetVerifiedAccessEndpointPolicyResult,
    GetVerifiedAccessEndpointTargetsResult,
    GetVerifiedAccessGroupPolicyResult,
    GetVpnConnectionDeviceSampleConfigurationResult,
    GetVpnConnectionDeviceTypesResult,
    GetVpnTunnelReplacementStatusResult,
    ImportClientVpnClientCertificateRevocationListResult,
    ImportImageResult,
    ImportInstanceResult,
    ImportKeyPairResult,
    ImportSnapshotResult,
    ImportVolumeResult,
    ListImagesInRecycleBinResult,
    ListSnapshotsInRecycleBinResult,
    LockSnapshotResult,
    ModifyAddressAttributeResult,
    ModifyAvailabilityZoneGroupResult,
    ModifyCapacityReservationFleetResult,
    ModifyCapacityReservationResult,
    ModifyClientVpnEndpointResult,
    ModifyDefaultCreditSpecificationResult,
    ModifyEbsDefaultKmsKeyIdResult,
    ModifyFleetResult,
    ModifyFpgaImageAttributeResult,
    ModifyHostsResult,
    ModifyInstanceCapacityReservationAttributesResult,
    ModifyInstanceConnectEndpointResult,
    ModifyInstanceCpuOptionsResult,
    ModifyInstanceCreditSpecificationResult,
    ModifyInstanceEventStartTimeResult,
    ModifyInstanceEventWindowResult,
    ModifyInstanceMaintenanceOptionsResult,
    ModifyInstanceMetadataDefaultsResult,
    ModifyInstanceMetadataOptionsResult,
    ModifyInstanceNetworkPerformanceOptionsResult,
    ModifyInstancePlacementResult,
    ModifyIpamPoolResult,
    ModifyIpamPrefixListResolverResult,
    ModifyIpamPrefixListResolverTargetResult,
    ModifyIpamResourceCidrResult,
    ModifyIpamResourceDiscoveryResult,
    ModifyIpamResult,
    ModifyIpamScopeResult,
    ModifyLaunchTemplateResult,
    ModifyLocalGatewayRouteResult,
    ModifyManagedPrefixListResult,
    ModifyPrivateDnsNameOptionsResult,
    ModifyPublicIpDnsNameOptionsResult,
    ModifyReservedInstancesResult,
    ModifyRouteServerResult,
    ModifySecurityGroupRulesResult,
    ModifySnapshotTierResult,
    ModifySpotFleetRequestResult,
    ModifyTrafficMirrorFilterNetworkServicesResult,
    ModifyTrafficMirrorFilterRuleResult,
    ModifyTrafficMirrorSessionResult,
    ModifyTransitGatewayPrefixListReferenceResult,
    ModifyTransitGatewayResult,
    ModifyTransitGatewayVpcAttachmentResult,
    ModifyVerifiedAccessEndpointPolicyResult,
    ModifyVerifiedAccessEndpointResult,
    ModifyVerifiedAccessGroupPolicyResult,
    ModifyVerifiedAccessGroupResult,
    ModifyVerifiedAccessInstanceLoggingConfigurationResult,
    ModifyVerifiedAccessInstanceResult,
    ModifyVerifiedAccessTrustProviderResult,
    ModifyVolumeResult,
    ModifyVpcBlockPublicAccessExclusionResult,
    ModifyVpcBlockPublicAccessOptionsResult,
    ModifyVpcEndpointConnectionNotificationResult,
    ModifyVpcEndpointResult,
    ModifyVpcEndpointServiceConfigurationResult,
    ModifyVpcEndpointServicePayerResponsibilityResult,
    ModifyVpcEndpointServicePermissionsResult,
    ModifyVpcPeeringConnectionOptionsResult,
    ModifyVpcTenancyResult,
    ModifyVpnConnectionOptionsResult,
    ModifyVpnConnectionResult,
    ModifyVpnTunnelCertificateResult,
    ModifyVpnTunnelOptionsResult,
    MonitorInstancesResult,
    MoveAddressToVpcResult,
    MoveByoipCidrToIpamResult,
    MoveCapacityReservationInstancesResult,
    ProvisionByoipCidrResult,
    ProvisionIpamByoasnResult,
    ProvisionIpamPoolCidrResult,
    ProvisionPublicIpv4PoolCidrResult,
    PurchaseCapacityBlockExtensionResult,
    PurchaseCapacityBlockResult,
    PurchaseHostReservationResult,
    PurchaseReservedInstancesOfferingResult,
    PurchaseScheduledInstancesResult,
    RegisterImageResult,
    RegisterInstanceEventNotificationAttributesResult,
    RegisterTransitGatewayMulticastGroupMembersResult,
    RegisterTransitGatewayMulticastGroupSourcesResult,
    RejectCapacityReservationBillingOwnershipResult,
    RejectTransitGatewayMulticastDomainAssociationsResult,
    RejectTransitGatewayPeeringAttachmentResult,
    RejectTransitGatewayVpcAttachmentResult,
    RejectVpcEndpointConnectionsResult,
    RejectVpcPeeringConnectionResult,
    ReleaseHostsResult,
    ReleaseIpamPoolAllocationResult,
    ReplaceIamInstanceProfileAssociationResult,
    ReplaceImageCriteriaInAllowedImagesSettingsResult,
    ReplaceNetworkAclAssociationResult,
    ReplaceRouteTableAssociationResult,
    ReplaceTransitGatewayRouteResult,
    ReplaceVpnTunnelResult,
    RequestSpotFleetResult,
    RequestSpotInstancesResult,
    ResetAddressAttributeResult,
    ResetEbsDefaultKmsKeyIdResult,
    ResetFpgaImageAttributeResult,
    RestoreAddressToClassicResult,
    RestoreImageFromRecycleBinResult,
    RestoreManagedPrefixListVersionResult,
    RestoreSnapshotFromRecycleBinResult,
    RestoreSnapshotTierResult,
    RevokeClientVpnIngressResult,
    RevokeSecurityGroupEgressResult,
    RevokeSecurityGroupIngressResult,
    RunInstancesResult,
    RunScheduledInstancesResult,
    SearchLocalGatewayRoutesResult,
    SearchTransitGatewayMulticastGroupsResult,
    SearchTransitGatewayRoutesResult,
    SecurityGroup,
    StartDeclarativePoliciesReportResult,
    StartNetworkInsightsAccessScopeAnalysisResult,
    StartNetworkInsightsAnalysisResult,
    StartVpcEndpointServicePrivateDnsVerificationResult,
    TerminateClientVpnConnectionsResult,
    UnassignIpv6AddressesResult,
    UnassignPrivateNatGatewayAddressResult,
    UnlockSnapshotResult,
    UnmonitorInstancesResult,
    UpdateCapacityManagerOrganizationsAccessResult,
    UpdateSecurityGroupRuleDescriptionsEgressResult,
    UpdateSecurityGroupRuleDescriptionsIngressResult,
    WithdrawByoipCidrResult,
    _parse_instance,
)
from aws_util.exceptions import AwsServiceError, wrap_aws_error

__all__ = [
    "AcceptAddressTransferResult",
    "AcceptCapacityReservationBillingOwnershipResult",
    "AcceptReservedInstancesExchangeQuoteResult",
    "AcceptTransitGatewayMulticastDomainAssociationsResult",
    "AcceptTransitGatewayPeeringAttachmentResult",
    "AcceptTransitGatewayVpcAttachmentResult",
    "AcceptVpcEndpointConnectionsResult",
    "AcceptVpcPeeringConnectionResult",
    "AdvertiseByoipCidrResult",
    "AllocateAddressResult",
    "AllocateHostsResult",
    "AllocateIpamPoolCidrResult",
    "ApplySecurityGroupsToClientVpnTargetNetworkResult",
    "AssignIpv6AddressesResult",
    "AssignPrivateIpAddressesResult",
    "AssignPrivateNatGatewayAddressResult",
    "AssociateAddressResult",
    "AssociateCapacityReservationBillingOwnerResult",
    "AssociateClientVpnTargetNetworkResult",
    "AssociateEnclaveCertificateIamRoleResult",
    "AssociateIamInstanceProfileResult",
    "AssociateInstanceEventWindowResult",
    "AssociateIpamByoasnResult",
    "AssociateIpamResourceDiscoveryResult",
    "AssociateNatGatewayAddressResult",
    "AssociateRouteServerResult",
    "AssociateRouteTableResult",
    "AssociateSecurityGroupVpcResult",
    "AssociateSubnetCidrBlockResult",
    "AssociateTransitGatewayMulticastDomainResult",
    "AssociateTransitGatewayPolicyTableResult",
    "AssociateTransitGatewayRouteTableResult",
    "AssociateTrunkInterfaceResult",
    "AssociateVpcCidrBlockResult",
    "AttachClassicLinkVpcResult",
    "AttachNetworkInterfaceResult",
    "AttachVerifiedAccessTrustProviderResult",
    "AttachVolumeResult",
    "AttachVpnGatewayResult",
    "AuthorizeClientVpnIngressResult",
    "AuthorizeSecurityGroupEgressResult",
    "AuthorizeSecurityGroupIngressResult",
    "BundleInstanceResult",
    "CancelBundleTaskResult",
    "CancelCapacityReservationFleetsResult",
    "CancelCapacityReservationResult",
    "CancelDeclarativePoliciesReportResult",
    "CancelImageLaunchPermissionResult",
    "CancelImportTaskResult",
    "CancelReservedInstancesListingResult",
    "CancelSpotFleetRequestsResult",
    "CancelSpotInstanceRequestsResult",
    "ConfirmProductInstanceResult",
    "CopyFpgaImageResult",
    "CopyImageResult",
    "CopySnapshotResult",
    "CopyVolumesResult",
    "CreateCapacityManagerDataExportResult",
    "CreateCapacityReservationBySplittingResult",
    "CreateCapacityReservationFleetResult",
    "CreateCapacityReservationResult",
    "CreateCarrierGatewayResult",
    "CreateClientVpnEndpointResult",
    "CreateClientVpnRouteResult",
    "CreateCoipCidrResult",
    "CreateCoipPoolResult",
    "CreateCustomerGatewayResult",
    "CreateDefaultSubnetResult",
    "CreateDefaultVpcResult",
    "CreateDelegateMacVolumeOwnershipTaskResult",
    "CreateDhcpOptionsResult",
    "CreateEgressOnlyInternetGatewayResult",
    "CreateFleetResult",
    "CreateFlowLogsResult",
    "CreateFpgaImageResult",
    "CreateImageUsageReportResult",
    "CreateInstanceConnectEndpointResult",
    "CreateInstanceEventWindowResult",
    "CreateInstanceExportTaskResult",
    "CreateInternetGatewayResult",
    "CreateIpamExternalResourceVerificationTokenResult",
    "CreateIpamPoolResult",
    "CreateIpamPrefixListResolverResult",
    "CreateIpamPrefixListResolverTargetResult",
    "CreateIpamResourceDiscoveryResult",
    "CreateIpamResult",
    "CreateIpamScopeResult",
    "CreateKeyPairResult",
    "CreateLaunchTemplateResult",
    "CreateLaunchTemplateVersionResult",
    "CreateLocalGatewayRouteResult",
    "CreateLocalGatewayRouteTableResult",
    "CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult",
    "CreateLocalGatewayRouteTableVpcAssociationResult",
    "CreateLocalGatewayVirtualInterfaceGroupResult",
    "CreateLocalGatewayVirtualInterfaceResult",
    "CreateMacSystemIntegrityProtectionModificationTaskResult",
    "CreateManagedPrefixListResult",
    "CreateNatGatewayResult",
    "CreateNetworkAclResult",
    "CreateNetworkInsightsAccessScopeResult",
    "CreateNetworkInsightsPathResult",
    "CreateNetworkInterfacePermissionResult",
    "CreateNetworkInterfaceResult",
    "CreatePlacementGroupResult",
    "CreatePublicIpv4PoolResult",
    "CreateReplaceRootVolumeTaskResult",
    "CreateReservedInstancesListingResult",
    "CreateRestoreImageTaskResult",
    "CreateRouteResult",
    "CreateRouteServerEndpointResult",
    "CreateRouteServerPeerResult",
    "CreateRouteServerResult",
    "CreateRouteTableResult",
    "CreateSecurityGroupResult",
    "CreateSnapshotResult",
    "CreateSnapshotsResult",
    "CreateSpotDatafeedSubscriptionResult",
    "CreateStoreImageTaskResult",
    "CreateSubnetCidrReservationResult",
    "CreateSubnetResult",
    "CreateTrafficMirrorFilterResult",
    "CreateTrafficMirrorFilterRuleResult",
    "CreateTrafficMirrorSessionResult",
    "CreateTrafficMirrorTargetResult",
    "CreateTransitGatewayConnectPeerResult",
    "CreateTransitGatewayConnectResult",
    "CreateTransitGatewayMulticastDomainResult",
    "CreateTransitGatewayPeeringAttachmentResult",
    "CreateTransitGatewayPolicyTableResult",
    "CreateTransitGatewayPrefixListReferenceResult",
    "CreateTransitGatewayResult",
    "CreateTransitGatewayRouteResult",
    "CreateTransitGatewayRouteTableAnnouncementResult",
    "CreateTransitGatewayRouteTableResult",
    "CreateTransitGatewayVpcAttachmentResult",
    "CreateVerifiedAccessEndpointResult",
    "CreateVerifiedAccessGroupResult",
    "CreateVerifiedAccessInstanceResult",
    "CreateVerifiedAccessTrustProviderResult",
    "CreateVolumeResult",
    "CreateVpcBlockPublicAccessExclusionResult",
    "CreateVpcEndpointConnectionNotificationResult",
    "CreateVpcEndpointResult",
    "CreateVpcEndpointServiceConfigurationResult",
    "CreateVpcPeeringConnectionResult",
    "CreateVpcResult",
    "CreateVpnConnectionResult",
    "CreateVpnGatewayResult",
    "DeleteCapacityManagerDataExportResult",
    "DeleteCarrierGatewayResult",
    "DeleteClientVpnEndpointResult",
    "DeleteClientVpnRouteResult",
    "DeleteCoipCidrResult",
    "DeleteCoipPoolResult",
    "DeleteEgressOnlyInternetGatewayResult",
    "DeleteFleetsResult",
    "DeleteFlowLogsResult",
    "DeleteFpgaImageResult",
    "DeleteImageUsageReportResult",
    "DeleteInstanceConnectEndpointResult",
    "DeleteInstanceEventWindowResult",
    "DeleteIpamExternalResourceVerificationTokenResult",
    "DeleteIpamPoolResult",
    "DeleteIpamPrefixListResolverResult",
    "DeleteIpamPrefixListResolverTargetResult",
    "DeleteIpamResourceDiscoveryResult",
    "DeleteIpamResult",
    "DeleteIpamScopeResult",
    "DeleteKeyPairResult",
    "DeleteLaunchTemplateResult",
    "DeleteLaunchTemplateVersionsResult",
    "DeleteLocalGatewayRouteResult",
    "DeleteLocalGatewayRouteTableResult",
    "DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult",
    "DeleteLocalGatewayRouteTableVpcAssociationResult",
    "DeleteLocalGatewayVirtualInterfaceGroupResult",
    "DeleteLocalGatewayVirtualInterfaceResult",
    "DeleteManagedPrefixListResult",
    "DeleteNatGatewayResult",
    "DeleteNetworkInsightsAccessScopeAnalysisResult",
    "DeleteNetworkInsightsAccessScopeResult",
    "DeleteNetworkInsightsAnalysisResult",
    "DeleteNetworkInsightsPathResult",
    "DeleteNetworkInterfacePermissionResult",
    "DeletePublicIpv4PoolResult",
    "DeleteQueuedReservedInstancesResult",
    "DeleteRouteServerEndpointResult",
    "DeleteRouteServerPeerResult",
    "DeleteRouteServerResult",
    "DeleteSecurityGroupResult",
    "DeleteSubnetCidrReservationResult",
    "DeleteTrafficMirrorFilterResult",
    "DeleteTrafficMirrorFilterRuleResult",
    "DeleteTrafficMirrorSessionResult",
    "DeleteTrafficMirrorTargetResult",
    "DeleteTransitGatewayConnectPeerResult",
    "DeleteTransitGatewayConnectResult",
    "DeleteTransitGatewayMulticastDomainResult",
    "DeleteTransitGatewayPeeringAttachmentResult",
    "DeleteTransitGatewayPolicyTableResult",
    "DeleteTransitGatewayPrefixListReferenceResult",
    "DeleteTransitGatewayResult",
    "DeleteTransitGatewayRouteResult",
    "DeleteTransitGatewayRouteTableAnnouncementResult",
    "DeleteTransitGatewayRouteTableResult",
    "DeleteTransitGatewayVpcAttachmentResult",
    "DeleteVerifiedAccessEndpointResult",
    "DeleteVerifiedAccessGroupResult",
    "DeleteVerifiedAccessInstanceResult",
    "DeleteVerifiedAccessTrustProviderResult",
    "DeleteVpcBlockPublicAccessExclusionResult",
    "DeleteVpcEndpointConnectionNotificationsResult",
    "DeleteVpcEndpointServiceConfigurationsResult",
    "DeleteVpcEndpointsResult",
    "DeleteVpcPeeringConnectionResult",
    "DeprovisionByoipCidrResult",
    "DeprovisionIpamByoasnResult",
    "DeprovisionIpamPoolCidrResult",
    "DeprovisionPublicIpv4PoolCidrResult",
    "DeregisterImageResult",
    "DeregisterInstanceEventNotificationAttributesResult",
    "DeregisterTransitGatewayMulticastGroupMembersResult",
    "DeregisterTransitGatewayMulticastGroupSourcesResult",
    "DescribeAccountAttributesResult",
    "DescribeAddressTransfersResult",
    "DescribeAddressesAttributeResult",
    "DescribeAddressesResult",
    "DescribeAggregateIdFormatResult",
    "DescribeAvailabilityZonesResult",
    "DescribeAwsNetworkPerformanceMetricSubscriptionsResult",
    "DescribeBundleTasksResult",
    "DescribeByoipCidrsResult",
    "DescribeCapacityBlockExtensionHistoryResult",
    "DescribeCapacityBlockExtensionOfferingsResult",
    "DescribeCapacityBlockOfferingsResult",
    "DescribeCapacityBlockStatusResult",
    "DescribeCapacityBlocksResult",
    "DescribeCapacityManagerDataExportsResult",
    "DescribeCapacityReservationBillingRequestsResult",
    "DescribeCapacityReservationFleetsResult",
    "DescribeCapacityReservationTopologyResult",
    "DescribeCapacityReservationsResult",
    "DescribeCarrierGatewaysResult",
    "DescribeClassicLinkInstancesResult",
    "DescribeClientVpnAuthorizationRulesResult",
    "DescribeClientVpnConnectionsResult",
    "DescribeClientVpnEndpointsResult",
    "DescribeClientVpnRoutesResult",
    "DescribeClientVpnTargetNetworksResult",
    "DescribeCoipPoolsResult",
    "DescribeConversionTasksResult",
    "DescribeCustomerGatewaysResult",
    "DescribeDeclarativePoliciesReportsResult",
    "DescribeDhcpOptionsResult",
    "DescribeEgressOnlyInternetGatewaysResult",
    "DescribeElasticGpusResult",
    "DescribeExportImageTasksResult",
    "DescribeExportTasksResult",
    "DescribeFastLaunchImagesResult",
    "DescribeFastSnapshotRestoresResult",
    "DescribeFleetHistoryResult",
    "DescribeFleetInstancesResult",
    "DescribeFleetsResult",
    "DescribeFlowLogsResult",
    "DescribeFpgaImageAttributeResult",
    "DescribeFpgaImagesResult",
    "DescribeHostReservationOfferingsResult",
    "DescribeHostReservationsResult",
    "DescribeHostsResult",
    "DescribeIamInstanceProfileAssociationsResult",
    "DescribeIdFormatResult",
    "DescribeIdentityIdFormatResult",
    "DescribeImageAttributeResult",
    "DescribeImageReferencesResult",
    "DescribeImageUsageReportEntriesResult",
    "DescribeImageUsageReportsResult",
    "DescribeImportImageTasksResult",
    "DescribeImportSnapshotTasksResult",
    "DescribeInstanceAttributeResult",
    "DescribeInstanceConnectEndpointsResult",
    "DescribeInstanceCreditSpecificationsResult",
    "DescribeInstanceEventNotificationAttributesResult",
    "DescribeInstanceEventWindowsResult",
    "DescribeInstanceImageMetadataResult",
    "DescribeInstanceStatusResult",
    "DescribeInstanceTopologyResult",
    "DescribeInstanceTypeOfferingsResult",
    "DescribeInstanceTypesResult",
    "DescribeInternetGatewaysResult",
    "DescribeIpamByoasnResult",
    "DescribeIpamExternalResourceVerificationTokensResult",
    "DescribeIpamPoolsResult",
    "DescribeIpamPrefixListResolverTargetsResult",
    "DescribeIpamPrefixListResolversResult",
    "DescribeIpamResourceDiscoveriesResult",
    "DescribeIpamResourceDiscoveryAssociationsResult",
    "DescribeIpamScopesResult",
    "DescribeIpamsResult",
    "DescribeIpv6PoolsResult",
    "DescribeKeyPairsResult",
    "DescribeLaunchTemplateVersionsResult",
    "DescribeLaunchTemplatesResult",
    "DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult",
    "DescribeLocalGatewayRouteTableVpcAssociationsResult",
    "DescribeLocalGatewayRouteTablesResult",
    "DescribeLocalGatewayVirtualInterfaceGroupsResult",
    "DescribeLocalGatewayVirtualInterfacesResult",
    "DescribeLocalGatewaysResult",
    "DescribeLockedSnapshotsResult",
    "DescribeMacHostsResult",
    "DescribeMacModificationTasksResult",
    "DescribeManagedPrefixListsResult",
    "DescribeMovingAddressesResult",
    "DescribeNatGatewaysResult",
    "DescribeNetworkAclsResult",
    "DescribeNetworkInsightsAccessScopeAnalysesResult",
    "DescribeNetworkInsightsAccessScopesResult",
    "DescribeNetworkInsightsAnalysesResult",
    "DescribeNetworkInsightsPathsResult",
    "DescribeNetworkInterfaceAttributeResult",
    "DescribeNetworkInterfacePermissionsResult",
    "DescribeNetworkInterfacesResult",
    "DescribeOutpostLagsResult",
    "DescribePlacementGroupsResult",
    "DescribePrefixListsResult",
    "DescribePrincipalIdFormatResult",
    "DescribePublicIpv4PoolsResult",
    "DescribeRegionsResult",
    "DescribeReplaceRootVolumeTasksResult",
    "DescribeReservedInstancesListingsResult",
    "DescribeReservedInstancesModificationsResult",
    "DescribeReservedInstancesOfferingsResult",
    "DescribeReservedInstancesResult",
    "DescribeRouteServerEndpointsResult",
    "DescribeRouteServerPeersResult",
    "DescribeRouteServersResult",
    "DescribeRouteTablesResult",
    "DescribeScheduledInstanceAvailabilityResult",
    "DescribeScheduledInstancesResult",
    "DescribeSecurityGroupReferencesResult",
    "DescribeSecurityGroupRulesResult",
    "DescribeSecurityGroupVpcAssociationsResult",
    "DescribeServiceLinkVirtualInterfacesResult",
    "DescribeSnapshotAttributeResult",
    "DescribeSnapshotTierStatusResult",
    "DescribeSnapshotsResult",
    "DescribeSpotDatafeedSubscriptionResult",
    "DescribeSpotFleetInstancesResult",
    "DescribeSpotFleetRequestHistoryResult",
    "DescribeSpotFleetRequestsResult",
    "DescribeSpotInstanceRequestsResult",
    "DescribeSpotPriceHistoryResult",
    "DescribeStaleSecurityGroupsResult",
    "DescribeStoreImageTasksResult",
    "DescribeSubnetsResult",
    "DescribeTagsResult",
    "DescribeTrafficMirrorFilterRulesResult",
    "DescribeTrafficMirrorFiltersResult",
    "DescribeTrafficMirrorSessionsResult",
    "DescribeTrafficMirrorTargetsResult",
    "DescribeTransitGatewayAttachmentsResult",
    "DescribeTransitGatewayConnectPeersResult",
    "DescribeTransitGatewayConnectsResult",
    "DescribeTransitGatewayMulticastDomainsResult",
    "DescribeTransitGatewayPeeringAttachmentsResult",
    "DescribeTransitGatewayPolicyTablesResult",
    "DescribeTransitGatewayRouteTableAnnouncementsResult",
    "DescribeTransitGatewayRouteTablesResult",
    "DescribeTransitGatewayVpcAttachmentsResult",
    "DescribeTransitGatewaysResult",
    "DescribeTrunkInterfaceAssociationsResult",
    "DescribeVerifiedAccessEndpointsResult",
    "DescribeVerifiedAccessGroupsResult",
    "DescribeVerifiedAccessInstanceLoggingConfigurationsResult",
    "DescribeVerifiedAccessInstancesResult",
    "DescribeVerifiedAccessTrustProvidersResult",
    "DescribeVolumeAttributeResult",
    "DescribeVolumeStatusResult",
    "DescribeVolumesModificationsResult",
    "DescribeVolumesResult",
    "DescribeVpcAttributeResult",
    "DescribeVpcBlockPublicAccessExclusionsResult",
    "DescribeVpcBlockPublicAccessOptionsResult",
    "DescribeVpcClassicLinkDnsSupportResult",
    "DescribeVpcClassicLinkResult",
    "DescribeVpcEndpointAssociationsResult",
    "DescribeVpcEndpointConnectionNotificationsResult",
    "DescribeVpcEndpointConnectionsResult",
    "DescribeVpcEndpointServiceConfigurationsResult",
    "DescribeVpcEndpointServicePermissionsResult",
    "DescribeVpcEndpointServicesResult",
    "DescribeVpcEndpointsResult",
    "DescribeVpcPeeringConnectionsResult",
    "DescribeVpcsResult",
    "DescribeVpnConnectionsResult",
    "DescribeVpnGatewaysResult",
    "DetachClassicLinkVpcResult",
    "DetachVerifiedAccessTrustProviderResult",
    "DetachVolumeResult",
    "DisableAddressTransferResult",
    "DisableAllowedImagesSettingsResult",
    "DisableAwsNetworkPerformanceMetricSubscriptionResult",
    "DisableCapacityManagerResult",
    "DisableEbsEncryptionByDefaultResult",
    "DisableFastLaunchResult",
    "DisableFastSnapshotRestoresResult",
    "DisableImageBlockPublicAccessResult",
    "DisableImageDeprecationResult",
    "DisableImageDeregistrationProtectionResult",
    "DisableImageResult",
    "DisableIpamOrganizationAdminAccountResult",
    "DisableRouteServerPropagationResult",
    "DisableSerialConsoleAccessResult",
    "DisableSnapshotBlockPublicAccessResult",
    "DisableTransitGatewayRouteTablePropagationResult",
    "DisableVpcClassicLinkDnsSupportResult",
    "DisableVpcClassicLinkResult",
    "DisassociateCapacityReservationBillingOwnerResult",
    "DisassociateClientVpnTargetNetworkResult",
    "DisassociateEnclaveCertificateIamRoleResult",
    "DisassociateIamInstanceProfileResult",
    "DisassociateInstanceEventWindowResult",
    "DisassociateIpamByoasnResult",
    "DisassociateIpamResourceDiscoveryResult",
    "DisassociateNatGatewayAddressResult",
    "DisassociateRouteServerResult",
    "DisassociateSecurityGroupVpcResult",
    "DisassociateSubnetCidrBlockResult",
    "DisassociateTransitGatewayMulticastDomainResult",
    "DisassociateTransitGatewayPolicyTableResult",
    "DisassociateTransitGatewayRouteTableResult",
    "DisassociateTrunkInterfaceResult",
    "DisassociateVpcCidrBlockResult",
    "EC2Image",
    "EC2Instance",
    "EnableAddressTransferResult",
    "EnableAllowedImagesSettingsResult",
    "EnableAwsNetworkPerformanceMetricSubscriptionResult",
    "EnableCapacityManagerResult",
    "EnableEbsEncryptionByDefaultResult",
    "EnableFastLaunchResult",
    "EnableFastSnapshotRestoresResult",
    "EnableImageBlockPublicAccessResult",
    "EnableImageDeprecationResult",
    "EnableImageDeregistrationProtectionResult",
    "EnableImageResult",
    "EnableIpamOrganizationAdminAccountResult",
    "EnableReachabilityAnalyzerOrganizationSharingResult",
    "EnableRouteServerPropagationResult",
    "EnableSerialConsoleAccessResult",
    "EnableSnapshotBlockPublicAccessResult",
    "EnableTransitGatewayRouteTablePropagationResult",
    "EnableVpcClassicLinkDnsSupportResult",
    "EnableVpcClassicLinkResult",
    "ExportClientVpnClientCertificateRevocationListResult",
    "ExportClientVpnClientConfigurationResult",
    "ExportImageResult",
    "ExportTransitGatewayRoutesResult",
    "ExportVerifiedAccessInstanceClientConfigurationResult",
    "GetActiveVpnTunnelStatusResult",
    "GetAllowedImagesSettingsResult",
    "GetAssociatedEnclaveCertificateIamRolesResult",
    "GetAssociatedIpv6PoolCidrsResult",
    "GetAwsNetworkPerformanceDataResult",
    "GetCapacityManagerAttributesResult",
    "GetCapacityManagerMetricDataResult",
    "GetCapacityManagerMetricDimensionsResult",
    "GetCapacityReservationUsageResult",
    "GetCoipPoolUsageResult",
    "GetConsoleOutputResult",
    "GetConsoleScreenshotResult",
    "GetDeclarativePoliciesReportSummaryResult",
    "GetDefaultCreditSpecificationResult",
    "GetEbsDefaultKmsKeyIdResult",
    "GetEbsEncryptionByDefaultResult",
    "GetFlowLogsIntegrationTemplateResult",
    "GetGroupsForCapacityReservationResult",
    "GetHostReservationPurchasePreviewResult",
    "GetImageAncestryResult",
    "GetImageBlockPublicAccessStateResult",
    "GetInstanceMetadataDefaultsResult",
    "GetInstanceTpmEkPubResult",
    "GetInstanceTypesFromInstanceRequirementsResult",
    "GetInstanceUefiDataResult",
    "GetIpamAddressHistoryResult",
    "GetIpamDiscoveredAccountsResult",
    "GetIpamDiscoveredPublicAddressesResult",
    "GetIpamDiscoveredResourceCidrsResult",
    "GetIpamPoolAllocationsResult",
    "GetIpamPoolCidrsResult",
    "GetIpamPrefixListResolverRulesResult",
    "GetIpamPrefixListResolverVersionEntriesResult",
    "GetIpamPrefixListResolverVersionsResult",
    "GetIpamResourceCidrsResult",
    "GetLaunchTemplateDataResult",
    "GetManagedPrefixListAssociationsResult",
    "GetManagedPrefixListEntriesResult",
    "GetNetworkInsightsAccessScopeAnalysisFindingsResult",
    "GetNetworkInsightsAccessScopeContentResult",
    "GetPasswordDataResult",
    "GetReservedInstancesExchangeQuoteResult",
    "GetRouteServerAssociationsResult",
    "GetRouteServerPropagationsResult",
    "GetRouteServerRoutingDatabaseResult",
    "GetSecurityGroupsForVpcResult",
    "GetSerialConsoleAccessStatusResult",
    "GetSnapshotBlockPublicAccessStateResult",
    "GetSpotPlacementScoresResult",
    "GetSubnetCidrReservationsResult",
    "GetTransitGatewayAttachmentPropagationsResult",
    "GetTransitGatewayMulticastDomainAssociationsResult",
    "GetTransitGatewayPolicyTableAssociationsResult",
    "GetTransitGatewayPolicyTableEntriesResult",
    "GetTransitGatewayPrefixListReferencesResult",
    "GetTransitGatewayRouteTableAssociationsResult",
    "GetTransitGatewayRouteTablePropagationsResult",
    "GetVerifiedAccessEndpointPolicyResult",
    "GetVerifiedAccessEndpointTargetsResult",
    "GetVerifiedAccessGroupPolicyResult",
    "GetVpnConnectionDeviceSampleConfigurationResult",
    "GetVpnConnectionDeviceTypesResult",
    "GetVpnTunnelReplacementStatusResult",
    "ImportClientVpnClientCertificateRevocationListResult",
    "ImportImageResult",
    "ImportInstanceResult",
    "ImportKeyPairResult",
    "ImportSnapshotResult",
    "ImportVolumeResult",
    "ListImagesInRecycleBinResult",
    "ListSnapshotsInRecycleBinResult",
    "LockSnapshotResult",
    "ModifyAddressAttributeResult",
    "ModifyAvailabilityZoneGroupResult",
    "ModifyCapacityReservationFleetResult",
    "ModifyCapacityReservationResult",
    "ModifyClientVpnEndpointResult",
    "ModifyDefaultCreditSpecificationResult",
    "ModifyEbsDefaultKmsKeyIdResult",
    "ModifyFleetResult",
    "ModifyFpgaImageAttributeResult",
    "ModifyHostsResult",
    "ModifyInstanceCapacityReservationAttributesResult",
    "ModifyInstanceConnectEndpointResult",
    "ModifyInstanceCpuOptionsResult",
    "ModifyInstanceCreditSpecificationResult",
    "ModifyInstanceEventStartTimeResult",
    "ModifyInstanceEventWindowResult",
    "ModifyInstanceMaintenanceOptionsResult",
    "ModifyInstanceMetadataDefaultsResult",
    "ModifyInstanceMetadataOptionsResult",
    "ModifyInstanceNetworkPerformanceOptionsResult",
    "ModifyInstancePlacementResult",
    "ModifyIpamPoolResult",
    "ModifyIpamPrefixListResolverResult",
    "ModifyIpamPrefixListResolverTargetResult",
    "ModifyIpamResourceCidrResult",
    "ModifyIpamResourceDiscoveryResult",
    "ModifyIpamResult",
    "ModifyIpamScopeResult",
    "ModifyLaunchTemplateResult",
    "ModifyLocalGatewayRouteResult",
    "ModifyManagedPrefixListResult",
    "ModifyPrivateDnsNameOptionsResult",
    "ModifyPublicIpDnsNameOptionsResult",
    "ModifyReservedInstancesResult",
    "ModifyRouteServerResult",
    "ModifySecurityGroupRulesResult",
    "ModifySnapshotTierResult",
    "ModifySpotFleetRequestResult",
    "ModifyTrafficMirrorFilterNetworkServicesResult",
    "ModifyTrafficMirrorFilterRuleResult",
    "ModifyTrafficMirrorSessionResult",
    "ModifyTransitGatewayPrefixListReferenceResult",
    "ModifyTransitGatewayResult",
    "ModifyTransitGatewayVpcAttachmentResult",
    "ModifyVerifiedAccessEndpointPolicyResult",
    "ModifyVerifiedAccessEndpointResult",
    "ModifyVerifiedAccessGroupPolicyResult",
    "ModifyVerifiedAccessGroupResult",
    "ModifyVerifiedAccessInstanceLoggingConfigurationResult",
    "ModifyVerifiedAccessInstanceResult",
    "ModifyVerifiedAccessTrustProviderResult",
    "ModifyVolumeResult",
    "ModifyVpcBlockPublicAccessExclusionResult",
    "ModifyVpcBlockPublicAccessOptionsResult",
    "ModifyVpcEndpointConnectionNotificationResult",
    "ModifyVpcEndpointResult",
    "ModifyVpcEndpointServiceConfigurationResult",
    "ModifyVpcEndpointServicePayerResponsibilityResult",
    "ModifyVpcEndpointServicePermissionsResult",
    "ModifyVpcPeeringConnectionOptionsResult",
    "ModifyVpcTenancyResult",
    "ModifyVpnConnectionOptionsResult",
    "ModifyVpnConnectionResult",
    "ModifyVpnTunnelCertificateResult",
    "ModifyVpnTunnelOptionsResult",
    "MonitorInstancesResult",
    "MoveAddressToVpcResult",
    "MoveByoipCidrToIpamResult",
    "MoveCapacityReservationInstancesResult",
    "ProvisionByoipCidrResult",
    "ProvisionIpamByoasnResult",
    "ProvisionIpamPoolCidrResult",
    "ProvisionPublicIpv4PoolCidrResult",
    "PurchaseCapacityBlockExtensionResult",
    "PurchaseCapacityBlockResult",
    "PurchaseHostReservationResult",
    "PurchaseReservedInstancesOfferingResult",
    "PurchaseScheduledInstancesResult",
    "RegisterImageResult",
    "RegisterInstanceEventNotificationAttributesResult",
    "RegisterTransitGatewayMulticastGroupMembersResult",
    "RegisterTransitGatewayMulticastGroupSourcesResult",
    "RejectCapacityReservationBillingOwnershipResult",
    "RejectTransitGatewayMulticastDomainAssociationsResult",
    "RejectTransitGatewayPeeringAttachmentResult",
    "RejectTransitGatewayVpcAttachmentResult",
    "RejectVpcEndpointConnectionsResult",
    "RejectVpcPeeringConnectionResult",
    "ReleaseHostsResult",
    "ReleaseIpamPoolAllocationResult",
    "ReplaceIamInstanceProfileAssociationResult",
    "ReplaceImageCriteriaInAllowedImagesSettingsResult",
    "ReplaceNetworkAclAssociationResult",
    "ReplaceRouteTableAssociationResult",
    "ReplaceTransitGatewayRouteResult",
    "ReplaceVpnTunnelResult",
    "RequestSpotFleetResult",
    "RequestSpotInstancesResult",
    "ResetAddressAttributeResult",
    "ResetEbsDefaultKmsKeyIdResult",
    "ResetFpgaImageAttributeResult",
    "RestoreAddressToClassicResult",
    "RestoreImageFromRecycleBinResult",
    "RestoreManagedPrefixListVersionResult",
    "RestoreSnapshotFromRecycleBinResult",
    "RestoreSnapshotTierResult",
    "RevokeClientVpnIngressResult",
    "RevokeSecurityGroupEgressResult",
    "RevokeSecurityGroupIngressResult",
    "RunInstancesResult",
    "RunScheduledInstancesResult",
    "SearchLocalGatewayRoutesResult",
    "SearchTransitGatewayMulticastGroupsResult",
    "SearchTransitGatewayRoutesResult",
    "SecurityGroup",
    "StartDeclarativePoliciesReportResult",
    "StartNetworkInsightsAccessScopeAnalysisResult",
    "StartNetworkInsightsAnalysisResult",
    "StartVpcEndpointServicePrivateDnsVerificationResult",
    "TerminateClientVpnConnectionsResult",
    "UnassignIpv6AddressesResult",
    "UnassignPrivateNatGatewayAddressResult",
    "UnlockSnapshotResult",
    "UnmonitorInstancesResult",
    "UpdateCapacityManagerOrganizationsAccessResult",
    "UpdateSecurityGroupRuleDescriptionsEgressResult",
    "UpdateSecurityGroupRuleDescriptionsIngressResult",
    "WithdrawByoipCidrResult",
    "accept_address_transfer",
    "accept_capacity_reservation_billing_ownership",
    "accept_reserved_instances_exchange_quote",
    "accept_transit_gateway_multicast_domain_associations",
    "accept_transit_gateway_peering_attachment",
    "accept_transit_gateway_vpc_attachment",
    "accept_vpc_endpoint_connections",
    "accept_vpc_peering_connection",
    "advertise_byoip_cidr",
    "allocate_address",
    "allocate_hosts",
    "allocate_ipam_pool_cidr",
    "apply_security_groups_to_client_vpn_target_network",
    "assign_ipv6_addresses",
    "assign_private_ip_addresses",
    "assign_private_nat_gateway_address",
    "associate_address",
    "associate_capacity_reservation_billing_owner",
    "associate_client_vpn_target_network",
    "associate_dhcp_options",
    "associate_enclave_certificate_iam_role",
    "associate_iam_instance_profile",
    "associate_instance_event_window",
    "associate_ipam_byoasn",
    "associate_ipam_resource_discovery",
    "associate_nat_gateway_address",
    "associate_route_server",
    "associate_route_table",
    "associate_security_group_vpc",
    "associate_subnet_cidr_block",
    "associate_transit_gateway_multicast_domain",
    "associate_transit_gateway_policy_table",
    "associate_transit_gateway_route_table",
    "associate_trunk_interface",
    "associate_vpc_cidr_block",
    "attach_classic_link_vpc",
    "attach_internet_gateway",
    "attach_network_interface",
    "attach_verified_access_trust_provider",
    "attach_volume",
    "attach_vpn_gateway",
    "authorize_client_vpn_ingress",
    "authorize_security_group_egress",
    "authorize_security_group_ingress",
    "bundle_instance",
    "cancel_bundle_task",
    "cancel_capacity_reservation",
    "cancel_capacity_reservation_fleets",
    "cancel_conversion_task",
    "cancel_declarative_policies_report",
    "cancel_export_task",
    "cancel_image_launch_permission",
    "cancel_import_task",
    "cancel_reserved_instances_listing",
    "cancel_spot_fleet_requests",
    "cancel_spot_instance_requests",
    "confirm_product_instance",
    "copy_fpga_image",
    "copy_image",
    "copy_snapshot",
    "copy_volumes",
    "create_capacity_manager_data_export",
    "create_capacity_reservation",
    "create_capacity_reservation_by_splitting",
    "create_capacity_reservation_fleet",
    "create_carrier_gateway",
    "create_client_vpn_endpoint",
    "create_client_vpn_route",
    "create_coip_cidr",
    "create_coip_pool",
    "create_customer_gateway",
    "create_default_subnet",
    "create_default_vpc",
    "create_delegate_mac_volume_ownership_task",
    "create_dhcp_options",
    "create_egress_only_internet_gateway",
    "create_fleet",
    "create_flow_logs",
    "create_fpga_image",
    "create_image",
    "create_image_usage_report",
    "create_instance_connect_endpoint",
    "create_instance_event_window",
    "create_instance_export_task",
    "create_internet_gateway",
    "create_ipam",
    "create_ipam_external_resource_verification_token",
    "create_ipam_pool",
    "create_ipam_prefix_list_resolver",
    "create_ipam_prefix_list_resolver_target",
    "create_ipam_resource_discovery",
    "create_ipam_scope",
    "create_key_pair",
    "create_launch_template",
    "create_launch_template_version",
    "create_local_gateway_route",
    "create_local_gateway_route_table",
    "create_local_gateway_route_table_virtual_interface_group_association",
    "create_local_gateway_route_table_vpc_association",
    "create_local_gateway_virtual_interface",
    "create_local_gateway_virtual_interface_group",
    "create_mac_system_integrity_protection_modification_task",
    "create_managed_prefix_list",
    "create_nat_gateway",
    "create_network_acl",
    "create_network_acl_entry",
    "create_network_insights_access_scope",
    "create_network_insights_path",
    "create_network_interface",
    "create_network_interface_permission",
    "create_placement_group",
    "create_public_ipv4_pool",
    "create_replace_root_volume_task",
    "create_reserved_instances_listing",
    "create_restore_image_task",
    "create_route",
    "create_route_server",
    "create_route_server_endpoint",
    "create_route_server_peer",
    "create_route_table",
    "create_security_group",
    "create_snapshot",
    "create_snapshots",
    "create_spot_datafeed_subscription",
    "create_store_image_task",
    "create_subnet",
    "create_subnet_cidr_reservation",
    "create_tags",
    "create_traffic_mirror_filter",
    "create_traffic_mirror_filter_rule",
    "create_traffic_mirror_session",
    "create_traffic_mirror_target",
    "create_transit_gateway",
    "create_transit_gateway_connect",
    "create_transit_gateway_connect_peer",
    "create_transit_gateway_multicast_domain",
    "create_transit_gateway_peering_attachment",
    "create_transit_gateway_policy_table",
    "create_transit_gateway_prefix_list_reference",
    "create_transit_gateway_route",
    "create_transit_gateway_route_table",
    "create_transit_gateway_route_table_announcement",
    "create_transit_gateway_vpc_attachment",
    "create_verified_access_endpoint",
    "create_verified_access_group",
    "create_verified_access_instance",
    "create_verified_access_trust_provider",
    "create_volume",
    "create_vpc",
    "create_vpc_block_public_access_exclusion",
    "create_vpc_endpoint",
    "create_vpc_endpoint_connection_notification",
    "create_vpc_endpoint_service_configuration",
    "create_vpc_peering_connection",
    "create_vpn_connection",
    "create_vpn_connection_route",
    "create_vpn_gateway",
    "delete_capacity_manager_data_export",
    "delete_carrier_gateway",
    "delete_client_vpn_endpoint",
    "delete_client_vpn_route",
    "delete_coip_cidr",
    "delete_coip_pool",
    "delete_customer_gateway",
    "delete_dhcp_options",
    "delete_egress_only_internet_gateway",
    "delete_fleets",
    "delete_flow_logs",
    "delete_fpga_image",
    "delete_image_usage_report",
    "delete_instance_connect_endpoint",
    "delete_instance_event_window",
    "delete_internet_gateway",
    "delete_ipam",
    "delete_ipam_external_resource_verification_token",
    "delete_ipam_pool",
    "delete_ipam_prefix_list_resolver",
    "delete_ipam_prefix_list_resolver_target",
    "delete_ipam_resource_discovery",
    "delete_ipam_scope",
    "delete_key_pair",
    "delete_launch_template",
    "delete_launch_template_versions",
    "delete_local_gateway_route",
    "delete_local_gateway_route_table",
    "delete_local_gateway_route_table_virtual_interface_group_association",
    "delete_local_gateway_route_table_vpc_association",
    "delete_local_gateway_virtual_interface",
    "delete_local_gateway_virtual_interface_group",
    "delete_managed_prefix_list",
    "delete_nat_gateway",
    "delete_network_acl",
    "delete_network_acl_entry",
    "delete_network_insights_access_scope",
    "delete_network_insights_access_scope_analysis",
    "delete_network_insights_analysis",
    "delete_network_insights_path",
    "delete_network_interface",
    "delete_network_interface_permission",
    "delete_placement_group",
    "delete_public_ipv4_pool",
    "delete_queued_reserved_instances",
    "delete_route",
    "delete_route_server",
    "delete_route_server_endpoint",
    "delete_route_server_peer",
    "delete_route_table",
    "delete_security_group",
    "delete_snapshot",
    "delete_spot_datafeed_subscription",
    "delete_subnet",
    "delete_subnet_cidr_reservation",
    "delete_tags",
    "delete_traffic_mirror_filter",
    "delete_traffic_mirror_filter_rule",
    "delete_traffic_mirror_session",
    "delete_traffic_mirror_target",
    "delete_transit_gateway",
    "delete_transit_gateway_connect",
    "delete_transit_gateway_connect_peer",
    "delete_transit_gateway_multicast_domain",
    "delete_transit_gateway_peering_attachment",
    "delete_transit_gateway_policy_table",
    "delete_transit_gateway_prefix_list_reference",
    "delete_transit_gateway_route",
    "delete_transit_gateway_route_table",
    "delete_transit_gateway_route_table_announcement",
    "delete_transit_gateway_vpc_attachment",
    "delete_verified_access_endpoint",
    "delete_verified_access_group",
    "delete_verified_access_instance",
    "delete_verified_access_trust_provider",
    "delete_volume",
    "delete_vpc",
    "delete_vpc_block_public_access_exclusion",
    "delete_vpc_endpoint_connection_notifications",
    "delete_vpc_endpoint_service_configurations",
    "delete_vpc_endpoints",
    "delete_vpc_peering_connection",
    "delete_vpn_connection",
    "delete_vpn_connection_route",
    "delete_vpn_gateway",
    "deprovision_byoip_cidr",
    "deprovision_ipam_byoasn",
    "deprovision_ipam_pool_cidr",
    "deprovision_public_ipv4_pool_cidr",
    "deregister_image",
    "deregister_instance_event_notification_attributes",
    "deregister_transit_gateway_multicast_group_members",
    "deregister_transit_gateway_multicast_group_sources",
    "describe_account_attributes",
    "describe_address_transfers",
    "describe_addresses",
    "describe_addresses_attribute",
    "describe_aggregate_id_format",
    "describe_availability_zones",
    "describe_aws_network_performance_metric_subscriptions",
    "describe_bundle_tasks",
    "describe_byoip_cidrs",
    "describe_capacity_block_extension_history",
    "describe_capacity_block_extension_offerings",
    "describe_capacity_block_offerings",
    "describe_capacity_block_status",
    "describe_capacity_blocks",
    "describe_capacity_manager_data_exports",
    "describe_capacity_reservation_billing_requests",
    "describe_capacity_reservation_fleets",
    "describe_capacity_reservation_topology",
    "describe_capacity_reservations",
    "describe_carrier_gateways",
    "describe_classic_link_instances",
    "describe_client_vpn_authorization_rules",
    "describe_client_vpn_connections",
    "describe_client_vpn_endpoints",
    "describe_client_vpn_routes",
    "describe_client_vpn_target_networks",
    "describe_coip_pools",
    "describe_conversion_tasks",
    "describe_customer_gateways",
    "describe_declarative_policies_reports",
    "describe_dhcp_options",
    "describe_egress_only_internet_gateways",
    "describe_elastic_gpus",
    "describe_export_image_tasks",
    "describe_export_tasks",
    "describe_fast_launch_images",
    "describe_fast_snapshot_restores",
    "describe_fleet_history",
    "describe_fleet_instances",
    "describe_fleets",
    "describe_flow_logs",
    "describe_fpga_image_attribute",
    "describe_fpga_images",
    "describe_host_reservation_offerings",
    "describe_host_reservations",
    "describe_hosts",
    "describe_iam_instance_profile_associations",
    "describe_id_format",
    "describe_identity_id_format",
    "describe_image_attribute",
    "describe_image_references",
    "describe_image_usage_report_entries",
    "describe_image_usage_reports",
    "describe_images",
    "describe_import_image_tasks",
    "describe_import_snapshot_tasks",
    "describe_instance_attribute",
    "describe_instance_connect_endpoints",
    "describe_instance_credit_specifications",
    "describe_instance_event_notification_attributes",
    "describe_instance_event_windows",
    "describe_instance_image_metadata",
    "describe_instance_status",
    "describe_instance_topology",
    "describe_instance_type_offerings",
    "describe_instance_types",
    "describe_instances",
    "describe_internet_gateways",
    "describe_ipam_byoasn",
    "describe_ipam_external_resource_verification_tokens",
    "describe_ipam_pools",
    "describe_ipam_prefix_list_resolver_targets",
    "describe_ipam_prefix_list_resolvers",
    "describe_ipam_resource_discoveries",
    "describe_ipam_resource_discovery_associations",
    "describe_ipam_scopes",
    "describe_ipams",
    "describe_ipv6_pools",
    "describe_key_pairs",
    "describe_launch_template_versions",
    "describe_launch_templates",
    "describe_local_gateway_route_table_virtual_interface_group_associations",
    "describe_local_gateway_route_table_vpc_associations",
    "describe_local_gateway_route_tables",
    "describe_local_gateway_virtual_interface_groups",
    "describe_local_gateway_virtual_interfaces",
    "describe_local_gateways",
    "describe_locked_snapshots",
    "describe_mac_hosts",
    "describe_mac_modification_tasks",
    "describe_managed_prefix_lists",
    "describe_moving_addresses",
    "describe_nat_gateways",
    "describe_network_acls",
    "describe_network_insights_access_scope_analyses",
    "describe_network_insights_access_scopes",
    "describe_network_insights_analyses",
    "describe_network_insights_paths",
    "describe_network_interface_attribute",
    "describe_network_interface_permissions",
    "describe_network_interfaces",
    "describe_outpost_lags",
    "describe_placement_groups",
    "describe_prefix_lists",
    "describe_principal_id_format",
    "describe_public_ipv4_pools",
    "describe_regions",
    "describe_replace_root_volume_tasks",
    "describe_reserved_instances",
    "describe_reserved_instances_listings",
    "describe_reserved_instances_modifications",
    "describe_reserved_instances_offerings",
    "describe_route_server_endpoints",
    "describe_route_server_peers",
    "describe_route_servers",
    "describe_route_tables",
    "describe_scheduled_instance_availability",
    "describe_scheduled_instances",
    "describe_security_group_references",
    "describe_security_group_rules",
    "describe_security_group_vpc_associations",
    "describe_security_groups",
    "describe_service_link_virtual_interfaces",
    "describe_snapshot_attribute",
    "describe_snapshot_tier_status",
    "describe_snapshots",
    "describe_spot_datafeed_subscription",
    "describe_spot_fleet_instances",
    "describe_spot_fleet_request_history",
    "describe_spot_fleet_requests",
    "describe_spot_instance_requests",
    "describe_spot_price_history",
    "describe_stale_security_groups",
    "describe_store_image_tasks",
    "describe_subnets",
    "describe_tags",
    "describe_traffic_mirror_filter_rules",
    "describe_traffic_mirror_filters",
    "describe_traffic_mirror_sessions",
    "describe_traffic_mirror_targets",
    "describe_transit_gateway_attachments",
    "describe_transit_gateway_connect_peers",
    "describe_transit_gateway_connects",
    "describe_transit_gateway_multicast_domains",
    "describe_transit_gateway_peering_attachments",
    "describe_transit_gateway_policy_tables",
    "describe_transit_gateway_route_table_announcements",
    "describe_transit_gateway_route_tables",
    "describe_transit_gateway_vpc_attachments",
    "describe_transit_gateways",
    "describe_trunk_interface_associations",
    "describe_verified_access_endpoints",
    "describe_verified_access_groups",
    "describe_verified_access_instance_logging_configurations",
    "describe_verified_access_instances",
    "describe_verified_access_trust_providers",
    "describe_volume_attribute",
    "describe_volume_status",
    "describe_volumes",
    "describe_volumes_modifications",
    "describe_vpc_attribute",
    "describe_vpc_block_public_access_exclusions",
    "describe_vpc_block_public_access_options",
    "describe_vpc_classic_link",
    "describe_vpc_classic_link_dns_support",
    "describe_vpc_endpoint_associations",
    "describe_vpc_endpoint_connection_notifications",
    "describe_vpc_endpoint_connections",
    "describe_vpc_endpoint_service_configurations",
    "describe_vpc_endpoint_service_permissions",
    "describe_vpc_endpoint_services",
    "describe_vpc_endpoints",
    "describe_vpc_peering_connections",
    "describe_vpcs",
    "describe_vpn_connections",
    "describe_vpn_gateways",
    "detach_classic_link_vpc",
    "detach_internet_gateway",
    "detach_network_interface",
    "detach_verified_access_trust_provider",
    "detach_volume",
    "detach_vpn_gateway",
    "disable_address_transfer",
    "disable_allowed_images_settings",
    "disable_aws_network_performance_metric_subscription",
    "disable_capacity_manager",
    "disable_ebs_encryption_by_default",
    "disable_fast_launch",
    "disable_fast_snapshot_restores",
    "disable_image",
    "disable_image_block_public_access",
    "disable_image_deprecation",
    "disable_image_deregistration_protection",
    "disable_ipam_organization_admin_account",
    "disable_route_server_propagation",
    "disable_serial_console_access",
    "disable_snapshot_block_public_access",
    "disable_transit_gateway_route_table_propagation",
    "disable_vgw_route_propagation",
    "disable_vpc_classic_link",
    "disable_vpc_classic_link_dns_support",
    "disassociate_address",
    "disassociate_capacity_reservation_billing_owner",
    "disassociate_client_vpn_target_network",
    "disassociate_enclave_certificate_iam_role",
    "disassociate_iam_instance_profile",
    "disassociate_instance_event_window",
    "disassociate_ipam_byoasn",
    "disassociate_ipam_resource_discovery",
    "disassociate_nat_gateway_address",
    "disassociate_route_server",
    "disassociate_route_table",
    "disassociate_security_group_vpc",
    "disassociate_subnet_cidr_block",
    "disassociate_transit_gateway_multicast_domain",
    "disassociate_transit_gateway_policy_table",
    "disassociate_transit_gateway_route_table",
    "disassociate_trunk_interface",
    "disassociate_vpc_cidr_block",
    "enable_address_transfer",
    "enable_allowed_images_settings",
    "enable_aws_network_performance_metric_subscription",
    "enable_capacity_manager",
    "enable_ebs_encryption_by_default",
    "enable_fast_launch",
    "enable_fast_snapshot_restores",
    "enable_image",
    "enable_image_block_public_access",
    "enable_image_deprecation",
    "enable_image_deregistration_protection",
    "enable_ipam_organization_admin_account",
    "enable_reachability_analyzer_organization_sharing",
    "enable_route_server_propagation",
    "enable_serial_console_access",
    "enable_snapshot_block_public_access",
    "enable_transit_gateway_route_table_propagation",
    "enable_vgw_route_propagation",
    "enable_volume_io",
    "enable_vpc_classic_link",
    "enable_vpc_classic_link_dns_support",
    "export_client_vpn_client_certificate_revocation_list",
    "export_client_vpn_client_configuration",
    "export_image",
    "export_transit_gateway_routes",
    "export_verified_access_instance_client_configuration",
    "get_active_vpn_tunnel_status",
    "get_allowed_images_settings",
    "get_associated_enclave_certificate_iam_roles",
    "get_associated_ipv6_pool_cidrs",
    "get_aws_network_performance_data",
    "get_capacity_manager_attributes",
    "get_capacity_manager_metric_data",
    "get_capacity_manager_metric_dimensions",
    "get_capacity_reservation_usage",
    "get_coip_pool_usage",
    "get_console_output",
    "get_console_screenshot",
    "get_declarative_policies_report_summary",
    "get_default_credit_specification",
    "get_ebs_default_kms_key_id",
    "get_ebs_encryption_by_default",
    "get_flow_logs_integration_template",
    "get_groups_for_capacity_reservation",
    "get_host_reservation_purchase_preview",
    "get_image_ancestry",
    "get_image_block_public_access_state",
    "get_instance",
    "get_instance_console_output",
    "get_instance_metadata_defaults",
    "get_instance_tpm_ek_pub",
    "get_instance_types_from_instance_requirements",
    "get_instance_uefi_data",
    "get_instances_by_tag",
    "get_ipam_address_history",
    "get_ipam_discovered_accounts",
    "get_ipam_discovered_public_addresses",
    "get_ipam_discovered_resource_cidrs",
    "get_ipam_pool_allocations",
    "get_ipam_pool_cidrs",
    "get_ipam_prefix_list_resolver_rules",
    "get_ipam_prefix_list_resolver_version_entries",
    "get_ipam_prefix_list_resolver_versions",
    "get_ipam_resource_cidrs",
    "get_latest_ami",
    "get_launch_template_data",
    "get_managed_prefix_list_associations",
    "get_managed_prefix_list_entries",
    "get_network_insights_access_scope_analysis_findings",
    "get_network_insights_access_scope_content",
    "get_password_data",
    "get_reserved_instances_exchange_quote",
    "get_route_server_associations",
    "get_route_server_propagations",
    "get_route_server_routing_database",
    "get_security_groups_for_vpc",
    "get_serial_console_access_status",
    "get_snapshot_block_public_access_state",
    "get_spot_placement_scores",
    "get_subnet_cidr_reservations",
    "get_transit_gateway_attachment_propagations",
    "get_transit_gateway_multicast_domain_associations",
    "get_transit_gateway_policy_table_associations",
    "get_transit_gateway_policy_table_entries",
    "get_transit_gateway_prefix_list_references",
    "get_transit_gateway_route_table_associations",
    "get_transit_gateway_route_table_propagations",
    "get_verified_access_endpoint_policy",
    "get_verified_access_endpoint_targets",
    "get_verified_access_group_policy",
    "get_vpn_connection_device_sample_configuration",
    "get_vpn_connection_device_types",
    "get_vpn_tunnel_replacement_status",
    "import_client_vpn_client_certificate_revocation_list",
    "import_image",
    "import_instance",
    "import_key_pair",
    "import_snapshot",
    "import_volume",
    "list_images_in_recycle_bin",
    "list_snapshots_in_recycle_bin",
    "lock_snapshot",
    "modify_address_attribute",
    "modify_availability_zone_group",
    "modify_capacity_reservation",
    "modify_capacity_reservation_fleet",
    "modify_client_vpn_endpoint",
    "modify_default_credit_specification",
    "modify_ebs_default_kms_key_id",
    "modify_fleet",
    "modify_fpga_image_attribute",
    "modify_hosts",
    "modify_id_format",
    "modify_identity_id_format",
    "modify_image_attribute",
    "modify_instance_attribute",
    "modify_instance_capacity_reservation_attributes",
    "modify_instance_connect_endpoint",
    "modify_instance_cpu_options",
    "modify_instance_credit_specification",
    "modify_instance_event_start_time",
    "modify_instance_event_window",
    "modify_instance_maintenance_options",
    "modify_instance_metadata_defaults",
    "modify_instance_metadata_options",
    "modify_instance_network_performance_options",
    "modify_instance_placement",
    "modify_ipam",
    "modify_ipam_pool",
    "modify_ipam_prefix_list_resolver",
    "modify_ipam_prefix_list_resolver_target",
    "modify_ipam_resource_cidr",
    "modify_ipam_resource_discovery",
    "modify_ipam_scope",
    "modify_launch_template",
    "modify_local_gateway_route",
    "modify_managed_prefix_list",
    "modify_network_interface_attribute",
    "modify_private_dns_name_options",
    "modify_public_ip_dns_name_options",
    "modify_reserved_instances",
    "modify_route_server",
    "modify_security_group_rules",
    "modify_snapshot_attribute",
    "modify_snapshot_tier",
    "modify_spot_fleet_request",
    "modify_subnet_attribute",
    "modify_traffic_mirror_filter_network_services",
    "modify_traffic_mirror_filter_rule",
    "modify_traffic_mirror_session",
    "modify_transit_gateway",
    "modify_transit_gateway_prefix_list_reference",
    "modify_transit_gateway_vpc_attachment",
    "modify_verified_access_endpoint",
    "modify_verified_access_endpoint_policy",
    "modify_verified_access_group",
    "modify_verified_access_group_policy",
    "modify_verified_access_instance",
    "modify_verified_access_instance_logging_configuration",
    "modify_verified_access_trust_provider",
    "modify_volume",
    "modify_volume_attribute",
    "modify_vpc_attribute",
    "modify_vpc_block_public_access_exclusion",
    "modify_vpc_block_public_access_options",
    "modify_vpc_endpoint",
    "modify_vpc_endpoint_connection_notification",
    "modify_vpc_endpoint_service_configuration",
    "modify_vpc_endpoint_service_payer_responsibility",
    "modify_vpc_endpoint_service_permissions",
    "modify_vpc_peering_connection_options",
    "modify_vpc_tenancy",
    "modify_vpn_connection",
    "modify_vpn_connection_options",
    "modify_vpn_tunnel_certificate",
    "modify_vpn_tunnel_options",
    "monitor_instances",
    "move_address_to_vpc",
    "move_byoip_cidr_to_ipam",
    "move_capacity_reservation_instances",
    "provision_byoip_cidr",
    "provision_ipam_byoasn",
    "provision_ipam_pool_cidr",
    "provision_public_ipv4_pool_cidr",
    "purchase_capacity_block",
    "purchase_capacity_block_extension",
    "purchase_host_reservation",
    "purchase_reserved_instances_offering",
    "purchase_scheduled_instances",
    "reboot_instances",
    "register_image",
    "register_instance_event_notification_attributes",
    "register_transit_gateway_multicast_group_members",
    "register_transit_gateway_multicast_group_sources",
    "reject_capacity_reservation_billing_ownership",
    "reject_transit_gateway_multicast_domain_associations",
    "reject_transit_gateway_peering_attachment",
    "reject_transit_gateway_vpc_attachment",
    "reject_vpc_endpoint_connections",
    "reject_vpc_peering_connection",
    "release_address",
    "release_hosts",
    "release_ipam_pool_allocation",
    "replace_iam_instance_profile_association",
    "replace_image_criteria_in_allowed_images_settings",
    "replace_network_acl_association",
    "replace_network_acl_entry",
    "replace_route",
    "replace_route_table_association",
    "replace_transit_gateway_route",
    "replace_vpn_tunnel",
    "report_instance_status",
    "request_spot_fleet",
    "request_spot_instances",
    "reset_address_attribute",
    "reset_ebs_default_kms_key_id",
    "reset_fpga_image_attribute",
    "reset_image_attribute",
    "reset_instance_attribute",
    "reset_network_interface_attribute",
    "reset_snapshot_attribute",
    "restore_address_to_classic",
    "restore_image_from_recycle_bin",
    "restore_managed_prefix_list_version",
    "restore_snapshot_from_recycle_bin",
    "restore_snapshot_tier",
    "revoke_client_vpn_ingress",
    "revoke_security_group_egress",
    "revoke_security_group_ingress",
    "run_instances",
    "run_scheduled_instances",
    "search_local_gateway_routes",
    "search_transit_gateway_multicast_groups",
    "search_transit_gateway_routes",
    "send_diagnostic_interrupt",
    "start_declarative_policies_report",
    "start_instances",
    "start_network_insights_access_scope_analysis",
    "start_network_insights_analysis",
    "start_vpc_endpoint_service_private_dns_verification",
    "stop_instances",
    "terminate_client_vpn_connections",
    "terminate_instances",
    "unassign_ipv6_addresses",
    "unassign_private_ip_addresses",
    "unassign_private_nat_gateway_address",
    "unlock_snapshot",
    "unmonitor_instances",
    "update_capacity_manager_organizations_access",
    "update_security_group_rule_descriptions_egress",
    "update_security_group_rule_descriptions_ingress",
    "wait_for_instance_state",
    "withdraw_byoip_cidr",
]


# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------


async def describe_instances(
    instance_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> list[EC2Instance]:
    """Describe one or more EC2 instances, with optional filters.

    Args:
        instance_ids: Specific instance IDs to describe.  ``None`` returns all
            instances visible to the caller.
        filters: boto3-style filter list, e.g.
            ``[{"Name": "instance-state-name", "Values": ["running"]}]``.
        region_name: AWS region override.

    Returns:
        A list of :class:`EC2Instance` objects.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if instance_ids:
        kwargs["InstanceIds"] = instance_ids
    if filters:
        kwargs["Filters"] = filters

    instances: list[EC2Instance] = []
    try:
        token: str | None = None
        while True:
            if token:
                kwargs["NextToken"] = token
            resp = await client.call("DescribeInstances", **kwargs)
            for reservation in resp.get("Reservations", []):
                for inst in reservation.get("Instances", []):
                    instances.append(_parse_instance(inst))
            token = resp.get("NextToken")
            if not token:
                break
    except Exception as exc:
        raise wrap_aws_error(exc, "describe_instances failed") from exc
    return instances


async def get_instance(
    instance_id: str,
    region_name: str | None = None,
) -> EC2Instance | None:
    """Fetch a single EC2 instance by ID.

    Returns:
        An :class:`EC2Instance`, or ``None`` if not found.
    """
    results = await describe_instances([instance_id], region_name=region_name)
    return results[0] if results else None


async def start_instances(
    instance_ids: list[str],
    region_name: str | None = None,
) -> None:
    """Start one or more stopped EC2 instances.

    Args:
        instance_ids: IDs of instances to start.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the start request fails.
    """
    client = async_client("ec2", region_name)
    try:
        await client.call("StartInstances", InstanceIds=instance_ids)
    except Exception as exc:
        raise wrap_aws_error(exc, f"Failed to start instances {instance_ids}") from exc


async def stop_instances(
    instance_ids: list[str],
    force: bool = False,
    region_name: str | None = None,
) -> None:
    """Stop one or more running EC2 instances.

    Args:
        instance_ids: IDs of instances to stop.
        force: Force-stop (equivalent to cutting power).  Use with caution --
            may corrupt the instance file system.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the stop request fails.
    """
    client = async_client("ec2", region_name)
    try:
        await client.call("StopInstances", InstanceIds=instance_ids, Force=force)
    except Exception as exc:
        raise wrap_aws_error(exc, f"Failed to stop instances {instance_ids}") from exc


async def reboot_instances(
    instance_ids: list[str],
    region_name: str | None = None,
) -> None:
    """Reboot one or more EC2 instances.

    Args:
        instance_ids: IDs of instances to reboot.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the reboot request fails.
    """
    client = async_client("ec2", region_name)
    try:
        await client.call("RebootInstances", InstanceIds=instance_ids)
    except Exception as exc:
        raise wrap_aws_error(exc, f"Failed to reboot instances {instance_ids}") from exc


async def terminate_instances(
    instance_ids: list[str],
    region_name: str | None = None,
) -> None:
    """Permanently terminate one or more EC2 instances.

    This is irreversible -- terminated instances cannot be restarted.

    Args:
        instance_ids: IDs of instances to terminate.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the terminate request fails.
    """
    client = async_client("ec2", region_name)
    try:
        await client.call("TerminateInstances", InstanceIds=instance_ids)
    except Exception as exc:
        raise wrap_aws_error(exc, f"Failed to terminate instances {instance_ids}") from exc


async def create_image(
    instance_id: str,
    name: str,
    description: str = "",
    no_reboot: bool = True,
    region_name: str | None = None,
) -> str:
    """Create an AMI from a running or stopped EC2 instance.

    Args:
        instance_id: Source instance ID.
        name: AMI name (must be unique in the account/region).
        description: Optional description.
        no_reboot: If ``True`` (default), the instance is not rebooted before
            the image is created.  The image may be less consistent.
        region_name: AWS region override.

    Returns:
        The new AMI ID.

    Raises:
        RuntimeError: If image creation fails.
    """
    client = async_client("ec2", region_name)
    try:
        resp = await client.call(
            "CreateImage",
            InstanceId=instance_id,
            Name=name,
            Description=description,
            NoReboot=no_reboot,
        )
    except Exception as exc:
        raise wrap_aws_error(exc, f"Failed to create image from {instance_id!r}") from exc
    return resp["ImageId"]


async def describe_images(
    image_ids: list[str] | None = None,
    owners: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> list[EC2Image]:
    """Describe AMIs visible to the caller.

    Args:
        image_ids: Specific AMI IDs to describe.
        owners: Filter by owner, e.g. ``["self", "amazon"]``.
        filters: boto3-style filter list.
        region_name: AWS region override.

    Returns:
        A list of :class:`EC2Image` objects.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if image_ids:
        kwargs["ImageIds"] = image_ids
    if owners:
        kwargs["Owners"] = owners
    if filters:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeImages", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "describe_images failed") from exc
    return [
        EC2Image(
            image_id=img["ImageId"],
            name=img.get("Name", ""),
            state=img["State"],
            creation_date=img.get("CreationDate"),
            description=img.get("Description") or None,
        )
        for img in resp.get("Images", [])
    ]


async def describe_security_groups(
    group_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> list[SecurityGroup]:
    """Describe EC2 security groups.

    Args:
        group_ids: Specific security group IDs.
        filters: boto3-style filter list.
        region_name: AWS region override.

    Returns:
        A list of :class:`SecurityGroup` objects.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if group_ids:
        kwargs["GroupIds"] = group_ids
    if filters:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeSecurityGroups", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "describe_security_groups failed") from exc
    return [
        SecurityGroup(
            group_id=sg["GroupId"],
            group_name=sg["GroupName"],
            description=sg["Description"],
            vpc_id=sg.get("VpcId") or None,
        )
        for sg in resp.get("SecurityGroups", [])
    ]


# ---------------------------------------------------------------------------
# Complex utilities
# ---------------------------------------------------------------------------


async def wait_for_instance_state(
    instance_id: str,
    target_state: str,
    timeout: float = 300.0,
    poll_interval: float = 10.0,
    region_name: str | None = None,
) -> EC2Instance:
    """Poll until an EC2 instance reaches the desired state.

    Args:
        instance_id: The instance ID to wait for.
        target_state: Target state name, e.g. ``"running"``, ``"stopped"``,
            ``"terminated"``.
        timeout: Maximum seconds to wait (default ``300``).
        poll_interval: Seconds between status checks (default ``10``).
        region_name: AWS region override.

    Returns:
        The :class:`EC2Instance` in the target state.

    Raises:
        TimeoutError: If the instance does not reach *target_state* within
            *timeout*.
        RuntimeError: If the instance is not found.
    """
    import time as _time

    deadline = _time.monotonic() + timeout
    while True:
        instance = await get_instance(instance_id, region_name=region_name)
        if instance is None:
            raise AwsServiceError(f"Instance {instance_id!r} not found")
        if instance.state == target_state:
            return instance
        if _time.monotonic() >= deadline:
            raise TimeoutError(
                f"Instance {instance_id!r} did not reach state "
                f"{target_state!r} within {timeout}s "
                f"(current: {instance.state!r})"
            )
        await asyncio.sleep(poll_interval)


async def get_instances_by_tag(
    tag_key: str,
    tag_value: str,
    region_name: str | None = None,
) -> list[EC2Instance]:
    """Find EC2 instances by a tag key/value pair.

    Args:
        tag_key: Tag key to filter by.
        tag_value: Tag value to filter by.
        region_name: AWS region override.

    Returns:
        A list of :class:`EC2Instance` objects matching the tag.
    """
    return await describe_instances(
        filters=[{"Name": f"tag:{tag_key}", "Values": [tag_value]}],
        region_name=region_name,
    )


async def get_latest_ami(
    name_filter: str,
    owners: list[str] | None = None,
    region_name: str | None = None,
) -> EC2Image | None:
    """Find the most recently created AMI matching a name pattern.

    Args:
        name_filter: Glob pattern matched against the AMI name, e.g.
            ``"amzn2-ami-hvm-*-x86_64-gp2"`` or ``"my-app-*"``.
        owners: Owner filter -- ``["self"]``, ``["amazon"]``, or specific
            account IDs.  Defaults to ``["self"]``.
        region_name: AWS region override.

    Returns:
        The most recent :class:`EC2Image`, or ``None`` if no match found.
    """
    images = await describe_images(
        filters=[
            {"Name": "name", "Values": [name_filter]},
            {"Name": "state", "Values": ["available"]},
        ],
        owners=owners or ["self"],
        region_name=region_name,
    )
    if not images:
        return None
    return sorted(images, key=lambda img: img.creation_date or "", reverse=True)[0]


async def get_instance_console_output(
    instance_id: str,
    region_name: str | None = None,
) -> str:
    """Retrieve the system console output of an EC2 instance.

    Useful for diagnosing boot failures.

    Args:
        instance_id: The instance ID.
        region_name: AWS region override.

    Returns:
        The console output as a decoded string (may be empty for newer
        instances).

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    try:
        resp = await client.call("GetConsoleOutput", InstanceId=instance_id)
    except Exception as exc:
        raise wrap_aws_error(exc, f"get_console_output failed for {instance_id!r}") from exc
    encoded = resp.get("Output", "")
    if not encoded:
        return ""
    return base64.b64decode(encoded).decode("utf-8", errors="replace")


async def accept_address_transfer(
    address: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> AcceptAddressTransferResult:
    """Accept address transfer.

    Args:
        address: Address.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Address"] = address
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("AcceptAddressTransfer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to accept address transfer") from exc
    return AcceptAddressTransferResult(
        address_transfer=resp.get("AddressTransfer"),
    )


async def accept_capacity_reservation_billing_ownership(
    capacity_reservation_id: str,
    region_name: str | None = None,
) -> AcceptCapacityReservationBillingOwnershipResult:
    """Accept capacity reservation billing ownership.

    Args:
        capacity_reservation_id: Capacity reservation id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityReservationId"] = capacity_reservation_id
    try:
        resp = await client.call("AcceptCapacityReservationBillingOwnership", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to accept capacity reservation billing ownership"
        ) from exc
    return AcceptCapacityReservationBillingOwnershipResult(
        return_value=resp.get("Return"),
    )


async def accept_reserved_instances_exchange_quote(
    reserved_instance_ids: list[str],
    *,
    target_configurations: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> AcceptReservedInstancesExchangeQuoteResult:
    """Accept reserved instances exchange quote.

    Args:
        reserved_instance_ids: Reserved instance ids.
        target_configurations: Target configurations.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ReservedInstanceIds"] = reserved_instance_ids
    if target_configurations is not None:
        kwargs["TargetConfigurations"] = target_configurations
    try:
        resp = await client.call("AcceptReservedInstancesExchangeQuote", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to accept reserved instances exchange quote") from exc
    return AcceptReservedInstancesExchangeQuoteResult(
        exchange_id=resp.get("ExchangeId"),
    )


async def accept_transit_gateway_multicast_domain_associations(
    *,
    transit_gateway_multicast_domain_id: str | None = None,
    transit_gateway_attachment_id: str | None = None,
    subnet_ids: list[str] | None = None,
    region_name: str | None = None,
) -> AcceptTransitGatewayMulticastDomainAssociationsResult:
    """Accept transit gateway multicast domain associations.

    Args:
        transit_gateway_multicast_domain_id: Transit gateway multicast domain id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        subnet_ids: Subnet ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_multicast_domain_id is not None:
        kwargs["TransitGatewayMulticastDomainId"] = transit_gateway_multicast_domain_id
    if transit_gateway_attachment_id is not None:
        kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    if subnet_ids is not None:
        kwargs["SubnetIds"] = subnet_ids
    try:
        resp = await client.call("AcceptTransitGatewayMulticastDomainAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to accept transit gateway multicast domain associations"
        ) from exc
    return AcceptTransitGatewayMulticastDomainAssociationsResult(
        associations=resp.get("Associations"),
    )


async def accept_transit_gateway_peering_attachment(
    transit_gateway_attachment_id: str,
    region_name: str | None = None,
) -> AcceptTransitGatewayPeeringAttachmentResult:
    """Accept transit gateway peering attachment.

    Args:
        transit_gateway_attachment_id: Transit gateway attachment id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    try:
        resp = await client.call("AcceptTransitGatewayPeeringAttachment", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to accept transit gateway peering attachment") from exc
    return AcceptTransitGatewayPeeringAttachmentResult(
        transit_gateway_peering_attachment=resp.get("TransitGatewayPeeringAttachment"),
    )


async def accept_transit_gateway_vpc_attachment(
    transit_gateway_attachment_id: str,
    region_name: str | None = None,
) -> AcceptTransitGatewayVpcAttachmentResult:
    """Accept transit gateway vpc attachment.

    Args:
        transit_gateway_attachment_id: Transit gateway attachment id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    try:
        resp = await client.call("AcceptTransitGatewayVpcAttachment", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to accept transit gateway vpc attachment") from exc
    return AcceptTransitGatewayVpcAttachmentResult(
        transit_gateway_vpc_attachment=resp.get("TransitGatewayVpcAttachment"),
    )


async def accept_vpc_endpoint_connections(
    service_id: str,
    vpc_endpoint_ids: list[str],
    region_name: str | None = None,
) -> AcceptVpcEndpointConnectionsResult:
    """Accept vpc endpoint connections.

    Args:
        service_id: Service id.
        vpc_endpoint_ids: Vpc endpoint ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ServiceId"] = service_id
    kwargs["VpcEndpointIds"] = vpc_endpoint_ids
    try:
        resp = await client.call("AcceptVpcEndpointConnections", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to accept vpc endpoint connections") from exc
    return AcceptVpcEndpointConnectionsResult(
        unsuccessful=resp.get("Unsuccessful"),
    )


async def accept_vpc_peering_connection(
    vpc_peering_connection_id: str,
    region_name: str | None = None,
) -> AcceptVpcPeeringConnectionResult:
    """Accept vpc peering connection.

    Args:
        vpc_peering_connection_id: Vpc peering connection id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcPeeringConnectionId"] = vpc_peering_connection_id
    try:
        resp = await client.call("AcceptVpcPeeringConnection", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to accept vpc peering connection") from exc
    return AcceptVpcPeeringConnectionResult(
        vpc_peering_connection=resp.get("VpcPeeringConnection"),
    )


async def advertise_byoip_cidr(
    cidr: str,
    *,
    asn: str | None = None,
    network_border_group: str | None = None,
    region_name: str | None = None,
) -> AdvertiseByoipCidrResult:
    """Advertise byoip cidr.

    Args:
        cidr: Cidr.
        asn: Asn.
        network_border_group: Network border group.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Cidr"] = cidr
    if asn is not None:
        kwargs["Asn"] = asn
    if network_border_group is not None:
        kwargs["NetworkBorderGroup"] = network_border_group
    try:
        resp = await client.call("AdvertiseByoipCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to advertise byoip cidr") from exc
    return AdvertiseByoipCidrResult(
        byoip_cidr=resp.get("ByoipCidr"),
    )


async def allocate_address(
    *,
    domain: str | None = None,
    address: str | None = None,
    public_ipv4_pool: str | None = None,
    network_border_group: str | None = None,
    customer_owned_ipv4_pool: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    ipam_pool_id: str | None = None,
    region_name: str | None = None,
) -> AllocateAddressResult:
    """Allocate address.

    Args:
        domain: Domain.
        address: Address.
        public_ipv4_pool: Public ipv4 pool.
        network_border_group: Network border group.
        customer_owned_ipv4_pool: Customer owned ipv4 pool.
        tag_specifications: Tag specifications.
        ipam_pool_id: Ipam pool id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if domain is not None:
        kwargs["Domain"] = domain
    if address is not None:
        kwargs["Address"] = address
    if public_ipv4_pool is not None:
        kwargs["PublicIpv4Pool"] = public_ipv4_pool
    if network_border_group is not None:
        kwargs["NetworkBorderGroup"] = network_border_group
    if customer_owned_ipv4_pool is not None:
        kwargs["CustomerOwnedIpv4Pool"] = customer_owned_ipv4_pool
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if ipam_pool_id is not None:
        kwargs["IpamPoolId"] = ipam_pool_id
    try:
        resp = await client.call("AllocateAddress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to allocate address") from exc
    return AllocateAddressResult(
        allocation_id=resp.get("AllocationId"),
        public_ipv4_pool=resp.get("PublicIpv4Pool"),
        network_border_group=resp.get("NetworkBorderGroup"),
        domain=resp.get("Domain"),
        customer_owned_ip=resp.get("CustomerOwnedIp"),
        customer_owned_ipv4_pool=resp.get("CustomerOwnedIpv4Pool"),
        carrier_ip=resp.get("CarrierIp"),
        public_ip=resp.get("PublicIp"),
    )


async def allocate_hosts(
    *,
    instance_family: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    host_recovery: str | None = None,
    outpost_arn: str | None = None,
    host_maintenance: str | None = None,
    asset_ids: list[str] | None = None,
    availability_zone_id: str | None = None,
    auto_placement: str | None = None,
    client_token: str | None = None,
    instance_type: str | None = None,
    quantity: int | None = None,
    availability_zone: str | None = None,
    region_name: str | None = None,
) -> AllocateHostsResult:
    """Allocate hosts.

    Args:
        instance_family: Instance family.
        tag_specifications: Tag specifications.
        host_recovery: Host recovery.
        outpost_arn: Outpost arn.
        host_maintenance: Host maintenance.
        asset_ids: Asset ids.
        availability_zone_id: Availability zone id.
        auto_placement: Auto placement.
        client_token: Client token.
        instance_type: Instance type.
        quantity: Quantity.
        availability_zone: Availability zone.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if instance_family is not None:
        kwargs["InstanceFamily"] = instance_family
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if host_recovery is not None:
        kwargs["HostRecovery"] = host_recovery
    if outpost_arn is not None:
        kwargs["OutpostArn"] = outpost_arn
    if host_maintenance is not None:
        kwargs["HostMaintenance"] = host_maintenance
    if asset_ids is not None:
        kwargs["AssetIds"] = asset_ids
    if availability_zone_id is not None:
        kwargs["AvailabilityZoneId"] = availability_zone_id
    if auto_placement is not None:
        kwargs["AutoPlacement"] = auto_placement
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if instance_type is not None:
        kwargs["InstanceType"] = instance_type
    if quantity is not None:
        kwargs["Quantity"] = quantity
    if availability_zone is not None:
        kwargs["AvailabilityZone"] = availability_zone
    try:
        resp = await client.call("AllocateHosts", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to allocate hosts") from exc
    return AllocateHostsResult(
        host_ids=resp.get("HostIds"),
    )


async def allocate_ipam_pool_cidr(
    ipam_pool_id: str,
    *,
    cidr: str | None = None,
    netmask_length: int | None = None,
    client_token: str | None = None,
    description: str | None = None,
    preview_next_cidr: bool | None = None,
    allowed_cidrs: list[str] | None = None,
    disallowed_cidrs: list[str] | None = None,
    region_name: str | None = None,
) -> AllocateIpamPoolCidrResult:
    """Allocate ipam pool cidr.

    Args:
        ipam_pool_id: Ipam pool id.
        cidr: Cidr.
        netmask_length: Netmask length.
        client_token: Client token.
        description: Description.
        preview_next_cidr: Preview next cidr.
        allowed_cidrs: Allowed cidrs.
        disallowed_cidrs: Disallowed cidrs.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPoolId"] = ipam_pool_id
    if cidr is not None:
        kwargs["Cidr"] = cidr
    if netmask_length is not None:
        kwargs["NetmaskLength"] = netmask_length
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if description is not None:
        kwargs["Description"] = description
    if preview_next_cidr is not None:
        kwargs["PreviewNextCidr"] = preview_next_cidr
    if allowed_cidrs is not None:
        kwargs["AllowedCidrs"] = allowed_cidrs
    if disallowed_cidrs is not None:
        kwargs["DisallowedCidrs"] = disallowed_cidrs
    try:
        resp = await client.call("AllocateIpamPoolCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to allocate ipam pool cidr") from exc
    return AllocateIpamPoolCidrResult(
        ipam_pool_allocation=resp.get("IpamPoolAllocation"),
    )


async def apply_security_groups_to_client_vpn_target_network(
    client_vpn_endpoint_id: str,
    vpc_id: str,
    security_group_ids: list[str],
    region_name: str | None = None,
) -> ApplySecurityGroupsToClientVpnTargetNetworkResult:
    """Apply security groups to client vpn target network.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        vpc_id: Vpc id.
        security_group_ids: Security group ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    kwargs["VpcId"] = vpc_id
    kwargs["SecurityGroupIds"] = security_group_ids
    try:
        resp = await client.call("ApplySecurityGroupsToClientVpnTargetNetwork", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to apply security groups to client vpn target network"
        ) from exc
    return ApplySecurityGroupsToClientVpnTargetNetworkResult(
        security_group_ids=resp.get("SecurityGroupIds"),
    )


async def assign_ipv6_addresses(
    network_interface_id: str,
    *,
    ipv6_prefix_count: int | None = None,
    ipv6_prefixes: list[str] | None = None,
    ipv6_addresses: list[str] | None = None,
    ipv6_address_count: int | None = None,
    region_name: str | None = None,
) -> AssignIpv6AddressesResult:
    """Assign ipv6 addresses.

    Args:
        network_interface_id: Network interface id.
        ipv6_prefix_count: Ipv6 prefix count.
        ipv6_prefixes: Ipv6 prefixes.
        ipv6_addresses: Ipv6 addresses.
        ipv6_address_count: Ipv6 address count.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    if ipv6_prefix_count is not None:
        kwargs["Ipv6PrefixCount"] = ipv6_prefix_count
    if ipv6_prefixes is not None:
        kwargs["Ipv6Prefixes"] = ipv6_prefixes
    if ipv6_addresses is not None:
        kwargs["Ipv6Addresses"] = ipv6_addresses
    if ipv6_address_count is not None:
        kwargs["Ipv6AddressCount"] = ipv6_address_count
    try:
        resp = await client.call("AssignIpv6Addresses", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to assign ipv6 addresses") from exc
    return AssignIpv6AddressesResult(
        assigned_ipv6_addresses=resp.get("AssignedIpv6Addresses"),
        assigned_ipv6_prefixes=resp.get("AssignedIpv6Prefixes"),
        network_interface_id=resp.get("NetworkInterfaceId"),
    )


async def assign_private_ip_addresses(
    network_interface_id: str,
    *,
    ipv4_prefixes: list[str] | None = None,
    ipv4_prefix_count: int | None = None,
    private_ip_addresses: list[str] | None = None,
    secondary_private_ip_address_count: int | None = None,
    allow_reassignment: bool | None = None,
    region_name: str | None = None,
) -> AssignPrivateIpAddressesResult:
    """Assign private ip addresses.

    Args:
        network_interface_id: Network interface id.
        ipv4_prefixes: Ipv4 prefixes.
        ipv4_prefix_count: Ipv4 prefix count.
        private_ip_addresses: Private ip addresses.
        secondary_private_ip_address_count: Secondary private ip address count.
        allow_reassignment: Allow reassignment.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    if ipv4_prefixes is not None:
        kwargs["Ipv4Prefixes"] = ipv4_prefixes
    if ipv4_prefix_count is not None:
        kwargs["Ipv4PrefixCount"] = ipv4_prefix_count
    if private_ip_addresses is not None:
        kwargs["PrivateIpAddresses"] = private_ip_addresses
    if secondary_private_ip_address_count is not None:
        kwargs["SecondaryPrivateIpAddressCount"] = secondary_private_ip_address_count
    if allow_reassignment is not None:
        kwargs["AllowReassignment"] = allow_reassignment
    try:
        resp = await client.call("AssignPrivateIpAddresses", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to assign private ip addresses") from exc
    return AssignPrivateIpAddressesResult(
        network_interface_id=resp.get("NetworkInterfaceId"),
        assigned_private_ip_addresses=resp.get("AssignedPrivateIpAddresses"),
        assigned_ipv4_prefixes=resp.get("AssignedIpv4Prefixes"),
    )


async def assign_private_nat_gateway_address(
    nat_gateway_id: str,
    *,
    private_ip_addresses: list[str] | None = None,
    private_ip_address_count: int | None = None,
    region_name: str | None = None,
) -> AssignPrivateNatGatewayAddressResult:
    """Assign private nat gateway address.

    Args:
        nat_gateway_id: Nat gateway id.
        private_ip_addresses: Private ip addresses.
        private_ip_address_count: Private ip address count.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NatGatewayId"] = nat_gateway_id
    if private_ip_addresses is not None:
        kwargs["PrivateIpAddresses"] = private_ip_addresses
    if private_ip_address_count is not None:
        kwargs["PrivateIpAddressCount"] = private_ip_address_count
    try:
        resp = await client.call("AssignPrivateNatGatewayAddress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to assign private nat gateway address") from exc
    return AssignPrivateNatGatewayAddressResult(
        nat_gateway_id=resp.get("NatGatewayId"),
        nat_gateway_addresses=resp.get("NatGatewayAddresses"),
    )


async def associate_address(
    *,
    allocation_id: str | None = None,
    instance_id: str | None = None,
    public_ip: str | None = None,
    network_interface_id: str | None = None,
    private_ip_address: str | None = None,
    allow_reassociation: bool | None = None,
    region_name: str | None = None,
) -> AssociateAddressResult:
    """Associate address.

    Args:
        allocation_id: Allocation id.
        instance_id: Instance id.
        public_ip: Public ip.
        network_interface_id: Network interface id.
        private_ip_address: Private ip address.
        allow_reassociation: Allow reassociation.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if allocation_id is not None:
        kwargs["AllocationId"] = allocation_id
    if instance_id is not None:
        kwargs["InstanceId"] = instance_id
    if public_ip is not None:
        kwargs["PublicIp"] = public_ip
    if network_interface_id is not None:
        kwargs["NetworkInterfaceId"] = network_interface_id
    if private_ip_address is not None:
        kwargs["PrivateIpAddress"] = private_ip_address
    if allow_reassociation is not None:
        kwargs["AllowReassociation"] = allow_reassociation
    try:
        resp = await client.call("AssociateAddress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate address") from exc
    return AssociateAddressResult(
        association_id=resp.get("AssociationId"),
    )


async def associate_capacity_reservation_billing_owner(
    capacity_reservation_id: str,
    unused_reservation_billing_owner_id: str,
    region_name: str | None = None,
) -> AssociateCapacityReservationBillingOwnerResult:
    """Associate capacity reservation billing owner.

    Args:
        capacity_reservation_id: Capacity reservation id.
        unused_reservation_billing_owner_id: Unused reservation billing owner id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityReservationId"] = capacity_reservation_id
    kwargs["UnusedReservationBillingOwnerId"] = unused_reservation_billing_owner_id
    try:
        resp = await client.call("AssociateCapacityReservationBillingOwner", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate capacity reservation billing owner") from exc
    return AssociateCapacityReservationBillingOwnerResult(
        return_value=resp.get("Return"),
    )


async def associate_client_vpn_target_network(
    client_vpn_endpoint_id: str,
    subnet_id: str,
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> AssociateClientVpnTargetNetworkResult:
    """Associate client vpn target network.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        subnet_id: Subnet id.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    kwargs["SubnetId"] = subnet_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("AssociateClientVpnTargetNetwork", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate client vpn target network") from exc
    return AssociateClientVpnTargetNetworkResult(
        association_id=resp.get("AssociationId"),
        status=resp.get("Status"),
    )


async def associate_dhcp_options(
    dhcp_options_id: str,
    vpc_id: str,
    region_name: str | None = None,
) -> None:
    """Associate dhcp options.

    Args:
        dhcp_options_id: Dhcp options id.
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["DhcpOptionsId"] = dhcp_options_id
    kwargs["VpcId"] = vpc_id
    try:
        await client.call("AssociateDhcpOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate dhcp options") from exc
    return None


async def associate_enclave_certificate_iam_role(
    certificate_arn: str,
    role_arn: str,
    region_name: str | None = None,
) -> AssociateEnclaveCertificateIamRoleResult:
    """Associate enclave certificate iam role.

    Args:
        certificate_arn: Certificate arn.
        role_arn: Role arn.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CertificateArn"] = certificate_arn
    kwargs["RoleArn"] = role_arn
    try:
        resp = await client.call("AssociateEnclaveCertificateIamRole", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate enclave certificate iam role") from exc
    return AssociateEnclaveCertificateIamRoleResult(
        certificate_s3_bucket_name=resp.get("CertificateS3BucketName"),
        certificate_s3_object_key=resp.get("CertificateS3ObjectKey"),
        encryption_kms_key_id=resp.get("EncryptionKmsKeyId"),
    )


async def associate_iam_instance_profile(
    iam_instance_profile: dict[str, Any],
    instance_id: str,
    region_name: str | None = None,
) -> AssociateIamInstanceProfileResult:
    """Associate iam instance profile.

    Args:
        iam_instance_profile: Iam instance profile.
        instance_id: Instance id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IamInstanceProfile"] = iam_instance_profile
    kwargs["InstanceId"] = instance_id
    try:
        resp = await client.call("AssociateIamInstanceProfile", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate iam instance profile") from exc
    return AssociateIamInstanceProfileResult(
        iam_instance_profile_association=resp.get("IamInstanceProfileAssociation"),
    )


async def associate_instance_event_window(
    instance_event_window_id: str,
    association_target: dict[str, Any],
    region_name: str | None = None,
) -> AssociateInstanceEventWindowResult:
    """Associate instance event window.

    Args:
        instance_event_window_id: Instance event window id.
        association_target: Association target.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceEventWindowId"] = instance_event_window_id
    kwargs["AssociationTarget"] = association_target
    try:
        resp = await client.call("AssociateInstanceEventWindow", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate instance event window") from exc
    return AssociateInstanceEventWindowResult(
        instance_event_window=resp.get("InstanceEventWindow"),
    )


async def associate_ipam_byoasn(
    asn: str,
    cidr: str,
    region_name: str | None = None,
) -> AssociateIpamByoasnResult:
    """Associate ipam byoasn.

    Args:
        asn: Asn.
        cidr: Cidr.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Asn"] = asn
    kwargs["Cidr"] = cidr
    try:
        resp = await client.call("AssociateIpamByoasn", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate ipam byoasn") from exc
    return AssociateIpamByoasnResult(
        asn_association=resp.get("AsnAssociation"),
    )


async def associate_ipam_resource_discovery(
    ipam_id: str,
    ipam_resource_discovery_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> AssociateIpamResourceDiscoveryResult:
    """Associate ipam resource discovery.

    Args:
        ipam_id: Ipam id.
        ipam_resource_discovery_id: Ipam resource discovery id.
        tag_specifications: Tag specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamId"] = ipam_id
    kwargs["IpamResourceDiscoveryId"] = ipam_resource_discovery_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("AssociateIpamResourceDiscovery", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate ipam resource discovery") from exc
    return AssociateIpamResourceDiscoveryResult(
        ipam_resource_discovery_association=resp.get("IpamResourceDiscoveryAssociation"),
    )


async def associate_nat_gateway_address(
    nat_gateway_id: str,
    allocation_ids: list[str],
    *,
    private_ip_addresses: list[str] | None = None,
    region_name: str | None = None,
) -> AssociateNatGatewayAddressResult:
    """Associate nat gateway address.

    Args:
        nat_gateway_id: Nat gateway id.
        allocation_ids: Allocation ids.
        private_ip_addresses: Private ip addresses.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NatGatewayId"] = nat_gateway_id
    kwargs["AllocationIds"] = allocation_ids
    if private_ip_addresses is not None:
        kwargs["PrivateIpAddresses"] = private_ip_addresses
    try:
        resp = await client.call("AssociateNatGatewayAddress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate nat gateway address") from exc
    return AssociateNatGatewayAddressResult(
        nat_gateway_id=resp.get("NatGatewayId"),
        nat_gateway_addresses=resp.get("NatGatewayAddresses"),
    )


async def associate_route_server(
    route_server_id: str,
    vpc_id: str,
    region_name: str | None = None,
) -> AssociateRouteServerResult:
    """Associate route server.

    Args:
        route_server_id: Route server id.
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerId"] = route_server_id
    kwargs["VpcId"] = vpc_id
    try:
        resp = await client.call("AssociateRouteServer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate route server") from exc
    return AssociateRouteServerResult(
        route_server_association=resp.get("RouteServerAssociation"),
    )


async def associate_route_table(
    route_table_id: str,
    *,
    gateway_id: str | None = None,
    public_ipv4_pool: str | None = None,
    subnet_id: str | None = None,
    region_name: str | None = None,
) -> AssociateRouteTableResult:
    """Associate route table.

    Args:
        route_table_id: Route table id.
        gateway_id: Gateway id.
        public_ipv4_pool: Public ipv4 pool.
        subnet_id: Subnet id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteTableId"] = route_table_id
    if gateway_id is not None:
        kwargs["GatewayId"] = gateway_id
    if public_ipv4_pool is not None:
        kwargs["PublicIpv4Pool"] = public_ipv4_pool
    if subnet_id is not None:
        kwargs["SubnetId"] = subnet_id
    try:
        resp = await client.call("AssociateRouteTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate route table") from exc
    return AssociateRouteTableResult(
        association_id=resp.get("AssociationId"),
        association_state=resp.get("AssociationState"),
    )


async def associate_security_group_vpc(
    group_id: str,
    vpc_id: str,
    region_name: str | None = None,
) -> AssociateSecurityGroupVpcResult:
    """Associate security group vpc.

    Args:
        group_id: Group id.
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["GroupId"] = group_id
    kwargs["VpcId"] = vpc_id
    try:
        resp = await client.call("AssociateSecurityGroupVpc", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate security group vpc") from exc
    return AssociateSecurityGroupVpcResult(
        state=resp.get("State"),
    )


async def associate_subnet_cidr_block(
    subnet_id: str,
    *,
    ipv6_ipam_pool_id: str | None = None,
    ipv6_netmask_length: int | None = None,
    ipv6_cidr_block: str | None = None,
    region_name: str | None = None,
) -> AssociateSubnetCidrBlockResult:
    """Associate subnet cidr block.

    Args:
        subnet_id: Subnet id.
        ipv6_ipam_pool_id: Ipv6 ipam pool id.
        ipv6_netmask_length: Ipv6 netmask length.
        ipv6_cidr_block: Ipv6 cidr block.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SubnetId"] = subnet_id
    if ipv6_ipam_pool_id is not None:
        kwargs["Ipv6IpamPoolId"] = ipv6_ipam_pool_id
    if ipv6_netmask_length is not None:
        kwargs["Ipv6NetmaskLength"] = ipv6_netmask_length
    if ipv6_cidr_block is not None:
        kwargs["Ipv6CidrBlock"] = ipv6_cidr_block
    try:
        resp = await client.call("AssociateSubnetCidrBlock", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate subnet cidr block") from exc
    return AssociateSubnetCidrBlockResult(
        ipv6_cidr_block_association=resp.get("Ipv6CidrBlockAssociation"),
        subnet_id=resp.get("SubnetId"),
    )


async def associate_transit_gateway_multicast_domain(
    transit_gateway_multicast_domain_id: str,
    transit_gateway_attachment_id: str,
    subnet_ids: list[str],
    region_name: str | None = None,
) -> AssociateTransitGatewayMulticastDomainResult:
    """Associate transit gateway multicast domain.

    Args:
        transit_gateway_multicast_domain_id: Transit gateway multicast domain id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        subnet_ids: Subnet ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayMulticastDomainId"] = transit_gateway_multicast_domain_id
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    kwargs["SubnetIds"] = subnet_ids
    try:
        resp = await client.call("AssociateTransitGatewayMulticastDomain", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate transit gateway multicast domain") from exc
    return AssociateTransitGatewayMulticastDomainResult(
        associations=resp.get("Associations"),
    )


async def associate_transit_gateway_policy_table(
    transit_gateway_policy_table_id: str,
    transit_gateway_attachment_id: str,
    region_name: str | None = None,
) -> AssociateTransitGatewayPolicyTableResult:
    """Associate transit gateway policy table.

    Args:
        transit_gateway_policy_table_id: Transit gateway policy table id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayPolicyTableId"] = transit_gateway_policy_table_id
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    try:
        resp = await client.call("AssociateTransitGatewayPolicyTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate transit gateway policy table") from exc
    return AssociateTransitGatewayPolicyTableResult(
        association=resp.get("Association"),
    )


async def associate_transit_gateway_route_table(
    transit_gateway_route_table_id: str,
    transit_gateway_attachment_id: str,
    region_name: str | None = None,
) -> AssociateTransitGatewayRouteTableResult:
    """Associate transit gateway route table.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    try:
        resp = await client.call("AssociateTransitGatewayRouteTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate transit gateway route table") from exc
    return AssociateTransitGatewayRouteTableResult(
        association=resp.get("Association"),
    )


async def associate_trunk_interface(
    branch_interface_id: str,
    trunk_interface_id: str,
    *,
    vlan_id: int | None = None,
    gre_key: int | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> AssociateTrunkInterfaceResult:
    """Associate trunk interface.

    Args:
        branch_interface_id: Branch interface id.
        trunk_interface_id: Trunk interface id.
        vlan_id: Vlan id.
        gre_key: Gre key.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["BranchInterfaceId"] = branch_interface_id
    kwargs["TrunkInterfaceId"] = trunk_interface_id
    if vlan_id is not None:
        kwargs["VlanId"] = vlan_id
    if gre_key is not None:
        kwargs["GreKey"] = gre_key
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("AssociateTrunkInterface", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate trunk interface") from exc
    return AssociateTrunkInterfaceResult(
        interface_association=resp.get("InterfaceAssociation"),
        client_token=resp.get("ClientToken"),
    )


async def associate_vpc_cidr_block(
    vpc_id: str,
    *,
    cidr_block: str | None = None,
    ipv6_cidr_block_network_border_group: str | None = None,
    ipv6_pool: str | None = None,
    ipv6_cidr_block: str | None = None,
    ipv4_ipam_pool_id: str | None = None,
    ipv4_netmask_length: int | None = None,
    ipv6_ipam_pool_id: str | None = None,
    ipv6_netmask_length: int | None = None,
    amazon_provided_ipv6_cidr_block: bool | None = None,
    region_name: str | None = None,
) -> AssociateVpcCidrBlockResult:
    """Associate vpc cidr block.

    Args:
        vpc_id: Vpc id.
        cidr_block: Cidr block.
        ipv6_cidr_block_network_border_group: Ipv6 cidr block network border group.
        ipv6_pool: Ipv6 pool.
        ipv6_cidr_block: Ipv6 cidr block.
        ipv4_ipam_pool_id: Ipv4 ipam pool id.
        ipv4_netmask_length: Ipv4 netmask length.
        ipv6_ipam_pool_id: Ipv6 ipam pool id.
        ipv6_netmask_length: Ipv6 netmask length.
        amazon_provided_ipv6_cidr_block: Amazon provided ipv6 cidr block.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    if cidr_block is not None:
        kwargs["CidrBlock"] = cidr_block
    if ipv6_cidr_block_network_border_group is not None:
        kwargs["Ipv6CidrBlockNetworkBorderGroup"] = ipv6_cidr_block_network_border_group
    if ipv6_pool is not None:
        kwargs["Ipv6Pool"] = ipv6_pool
    if ipv6_cidr_block is not None:
        kwargs["Ipv6CidrBlock"] = ipv6_cidr_block
    if ipv4_ipam_pool_id is not None:
        kwargs["Ipv4IpamPoolId"] = ipv4_ipam_pool_id
    if ipv4_netmask_length is not None:
        kwargs["Ipv4NetmaskLength"] = ipv4_netmask_length
    if ipv6_ipam_pool_id is not None:
        kwargs["Ipv6IpamPoolId"] = ipv6_ipam_pool_id
    if ipv6_netmask_length is not None:
        kwargs["Ipv6NetmaskLength"] = ipv6_netmask_length
    if amazon_provided_ipv6_cidr_block is not None:
        kwargs["AmazonProvidedIpv6CidrBlock"] = amazon_provided_ipv6_cidr_block
    try:
        resp = await client.call("AssociateVpcCidrBlock", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to associate vpc cidr block") from exc
    return AssociateVpcCidrBlockResult(
        ipv6_cidr_block_association=resp.get("Ipv6CidrBlockAssociation"),
        cidr_block_association=resp.get("CidrBlockAssociation"),
        vpc_id=resp.get("VpcId"),
    )


async def attach_classic_link_vpc(
    instance_id: str,
    vpc_id: str,
    groups: list[str],
    region_name: str | None = None,
) -> AttachClassicLinkVpcResult:
    """Attach classic link vpc.

    Args:
        instance_id: Instance id.
        vpc_id: Vpc id.
        groups: Groups.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["VpcId"] = vpc_id
    kwargs["Groups"] = groups
    try:
        resp = await client.call("AttachClassicLinkVpc", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to attach classic link vpc") from exc
    return AttachClassicLinkVpcResult(
        return_value=resp.get("Return"),
    )


async def attach_internet_gateway(
    internet_gateway_id: str,
    vpc_id: str,
    region_name: str | None = None,
) -> None:
    """Attach internet gateway.

    Args:
        internet_gateway_id: Internet gateway id.
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InternetGatewayId"] = internet_gateway_id
    kwargs["VpcId"] = vpc_id
    try:
        await client.call("AttachInternetGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to attach internet gateway") from exc
    return None


async def attach_network_interface(
    network_interface_id: str,
    instance_id: str,
    device_index: int,
    *,
    network_card_index: int | None = None,
    ena_srd_specification: dict[str, Any] | None = None,
    ena_queue_count: int | None = None,
    region_name: str | None = None,
) -> AttachNetworkInterfaceResult:
    """Attach network interface.

    Args:
        network_interface_id: Network interface id.
        instance_id: Instance id.
        device_index: Device index.
        network_card_index: Network card index.
        ena_srd_specification: Ena srd specification.
        ena_queue_count: Ena queue count.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    kwargs["InstanceId"] = instance_id
    kwargs["DeviceIndex"] = device_index
    if network_card_index is not None:
        kwargs["NetworkCardIndex"] = network_card_index
    if ena_srd_specification is not None:
        kwargs["EnaSrdSpecification"] = ena_srd_specification
    if ena_queue_count is not None:
        kwargs["EnaQueueCount"] = ena_queue_count
    try:
        resp = await client.call("AttachNetworkInterface", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to attach network interface") from exc
    return AttachNetworkInterfaceResult(
        attachment_id=resp.get("AttachmentId"),
        network_card_index=resp.get("NetworkCardIndex"),
    )


async def attach_verified_access_trust_provider(
    verified_access_instance_id: str,
    verified_access_trust_provider_id: str,
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> AttachVerifiedAccessTrustProviderResult:
    """Attach verified access trust provider.

    Args:
        verified_access_instance_id: Verified access instance id.
        verified_access_trust_provider_id: Verified access trust provider id.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessInstanceId"] = verified_access_instance_id
    kwargs["VerifiedAccessTrustProviderId"] = verified_access_trust_provider_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("AttachVerifiedAccessTrustProvider", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to attach verified access trust provider") from exc
    return AttachVerifiedAccessTrustProviderResult(
        verified_access_trust_provider=resp.get("VerifiedAccessTrustProvider"),
        verified_access_instance=resp.get("VerifiedAccessInstance"),
    )


async def attach_volume(
    device: str,
    instance_id: str,
    volume_id: str,
    region_name: str | None = None,
) -> AttachVolumeResult:
    """Attach volume.

    Args:
        device: Device.
        instance_id: Instance id.
        volume_id: Volume id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Device"] = device
    kwargs["InstanceId"] = instance_id
    kwargs["VolumeId"] = volume_id
    try:
        resp = await client.call("AttachVolume", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to attach volume") from exc
    return AttachVolumeResult(
        delete_on_termination=resp.get("DeleteOnTermination"),
        associated_resource=resp.get("AssociatedResource"),
        instance_owning_service=resp.get("InstanceOwningService"),
        volume_id=resp.get("VolumeId"),
        instance_id=resp.get("InstanceId"),
        device=resp.get("Device"),
        state=resp.get("State"),
        attach_time=resp.get("AttachTime"),
    )


async def attach_vpn_gateway(
    vpc_id: str,
    vpn_gateway_id: str,
    region_name: str | None = None,
) -> AttachVpnGatewayResult:
    """Attach vpn gateway.

    Args:
        vpc_id: Vpc id.
        vpn_gateway_id: Vpn gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    kwargs["VpnGatewayId"] = vpn_gateway_id
    try:
        resp = await client.call("AttachVpnGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to attach vpn gateway") from exc
    return AttachVpnGatewayResult(
        vpc_attachment=resp.get("VpcAttachment"),
    )


async def authorize_client_vpn_ingress(
    client_vpn_endpoint_id: str,
    target_network_cidr: str,
    *,
    access_group_id: str | None = None,
    authorize_all_groups: bool | None = None,
    description: str | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> AuthorizeClientVpnIngressResult:
    """Authorize client vpn ingress.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        target_network_cidr: Target network cidr.
        access_group_id: Access group id.
        authorize_all_groups: Authorize all groups.
        description: Description.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    kwargs["TargetNetworkCidr"] = target_network_cidr
    if access_group_id is not None:
        kwargs["AccessGroupId"] = access_group_id
    if authorize_all_groups is not None:
        kwargs["AuthorizeAllGroups"] = authorize_all_groups
    if description is not None:
        kwargs["Description"] = description
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("AuthorizeClientVpnIngress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to authorize client vpn ingress") from exc
    return AuthorizeClientVpnIngressResult(
        status=resp.get("Status"),
    )


async def authorize_security_group_egress(
    group_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    source_security_group_name: str | None = None,
    source_security_group_owner_id: str | None = None,
    ip_protocol: str | None = None,
    from_port: int | None = None,
    to_port: int | None = None,
    cidr_ip: str | None = None,
    ip_permissions: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> AuthorizeSecurityGroupEgressResult:
    """Authorize security group egress.

    Args:
        group_id: Group id.
        tag_specifications: Tag specifications.
        source_security_group_name: Source security group name.
        source_security_group_owner_id: Source security group owner id.
        ip_protocol: Ip protocol.
        from_port: From port.
        to_port: To port.
        cidr_ip: Cidr ip.
        ip_permissions: Ip permissions.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["GroupId"] = group_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if source_security_group_name is not None:
        kwargs["SourceSecurityGroupName"] = source_security_group_name
    if source_security_group_owner_id is not None:
        kwargs["SourceSecurityGroupOwnerId"] = source_security_group_owner_id
    if ip_protocol is not None:
        kwargs["IpProtocol"] = ip_protocol
    if from_port is not None:
        kwargs["FromPort"] = from_port
    if to_port is not None:
        kwargs["ToPort"] = to_port
    if cidr_ip is not None:
        kwargs["CidrIp"] = cidr_ip
    if ip_permissions is not None:
        kwargs["IpPermissions"] = ip_permissions
    try:
        resp = await client.call("AuthorizeSecurityGroupEgress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to authorize security group egress") from exc
    return AuthorizeSecurityGroupEgressResult(
        return_value=resp.get("Return"),
        security_group_rules=resp.get("SecurityGroupRules"),
    )


async def authorize_security_group_ingress(
    *,
    cidr_ip: str | None = None,
    from_port: int | None = None,
    group_id: str | None = None,
    group_name: str | None = None,
    ip_permissions: list[dict[str, Any]] | None = None,
    ip_protocol: str | None = None,
    source_security_group_name: str | None = None,
    source_security_group_owner_id: str | None = None,
    to_port: int | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> AuthorizeSecurityGroupIngressResult:
    """Authorize security group ingress.

    Args:
        cidr_ip: Cidr ip.
        from_port: From port.
        group_id: Group id.
        group_name: Group name.
        ip_permissions: Ip permissions.
        ip_protocol: Ip protocol.
        source_security_group_name: Source security group name.
        source_security_group_owner_id: Source security group owner id.
        to_port: To port.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if cidr_ip is not None:
        kwargs["CidrIp"] = cidr_ip
    if from_port is not None:
        kwargs["FromPort"] = from_port
    if group_id is not None:
        kwargs["GroupId"] = group_id
    if group_name is not None:
        kwargs["GroupName"] = group_name
    if ip_permissions is not None:
        kwargs["IpPermissions"] = ip_permissions
    if ip_protocol is not None:
        kwargs["IpProtocol"] = ip_protocol
    if source_security_group_name is not None:
        kwargs["SourceSecurityGroupName"] = source_security_group_name
    if source_security_group_owner_id is not None:
        kwargs["SourceSecurityGroupOwnerId"] = source_security_group_owner_id
    if to_port is not None:
        kwargs["ToPort"] = to_port
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("AuthorizeSecurityGroupIngress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to authorize security group ingress") from exc
    return AuthorizeSecurityGroupIngressResult(
        return_value=resp.get("Return"),
        security_group_rules=resp.get("SecurityGroupRules"),
    )


async def bundle_instance(
    instance_id: str,
    storage: dict[str, Any],
    region_name: str | None = None,
) -> BundleInstanceResult:
    """Bundle instance.

    Args:
        instance_id: Instance id.
        storage: Storage.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["Storage"] = storage
    try:
        resp = await client.call("BundleInstance", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to bundle instance") from exc
    return BundleInstanceResult(
        bundle_task=resp.get("BundleTask"),
    )


async def cancel_bundle_task(
    bundle_id: str,
    region_name: str | None = None,
) -> CancelBundleTaskResult:
    """Cancel bundle task.

    Args:
        bundle_id: Bundle id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["BundleId"] = bundle_id
    try:
        resp = await client.call("CancelBundleTask", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to cancel bundle task") from exc
    return CancelBundleTaskResult(
        bundle_task=resp.get("BundleTask"),
    )


async def cancel_capacity_reservation(
    capacity_reservation_id: str,
    region_name: str | None = None,
) -> CancelCapacityReservationResult:
    """Cancel capacity reservation.

    Args:
        capacity_reservation_id: Capacity reservation id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityReservationId"] = capacity_reservation_id
    try:
        resp = await client.call("CancelCapacityReservation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to cancel capacity reservation") from exc
    return CancelCapacityReservationResult(
        return_value=resp.get("Return"),
    )


async def cancel_capacity_reservation_fleets(
    capacity_reservation_fleet_ids: list[str],
    region_name: str | None = None,
) -> CancelCapacityReservationFleetsResult:
    """Cancel capacity reservation fleets.

    Args:
        capacity_reservation_fleet_ids: Capacity reservation fleet ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityReservationFleetIds"] = capacity_reservation_fleet_ids
    try:
        resp = await client.call("CancelCapacityReservationFleets", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to cancel capacity reservation fleets") from exc
    return CancelCapacityReservationFleetsResult(
        successful_fleet_cancellations=resp.get("SuccessfulFleetCancellations"),
        failed_fleet_cancellations=resp.get("FailedFleetCancellations"),
    )


async def cancel_conversion_task(
    conversion_task_id: str,
    *,
    reason_message: str | None = None,
    region_name: str | None = None,
) -> None:
    """Cancel conversion task.

    Args:
        conversion_task_id: Conversion task id.
        reason_message: Reason message.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ConversionTaskId"] = conversion_task_id
    if reason_message is not None:
        kwargs["ReasonMessage"] = reason_message
    try:
        await client.call("CancelConversionTask", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to cancel conversion task") from exc
    return None


async def cancel_declarative_policies_report(
    report_id: str,
    region_name: str | None = None,
) -> CancelDeclarativePoliciesReportResult:
    """Cancel declarative policies report.

    Args:
        report_id: Report id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ReportId"] = report_id
    try:
        resp = await client.call("CancelDeclarativePoliciesReport", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to cancel declarative policies report") from exc
    return CancelDeclarativePoliciesReportResult(
        return_value=resp.get("Return"),
    )


async def cancel_export_task(
    export_task_id: str,
    region_name: str | None = None,
) -> None:
    """Cancel export task.

    Args:
        export_task_id: Export task id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ExportTaskId"] = export_task_id
    try:
        await client.call("CancelExportTask", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to cancel export task") from exc
    return None


async def cancel_image_launch_permission(
    image_id: str,
    region_name: str | None = None,
) -> CancelImageLaunchPermissionResult:
    """Cancel image launch permission.

    Args:
        image_id: Image id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    try:
        resp = await client.call("CancelImageLaunchPermission", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to cancel image launch permission") from exc
    return CancelImageLaunchPermissionResult(
        return_value=resp.get("Return"),
    )


async def cancel_import_task(
    *,
    cancel_reason: str | None = None,
    import_task_id: str | None = None,
    region_name: str | None = None,
) -> CancelImportTaskResult:
    """Cancel import task.

    Args:
        cancel_reason: Cancel reason.
        import_task_id: Import task id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if cancel_reason is not None:
        kwargs["CancelReason"] = cancel_reason
    if import_task_id is not None:
        kwargs["ImportTaskId"] = import_task_id
    try:
        resp = await client.call("CancelImportTask", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to cancel import task") from exc
    return CancelImportTaskResult(
        import_task_id=resp.get("ImportTaskId"),
        previous_state=resp.get("PreviousState"),
        state=resp.get("State"),
    )


async def cancel_reserved_instances_listing(
    reserved_instances_listing_id: str,
    region_name: str | None = None,
) -> CancelReservedInstancesListingResult:
    """Cancel reserved instances listing.

    Args:
        reserved_instances_listing_id: Reserved instances listing id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ReservedInstancesListingId"] = reserved_instances_listing_id
    try:
        resp = await client.call("CancelReservedInstancesListing", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to cancel reserved instances listing") from exc
    return CancelReservedInstancesListingResult(
        reserved_instances_listings=resp.get("ReservedInstancesListings"),
    )


async def cancel_spot_fleet_requests(
    spot_fleet_request_ids: list[str],
    terminate_instances: bool,
    region_name: str | None = None,
) -> CancelSpotFleetRequestsResult:
    """Cancel spot fleet requests.

    Args:
        spot_fleet_request_ids: Spot fleet request ids.
        terminate_instances: Terminate instances.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SpotFleetRequestIds"] = spot_fleet_request_ids
    kwargs["TerminateInstances"] = terminate_instances
    try:
        resp = await client.call("CancelSpotFleetRequests", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to cancel spot fleet requests") from exc
    return CancelSpotFleetRequestsResult(
        successful_fleet_requests=resp.get("SuccessfulFleetRequests"),
        unsuccessful_fleet_requests=resp.get("UnsuccessfulFleetRequests"),
    )


async def cancel_spot_instance_requests(
    spot_instance_request_ids: list[str],
    region_name: str | None = None,
) -> CancelSpotInstanceRequestsResult:
    """Cancel spot instance requests.

    Args:
        spot_instance_request_ids: Spot instance request ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SpotInstanceRequestIds"] = spot_instance_request_ids
    try:
        resp = await client.call("CancelSpotInstanceRequests", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to cancel spot instance requests") from exc
    return CancelSpotInstanceRequestsResult(
        cancelled_spot_instance_requests=resp.get("CancelledSpotInstanceRequests"),
    )


async def confirm_product_instance(
    instance_id: str,
    product_code: str,
    region_name: str | None = None,
) -> ConfirmProductInstanceResult:
    """Confirm product instance.

    Args:
        instance_id: Instance id.
        product_code: Product code.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["ProductCode"] = product_code
    try:
        resp = await client.call("ConfirmProductInstance", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to confirm product instance") from exc
    return ConfirmProductInstanceResult(
        return_value=resp.get("Return"),
        owner_id=resp.get("OwnerId"),
    )


async def copy_fpga_image(
    source_fpga_image_id: str,
    source_region: str,
    *,
    description: str | None = None,
    name: str | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CopyFpgaImageResult:
    """Copy fpga image.

    Args:
        source_fpga_image_id: Source fpga image id.
        source_region: Source region.
        description: Description.
        name: Name.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SourceFpgaImageId"] = source_fpga_image_id
    kwargs["SourceRegion"] = source_region
    if description is not None:
        kwargs["Description"] = description
    if name is not None:
        kwargs["Name"] = name
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CopyFpgaImage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to copy fpga image") from exc
    return CopyFpgaImageResult(
        fpga_image_id=resp.get("FpgaImageId"),
    )


async def copy_image(
    name: str,
    source_image_id: str,
    source_region: str,
    *,
    client_token: str | None = None,
    description: str | None = None,
    encrypted: bool | None = None,
    kms_key_id: str | None = None,
    destination_outpost_arn: str | None = None,
    copy_image_tags: bool | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    snapshot_copy_completion_duration_minutes: int | None = None,
    destination_availability_zone: str | None = None,
    destination_availability_zone_id: str | None = None,
    region_name: str | None = None,
) -> CopyImageResult:
    """Copy image.

    Args:
        name: Name.
        source_image_id: Source image id.
        source_region: Source region.
        client_token: Client token.
        description: Description.
        encrypted: Encrypted.
        kms_key_id: Kms key id.
        destination_outpost_arn: Destination outpost arn.
        copy_image_tags: Copy image tags.
        tag_specifications: Tag specifications.
        snapshot_copy_completion_duration_minutes: Snapshot copy completion duration minutes.
        destination_availability_zone: Destination availability zone.
        destination_availability_zone_id: Destination availability zone id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Name"] = name
    kwargs["SourceImageId"] = source_image_id
    kwargs["SourceRegion"] = source_region
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if description is not None:
        kwargs["Description"] = description
    if encrypted is not None:
        kwargs["Encrypted"] = encrypted
    if kms_key_id is not None:
        kwargs["KmsKeyId"] = kms_key_id
    if destination_outpost_arn is not None:
        kwargs["DestinationOutpostArn"] = destination_outpost_arn
    if copy_image_tags is not None:
        kwargs["CopyImageTags"] = copy_image_tags
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if snapshot_copy_completion_duration_minutes is not None:
        kwargs["SnapshotCopyCompletionDurationMinutes"] = snapshot_copy_completion_duration_minutes
    if destination_availability_zone is not None:
        kwargs["DestinationAvailabilityZone"] = destination_availability_zone
    if destination_availability_zone_id is not None:
        kwargs["DestinationAvailabilityZoneId"] = destination_availability_zone_id
    try:
        resp = await client.call("CopyImage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to copy image") from exc
    return CopyImageResult(
        image_id=resp.get("ImageId"),
    )


async def copy_snapshot(
    source_region: str,
    source_snapshot_id: str,
    *,
    description: str | None = None,
    destination_outpost_arn: str | None = None,
    destination_region: str | None = None,
    encrypted: bool | None = None,
    kms_key_id: str | None = None,
    presigned_url: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    completion_duration_minutes: int | None = None,
    destination_availability_zone: str | None = None,
    region_name: str | None = None,
) -> CopySnapshotResult:
    """Copy snapshot.

    Args:
        source_region: Source region.
        source_snapshot_id: Source snapshot id.
        description: Description.
        destination_outpost_arn: Destination outpost arn.
        destination_region: Destination region.
        encrypted: Encrypted.
        kms_key_id: Kms key id.
        presigned_url: Presigned url.
        tag_specifications: Tag specifications.
        completion_duration_minutes: Completion duration minutes.
        destination_availability_zone: Destination availability zone.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SourceRegion"] = source_region
    kwargs["SourceSnapshotId"] = source_snapshot_id
    if description is not None:
        kwargs["Description"] = description
    if destination_outpost_arn is not None:
        kwargs["DestinationOutpostArn"] = destination_outpost_arn
    if destination_region is not None:
        kwargs["DestinationRegion"] = destination_region
    if encrypted is not None:
        kwargs["Encrypted"] = encrypted
    if kms_key_id is not None:
        kwargs["KmsKeyId"] = kms_key_id
    if presigned_url is not None:
        kwargs["PresignedUrl"] = presigned_url
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if completion_duration_minutes is not None:
        kwargs["CompletionDurationMinutes"] = completion_duration_minutes
    if destination_availability_zone is not None:
        kwargs["DestinationAvailabilityZone"] = destination_availability_zone
    try:
        resp = await client.call("CopySnapshot", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to copy snapshot") from exc
    return CopySnapshotResult(
        tags=resp.get("Tags"),
        snapshot_id=resp.get("SnapshotId"),
    )


async def copy_volumes(
    source_volume_id: str,
    *,
    iops: int | None = None,
    size: int | None = None,
    volume_type: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    multi_attach_enabled: bool | None = None,
    throughput: int | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CopyVolumesResult:
    """Copy volumes.

    Args:
        source_volume_id: Source volume id.
        iops: Iops.
        size: Size.
        volume_type: Volume type.
        tag_specifications: Tag specifications.
        multi_attach_enabled: Multi attach enabled.
        throughput: Throughput.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SourceVolumeId"] = source_volume_id
    if iops is not None:
        kwargs["Iops"] = iops
    if size is not None:
        kwargs["Size"] = size
    if volume_type is not None:
        kwargs["VolumeType"] = volume_type
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if multi_attach_enabled is not None:
        kwargs["MultiAttachEnabled"] = multi_attach_enabled
    if throughput is not None:
        kwargs["Throughput"] = throughput
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CopyVolumes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to copy volumes") from exc
    return CopyVolumesResult(
        volumes=resp.get("Volumes"),
    )


async def create_capacity_manager_data_export(
    s3_bucket_name: str,
    schedule: str,
    output_format: str,
    *,
    s3_bucket_prefix: str | None = None,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateCapacityManagerDataExportResult:
    """Create capacity manager data export.

    Args:
        s3_bucket_name: S3 bucket name.
        schedule: Schedule.
        output_format: Output format.
        s3_bucket_prefix: S3 bucket prefix.
        client_token: Client token.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["S3BucketName"] = s3_bucket_name
    kwargs["Schedule"] = schedule
    kwargs["OutputFormat"] = output_format
    if s3_bucket_prefix is not None:
        kwargs["S3BucketPrefix"] = s3_bucket_prefix
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateCapacityManagerDataExport", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create capacity manager data export") from exc
    return CreateCapacityManagerDataExportResult(
        capacity_manager_data_export_id=resp.get("CapacityManagerDataExportId"),
    )


async def create_capacity_reservation(
    instance_type: str,
    instance_platform: str,
    instance_count: int,
    *,
    client_token: str | None = None,
    availability_zone: str | None = None,
    availability_zone_id: str | None = None,
    tenancy: str | None = None,
    ebs_optimized: bool | None = None,
    ephemeral_storage: bool | None = None,
    end_date: str | None = None,
    end_date_type: str | None = None,
    instance_match_criteria: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    outpost_arn: str | None = None,
    placement_group_arn: str | None = None,
    start_date: str | None = None,
    commitment_duration: int | None = None,
    delivery_preference: str | None = None,
    region_name: str | None = None,
) -> CreateCapacityReservationResult:
    """Create capacity reservation.

    Args:
        instance_type: Instance type.
        instance_platform: Instance platform.
        instance_count: Instance count.
        client_token: Client token.
        availability_zone: Availability zone.
        availability_zone_id: Availability zone id.
        tenancy: Tenancy.
        ebs_optimized: Ebs optimized.
        ephemeral_storage: Ephemeral storage.
        end_date: End date.
        end_date_type: End date type.
        instance_match_criteria: Instance match criteria.
        tag_specifications: Tag specifications.
        outpost_arn: Outpost arn.
        placement_group_arn: Placement group arn.
        start_date: Start date.
        commitment_duration: Commitment duration.
        delivery_preference: Delivery preference.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceType"] = instance_type
    kwargs["InstancePlatform"] = instance_platform
    kwargs["InstanceCount"] = instance_count
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if availability_zone is not None:
        kwargs["AvailabilityZone"] = availability_zone
    if availability_zone_id is not None:
        kwargs["AvailabilityZoneId"] = availability_zone_id
    if tenancy is not None:
        kwargs["Tenancy"] = tenancy
    if ebs_optimized is not None:
        kwargs["EbsOptimized"] = ebs_optimized
    if ephemeral_storage is not None:
        kwargs["EphemeralStorage"] = ephemeral_storage
    if end_date is not None:
        kwargs["EndDate"] = end_date
    if end_date_type is not None:
        kwargs["EndDateType"] = end_date_type
    if instance_match_criteria is not None:
        kwargs["InstanceMatchCriteria"] = instance_match_criteria
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if outpost_arn is not None:
        kwargs["OutpostArn"] = outpost_arn
    if placement_group_arn is not None:
        kwargs["PlacementGroupArn"] = placement_group_arn
    if start_date is not None:
        kwargs["StartDate"] = start_date
    if commitment_duration is not None:
        kwargs["CommitmentDuration"] = commitment_duration
    if delivery_preference is not None:
        kwargs["DeliveryPreference"] = delivery_preference
    try:
        resp = await client.call("CreateCapacityReservation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create capacity reservation") from exc
    return CreateCapacityReservationResult(
        capacity_reservation=resp.get("CapacityReservation"),
    )


async def create_capacity_reservation_by_splitting(
    source_capacity_reservation_id: str,
    instance_count: int,
    *,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateCapacityReservationBySplittingResult:
    """Create capacity reservation by splitting.

    Args:
        source_capacity_reservation_id: Source capacity reservation id.
        instance_count: Instance count.
        client_token: Client token.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SourceCapacityReservationId"] = source_capacity_reservation_id
    kwargs["InstanceCount"] = instance_count
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateCapacityReservationBySplitting", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create capacity reservation by splitting") from exc
    return CreateCapacityReservationBySplittingResult(
        source_capacity_reservation=resp.get("SourceCapacityReservation"),
        destination_capacity_reservation=resp.get("DestinationCapacityReservation"),
        instance_count=resp.get("InstanceCount"),
    )


async def create_capacity_reservation_fleet(
    instance_type_specifications: list[dict[str, Any]],
    total_target_capacity: int,
    *,
    allocation_strategy: str | None = None,
    client_token: str | None = None,
    tenancy: str | None = None,
    end_date: str | None = None,
    instance_match_criteria: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateCapacityReservationFleetResult:
    """Create capacity reservation fleet.

    Args:
        instance_type_specifications: Instance type specifications.
        total_target_capacity: Total target capacity.
        allocation_strategy: Allocation strategy.
        client_token: Client token.
        tenancy: Tenancy.
        end_date: End date.
        instance_match_criteria: Instance match criteria.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceTypeSpecifications"] = instance_type_specifications
    kwargs["TotalTargetCapacity"] = total_target_capacity
    if allocation_strategy is not None:
        kwargs["AllocationStrategy"] = allocation_strategy
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tenancy is not None:
        kwargs["Tenancy"] = tenancy
    if end_date is not None:
        kwargs["EndDate"] = end_date
    if instance_match_criteria is not None:
        kwargs["InstanceMatchCriteria"] = instance_match_criteria
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateCapacityReservationFleet", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create capacity reservation fleet") from exc
    return CreateCapacityReservationFleetResult(
        capacity_reservation_fleet_id=resp.get("CapacityReservationFleetId"),
        state=resp.get("State"),
        total_target_capacity=resp.get("TotalTargetCapacity"),
        total_fulfilled_capacity=resp.get("TotalFulfilledCapacity"),
        instance_match_criteria=resp.get("InstanceMatchCriteria"),
        allocation_strategy=resp.get("AllocationStrategy"),
        create_time=resp.get("CreateTime"),
        end_date=resp.get("EndDate"),
        tenancy=resp.get("Tenancy"),
        fleet_capacity_reservations=resp.get("FleetCapacityReservations"),
        tags=resp.get("Tags"),
    )


async def create_carrier_gateway(
    vpc_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateCarrierGatewayResult:
    """Create carrier gateway.

    Args:
        vpc_id: Vpc id.
        tag_specifications: Tag specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateCarrierGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create carrier gateway") from exc
    return CreateCarrierGatewayResult(
        carrier_gateway=resp.get("CarrierGateway"),
    )


async def create_client_vpn_endpoint(
    server_certificate_arn: str,
    authentication_options: list[dict[str, Any]],
    connection_log_options: dict[str, Any],
    *,
    client_cidr_block: str | None = None,
    dns_servers: list[str] | None = None,
    transport_protocol: str | None = None,
    vpn_port: int | None = None,
    description: str | None = None,
    split_tunnel: bool | None = None,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    security_group_ids: list[str] | None = None,
    vpc_id: str | None = None,
    self_service_portal: str | None = None,
    client_connect_options: dict[str, Any] | None = None,
    session_timeout_hours: int | None = None,
    client_login_banner_options: dict[str, Any] | None = None,
    client_route_enforcement_options: dict[str, Any] | None = None,
    disconnect_on_session_timeout: bool | None = None,
    endpoint_ip_address_type: str | None = None,
    traffic_ip_address_type: str | None = None,
    region_name: str | None = None,
) -> CreateClientVpnEndpointResult:
    """Create client vpn endpoint.

    Args:
        server_certificate_arn: Server certificate arn.
        authentication_options: Authentication options.
        connection_log_options: Connection log options.
        client_cidr_block: Client cidr block.
        dns_servers: Dns servers.
        transport_protocol: Transport protocol.
        vpn_port: Vpn port.
        description: Description.
        split_tunnel: Split tunnel.
        client_token: Client token.
        tag_specifications: Tag specifications.
        security_group_ids: Security group ids.
        vpc_id: Vpc id.
        self_service_portal: Self service portal.
        client_connect_options: Client connect options.
        session_timeout_hours: Session timeout hours.
        client_login_banner_options: Client login banner options.
        client_route_enforcement_options: Client route enforcement options.
        disconnect_on_session_timeout: Disconnect on session timeout.
        endpoint_ip_address_type: Endpoint ip address type.
        traffic_ip_address_type: Traffic ip address type.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ServerCertificateArn"] = server_certificate_arn
    kwargs["AuthenticationOptions"] = authentication_options
    kwargs["ConnectionLogOptions"] = connection_log_options
    if client_cidr_block is not None:
        kwargs["ClientCidrBlock"] = client_cidr_block
    if dns_servers is not None:
        kwargs["DnsServers"] = dns_servers
    if transport_protocol is not None:
        kwargs["TransportProtocol"] = transport_protocol
    if vpn_port is not None:
        kwargs["VpnPort"] = vpn_port
    if description is not None:
        kwargs["Description"] = description
    if split_tunnel is not None:
        kwargs["SplitTunnel"] = split_tunnel
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if security_group_ids is not None:
        kwargs["SecurityGroupIds"] = security_group_ids
    if vpc_id is not None:
        kwargs["VpcId"] = vpc_id
    if self_service_portal is not None:
        kwargs["SelfServicePortal"] = self_service_portal
    if client_connect_options is not None:
        kwargs["ClientConnectOptions"] = client_connect_options
    if session_timeout_hours is not None:
        kwargs["SessionTimeoutHours"] = session_timeout_hours
    if client_login_banner_options is not None:
        kwargs["ClientLoginBannerOptions"] = client_login_banner_options
    if client_route_enforcement_options is not None:
        kwargs["ClientRouteEnforcementOptions"] = client_route_enforcement_options
    if disconnect_on_session_timeout is not None:
        kwargs["DisconnectOnSessionTimeout"] = disconnect_on_session_timeout
    if endpoint_ip_address_type is not None:
        kwargs["EndpointIpAddressType"] = endpoint_ip_address_type
    if traffic_ip_address_type is not None:
        kwargs["TrafficIpAddressType"] = traffic_ip_address_type
    try:
        resp = await client.call("CreateClientVpnEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create client vpn endpoint") from exc
    return CreateClientVpnEndpointResult(
        client_vpn_endpoint_id=resp.get("ClientVpnEndpointId"),
        status=resp.get("Status"),
        dns_name=resp.get("DnsName"),
    )


async def create_client_vpn_route(
    client_vpn_endpoint_id: str,
    destination_cidr_block: str,
    target_vpc_subnet_id: str,
    *,
    description: str | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateClientVpnRouteResult:
    """Create client vpn route.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        destination_cidr_block: Destination cidr block.
        target_vpc_subnet_id: Target vpc subnet id.
        description: Description.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    kwargs["DestinationCidrBlock"] = destination_cidr_block
    kwargs["TargetVpcSubnetId"] = target_vpc_subnet_id
    if description is not None:
        kwargs["Description"] = description
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateClientVpnRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create client vpn route") from exc
    return CreateClientVpnRouteResult(
        status=resp.get("Status"),
    )


async def create_coip_cidr(
    cidr: str,
    coip_pool_id: str,
    region_name: str | None = None,
) -> CreateCoipCidrResult:
    """Create coip cidr.

    Args:
        cidr: Cidr.
        coip_pool_id: Coip pool id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Cidr"] = cidr
    kwargs["CoipPoolId"] = coip_pool_id
    try:
        resp = await client.call("CreateCoipCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create coip cidr") from exc
    return CreateCoipCidrResult(
        coip_cidr=resp.get("CoipCidr"),
    )


async def create_coip_pool(
    local_gateway_route_table_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateCoipPoolResult:
    """Create coip pool.

    Args:
        local_gateway_route_table_id: Local gateway route table id.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayRouteTableId"] = local_gateway_route_table_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateCoipPool", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create coip pool") from exc
    return CreateCoipPoolResult(
        coip_pool=resp.get("CoipPool"),
    )


async def create_customer_gateway(
    type_value: str,
    *,
    bgp_asn: int | None = None,
    public_ip: str | None = None,
    certificate_arn: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    device_name: str | None = None,
    ip_address: str | None = None,
    bgp_asn_extended: int | None = None,
    region_name: str | None = None,
) -> CreateCustomerGatewayResult:
    """Create customer gateway.

    Args:
        type_value: Type value.
        bgp_asn: Bgp asn.
        public_ip: Public ip.
        certificate_arn: Certificate arn.
        tag_specifications: Tag specifications.
        device_name: Device name.
        ip_address: Ip address.
        bgp_asn_extended: Bgp asn extended.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Type"] = type_value
    if bgp_asn is not None:
        kwargs["BgpAsn"] = bgp_asn
    if public_ip is not None:
        kwargs["PublicIp"] = public_ip
    if certificate_arn is not None:
        kwargs["CertificateArn"] = certificate_arn
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if device_name is not None:
        kwargs["DeviceName"] = device_name
    if ip_address is not None:
        kwargs["IpAddress"] = ip_address
    if bgp_asn_extended is not None:
        kwargs["BgpAsnExtended"] = bgp_asn_extended
    try:
        resp = await client.call("CreateCustomerGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create customer gateway") from exc
    return CreateCustomerGatewayResult(
        customer_gateway=resp.get("CustomerGateway"),
    )


async def create_default_subnet(
    *,
    availability_zone: str | None = None,
    ipv6_native: bool | None = None,
    availability_zone_id: str | None = None,
    region_name: str | None = None,
) -> CreateDefaultSubnetResult:
    """Create default subnet.

    Args:
        availability_zone: Availability zone.
        ipv6_native: Ipv6 native.
        availability_zone_id: Availability zone id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if availability_zone is not None:
        kwargs["AvailabilityZone"] = availability_zone
    if ipv6_native is not None:
        kwargs["Ipv6Native"] = ipv6_native
    if availability_zone_id is not None:
        kwargs["AvailabilityZoneId"] = availability_zone_id
    try:
        resp = await client.call("CreateDefaultSubnet", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create default subnet") from exc
    return CreateDefaultSubnetResult(
        subnet=resp.get("Subnet"),
    )


async def create_default_vpc(
    region_name: str | None = None,
) -> CreateDefaultVpcResult:
    """Create default vpc.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("CreateDefaultVpc", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create default vpc") from exc
    return CreateDefaultVpcResult(
        vpc=resp.get("Vpc"),
    )


async def create_delegate_mac_volume_ownership_task(
    instance_id: str,
    mac_credentials: str,
    *,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateDelegateMacVolumeOwnershipTaskResult:
    """Create delegate mac volume ownership task.

    Args:
        instance_id: Instance id.
        mac_credentials: Mac credentials.
        client_token: Client token.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["MacCredentials"] = mac_credentials
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateDelegateMacVolumeOwnershipTask", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create delegate mac volume ownership task") from exc
    return CreateDelegateMacVolumeOwnershipTaskResult(
        mac_modification_task=resp.get("MacModificationTask"),
    )


async def create_dhcp_options(
    dhcp_configurations: list[dict[str, Any]],
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateDhcpOptionsResult:
    """Create dhcp options.

    Args:
        dhcp_configurations: Dhcp configurations.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["DhcpConfigurations"] = dhcp_configurations
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateDhcpOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create dhcp options") from exc
    return CreateDhcpOptionsResult(
        dhcp_options=resp.get("DhcpOptions"),
    )


async def create_egress_only_internet_gateway(
    vpc_id: str,
    *,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateEgressOnlyInternetGatewayResult:
    """Create egress only internet gateway.

    Args:
        vpc_id: Vpc id.
        client_token: Client token.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateEgressOnlyInternetGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create egress only internet gateway") from exc
    return CreateEgressOnlyInternetGatewayResult(
        client_token=resp.get("ClientToken"),
        egress_only_internet_gateway=resp.get("EgressOnlyInternetGateway"),
    )


async def create_fleet(
    launch_template_configs: list[dict[str, Any]],
    target_capacity_specification: dict[str, Any],
    *,
    client_token: str | None = None,
    spot_options: dict[str, Any] | None = None,
    on_demand_options: dict[str, Any] | None = None,
    excess_capacity_termination_policy: str | None = None,
    terminate_instances_with_expiration: bool | None = None,
    type_value: str | None = None,
    valid_from: str | None = None,
    valid_until: str | None = None,
    replace_unhealthy_instances: bool | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    context: str | None = None,
    region_name: str | None = None,
) -> CreateFleetResult:
    """Create fleet.

    Args:
        launch_template_configs: Launch template configs.
        target_capacity_specification: Target capacity specification.
        client_token: Client token.
        spot_options: Spot options.
        on_demand_options: On demand options.
        excess_capacity_termination_policy: Excess capacity termination policy.
        terminate_instances_with_expiration: Terminate instances with expiration.
        type_value: Type value.
        valid_from: Valid from.
        valid_until: Valid until.
        replace_unhealthy_instances: Replace unhealthy instances.
        tag_specifications: Tag specifications.
        context: Context.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LaunchTemplateConfigs"] = launch_template_configs
    kwargs["TargetCapacitySpecification"] = target_capacity_specification
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if spot_options is not None:
        kwargs["SpotOptions"] = spot_options
    if on_demand_options is not None:
        kwargs["OnDemandOptions"] = on_demand_options
    if excess_capacity_termination_policy is not None:
        kwargs["ExcessCapacityTerminationPolicy"] = excess_capacity_termination_policy
    if terminate_instances_with_expiration is not None:
        kwargs["TerminateInstancesWithExpiration"] = terminate_instances_with_expiration
    if type_value is not None:
        kwargs["Type"] = type_value
    if valid_from is not None:
        kwargs["ValidFrom"] = valid_from
    if valid_until is not None:
        kwargs["ValidUntil"] = valid_until
    if replace_unhealthy_instances is not None:
        kwargs["ReplaceUnhealthyInstances"] = replace_unhealthy_instances
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if context is not None:
        kwargs["Context"] = context
    try:
        resp = await client.call("CreateFleet", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create fleet") from exc
    return CreateFleetResult(
        fleet_id=resp.get("FleetId"),
        errors=resp.get("Errors"),
        instances=resp.get("Instances"),
    )


async def create_flow_logs(
    resource_ids: list[str],
    resource_type: str,
    *,
    client_token: str | None = None,
    deliver_logs_permission_arn: str | None = None,
    deliver_cross_account_role: str | None = None,
    log_group_name: str | None = None,
    traffic_type: str | None = None,
    log_destination_type: str | None = None,
    log_destination: str | None = None,
    log_format: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    max_aggregation_interval: int | None = None,
    destination_options: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> CreateFlowLogsResult:
    """Create flow logs.

    Args:
        resource_ids: Resource ids.
        resource_type: Resource type.
        client_token: Client token.
        deliver_logs_permission_arn: Deliver logs permission arn.
        deliver_cross_account_role: Deliver cross account role.
        log_group_name: Log group name.
        traffic_type: Traffic type.
        log_destination_type: Log destination type.
        log_destination: Log destination.
        log_format: Log format.
        tag_specifications: Tag specifications.
        max_aggregation_interval: Max aggregation interval.
        destination_options: Destination options.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ResourceIds"] = resource_ids
    kwargs["ResourceType"] = resource_type
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if deliver_logs_permission_arn is not None:
        kwargs["DeliverLogsPermissionArn"] = deliver_logs_permission_arn
    if deliver_cross_account_role is not None:
        kwargs["DeliverCrossAccountRole"] = deliver_cross_account_role
    if log_group_name is not None:
        kwargs["LogGroupName"] = log_group_name
    if traffic_type is not None:
        kwargs["TrafficType"] = traffic_type
    if log_destination_type is not None:
        kwargs["LogDestinationType"] = log_destination_type
    if log_destination is not None:
        kwargs["LogDestination"] = log_destination
    if log_format is not None:
        kwargs["LogFormat"] = log_format
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if max_aggregation_interval is not None:
        kwargs["MaxAggregationInterval"] = max_aggregation_interval
    if destination_options is not None:
        kwargs["DestinationOptions"] = destination_options
    try:
        resp = await client.call("CreateFlowLogs", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create flow logs") from exc
    return CreateFlowLogsResult(
        client_token=resp.get("ClientToken"),
        flow_log_ids=resp.get("FlowLogIds"),
        unsuccessful=resp.get("Unsuccessful"),
    )


async def create_fpga_image(
    input_storage_location: dict[str, Any],
    *,
    logs_storage_location: dict[str, Any] | None = None,
    description: str | None = None,
    name: str | None = None,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateFpgaImageResult:
    """Create fpga image.

    Args:
        input_storage_location: Input storage location.
        logs_storage_location: Logs storage location.
        description: Description.
        name: Name.
        client_token: Client token.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InputStorageLocation"] = input_storage_location
    if logs_storage_location is not None:
        kwargs["LogsStorageLocation"] = logs_storage_location
    if description is not None:
        kwargs["Description"] = description
    if name is not None:
        kwargs["Name"] = name
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateFpgaImage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create fpga image") from exc
    return CreateFpgaImageResult(
        fpga_image_id=resp.get("FpgaImageId"),
        fpga_image_global_id=resp.get("FpgaImageGlobalId"),
    )


async def create_image_usage_report(
    image_id: str,
    resource_types: list[dict[str, Any]],
    *,
    account_ids: list[str] | None = None,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateImageUsageReportResult:
    """Create image usage report.

    Args:
        image_id: Image id.
        resource_types: Resource types.
        account_ids: Account ids.
        client_token: Client token.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    kwargs["ResourceTypes"] = resource_types
    if account_ids is not None:
        kwargs["AccountIds"] = account_ids
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateImageUsageReport", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create image usage report") from exc
    return CreateImageUsageReportResult(
        report_id=resp.get("ReportId"),
    )


async def create_instance_connect_endpoint(
    subnet_id: str,
    *,
    security_group_ids: list[str] | None = None,
    preserve_client_ip: bool | None = None,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    ip_address_type: str | None = None,
    region_name: str | None = None,
) -> CreateInstanceConnectEndpointResult:
    """Create instance connect endpoint.

    Args:
        subnet_id: Subnet id.
        security_group_ids: Security group ids.
        preserve_client_ip: Preserve client ip.
        client_token: Client token.
        tag_specifications: Tag specifications.
        ip_address_type: Ip address type.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SubnetId"] = subnet_id
    if security_group_ids is not None:
        kwargs["SecurityGroupIds"] = security_group_ids
    if preserve_client_ip is not None:
        kwargs["PreserveClientIp"] = preserve_client_ip
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if ip_address_type is not None:
        kwargs["IpAddressType"] = ip_address_type
    try:
        resp = await client.call("CreateInstanceConnectEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create instance connect endpoint") from exc
    return CreateInstanceConnectEndpointResult(
        instance_connect_endpoint=resp.get("InstanceConnectEndpoint"),
        client_token=resp.get("ClientToken"),
    )


async def create_instance_event_window(
    *,
    name: str | None = None,
    time_ranges: list[dict[str, Any]] | None = None,
    cron_expression: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateInstanceEventWindowResult:
    """Create instance event window.

    Args:
        name: Name.
        time_ranges: Time ranges.
        cron_expression: Cron expression.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if name is not None:
        kwargs["Name"] = name
    if time_ranges is not None:
        kwargs["TimeRanges"] = time_ranges
    if cron_expression is not None:
        kwargs["CronExpression"] = cron_expression
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateInstanceEventWindow", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create instance event window") from exc
    return CreateInstanceEventWindowResult(
        instance_event_window=resp.get("InstanceEventWindow"),
    )


async def create_instance_export_task(
    instance_id: str,
    target_environment: str,
    export_to_s3_task: dict[str, Any],
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    description: str | None = None,
    region_name: str | None = None,
) -> CreateInstanceExportTaskResult:
    """Create instance export task.

    Args:
        instance_id: Instance id.
        target_environment: Target environment.
        export_to_s3_task: Export to s3 task.
        tag_specifications: Tag specifications.
        description: Description.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["TargetEnvironment"] = target_environment
    kwargs["ExportToS3Task"] = export_to_s3_task
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if description is not None:
        kwargs["Description"] = description
    try:
        resp = await client.call("CreateInstanceExportTask", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create instance export task") from exc
    return CreateInstanceExportTaskResult(
        export_task=resp.get("ExportTask"),
    )


async def create_internet_gateway(
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateInternetGatewayResult:
    """Create internet gateway.

    Args:
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateInternetGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create internet gateway") from exc
    return CreateInternetGatewayResult(
        internet_gateway=resp.get("InternetGateway"),
    )


async def create_ipam(
    *,
    description: str | None = None,
    operating_regions: list[dict[str, Any]] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    tier: str | None = None,
    enable_private_gua: bool | None = None,
    metered_account: str | None = None,
    region_name: str | None = None,
) -> CreateIpamResult:
    """Create ipam.

    Args:
        description: Description.
        operating_regions: Operating regions.
        tag_specifications: Tag specifications.
        client_token: Client token.
        tier: Tier.
        enable_private_gua: Enable private gua.
        metered_account: Metered account.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if description is not None:
        kwargs["Description"] = description
    if operating_regions is not None:
        kwargs["OperatingRegions"] = operating_regions
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tier is not None:
        kwargs["Tier"] = tier
    if enable_private_gua is not None:
        kwargs["EnablePrivateGua"] = enable_private_gua
    if metered_account is not None:
        kwargs["MeteredAccount"] = metered_account
    try:
        resp = await client.call("CreateIpam", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create ipam") from exc
    return CreateIpamResult(
        ipam=resp.get("Ipam"),
    )


async def create_ipam_external_resource_verification_token(
    ipam_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateIpamExternalResourceVerificationTokenResult:
    """Create ipam external resource verification token.

    Args:
        ipam_id: Ipam id.
        tag_specifications: Tag specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamId"] = ipam_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateIpamExternalResourceVerificationToken", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to create ipam external resource verification token"
        ) from exc
    return CreateIpamExternalResourceVerificationTokenResult(
        ipam_external_resource_verification_token=resp.get("IpamExternalResourceVerificationToken"),
    )


async def create_ipam_pool(
    ipam_scope_id: str,
    address_family: str,
    *,
    locale: str | None = None,
    source_ipam_pool_id: str | None = None,
    description: str | None = None,
    auto_import: bool | None = None,
    publicly_advertisable: bool | None = None,
    allocation_min_netmask_length: int | None = None,
    allocation_max_netmask_length: int | None = None,
    allocation_default_netmask_length: int | None = None,
    allocation_resource_tags: list[dict[str, Any]] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    aws_service: str | None = None,
    public_ip_source: str | None = None,
    source_resource: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> CreateIpamPoolResult:
    """Create ipam pool.

    Args:
        ipam_scope_id: Ipam scope id.
        address_family: Address family.
        locale: Locale.
        source_ipam_pool_id: Source ipam pool id.
        description: Description.
        auto_import: Auto import.
        publicly_advertisable: Publicly advertisable.
        allocation_min_netmask_length: Allocation min netmask length.
        allocation_max_netmask_length: Allocation max netmask length.
        allocation_default_netmask_length: Allocation default netmask length.
        allocation_resource_tags: Allocation resource tags.
        tag_specifications: Tag specifications.
        client_token: Client token.
        aws_service: Aws service.
        public_ip_source: Public ip source.
        source_resource: Source resource.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamScopeId"] = ipam_scope_id
    kwargs["AddressFamily"] = address_family
    if locale is not None:
        kwargs["Locale"] = locale
    if source_ipam_pool_id is not None:
        kwargs["SourceIpamPoolId"] = source_ipam_pool_id
    if description is not None:
        kwargs["Description"] = description
    if auto_import is not None:
        kwargs["AutoImport"] = auto_import
    if publicly_advertisable is not None:
        kwargs["PubliclyAdvertisable"] = publicly_advertisable
    if allocation_min_netmask_length is not None:
        kwargs["AllocationMinNetmaskLength"] = allocation_min_netmask_length
    if allocation_max_netmask_length is not None:
        kwargs["AllocationMaxNetmaskLength"] = allocation_max_netmask_length
    if allocation_default_netmask_length is not None:
        kwargs["AllocationDefaultNetmaskLength"] = allocation_default_netmask_length
    if allocation_resource_tags is not None:
        kwargs["AllocationResourceTags"] = allocation_resource_tags
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if aws_service is not None:
        kwargs["AwsService"] = aws_service
    if public_ip_source is not None:
        kwargs["PublicIpSource"] = public_ip_source
    if source_resource is not None:
        kwargs["SourceResource"] = source_resource
    try:
        resp = await client.call("CreateIpamPool", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create ipam pool") from exc
    return CreateIpamPoolResult(
        ipam_pool=resp.get("IpamPool"),
    )


async def create_ipam_prefix_list_resolver(
    ipam_id: str,
    address_family: str,
    *,
    description: str | None = None,
    rules: list[dict[str, Any]] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateIpamPrefixListResolverResult:
    """Create ipam prefix list resolver.

    Args:
        ipam_id: Ipam id.
        address_family: Address family.
        description: Description.
        rules: Rules.
        tag_specifications: Tag specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamId"] = ipam_id
    kwargs["AddressFamily"] = address_family
    if description is not None:
        kwargs["Description"] = description
    if rules is not None:
        kwargs["Rules"] = rules
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateIpamPrefixListResolver", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create ipam prefix list resolver") from exc
    return CreateIpamPrefixListResolverResult(
        ipam_prefix_list_resolver=resp.get("IpamPrefixListResolver"),
    )


async def create_ipam_prefix_list_resolver_target(
    ipam_prefix_list_resolver_id: str,
    prefix_list_id: str,
    prefix_list_region: str,
    track_latest_version: bool,
    *,
    desired_version: int | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateIpamPrefixListResolverTargetResult:
    """Create ipam prefix list resolver target.

    Args:
        ipam_prefix_list_resolver_id: Ipam prefix list resolver id.
        prefix_list_id: Prefix list id.
        prefix_list_region: Prefix list region.
        track_latest_version: Track latest version.
        desired_version: Desired version.
        tag_specifications: Tag specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPrefixListResolverId"] = ipam_prefix_list_resolver_id
    kwargs["PrefixListId"] = prefix_list_id
    kwargs["PrefixListRegion"] = prefix_list_region
    kwargs["TrackLatestVersion"] = track_latest_version
    if desired_version is not None:
        kwargs["DesiredVersion"] = desired_version
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateIpamPrefixListResolverTarget", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create ipam prefix list resolver target") from exc
    return CreateIpamPrefixListResolverTargetResult(
        ipam_prefix_list_resolver_target=resp.get("IpamPrefixListResolverTarget"),
    )


async def create_ipam_resource_discovery(
    *,
    description: str | None = None,
    operating_regions: list[dict[str, Any]] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateIpamResourceDiscoveryResult:
    """Create ipam resource discovery.

    Args:
        description: Description.
        operating_regions: Operating regions.
        tag_specifications: Tag specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if description is not None:
        kwargs["Description"] = description
    if operating_regions is not None:
        kwargs["OperatingRegions"] = operating_regions
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateIpamResourceDiscovery", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create ipam resource discovery") from exc
    return CreateIpamResourceDiscoveryResult(
        ipam_resource_discovery=resp.get("IpamResourceDiscovery"),
    )


async def create_ipam_scope(
    ipam_id: str,
    *,
    description: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    external_authority_configuration: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> CreateIpamScopeResult:
    """Create ipam scope.

    Args:
        ipam_id: Ipam id.
        description: Description.
        tag_specifications: Tag specifications.
        client_token: Client token.
        external_authority_configuration: External authority configuration.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamId"] = ipam_id
    if description is not None:
        kwargs["Description"] = description
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if external_authority_configuration is not None:
        kwargs["ExternalAuthorityConfiguration"] = external_authority_configuration
    try:
        resp = await client.call("CreateIpamScope", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create ipam scope") from exc
    return CreateIpamScopeResult(
        ipam_scope=resp.get("IpamScope"),
    )


async def create_key_pair(
    key_name: str,
    *,
    key_type: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    key_format: str | None = None,
    region_name: str | None = None,
) -> CreateKeyPairResult:
    """Create key pair.

    Args:
        key_name: Key name.
        key_type: Key type.
        tag_specifications: Tag specifications.
        key_format: Key format.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["KeyName"] = key_name
    if key_type is not None:
        kwargs["KeyType"] = key_type
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if key_format is not None:
        kwargs["KeyFormat"] = key_format
    try:
        resp = await client.call("CreateKeyPair", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create key pair") from exc
    return CreateKeyPairResult(
        key_pair_id=resp.get("KeyPairId"),
        tags=resp.get("Tags"),
        key_name=resp.get("KeyName"),
        key_fingerprint=resp.get("KeyFingerprint"),
        key_material=resp.get("KeyMaterial"),
    )


async def create_launch_template(
    launch_template_name: str,
    launch_template_data: dict[str, Any],
    *,
    client_token: str | None = None,
    version_description: str | None = None,
    operator: dict[str, Any] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateLaunchTemplateResult:
    """Create launch template.

    Args:
        launch_template_name: Launch template name.
        launch_template_data: Launch template data.
        client_token: Client token.
        version_description: Version description.
        operator: Operator.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LaunchTemplateName"] = launch_template_name
    kwargs["LaunchTemplateData"] = launch_template_data
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if version_description is not None:
        kwargs["VersionDescription"] = version_description
    if operator is not None:
        kwargs["Operator"] = operator
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateLaunchTemplate", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create launch template") from exc
    return CreateLaunchTemplateResult(
        launch_template=resp.get("LaunchTemplate"),
        warning=resp.get("Warning"),
    )


async def create_launch_template_version(
    launch_template_data: dict[str, Any],
    *,
    client_token: str | None = None,
    launch_template_id: str | None = None,
    launch_template_name: str | None = None,
    source_version: str | None = None,
    version_description: str | None = None,
    resolve_alias: bool | None = None,
    region_name: str | None = None,
) -> CreateLaunchTemplateVersionResult:
    """Create launch template version.

    Args:
        launch_template_data: Launch template data.
        client_token: Client token.
        launch_template_id: Launch template id.
        launch_template_name: Launch template name.
        source_version: Source version.
        version_description: Version description.
        resolve_alias: Resolve alias.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LaunchTemplateData"] = launch_template_data
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if launch_template_id is not None:
        kwargs["LaunchTemplateId"] = launch_template_id
    if launch_template_name is not None:
        kwargs["LaunchTemplateName"] = launch_template_name
    if source_version is not None:
        kwargs["SourceVersion"] = source_version
    if version_description is not None:
        kwargs["VersionDescription"] = version_description
    if resolve_alias is not None:
        kwargs["ResolveAlias"] = resolve_alias
    try:
        resp = await client.call("CreateLaunchTemplateVersion", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create launch template version") from exc
    return CreateLaunchTemplateVersionResult(
        launch_template_version=resp.get("LaunchTemplateVersion"),
        warning=resp.get("Warning"),
    )


async def create_local_gateway_route(
    local_gateway_route_table_id: str,
    *,
    destination_cidr_block: str | None = None,
    local_gateway_virtual_interface_group_id: str | None = None,
    network_interface_id: str | None = None,
    destination_prefix_list_id: str | None = None,
    region_name: str | None = None,
) -> CreateLocalGatewayRouteResult:
    """Create local gateway route.

    Args:
        local_gateway_route_table_id: Local gateway route table id.
        destination_cidr_block: Destination cidr block.
        local_gateway_virtual_interface_group_id: Local gateway virtual interface group id.
        network_interface_id: Network interface id.
        destination_prefix_list_id: Destination prefix list id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayRouteTableId"] = local_gateway_route_table_id
    if destination_cidr_block is not None:
        kwargs["DestinationCidrBlock"] = destination_cidr_block
    if local_gateway_virtual_interface_group_id is not None:
        kwargs["LocalGatewayVirtualInterfaceGroupId"] = local_gateway_virtual_interface_group_id
    if network_interface_id is not None:
        kwargs["NetworkInterfaceId"] = network_interface_id
    if destination_prefix_list_id is not None:
        kwargs["DestinationPrefixListId"] = destination_prefix_list_id
    try:
        resp = await client.call("CreateLocalGatewayRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create local gateway route") from exc
    return CreateLocalGatewayRouteResult(
        route=resp.get("Route"),
    )


async def create_local_gateway_route_table(
    local_gateway_id: str,
    *,
    mode: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateLocalGatewayRouteTableResult:
    """Create local gateway route table.

    Args:
        local_gateway_id: Local gateway id.
        mode: Mode.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayId"] = local_gateway_id
    if mode is not None:
        kwargs["Mode"] = mode
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateLocalGatewayRouteTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create local gateway route table") from exc
    return CreateLocalGatewayRouteTableResult(
        local_gateway_route_table=resp.get("LocalGatewayRouteTable"),
    )


async def create_local_gateway_route_table_virtual_interface_group_association(
    local_gateway_route_table_id: str,
    local_gateway_virtual_interface_group_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult:
    """Create local gateway route table virtual interface group association.

    Args:
        local_gateway_route_table_id: Local gateway route table id.
        local_gateway_virtual_interface_group_id: Local gateway virtual interface group id.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayRouteTableId"] = local_gateway_route_table_id
    kwargs["LocalGatewayVirtualInterfaceGroupId"] = local_gateway_virtual_interface_group_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call(
            "CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociation", **kwargs
        )
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to create local gateway route table virtual interface group association"
        ) from exc
    return CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult(
        local_gateway_route_table_virtual_interface_group_association=resp.get(
            "LocalGatewayRouteTableVirtualInterfaceGroupAssociation"
        ),
    )


async def create_local_gateway_route_table_vpc_association(
    local_gateway_route_table_id: str,
    vpc_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateLocalGatewayRouteTableVpcAssociationResult:
    """Create local gateway route table vpc association.

    Args:
        local_gateway_route_table_id: Local gateway route table id.
        vpc_id: Vpc id.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayRouteTableId"] = local_gateway_route_table_id
    kwargs["VpcId"] = vpc_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateLocalGatewayRouteTableVpcAssociation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to create local gateway route table vpc association"
        ) from exc
    return CreateLocalGatewayRouteTableVpcAssociationResult(
        local_gateway_route_table_vpc_association=resp.get("LocalGatewayRouteTableVpcAssociation"),
    )


async def create_local_gateway_virtual_interface(
    local_gateway_virtual_interface_group_id: str,
    outpost_lag_id: str,
    vlan: int,
    local_address: str,
    peer_address: str,
    *,
    peer_bgp_asn: int | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    peer_bgp_asn_extended: int | None = None,
    region_name: str | None = None,
) -> CreateLocalGatewayVirtualInterfaceResult:
    """Create local gateway virtual interface.

    Args:
        local_gateway_virtual_interface_group_id: Local gateway virtual interface group id.
        outpost_lag_id: Outpost lag id.
        vlan: Vlan.
        local_address: Local address.
        peer_address: Peer address.
        peer_bgp_asn: Peer bgp asn.
        tag_specifications: Tag specifications.
        peer_bgp_asn_extended: Peer bgp asn extended.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayVirtualInterfaceGroupId"] = local_gateway_virtual_interface_group_id
    kwargs["OutpostLagId"] = outpost_lag_id
    kwargs["Vlan"] = vlan
    kwargs["LocalAddress"] = local_address
    kwargs["PeerAddress"] = peer_address
    if peer_bgp_asn is not None:
        kwargs["PeerBgpAsn"] = peer_bgp_asn
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if peer_bgp_asn_extended is not None:
        kwargs["PeerBgpAsnExtended"] = peer_bgp_asn_extended
    try:
        resp = await client.call("CreateLocalGatewayVirtualInterface", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create local gateway virtual interface") from exc
    return CreateLocalGatewayVirtualInterfaceResult(
        local_gateway_virtual_interface=resp.get("LocalGatewayVirtualInterface"),
    )


async def create_local_gateway_virtual_interface_group(
    local_gateway_id: str,
    *,
    local_bgp_asn: int | None = None,
    local_bgp_asn_extended: int | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateLocalGatewayVirtualInterfaceGroupResult:
    """Create local gateway virtual interface group.

    Args:
        local_gateway_id: Local gateway id.
        local_bgp_asn: Local bgp asn.
        local_bgp_asn_extended: Local bgp asn extended.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayId"] = local_gateway_id
    if local_bgp_asn is not None:
        kwargs["LocalBgpAsn"] = local_bgp_asn
    if local_bgp_asn_extended is not None:
        kwargs["LocalBgpAsnExtended"] = local_bgp_asn_extended
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateLocalGatewayVirtualInterfaceGroup", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create local gateway virtual interface group") from exc
    return CreateLocalGatewayVirtualInterfaceGroupResult(
        local_gateway_virtual_interface_group=resp.get("LocalGatewayVirtualInterfaceGroup"),
    )


async def create_mac_system_integrity_protection_modification_task(
    instance_id: str,
    mac_system_integrity_protection_status: str,
    *,
    client_token: str | None = None,
    mac_credentials: str | None = None,
    mac_system_integrity_protection_configuration: dict[str, Any] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateMacSystemIntegrityProtectionModificationTaskResult:
    """Create mac system integrity protection modification task.

    Args:
        instance_id: Instance id.
        mac_system_integrity_protection_status: Mac system integrity protection status.
        client_token: Client token.
        mac_credentials: Mac credentials.
        mac_system_integrity_protection_configuration: Mac system integrity protection configuration.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["MacSystemIntegrityProtectionStatus"] = mac_system_integrity_protection_status
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if mac_credentials is not None:
        kwargs["MacCredentials"] = mac_credentials
    if mac_system_integrity_protection_configuration is not None:
        kwargs["MacSystemIntegrityProtectionConfiguration"] = (
            mac_system_integrity_protection_configuration
        )
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateMacSystemIntegrityProtectionModificationTask", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to create mac system integrity protection modification task"
        ) from exc
    return CreateMacSystemIntegrityProtectionModificationTaskResult(
        mac_modification_task=resp.get("MacModificationTask"),
    )


async def create_managed_prefix_list(
    prefix_list_name: str,
    max_entries: int,
    address_family: str,
    *,
    entries: list[dict[str, Any]] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateManagedPrefixListResult:
    """Create managed prefix list.

    Args:
        prefix_list_name: Prefix list name.
        max_entries: Max entries.
        address_family: Address family.
        entries: Entries.
        tag_specifications: Tag specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PrefixListName"] = prefix_list_name
    kwargs["MaxEntries"] = max_entries
    kwargs["AddressFamily"] = address_family
    if entries is not None:
        kwargs["Entries"] = entries
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateManagedPrefixList", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create managed prefix list") from exc
    return CreateManagedPrefixListResult(
        prefix_list=resp.get("PrefixList"),
    )


async def create_nat_gateway(
    subnet_id: str,
    *,
    allocation_id: str | None = None,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    connectivity_type: str | None = None,
    private_ip_address: str | None = None,
    secondary_allocation_ids: list[str] | None = None,
    secondary_private_ip_addresses: list[str] | None = None,
    secondary_private_ip_address_count: int | None = None,
    region_name: str | None = None,
) -> CreateNatGatewayResult:
    """Create nat gateway.

    Args:
        subnet_id: Subnet id.
        allocation_id: Allocation id.
        client_token: Client token.
        tag_specifications: Tag specifications.
        connectivity_type: Connectivity type.
        private_ip_address: Private ip address.
        secondary_allocation_ids: Secondary allocation ids.
        secondary_private_ip_addresses: Secondary private ip addresses.
        secondary_private_ip_address_count: Secondary private ip address count.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SubnetId"] = subnet_id
    if allocation_id is not None:
        kwargs["AllocationId"] = allocation_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if connectivity_type is not None:
        kwargs["ConnectivityType"] = connectivity_type
    if private_ip_address is not None:
        kwargs["PrivateIpAddress"] = private_ip_address
    if secondary_allocation_ids is not None:
        kwargs["SecondaryAllocationIds"] = secondary_allocation_ids
    if secondary_private_ip_addresses is not None:
        kwargs["SecondaryPrivateIpAddresses"] = secondary_private_ip_addresses
    if secondary_private_ip_address_count is not None:
        kwargs["SecondaryPrivateIpAddressCount"] = secondary_private_ip_address_count
    try:
        resp = await client.call("CreateNatGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create nat gateway") from exc
    return CreateNatGatewayResult(
        client_token=resp.get("ClientToken"),
        nat_gateway=resp.get("NatGateway"),
    )


async def create_network_acl(
    vpc_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateNetworkAclResult:
    """Create network acl.

    Args:
        vpc_id: Vpc id.
        tag_specifications: Tag specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateNetworkAcl", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create network acl") from exc
    return CreateNetworkAclResult(
        network_acl=resp.get("NetworkAcl"),
        client_token=resp.get("ClientToken"),
    )


async def create_network_acl_entry(
    network_acl_id: str,
    rule_number: int,
    protocol: str,
    rule_action: str,
    egress: bool,
    *,
    cidr_block: str | None = None,
    ipv6_cidr_block: str | None = None,
    icmp_type_code: dict[str, Any] | None = None,
    port_range: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> None:
    """Create network acl entry.

    Args:
        network_acl_id: Network acl id.
        rule_number: Rule number.
        protocol: Protocol.
        rule_action: Rule action.
        egress: Egress.
        cidr_block: Cidr block.
        ipv6_cidr_block: Ipv6 cidr block.
        icmp_type_code: Icmp type code.
        port_range: Port range.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkAclId"] = network_acl_id
    kwargs["RuleNumber"] = rule_number
    kwargs["Protocol"] = protocol
    kwargs["RuleAction"] = rule_action
    kwargs["Egress"] = egress
    if cidr_block is not None:
        kwargs["CidrBlock"] = cidr_block
    if ipv6_cidr_block is not None:
        kwargs["Ipv6CidrBlock"] = ipv6_cidr_block
    if icmp_type_code is not None:
        kwargs["IcmpTypeCode"] = icmp_type_code
    if port_range is not None:
        kwargs["PortRange"] = port_range
    try:
        await client.call("CreateNetworkAclEntry", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create network acl entry") from exc
    return None


async def create_network_insights_access_scope(
    client_token: str,
    *,
    match_paths: list[dict[str, Any]] | None = None,
    exclude_paths: list[dict[str, Any]] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateNetworkInsightsAccessScopeResult:
    """Create network insights access scope.

    Args:
        client_token: Client token.
        match_paths: Match paths.
        exclude_paths: Exclude paths.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientToken"] = client_token
    if match_paths is not None:
        kwargs["MatchPaths"] = match_paths
    if exclude_paths is not None:
        kwargs["ExcludePaths"] = exclude_paths
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateNetworkInsightsAccessScope", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create network insights access scope") from exc
    return CreateNetworkInsightsAccessScopeResult(
        network_insights_access_scope=resp.get("NetworkInsightsAccessScope"),
        network_insights_access_scope_content=resp.get("NetworkInsightsAccessScopeContent"),
    )


async def create_network_insights_path(
    source: str,
    protocol: str,
    client_token: str,
    *,
    source_ip: str | None = None,
    destination_ip: str | None = None,
    destination: str | None = None,
    destination_port: int | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    filter_at_source: dict[str, Any] | None = None,
    filter_at_destination: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> CreateNetworkInsightsPathResult:
    """Create network insights path.

    Args:
        source: Source.
        protocol: Protocol.
        client_token: Client token.
        source_ip: Source ip.
        destination_ip: Destination ip.
        destination: Destination.
        destination_port: Destination port.
        tag_specifications: Tag specifications.
        filter_at_source: Filter at source.
        filter_at_destination: Filter at destination.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Source"] = source
    kwargs["Protocol"] = protocol
    kwargs["ClientToken"] = client_token
    if source_ip is not None:
        kwargs["SourceIp"] = source_ip
    if destination_ip is not None:
        kwargs["DestinationIp"] = destination_ip
    if destination is not None:
        kwargs["Destination"] = destination
    if destination_port is not None:
        kwargs["DestinationPort"] = destination_port
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if filter_at_source is not None:
        kwargs["FilterAtSource"] = filter_at_source
    if filter_at_destination is not None:
        kwargs["FilterAtDestination"] = filter_at_destination
    try:
        resp = await client.call("CreateNetworkInsightsPath", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create network insights path") from exc
    return CreateNetworkInsightsPathResult(
        network_insights_path=resp.get("NetworkInsightsPath"),
    )


async def create_network_interface(
    subnet_id: str,
    *,
    ipv4_prefixes: list[dict[str, Any]] | None = None,
    ipv4_prefix_count: int | None = None,
    ipv6_prefixes: list[dict[str, Any]] | None = None,
    ipv6_prefix_count: int | None = None,
    interface_type: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    enable_primary_ipv6: bool | None = None,
    connection_tracking_specification: dict[str, Any] | None = None,
    operator: dict[str, Any] | None = None,
    description: str | None = None,
    private_ip_address: str | None = None,
    groups: list[str] | None = None,
    private_ip_addresses: list[dict[str, Any]] | None = None,
    secondary_private_ip_address_count: int | None = None,
    ipv6_addresses: list[dict[str, Any]] | None = None,
    ipv6_address_count: int | None = None,
    region_name: str | None = None,
) -> CreateNetworkInterfaceResult:
    """Create network interface.

    Args:
        subnet_id: Subnet id.
        ipv4_prefixes: Ipv4 prefixes.
        ipv4_prefix_count: Ipv4 prefix count.
        ipv6_prefixes: Ipv6 prefixes.
        ipv6_prefix_count: Ipv6 prefix count.
        interface_type: Interface type.
        tag_specifications: Tag specifications.
        client_token: Client token.
        enable_primary_ipv6: Enable primary ipv6.
        connection_tracking_specification: Connection tracking specification.
        operator: Operator.
        description: Description.
        private_ip_address: Private ip address.
        groups: Groups.
        private_ip_addresses: Private ip addresses.
        secondary_private_ip_address_count: Secondary private ip address count.
        ipv6_addresses: Ipv6 addresses.
        ipv6_address_count: Ipv6 address count.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SubnetId"] = subnet_id
    if ipv4_prefixes is not None:
        kwargs["Ipv4Prefixes"] = ipv4_prefixes
    if ipv4_prefix_count is not None:
        kwargs["Ipv4PrefixCount"] = ipv4_prefix_count
    if ipv6_prefixes is not None:
        kwargs["Ipv6Prefixes"] = ipv6_prefixes
    if ipv6_prefix_count is not None:
        kwargs["Ipv6PrefixCount"] = ipv6_prefix_count
    if interface_type is not None:
        kwargs["InterfaceType"] = interface_type
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if enable_primary_ipv6 is not None:
        kwargs["EnablePrimaryIpv6"] = enable_primary_ipv6
    if connection_tracking_specification is not None:
        kwargs["ConnectionTrackingSpecification"] = connection_tracking_specification
    if operator is not None:
        kwargs["Operator"] = operator
    if description is not None:
        kwargs["Description"] = description
    if private_ip_address is not None:
        kwargs["PrivateIpAddress"] = private_ip_address
    if groups is not None:
        kwargs["Groups"] = groups
    if private_ip_addresses is not None:
        kwargs["PrivateIpAddresses"] = private_ip_addresses
    if secondary_private_ip_address_count is not None:
        kwargs["SecondaryPrivateIpAddressCount"] = secondary_private_ip_address_count
    if ipv6_addresses is not None:
        kwargs["Ipv6Addresses"] = ipv6_addresses
    if ipv6_address_count is not None:
        kwargs["Ipv6AddressCount"] = ipv6_address_count
    try:
        resp = await client.call("CreateNetworkInterface", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create network interface") from exc
    return CreateNetworkInterfaceResult(
        network_interface=resp.get("NetworkInterface"),
        client_token=resp.get("ClientToken"),
    )


async def create_network_interface_permission(
    network_interface_id: str,
    permission: str,
    *,
    aws_account_id: str | None = None,
    aws_service: str | None = None,
    region_name: str | None = None,
) -> CreateNetworkInterfacePermissionResult:
    """Create network interface permission.

    Args:
        network_interface_id: Network interface id.
        permission: Permission.
        aws_account_id: Aws account id.
        aws_service: Aws service.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    kwargs["Permission"] = permission
    if aws_account_id is not None:
        kwargs["AwsAccountId"] = aws_account_id
    if aws_service is not None:
        kwargs["AwsService"] = aws_service
    try:
        resp = await client.call("CreateNetworkInterfacePermission", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create network interface permission") from exc
    return CreateNetworkInterfacePermissionResult(
        interface_permission=resp.get("InterfacePermission"),
    )


async def create_placement_group(
    *,
    partition_count: int | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    spread_level: str | None = None,
    group_name: str | None = None,
    strategy: str | None = None,
    region_name: str | None = None,
) -> CreatePlacementGroupResult:
    """Create placement group.

    Args:
        partition_count: Partition count.
        tag_specifications: Tag specifications.
        spread_level: Spread level.
        group_name: Group name.
        strategy: Strategy.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if partition_count is not None:
        kwargs["PartitionCount"] = partition_count
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if spread_level is not None:
        kwargs["SpreadLevel"] = spread_level
    if group_name is not None:
        kwargs["GroupName"] = group_name
    if strategy is not None:
        kwargs["Strategy"] = strategy
    try:
        resp = await client.call("CreatePlacementGroup", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create placement group") from exc
    return CreatePlacementGroupResult(
        placement_group=resp.get("PlacementGroup"),
    )


async def create_public_ipv4_pool(
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    network_border_group: str | None = None,
    region_name: str | None = None,
) -> CreatePublicIpv4PoolResult:
    """Create public ipv4 pool.

    Args:
        tag_specifications: Tag specifications.
        network_border_group: Network border group.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if network_border_group is not None:
        kwargs["NetworkBorderGroup"] = network_border_group
    try:
        resp = await client.call("CreatePublicIpv4Pool", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create public ipv4 pool") from exc
    return CreatePublicIpv4PoolResult(
        pool_id=resp.get("PoolId"),
    )


async def create_replace_root_volume_task(
    instance_id: str,
    *,
    snapshot_id: str | None = None,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    image_id: str | None = None,
    delete_replaced_root_volume: bool | None = None,
    volume_initialization_rate: int | None = None,
    region_name: str | None = None,
) -> CreateReplaceRootVolumeTaskResult:
    """Create replace root volume task.

    Args:
        instance_id: Instance id.
        snapshot_id: Snapshot id.
        client_token: Client token.
        tag_specifications: Tag specifications.
        image_id: Image id.
        delete_replaced_root_volume: Delete replaced root volume.
        volume_initialization_rate: Volume initialization rate.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    if snapshot_id is not None:
        kwargs["SnapshotId"] = snapshot_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if image_id is not None:
        kwargs["ImageId"] = image_id
    if delete_replaced_root_volume is not None:
        kwargs["DeleteReplacedRootVolume"] = delete_replaced_root_volume
    if volume_initialization_rate is not None:
        kwargs["VolumeInitializationRate"] = volume_initialization_rate
    try:
        resp = await client.call("CreateReplaceRootVolumeTask", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create replace root volume task") from exc
    return CreateReplaceRootVolumeTaskResult(
        replace_root_volume_task=resp.get("ReplaceRootVolumeTask"),
    )


async def create_reserved_instances_listing(
    reserved_instances_id: str,
    instance_count: int,
    price_schedules: list[dict[str, Any]],
    client_token: str,
    region_name: str | None = None,
) -> CreateReservedInstancesListingResult:
    """Create reserved instances listing.

    Args:
        reserved_instances_id: Reserved instances id.
        instance_count: Instance count.
        price_schedules: Price schedules.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ReservedInstancesId"] = reserved_instances_id
    kwargs["InstanceCount"] = instance_count
    kwargs["PriceSchedules"] = price_schedules
    kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateReservedInstancesListing", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create reserved instances listing") from exc
    return CreateReservedInstancesListingResult(
        reserved_instances_listings=resp.get("ReservedInstancesListings"),
    )


async def create_restore_image_task(
    bucket: str,
    object_key: str,
    *,
    name: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateRestoreImageTaskResult:
    """Create restore image task.

    Args:
        bucket: Bucket.
        object_key: Object key.
        name: Name.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Bucket"] = bucket
    kwargs["ObjectKey"] = object_key
    if name is not None:
        kwargs["Name"] = name
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateRestoreImageTask", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create restore image task") from exc
    return CreateRestoreImageTaskResult(
        image_id=resp.get("ImageId"),
    )


async def create_route(
    route_table_id: str,
    *,
    destination_prefix_list_id: str | None = None,
    vpc_endpoint_id: str | None = None,
    transit_gateway_id: str | None = None,
    local_gateway_id: str | None = None,
    carrier_gateway_id: str | None = None,
    core_network_arn: str | None = None,
    odb_network_arn: str | None = None,
    destination_cidr_block: str | None = None,
    gateway_id: str | None = None,
    destination_ipv6_cidr_block: str | None = None,
    egress_only_internet_gateway_id: str | None = None,
    instance_id: str | None = None,
    network_interface_id: str | None = None,
    vpc_peering_connection_id: str | None = None,
    nat_gateway_id: str | None = None,
    region_name: str | None = None,
) -> CreateRouteResult:
    """Create route.

    Args:
        route_table_id: Route table id.
        destination_prefix_list_id: Destination prefix list id.
        vpc_endpoint_id: Vpc endpoint id.
        transit_gateway_id: Transit gateway id.
        local_gateway_id: Local gateway id.
        carrier_gateway_id: Carrier gateway id.
        core_network_arn: Core network arn.
        odb_network_arn: Odb network arn.
        destination_cidr_block: Destination cidr block.
        gateway_id: Gateway id.
        destination_ipv6_cidr_block: Destination ipv6 cidr block.
        egress_only_internet_gateway_id: Egress only internet gateway id.
        instance_id: Instance id.
        network_interface_id: Network interface id.
        vpc_peering_connection_id: Vpc peering connection id.
        nat_gateway_id: Nat gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteTableId"] = route_table_id
    if destination_prefix_list_id is not None:
        kwargs["DestinationPrefixListId"] = destination_prefix_list_id
    if vpc_endpoint_id is not None:
        kwargs["VpcEndpointId"] = vpc_endpoint_id
    if transit_gateway_id is not None:
        kwargs["TransitGatewayId"] = transit_gateway_id
    if local_gateway_id is not None:
        kwargs["LocalGatewayId"] = local_gateway_id
    if carrier_gateway_id is not None:
        kwargs["CarrierGatewayId"] = carrier_gateway_id
    if core_network_arn is not None:
        kwargs["CoreNetworkArn"] = core_network_arn
    if odb_network_arn is not None:
        kwargs["OdbNetworkArn"] = odb_network_arn
    if destination_cidr_block is not None:
        kwargs["DestinationCidrBlock"] = destination_cidr_block
    if gateway_id is not None:
        kwargs["GatewayId"] = gateway_id
    if destination_ipv6_cidr_block is not None:
        kwargs["DestinationIpv6CidrBlock"] = destination_ipv6_cidr_block
    if egress_only_internet_gateway_id is not None:
        kwargs["EgressOnlyInternetGatewayId"] = egress_only_internet_gateway_id
    if instance_id is not None:
        kwargs["InstanceId"] = instance_id
    if network_interface_id is not None:
        kwargs["NetworkInterfaceId"] = network_interface_id
    if vpc_peering_connection_id is not None:
        kwargs["VpcPeeringConnectionId"] = vpc_peering_connection_id
    if nat_gateway_id is not None:
        kwargs["NatGatewayId"] = nat_gateway_id
    try:
        resp = await client.call("CreateRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create route") from exc
    return CreateRouteResult(
        return_value=resp.get("Return"),
    )


async def create_route_server(
    amazon_side_asn: int,
    *,
    client_token: str | None = None,
    persist_routes: str | None = None,
    persist_routes_duration: int | None = None,
    sns_notifications_enabled: bool | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateRouteServerResult:
    """Create route server.

    Args:
        amazon_side_asn: Amazon side asn.
        client_token: Client token.
        persist_routes: Persist routes.
        persist_routes_duration: Persist routes duration.
        sns_notifications_enabled: Sns notifications enabled.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AmazonSideAsn"] = amazon_side_asn
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if persist_routes is not None:
        kwargs["PersistRoutes"] = persist_routes
    if persist_routes_duration is not None:
        kwargs["PersistRoutesDuration"] = persist_routes_duration
    if sns_notifications_enabled is not None:
        kwargs["SnsNotificationsEnabled"] = sns_notifications_enabled
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateRouteServer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create route server") from exc
    return CreateRouteServerResult(
        route_server=resp.get("RouteServer"),
    )


async def create_route_server_endpoint(
    route_server_id: str,
    subnet_id: str,
    *,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateRouteServerEndpointResult:
    """Create route server endpoint.

    Args:
        route_server_id: Route server id.
        subnet_id: Subnet id.
        client_token: Client token.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerId"] = route_server_id
    kwargs["SubnetId"] = subnet_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateRouteServerEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create route server endpoint") from exc
    return CreateRouteServerEndpointResult(
        route_server_endpoint=resp.get("RouteServerEndpoint"),
    )


async def create_route_server_peer(
    route_server_endpoint_id: str,
    peer_address: str,
    bgp_options: dict[str, Any],
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateRouteServerPeerResult:
    """Create route server peer.

    Args:
        route_server_endpoint_id: Route server endpoint id.
        peer_address: Peer address.
        bgp_options: Bgp options.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerEndpointId"] = route_server_endpoint_id
    kwargs["PeerAddress"] = peer_address
    kwargs["BgpOptions"] = bgp_options
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateRouteServerPeer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create route server peer") from exc
    return CreateRouteServerPeerResult(
        route_server_peer=resp.get("RouteServerPeer"),
    )


async def create_route_table(
    vpc_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateRouteTableResult:
    """Create route table.

    Args:
        vpc_id: Vpc id.
        tag_specifications: Tag specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateRouteTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create route table") from exc
    return CreateRouteTableResult(
        route_table=resp.get("RouteTable"),
        client_token=resp.get("ClientToken"),
    )


async def create_security_group(
    description: str,
    group_name: str,
    *,
    vpc_id: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateSecurityGroupResult:
    """Create security group.

    Args:
        description: Description.
        group_name: Group name.
        vpc_id: Vpc id.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Description"] = description
    kwargs["GroupName"] = group_name
    if vpc_id is not None:
        kwargs["VpcId"] = vpc_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateSecurityGroup", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create security group") from exc
    return CreateSecurityGroupResult(
        group_id=resp.get("GroupId"),
        tags=resp.get("Tags"),
        security_group_arn=resp.get("SecurityGroupArn"),
    )


async def create_snapshot(
    volume_id: str,
    *,
    description: str | None = None,
    outpost_arn: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    location: str | None = None,
    region_name: str | None = None,
) -> CreateSnapshotResult:
    """Create snapshot.

    Args:
        volume_id: Volume id.
        description: Description.
        outpost_arn: Outpost arn.
        tag_specifications: Tag specifications.
        location: Location.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VolumeId"] = volume_id
    if description is not None:
        kwargs["Description"] = description
    if outpost_arn is not None:
        kwargs["OutpostArn"] = outpost_arn
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if location is not None:
        kwargs["Location"] = location
    try:
        resp = await client.call("CreateSnapshot", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create snapshot") from exc
    return CreateSnapshotResult(
        owner_alias=resp.get("OwnerAlias"),
        outpost_arn=resp.get("OutpostArn"),
        tags=resp.get("Tags"),
        storage_tier=resp.get("StorageTier"),
        restore_expiry_time=resp.get("RestoreExpiryTime"),
        sse_type=resp.get("SseType"),
        availability_zone=resp.get("AvailabilityZone"),
        transfer_type=resp.get("TransferType"),
        completion_duration_minutes=resp.get("CompletionDurationMinutes"),
        completion_time=resp.get("CompletionTime"),
        full_snapshot_size_in_bytes=resp.get("FullSnapshotSizeInBytes"),
        snapshot_id=resp.get("SnapshotId"),
        volume_id=resp.get("VolumeId"),
        state=resp.get("State"),
        state_message=resp.get("StateMessage"),
        start_time=resp.get("StartTime"),
        progress=resp.get("Progress"),
        owner_id=resp.get("OwnerId"),
        description=resp.get("Description"),
        volume_size=resp.get("VolumeSize"),
        encrypted=resp.get("Encrypted"),
        kms_key_id=resp.get("KmsKeyId"),
        data_encryption_key_id=resp.get("DataEncryptionKeyId"),
    )


async def create_snapshots(
    instance_specification: dict[str, Any],
    *,
    description: str | None = None,
    outpost_arn: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    copy_tags_from_source: str | None = None,
    location: str | None = None,
    region_name: str | None = None,
) -> CreateSnapshotsResult:
    """Create snapshots.

    Args:
        instance_specification: Instance specification.
        description: Description.
        outpost_arn: Outpost arn.
        tag_specifications: Tag specifications.
        copy_tags_from_source: Copy tags from source.
        location: Location.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceSpecification"] = instance_specification
    if description is not None:
        kwargs["Description"] = description
    if outpost_arn is not None:
        kwargs["OutpostArn"] = outpost_arn
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if copy_tags_from_source is not None:
        kwargs["CopyTagsFromSource"] = copy_tags_from_source
    if location is not None:
        kwargs["Location"] = location
    try:
        resp = await client.call("CreateSnapshots", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create snapshots") from exc
    return CreateSnapshotsResult(
        snapshots=resp.get("Snapshots"),
    )


async def create_spot_datafeed_subscription(
    bucket: str,
    *,
    prefix: str | None = None,
    region_name: str | None = None,
) -> CreateSpotDatafeedSubscriptionResult:
    """Create spot datafeed subscription.

    Args:
        bucket: Bucket.
        prefix: Prefix.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Bucket"] = bucket
    if prefix is not None:
        kwargs["Prefix"] = prefix
    try:
        resp = await client.call("CreateSpotDatafeedSubscription", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create spot datafeed subscription") from exc
    return CreateSpotDatafeedSubscriptionResult(
        spot_datafeed_subscription=resp.get("SpotDatafeedSubscription"),
    )


async def create_store_image_task(
    image_id: str,
    bucket: str,
    *,
    s3_object_tags: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateStoreImageTaskResult:
    """Create store image task.

    Args:
        image_id: Image id.
        bucket: Bucket.
        s3_object_tags: S3 object tags.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    kwargs["Bucket"] = bucket
    if s3_object_tags is not None:
        kwargs["S3ObjectTags"] = s3_object_tags
    try:
        resp = await client.call("CreateStoreImageTask", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create store image task") from exc
    return CreateStoreImageTaskResult(
        object_key=resp.get("ObjectKey"),
    )


async def create_subnet(
    vpc_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    availability_zone: str | None = None,
    availability_zone_id: str | None = None,
    cidr_block: str | None = None,
    ipv6_cidr_block: str | None = None,
    outpost_arn: str | None = None,
    ipv6_native: bool | None = None,
    ipv4_ipam_pool_id: str | None = None,
    ipv4_netmask_length: int | None = None,
    ipv6_ipam_pool_id: str | None = None,
    ipv6_netmask_length: int | None = None,
    region_name: str | None = None,
) -> CreateSubnetResult:
    """Create subnet.

    Args:
        vpc_id: Vpc id.
        tag_specifications: Tag specifications.
        availability_zone: Availability zone.
        availability_zone_id: Availability zone id.
        cidr_block: Cidr block.
        ipv6_cidr_block: Ipv6 cidr block.
        outpost_arn: Outpost arn.
        ipv6_native: Ipv6 native.
        ipv4_ipam_pool_id: Ipv4 ipam pool id.
        ipv4_netmask_length: Ipv4 netmask length.
        ipv6_ipam_pool_id: Ipv6 ipam pool id.
        ipv6_netmask_length: Ipv6 netmask length.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if availability_zone is not None:
        kwargs["AvailabilityZone"] = availability_zone
    if availability_zone_id is not None:
        kwargs["AvailabilityZoneId"] = availability_zone_id
    if cidr_block is not None:
        kwargs["CidrBlock"] = cidr_block
    if ipv6_cidr_block is not None:
        kwargs["Ipv6CidrBlock"] = ipv6_cidr_block
    if outpost_arn is not None:
        kwargs["OutpostArn"] = outpost_arn
    if ipv6_native is not None:
        kwargs["Ipv6Native"] = ipv6_native
    if ipv4_ipam_pool_id is not None:
        kwargs["Ipv4IpamPoolId"] = ipv4_ipam_pool_id
    if ipv4_netmask_length is not None:
        kwargs["Ipv4NetmaskLength"] = ipv4_netmask_length
    if ipv6_ipam_pool_id is not None:
        kwargs["Ipv6IpamPoolId"] = ipv6_ipam_pool_id
    if ipv6_netmask_length is not None:
        kwargs["Ipv6NetmaskLength"] = ipv6_netmask_length
    try:
        resp = await client.call("CreateSubnet", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create subnet") from exc
    return CreateSubnetResult(
        subnet=resp.get("Subnet"),
    )


async def create_subnet_cidr_reservation(
    subnet_id: str,
    cidr: str,
    reservation_type: str,
    *,
    description: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateSubnetCidrReservationResult:
    """Create subnet cidr reservation.

    Args:
        subnet_id: Subnet id.
        cidr: Cidr.
        reservation_type: Reservation type.
        description: Description.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SubnetId"] = subnet_id
    kwargs["Cidr"] = cidr
    kwargs["ReservationType"] = reservation_type
    if description is not None:
        kwargs["Description"] = description
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateSubnetCidrReservation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create subnet cidr reservation") from exc
    return CreateSubnetCidrReservationResult(
        subnet_cidr_reservation=resp.get("SubnetCidrReservation"),
    )


async def create_tags(
    resources: list[str],
    tags: list[dict[str, Any]],
    region_name: str | None = None,
) -> None:
    """Create tags.

    Args:
        resources: Resources.
        tags: Tags.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Resources"] = resources
    kwargs["Tags"] = tags
    try:
        await client.call("CreateTags", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create tags") from exc
    return None


async def create_traffic_mirror_filter(
    *,
    description: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateTrafficMirrorFilterResult:
    """Create traffic mirror filter.

    Args:
        description: Description.
        tag_specifications: Tag specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if description is not None:
        kwargs["Description"] = description
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateTrafficMirrorFilter", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create traffic mirror filter") from exc
    return CreateTrafficMirrorFilterResult(
        traffic_mirror_filter=resp.get("TrafficMirrorFilter"),
        client_token=resp.get("ClientToken"),
    )


async def create_traffic_mirror_filter_rule(
    traffic_mirror_filter_id: str,
    traffic_direction: str,
    rule_number: int,
    rule_action: str,
    destination_cidr_block: str,
    source_cidr_block: str,
    *,
    destination_port_range: dict[str, Any] | None = None,
    source_port_range: dict[str, Any] | None = None,
    protocol: int | None = None,
    description: str | None = None,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateTrafficMirrorFilterRuleResult:
    """Create traffic mirror filter rule.

    Args:
        traffic_mirror_filter_id: Traffic mirror filter id.
        traffic_direction: Traffic direction.
        rule_number: Rule number.
        rule_action: Rule action.
        destination_cidr_block: Destination cidr block.
        source_cidr_block: Source cidr block.
        destination_port_range: Destination port range.
        source_port_range: Source port range.
        protocol: Protocol.
        description: Description.
        client_token: Client token.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TrafficMirrorFilterId"] = traffic_mirror_filter_id
    kwargs["TrafficDirection"] = traffic_direction
    kwargs["RuleNumber"] = rule_number
    kwargs["RuleAction"] = rule_action
    kwargs["DestinationCidrBlock"] = destination_cidr_block
    kwargs["SourceCidrBlock"] = source_cidr_block
    if destination_port_range is not None:
        kwargs["DestinationPortRange"] = destination_port_range
    if source_port_range is not None:
        kwargs["SourcePortRange"] = source_port_range
    if protocol is not None:
        kwargs["Protocol"] = protocol
    if description is not None:
        kwargs["Description"] = description
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateTrafficMirrorFilterRule", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create traffic mirror filter rule") from exc
    return CreateTrafficMirrorFilterRuleResult(
        traffic_mirror_filter_rule=resp.get("TrafficMirrorFilterRule"),
        client_token=resp.get("ClientToken"),
    )


async def create_traffic_mirror_session(
    network_interface_id: str,
    traffic_mirror_target_id: str,
    traffic_mirror_filter_id: str,
    session_number: int,
    *,
    packet_length: int | None = None,
    virtual_network_id: int | None = None,
    description: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateTrafficMirrorSessionResult:
    """Create traffic mirror session.

    Args:
        network_interface_id: Network interface id.
        traffic_mirror_target_id: Traffic mirror target id.
        traffic_mirror_filter_id: Traffic mirror filter id.
        session_number: Session number.
        packet_length: Packet length.
        virtual_network_id: Virtual network id.
        description: Description.
        tag_specifications: Tag specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    kwargs["TrafficMirrorTargetId"] = traffic_mirror_target_id
    kwargs["TrafficMirrorFilterId"] = traffic_mirror_filter_id
    kwargs["SessionNumber"] = session_number
    if packet_length is not None:
        kwargs["PacketLength"] = packet_length
    if virtual_network_id is not None:
        kwargs["VirtualNetworkId"] = virtual_network_id
    if description is not None:
        kwargs["Description"] = description
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateTrafficMirrorSession", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create traffic mirror session") from exc
    return CreateTrafficMirrorSessionResult(
        traffic_mirror_session=resp.get("TrafficMirrorSession"),
        client_token=resp.get("ClientToken"),
    )


async def create_traffic_mirror_target(
    *,
    network_interface_id: str | None = None,
    network_load_balancer_arn: str | None = None,
    description: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    gateway_load_balancer_endpoint_id: str | None = None,
    region_name: str | None = None,
) -> CreateTrafficMirrorTargetResult:
    """Create traffic mirror target.

    Args:
        network_interface_id: Network interface id.
        network_load_balancer_arn: Network load balancer arn.
        description: Description.
        tag_specifications: Tag specifications.
        client_token: Client token.
        gateway_load_balancer_endpoint_id: Gateway load balancer endpoint id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if network_interface_id is not None:
        kwargs["NetworkInterfaceId"] = network_interface_id
    if network_load_balancer_arn is not None:
        kwargs["NetworkLoadBalancerArn"] = network_load_balancer_arn
    if description is not None:
        kwargs["Description"] = description
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if gateway_load_balancer_endpoint_id is not None:
        kwargs["GatewayLoadBalancerEndpointId"] = gateway_load_balancer_endpoint_id
    try:
        resp = await client.call("CreateTrafficMirrorTarget", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create traffic mirror target") from exc
    return CreateTrafficMirrorTargetResult(
        traffic_mirror_target=resp.get("TrafficMirrorTarget"),
        client_token=resp.get("ClientToken"),
    )


async def create_transit_gateway(
    *,
    description: str | None = None,
    options: dict[str, Any] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateTransitGatewayResult:
    """Create transit gateway.

    Args:
        description: Description.
        options: Options.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if description is not None:
        kwargs["Description"] = description
    if options is not None:
        kwargs["Options"] = options
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateTransitGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create transit gateway") from exc
    return CreateTransitGatewayResult(
        transit_gateway=resp.get("TransitGateway"),
    )


async def create_transit_gateway_connect(
    transport_transit_gateway_attachment_id: str,
    options: dict[str, Any],
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateTransitGatewayConnectResult:
    """Create transit gateway connect.

    Args:
        transport_transit_gateway_attachment_id: Transport transit gateway attachment id.
        options: Options.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransportTransitGatewayAttachmentId"] = transport_transit_gateway_attachment_id
    kwargs["Options"] = options
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateTransitGatewayConnect", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create transit gateway connect") from exc
    return CreateTransitGatewayConnectResult(
        transit_gateway_connect=resp.get("TransitGatewayConnect"),
    )


async def create_transit_gateway_connect_peer(
    transit_gateway_attachment_id: str,
    peer_address: str,
    inside_cidr_blocks: list[str],
    *,
    transit_gateway_address: str | None = None,
    bgp_options: dict[str, Any] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateTransitGatewayConnectPeerResult:
    """Create transit gateway connect peer.

    Args:
        transit_gateway_attachment_id: Transit gateway attachment id.
        peer_address: Peer address.
        inside_cidr_blocks: Inside cidr blocks.
        transit_gateway_address: Transit gateway address.
        bgp_options: Bgp options.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    kwargs["PeerAddress"] = peer_address
    kwargs["InsideCidrBlocks"] = inside_cidr_blocks
    if transit_gateway_address is not None:
        kwargs["TransitGatewayAddress"] = transit_gateway_address
    if bgp_options is not None:
        kwargs["BgpOptions"] = bgp_options
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateTransitGatewayConnectPeer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create transit gateway connect peer") from exc
    return CreateTransitGatewayConnectPeerResult(
        transit_gateway_connect_peer=resp.get("TransitGatewayConnectPeer"),
    )


async def create_transit_gateway_multicast_domain(
    transit_gateway_id: str,
    *,
    options: dict[str, Any] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateTransitGatewayMulticastDomainResult:
    """Create transit gateway multicast domain.

    Args:
        transit_gateway_id: Transit gateway id.
        options: Options.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayId"] = transit_gateway_id
    if options is not None:
        kwargs["Options"] = options
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateTransitGatewayMulticastDomain", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create transit gateway multicast domain") from exc
    return CreateTransitGatewayMulticastDomainResult(
        transit_gateway_multicast_domain=resp.get("TransitGatewayMulticastDomain"),
    )


async def create_transit_gateway_peering_attachment(
    transit_gateway_id: str,
    peer_transit_gateway_id: str,
    peer_account_id: str,
    peer_region: str,
    *,
    options: dict[str, Any] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateTransitGatewayPeeringAttachmentResult:
    """Create transit gateway peering attachment.

    Args:
        transit_gateway_id: Transit gateway id.
        peer_transit_gateway_id: Peer transit gateway id.
        peer_account_id: Peer account id.
        peer_region: Peer region.
        options: Options.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayId"] = transit_gateway_id
    kwargs["PeerTransitGatewayId"] = peer_transit_gateway_id
    kwargs["PeerAccountId"] = peer_account_id
    kwargs["PeerRegion"] = peer_region
    if options is not None:
        kwargs["Options"] = options
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateTransitGatewayPeeringAttachment", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create transit gateway peering attachment") from exc
    return CreateTransitGatewayPeeringAttachmentResult(
        transit_gateway_peering_attachment=resp.get("TransitGatewayPeeringAttachment"),
    )


async def create_transit_gateway_policy_table(
    transit_gateway_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateTransitGatewayPolicyTableResult:
    """Create transit gateway policy table.

    Args:
        transit_gateway_id: Transit gateway id.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayId"] = transit_gateway_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateTransitGatewayPolicyTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create transit gateway policy table") from exc
    return CreateTransitGatewayPolicyTableResult(
        transit_gateway_policy_table=resp.get("TransitGatewayPolicyTable"),
    )


async def create_transit_gateway_prefix_list_reference(
    transit_gateway_route_table_id: str,
    prefix_list_id: str,
    *,
    transit_gateway_attachment_id: str | None = None,
    blackhole: bool | None = None,
    region_name: str | None = None,
) -> CreateTransitGatewayPrefixListReferenceResult:
    """Create transit gateway prefix list reference.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        prefix_list_id: Prefix list id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        blackhole: Blackhole.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    kwargs["PrefixListId"] = prefix_list_id
    if transit_gateway_attachment_id is not None:
        kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    if blackhole is not None:
        kwargs["Blackhole"] = blackhole
    try:
        resp = await client.call("CreateTransitGatewayPrefixListReference", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create transit gateway prefix list reference") from exc
    return CreateTransitGatewayPrefixListReferenceResult(
        transit_gateway_prefix_list_reference=resp.get("TransitGatewayPrefixListReference"),
    )


async def create_transit_gateway_route(
    destination_cidr_block: str,
    transit_gateway_route_table_id: str,
    *,
    transit_gateway_attachment_id: str | None = None,
    blackhole: bool | None = None,
    region_name: str | None = None,
) -> CreateTransitGatewayRouteResult:
    """Create transit gateway route.

    Args:
        destination_cidr_block: Destination cidr block.
        transit_gateway_route_table_id: Transit gateway route table id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        blackhole: Blackhole.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["DestinationCidrBlock"] = destination_cidr_block
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    if transit_gateway_attachment_id is not None:
        kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    if blackhole is not None:
        kwargs["Blackhole"] = blackhole
    try:
        resp = await client.call("CreateTransitGatewayRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create transit gateway route") from exc
    return CreateTransitGatewayRouteResult(
        route=resp.get("Route"),
    )


async def create_transit_gateway_route_table(
    transit_gateway_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateTransitGatewayRouteTableResult:
    """Create transit gateway route table.

    Args:
        transit_gateway_id: Transit gateway id.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayId"] = transit_gateway_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateTransitGatewayRouteTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create transit gateway route table") from exc
    return CreateTransitGatewayRouteTableResult(
        transit_gateway_route_table=resp.get("TransitGatewayRouteTable"),
    )


async def create_transit_gateway_route_table_announcement(
    transit_gateway_route_table_id: str,
    peering_attachment_id: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateTransitGatewayRouteTableAnnouncementResult:
    """Create transit gateway route table announcement.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        peering_attachment_id: Peering attachment id.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    kwargs["PeeringAttachmentId"] = peering_attachment_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateTransitGatewayRouteTableAnnouncement", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to create transit gateway route table announcement"
        ) from exc
    return CreateTransitGatewayRouteTableAnnouncementResult(
        transit_gateway_route_table_announcement=resp.get("TransitGatewayRouteTableAnnouncement"),
    )


async def create_transit_gateway_vpc_attachment(
    transit_gateway_id: str,
    vpc_id: str,
    subnet_ids: list[str],
    *,
    options: dict[str, Any] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateTransitGatewayVpcAttachmentResult:
    """Create transit gateway vpc attachment.

    Args:
        transit_gateway_id: Transit gateway id.
        vpc_id: Vpc id.
        subnet_ids: Subnet ids.
        options: Options.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayId"] = transit_gateway_id
    kwargs["VpcId"] = vpc_id
    kwargs["SubnetIds"] = subnet_ids
    if options is not None:
        kwargs["Options"] = options
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateTransitGatewayVpcAttachment", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create transit gateway vpc attachment") from exc
    return CreateTransitGatewayVpcAttachmentResult(
        transit_gateway_vpc_attachment=resp.get("TransitGatewayVpcAttachment"),
    )


async def create_verified_access_endpoint(
    verified_access_group_id: str,
    endpoint_type: str,
    attachment_type: str,
    *,
    domain_certificate_arn: str | None = None,
    application_domain: str | None = None,
    endpoint_domain_prefix: str | None = None,
    security_group_ids: list[str] | None = None,
    load_balancer_options: dict[str, Any] | None = None,
    network_interface_options: dict[str, Any] | None = None,
    description: str | None = None,
    policy_document: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    sse_specification: dict[str, Any] | None = None,
    rds_options: dict[str, Any] | None = None,
    cidr_options: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> CreateVerifiedAccessEndpointResult:
    """Create verified access endpoint.

    Args:
        verified_access_group_id: Verified access group id.
        endpoint_type: Endpoint type.
        attachment_type: Attachment type.
        domain_certificate_arn: Domain certificate arn.
        application_domain: Application domain.
        endpoint_domain_prefix: Endpoint domain prefix.
        security_group_ids: Security group ids.
        load_balancer_options: Load balancer options.
        network_interface_options: Network interface options.
        description: Description.
        policy_document: Policy document.
        tag_specifications: Tag specifications.
        client_token: Client token.
        sse_specification: Sse specification.
        rds_options: Rds options.
        cidr_options: Cidr options.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessGroupId"] = verified_access_group_id
    kwargs["EndpointType"] = endpoint_type
    kwargs["AttachmentType"] = attachment_type
    if domain_certificate_arn is not None:
        kwargs["DomainCertificateArn"] = domain_certificate_arn
    if application_domain is not None:
        kwargs["ApplicationDomain"] = application_domain
    if endpoint_domain_prefix is not None:
        kwargs["EndpointDomainPrefix"] = endpoint_domain_prefix
    if security_group_ids is not None:
        kwargs["SecurityGroupIds"] = security_group_ids
    if load_balancer_options is not None:
        kwargs["LoadBalancerOptions"] = load_balancer_options
    if network_interface_options is not None:
        kwargs["NetworkInterfaceOptions"] = network_interface_options
    if description is not None:
        kwargs["Description"] = description
    if policy_document is not None:
        kwargs["PolicyDocument"] = policy_document
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if sse_specification is not None:
        kwargs["SseSpecification"] = sse_specification
    if rds_options is not None:
        kwargs["RdsOptions"] = rds_options
    if cidr_options is not None:
        kwargs["CidrOptions"] = cidr_options
    try:
        resp = await client.call("CreateVerifiedAccessEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create verified access endpoint") from exc
    return CreateVerifiedAccessEndpointResult(
        verified_access_endpoint=resp.get("VerifiedAccessEndpoint"),
    )


async def create_verified_access_group(
    verified_access_instance_id: str,
    *,
    description: str | None = None,
    policy_document: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    sse_specification: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> CreateVerifiedAccessGroupResult:
    """Create verified access group.

    Args:
        verified_access_instance_id: Verified access instance id.
        description: Description.
        policy_document: Policy document.
        tag_specifications: Tag specifications.
        client_token: Client token.
        sse_specification: Sse specification.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessInstanceId"] = verified_access_instance_id
    if description is not None:
        kwargs["Description"] = description
    if policy_document is not None:
        kwargs["PolicyDocument"] = policy_document
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if sse_specification is not None:
        kwargs["SseSpecification"] = sse_specification
    try:
        resp = await client.call("CreateVerifiedAccessGroup", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create verified access group") from exc
    return CreateVerifiedAccessGroupResult(
        verified_access_group=resp.get("VerifiedAccessGroup"),
    )


async def create_verified_access_instance(
    *,
    description: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    fips_enabled: bool | None = None,
    cidr_endpoints_custom_sub_domain: str | None = None,
    region_name: str | None = None,
) -> CreateVerifiedAccessInstanceResult:
    """Create verified access instance.

    Args:
        description: Description.
        tag_specifications: Tag specifications.
        client_token: Client token.
        fips_enabled: Fips enabled.
        cidr_endpoints_custom_sub_domain: Cidr endpoints custom sub domain.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if description is not None:
        kwargs["Description"] = description
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if fips_enabled is not None:
        kwargs["FIPSEnabled"] = fips_enabled
    if cidr_endpoints_custom_sub_domain is not None:
        kwargs["CidrEndpointsCustomSubDomain"] = cidr_endpoints_custom_sub_domain
    try:
        resp = await client.call("CreateVerifiedAccessInstance", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create verified access instance") from exc
    return CreateVerifiedAccessInstanceResult(
        verified_access_instance=resp.get("VerifiedAccessInstance"),
    )


async def create_verified_access_trust_provider(
    trust_provider_type: str,
    policy_reference_name: str,
    *,
    user_trust_provider_type: str | None = None,
    device_trust_provider_type: str | None = None,
    oidc_options: dict[str, Any] | None = None,
    device_options: dict[str, Any] | None = None,
    description: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    client_token: str | None = None,
    sse_specification: dict[str, Any] | None = None,
    native_application_oidc_options: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> CreateVerifiedAccessTrustProviderResult:
    """Create verified access trust provider.

    Args:
        trust_provider_type: Trust provider type.
        policy_reference_name: Policy reference name.
        user_trust_provider_type: User trust provider type.
        device_trust_provider_type: Device trust provider type.
        oidc_options: Oidc options.
        device_options: Device options.
        description: Description.
        tag_specifications: Tag specifications.
        client_token: Client token.
        sse_specification: Sse specification.
        native_application_oidc_options: Native application oidc options.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TrustProviderType"] = trust_provider_type
    kwargs["PolicyReferenceName"] = policy_reference_name
    if user_trust_provider_type is not None:
        kwargs["UserTrustProviderType"] = user_trust_provider_type
    if device_trust_provider_type is not None:
        kwargs["DeviceTrustProviderType"] = device_trust_provider_type
    if oidc_options is not None:
        kwargs["OidcOptions"] = oidc_options
    if device_options is not None:
        kwargs["DeviceOptions"] = device_options
    if description is not None:
        kwargs["Description"] = description
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if sse_specification is not None:
        kwargs["SseSpecification"] = sse_specification
    if native_application_oidc_options is not None:
        kwargs["NativeApplicationOidcOptions"] = native_application_oidc_options
    try:
        resp = await client.call("CreateVerifiedAccessTrustProvider", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create verified access trust provider") from exc
    return CreateVerifiedAccessTrustProviderResult(
        verified_access_trust_provider=resp.get("VerifiedAccessTrustProvider"),
    )


async def create_volume(
    *,
    availability_zone: str | None = None,
    availability_zone_id: str | None = None,
    encrypted: bool | None = None,
    iops: int | None = None,
    kms_key_id: str | None = None,
    outpost_arn: str | None = None,
    size: int | None = None,
    snapshot_id: str | None = None,
    volume_type: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    multi_attach_enabled: bool | None = None,
    throughput: int | None = None,
    client_token: str | None = None,
    volume_initialization_rate: int | None = None,
    operator: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> CreateVolumeResult:
    """Create volume.

    Args:
        availability_zone: Availability zone.
        availability_zone_id: Availability zone id.
        encrypted: Encrypted.
        iops: Iops.
        kms_key_id: Kms key id.
        outpost_arn: Outpost arn.
        size: Size.
        snapshot_id: Snapshot id.
        volume_type: Volume type.
        tag_specifications: Tag specifications.
        multi_attach_enabled: Multi attach enabled.
        throughput: Throughput.
        client_token: Client token.
        volume_initialization_rate: Volume initialization rate.
        operator: Operator.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if availability_zone is not None:
        kwargs["AvailabilityZone"] = availability_zone
    if availability_zone_id is not None:
        kwargs["AvailabilityZoneId"] = availability_zone_id
    if encrypted is not None:
        kwargs["Encrypted"] = encrypted
    if iops is not None:
        kwargs["Iops"] = iops
    if kms_key_id is not None:
        kwargs["KmsKeyId"] = kms_key_id
    if outpost_arn is not None:
        kwargs["OutpostArn"] = outpost_arn
    if size is not None:
        kwargs["Size"] = size
    if snapshot_id is not None:
        kwargs["SnapshotId"] = snapshot_id
    if volume_type is not None:
        kwargs["VolumeType"] = volume_type
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if multi_attach_enabled is not None:
        kwargs["MultiAttachEnabled"] = multi_attach_enabled
    if throughput is not None:
        kwargs["Throughput"] = throughput
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if volume_initialization_rate is not None:
        kwargs["VolumeInitializationRate"] = volume_initialization_rate
    if operator is not None:
        kwargs["Operator"] = operator
    try:
        resp = await client.call("CreateVolume", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create volume") from exc
    return CreateVolumeResult(
        availability_zone_id=resp.get("AvailabilityZoneId"),
        outpost_arn=resp.get("OutpostArn"),
        source_volume_id=resp.get("SourceVolumeId"),
        iops=resp.get("Iops"),
        tags=resp.get("Tags"),
        volume_type=resp.get("VolumeType"),
        fast_restored=resp.get("FastRestored"),
        multi_attach_enabled=resp.get("MultiAttachEnabled"),
        throughput=resp.get("Throughput"),
        sse_type=resp.get("SseType"),
        operator=resp.get("Operator"),
        volume_initialization_rate=resp.get("VolumeInitializationRate"),
        volume_id=resp.get("VolumeId"),
        size=resp.get("Size"),
        snapshot_id=resp.get("SnapshotId"),
        availability_zone=resp.get("AvailabilityZone"),
        state=resp.get("State"),
        create_time=resp.get("CreateTime"),
        attachments=resp.get("Attachments"),
        encrypted=resp.get("Encrypted"),
        kms_key_id=resp.get("KmsKeyId"),
    )


async def create_vpc(
    *,
    cidr_block: str | None = None,
    ipv6_pool: str | None = None,
    ipv6_cidr_block: str | None = None,
    ipv4_ipam_pool_id: str | None = None,
    ipv4_netmask_length: int | None = None,
    ipv6_ipam_pool_id: str | None = None,
    ipv6_netmask_length: int | None = None,
    ipv6_cidr_block_network_border_group: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    instance_tenancy: str | None = None,
    amazon_provided_ipv6_cidr_block: bool | None = None,
    region_name: str | None = None,
) -> CreateVpcResult:
    """Create vpc.

    Args:
        cidr_block: Cidr block.
        ipv6_pool: Ipv6 pool.
        ipv6_cidr_block: Ipv6 cidr block.
        ipv4_ipam_pool_id: Ipv4 ipam pool id.
        ipv4_netmask_length: Ipv4 netmask length.
        ipv6_ipam_pool_id: Ipv6 ipam pool id.
        ipv6_netmask_length: Ipv6 netmask length.
        ipv6_cidr_block_network_border_group: Ipv6 cidr block network border group.
        tag_specifications: Tag specifications.
        instance_tenancy: Instance tenancy.
        amazon_provided_ipv6_cidr_block: Amazon provided ipv6 cidr block.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if cidr_block is not None:
        kwargs["CidrBlock"] = cidr_block
    if ipv6_pool is not None:
        kwargs["Ipv6Pool"] = ipv6_pool
    if ipv6_cidr_block is not None:
        kwargs["Ipv6CidrBlock"] = ipv6_cidr_block
    if ipv4_ipam_pool_id is not None:
        kwargs["Ipv4IpamPoolId"] = ipv4_ipam_pool_id
    if ipv4_netmask_length is not None:
        kwargs["Ipv4NetmaskLength"] = ipv4_netmask_length
    if ipv6_ipam_pool_id is not None:
        kwargs["Ipv6IpamPoolId"] = ipv6_ipam_pool_id
    if ipv6_netmask_length is not None:
        kwargs["Ipv6NetmaskLength"] = ipv6_netmask_length
    if ipv6_cidr_block_network_border_group is not None:
        kwargs["Ipv6CidrBlockNetworkBorderGroup"] = ipv6_cidr_block_network_border_group
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if instance_tenancy is not None:
        kwargs["InstanceTenancy"] = instance_tenancy
    if amazon_provided_ipv6_cidr_block is not None:
        kwargs["AmazonProvidedIpv6CidrBlock"] = amazon_provided_ipv6_cidr_block
    try:
        resp = await client.call("CreateVpc", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create vpc") from exc
    return CreateVpcResult(
        vpc=resp.get("Vpc"),
    )


async def create_vpc_block_public_access_exclusion(
    internet_gateway_exclusion_mode: str,
    *,
    subnet_id: str | None = None,
    vpc_id: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateVpcBlockPublicAccessExclusionResult:
    """Create vpc block public access exclusion.

    Args:
        internet_gateway_exclusion_mode: Internet gateway exclusion mode.
        subnet_id: Subnet id.
        vpc_id: Vpc id.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InternetGatewayExclusionMode"] = internet_gateway_exclusion_mode
    if subnet_id is not None:
        kwargs["SubnetId"] = subnet_id
    if vpc_id is not None:
        kwargs["VpcId"] = vpc_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateVpcBlockPublicAccessExclusion", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create vpc block public access exclusion") from exc
    return CreateVpcBlockPublicAccessExclusionResult(
        vpc_block_public_access_exclusion=resp.get("VpcBlockPublicAccessExclusion"),
    )


async def create_vpc_endpoint(
    vpc_id: str,
    *,
    vpc_endpoint_type: str | None = None,
    service_name: str | None = None,
    policy_document: str | None = None,
    route_table_ids: list[str] | None = None,
    subnet_ids: list[str] | None = None,
    security_group_ids: list[str] | None = None,
    ip_address_type: str | None = None,
    dns_options: dict[str, Any] | None = None,
    client_token: str | None = None,
    private_dns_enabled: bool | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    subnet_configurations: list[dict[str, Any]] | None = None,
    service_network_arn: str | None = None,
    resource_configuration_arn: str | None = None,
    service_region: str | None = None,
    region_name: str | None = None,
) -> CreateVpcEndpointResult:
    """Create vpc endpoint.

    Args:
        vpc_id: Vpc id.
        vpc_endpoint_type: Vpc endpoint type.
        service_name: Service name.
        policy_document: Policy document.
        route_table_ids: Route table ids.
        subnet_ids: Subnet ids.
        security_group_ids: Security group ids.
        ip_address_type: Ip address type.
        dns_options: Dns options.
        client_token: Client token.
        private_dns_enabled: Private dns enabled.
        tag_specifications: Tag specifications.
        subnet_configurations: Subnet configurations.
        service_network_arn: Service network arn.
        resource_configuration_arn: Resource configuration arn.
        service_region: Service region.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    if vpc_endpoint_type is not None:
        kwargs["VpcEndpointType"] = vpc_endpoint_type
    if service_name is not None:
        kwargs["ServiceName"] = service_name
    if policy_document is not None:
        kwargs["PolicyDocument"] = policy_document
    if route_table_ids is not None:
        kwargs["RouteTableIds"] = route_table_ids
    if subnet_ids is not None:
        kwargs["SubnetIds"] = subnet_ids
    if security_group_ids is not None:
        kwargs["SecurityGroupIds"] = security_group_ids
    if ip_address_type is not None:
        kwargs["IpAddressType"] = ip_address_type
    if dns_options is not None:
        kwargs["DnsOptions"] = dns_options
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if private_dns_enabled is not None:
        kwargs["PrivateDnsEnabled"] = private_dns_enabled
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if subnet_configurations is not None:
        kwargs["SubnetConfigurations"] = subnet_configurations
    if service_network_arn is not None:
        kwargs["ServiceNetworkArn"] = service_network_arn
    if resource_configuration_arn is not None:
        kwargs["ResourceConfigurationArn"] = resource_configuration_arn
    if service_region is not None:
        kwargs["ServiceRegion"] = service_region
    try:
        resp = await client.call("CreateVpcEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create vpc endpoint") from exc
    return CreateVpcEndpointResult(
        vpc_endpoint=resp.get("VpcEndpoint"),
        client_token=resp.get("ClientToken"),
    )


async def create_vpc_endpoint_connection_notification(
    connection_notification_arn: str,
    connection_events: list[str],
    *,
    service_id: str | None = None,
    vpc_endpoint_id: str | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> CreateVpcEndpointConnectionNotificationResult:
    """Create vpc endpoint connection notification.

    Args:
        connection_notification_arn: Connection notification arn.
        connection_events: Connection events.
        service_id: Service id.
        vpc_endpoint_id: Vpc endpoint id.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ConnectionNotificationArn"] = connection_notification_arn
    kwargs["ConnectionEvents"] = connection_events
    if service_id is not None:
        kwargs["ServiceId"] = service_id
    if vpc_endpoint_id is not None:
        kwargs["VpcEndpointId"] = vpc_endpoint_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("CreateVpcEndpointConnectionNotification", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create vpc endpoint connection notification") from exc
    return CreateVpcEndpointConnectionNotificationResult(
        connection_notification=resp.get("ConnectionNotification"),
        client_token=resp.get("ClientToken"),
    )


async def create_vpc_endpoint_service_configuration(
    *,
    acceptance_required: bool | None = None,
    private_dns_name: str | None = None,
    network_load_balancer_arns: list[str] | None = None,
    gateway_load_balancer_arns: list[str] | None = None,
    supported_ip_address_types: list[str] | None = None,
    supported_regions: list[str] | None = None,
    client_token: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> CreateVpcEndpointServiceConfigurationResult:
    """Create vpc endpoint service configuration.

    Args:
        acceptance_required: Acceptance required.
        private_dns_name: Private dns name.
        network_load_balancer_arns: Network load balancer arns.
        gateway_load_balancer_arns: Gateway load balancer arns.
        supported_ip_address_types: Supported ip address types.
        supported_regions: Supported regions.
        client_token: Client token.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if acceptance_required is not None:
        kwargs["AcceptanceRequired"] = acceptance_required
    if private_dns_name is not None:
        kwargs["PrivateDnsName"] = private_dns_name
    if network_load_balancer_arns is not None:
        kwargs["NetworkLoadBalancerArns"] = network_load_balancer_arns
    if gateway_load_balancer_arns is not None:
        kwargs["GatewayLoadBalancerArns"] = gateway_load_balancer_arns
    if supported_ip_address_types is not None:
        kwargs["SupportedIpAddressTypes"] = supported_ip_address_types
    if supported_regions is not None:
        kwargs["SupportedRegions"] = supported_regions
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("CreateVpcEndpointServiceConfiguration", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create vpc endpoint service configuration") from exc
    return CreateVpcEndpointServiceConfigurationResult(
        service_configuration=resp.get("ServiceConfiguration"),
        client_token=resp.get("ClientToken"),
    )


async def create_vpc_peering_connection(
    vpc_id: str,
    *,
    peer_region: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    peer_vpc_id: str | None = None,
    peer_owner_id: str | None = None,
    region_name: str | None = None,
) -> CreateVpcPeeringConnectionResult:
    """Create vpc peering connection.

    Args:
        vpc_id: Vpc id.
        peer_region: Peer region.
        tag_specifications: Tag specifications.
        peer_vpc_id: Peer vpc id.
        peer_owner_id: Peer owner id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    if peer_region is not None:
        kwargs["PeerRegion"] = peer_region
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if peer_vpc_id is not None:
        kwargs["PeerVpcId"] = peer_vpc_id
    if peer_owner_id is not None:
        kwargs["PeerOwnerId"] = peer_owner_id
    try:
        resp = await client.call("CreateVpcPeeringConnection", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create vpc peering connection") from exc
    return CreateVpcPeeringConnectionResult(
        vpc_peering_connection=resp.get("VpcPeeringConnection"),
    )


async def create_vpn_connection(
    customer_gateway_id: str,
    type_value: str,
    *,
    vpn_gateway_id: str | None = None,
    transit_gateway_id: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    pre_shared_key_storage: str | None = None,
    options: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> CreateVpnConnectionResult:
    """Create vpn connection.

    Args:
        customer_gateway_id: Customer gateway id.
        type_value: Type value.
        vpn_gateway_id: Vpn gateway id.
        transit_gateway_id: Transit gateway id.
        tag_specifications: Tag specifications.
        pre_shared_key_storage: Pre shared key storage.
        options: Options.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CustomerGatewayId"] = customer_gateway_id
    kwargs["Type"] = type_value
    if vpn_gateway_id is not None:
        kwargs["VpnGatewayId"] = vpn_gateway_id
    if transit_gateway_id is not None:
        kwargs["TransitGatewayId"] = transit_gateway_id
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if pre_shared_key_storage is not None:
        kwargs["PreSharedKeyStorage"] = pre_shared_key_storage
    if options is not None:
        kwargs["Options"] = options
    try:
        resp = await client.call("CreateVpnConnection", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create vpn connection") from exc
    return CreateVpnConnectionResult(
        vpn_connection=resp.get("VpnConnection"),
    )


async def create_vpn_connection_route(
    destination_cidr_block: str,
    vpn_connection_id: str,
    region_name: str | None = None,
) -> None:
    """Create vpn connection route.

    Args:
        destination_cidr_block: Destination cidr block.
        vpn_connection_id: Vpn connection id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["DestinationCidrBlock"] = destination_cidr_block
    kwargs["VpnConnectionId"] = vpn_connection_id
    try:
        await client.call("CreateVpnConnectionRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create vpn connection route") from exc
    return None


async def create_vpn_gateway(
    type_value: str,
    *,
    availability_zone: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    amazon_side_asn: int | None = None,
    region_name: str | None = None,
) -> CreateVpnGatewayResult:
    """Create vpn gateway.

    Args:
        type_value: Type value.
        availability_zone: Availability zone.
        tag_specifications: Tag specifications.
        amazon_side_asn: Amazon side asn.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Type"] = type_value
    if availability_zone is not None:
        kwargs["AvailabilityZone"] = availability_zone
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if amazon_side_asn is not None:
        kwargs["AmazonSideAsn"] = amazon_side_asn
    try:
        resp = await client.call("CreateVpnGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to create vpn gateway") from exc
    return CreateVpnGatewayResult(
        vpn_gateway=resp.get("VpnGateway"),
    )


async def delete_capacity_manager_data_export(
    capacity_manager_data_export_id: str,
    region_name: str | None = None,
) -> DeleteCapacityManagerDataExportResult:
    """Delete capacity manager data export.

    Args:
        capacity_manager_data_export_id: Capacity manager data export id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityManagerDataExportId"] = capacity_manager_data_export_id
    try:
        resp = await client.call("DeleteCapacityManagerDataExport", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete capacity manager data export") from exc
    return DeleteCapacityManagerDataExportResult(
        capacity_manager_data_export_id=resp.get("CapacityManagerDataExportId"),
    )


async def delete_carrier_gateway(
    carrier_gateway_id: str,
    region_name: str | None = None,
) -> DeleteCarrierGatewayResult:
    """Delete carrier gateway.

    Args:
        carrier_gateway_id: Carrier gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CarrierGatewayId"] = carrier_gateway_id
    try:
        resp = await client.call("DeleteCarrierGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete carrier gateway") from exc
    return DeleteCarrierGatewayResult(
        carrier_gateway=resp.get("CarrierGateway"),
    )


async def delete_client_vpn_endpoint(
    client_vpn_endpoint_id: str,
    region_name: str | None = None,
) -> DeleteClientVpnEndpointResult:
    """Delete client vpn endpoint.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    try:
        resp = await client.call("DeleteClientVpnEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete client vpn endpoint") from exc
    return DeleteClientVpnEndpointResult(
        status=resp.get("Status"),
    )


async def delete_client_vpn_route(
    client_vpn_endpoint_id: str,
    destination_cidr_block: str,
    *,
    target_vpc_subnet_id: str | None = None,
    region_name: str | None = None,
) -> DeleteClientVpnRouteResult:
    """Delete client vpn route.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        destination_cidr_block: Destination cidr block.
        target_vpc_subnet_id: Target vpc subnet id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    kwargs["DestinationCidrBlock"] = destination_cidr_block
    if target_vpc_subnet_id is not None:
        kwargs["TargetVpcSubnetId"] = target_vpc_subnet_id
    try:
        resp = await client.call("DeleteClientVpnRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete client vpn route") from exc
    return DeleteClientVpnRouteResult(
        status=resp.get("Status"),
    )


async def delete_coip_cidr(
    cidr: str,
    coip_pool_id: str,
    region_name: str | None = None,
) -> DeleteCoipCidrResult:
    """Delete coip cidr.

    Args:
        cidr: Cidr.
        coip_pool_id: Coip pool id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Cidr"] = cidr
    kwargs["CoipPoolId"] = coip_pool_id
    try:
        resp = await client.call("DeleteCoipCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete coip cidr") from exc
    return DeleteCoipCidrResult(
        coip_cidr=resp.get("CoipCidr"),
    )


async def delete_coip_pool(
    coip_pool_id: str,
    region_name: str | None = None,
) -> DeleteCoipPoolResult:
    """Delete coip pool.

    Args:
        coip_pool_id: Coip pool id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CoipPoolId"] = coip_pool_id
    try:
        resp = await client.call("DeleteCoipPool", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete coip pool") from exc
    return DeleteCoipPoolResult(
        coip_pool=resp.get("CoipPool"),
    )


async def delete_customer_gateway(
    customer_gateway_id: str,
    region_name: str | None = None,
) -> None:
    """Delete customer gateway.

    Args:
        customer_gateway_id: Customer gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CustomerGatewayId"] = customer_gateway_id
    try:
        await client.call("DeleteCustomerGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete customer gateway") from exc
    return None


async def delete_dhcp_options(
    dhcp_options_id: str,
    region_name: str | None = None,
) -> None:
    """Delete dhcp options.

    Args:
        dhcp_options_id: Dhcp options id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["DhcpOptionsId"] = dhcp_options_id
    try:
        await client.call("DeleteDhcpOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete dhcp options") from exc
    return None


async def delete_egress_only_internet_gateway(
    egress_only_internet_gateway_id: str,
    region_name: str | None = None,
) -> DeleteEgressOnlyInternetGatewayResult:
    """Delete egress only internet gateway.

    Args:
        egress_only_internet_gateway_id: Egress only internet gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["EgressOnlyInternetGatewayId"] = egress_only_internet_gateway_id
    try:
        resp = await client.call("DeleteEgressOnlyInternetGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete egress only internet gateway") from exc
    return DeleteEgressOnlyInternetGatewayResult(
        return_code=resp.get("ReturnCode"),
    )


async def delete_fleets(
    fleet_ids: list[str],
    terminate_instances: bool,
    region_name: str | None = None,
) -> DeleteFleetsResult:
    """Delete fleets.

    Args:
        fleet_ids: Fleet ids.
        terminate_instances: Terminate instances.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["FleetIds"] = fleet_ids
    kwargs["TerminateInstances"] = terminate_instances
    try:
        resp = await client.call("DeleteFleets", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete fleets") from exc
    return DeleteFleetsResult(
        successful_fleet_deletions=resp.get("SuccessfulFleetDeletions"),
        unsuccessful_fleet_deletions=resp.get("UnsuccessfulFleetDeletions"),
    )


async def delete_flow_logs(
    flow_log_ids: list[str],
    region_name: str | None = None,
) -> DeleteFlowLogsResult:
    """Delete flow logs.

    Args:
        flow_log_ids: Flow log ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["FlowLogIds"] = flow_log_ids
    try:
        resp = await client.call("DeleteFlowLogs", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete flow logs") from exc
    return DeleteFlowLogsResult(
        unsuccessful=resp.get("Unsuccessful"),
    )


async def delete_fpga_image(
    fpga_image_id: str,
    region_name: str | None = None,
) -> DeleteFpgaImageResult:
    """Delete fpga image.

    Args:
        fpga_image_id: Fpga image id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["FpgaImageId"] = fpga_image_id
    try:
        resp = await client.call("DeleteFpgaImage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete fpga image") from exc
    return DeleteFpgaImageResult(
        return_value=resp.get("Return"),
    )


async def delete_image_usage_report(
    report_id: str,
    region_name: str | None = None,
) -> DeleteImageUsageReportResult:
    """Delete image usage report.

    Args:
        report_id: Report id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ReportId"] = report_id
    try:
        resp = await client.call("DeleteImageUsageReport", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete image usage report") from exc
    return DeleteImageUsageReportResult(
        return_value=resp.get("Return"),
    )


async def delete_instance_connect_endpoint(
    instance_connect_endpoint_id: str,
    region_name: str | None = None,
) -> DeleteInstanceConnectEndpointResult:
    """Delete instance connect endpoint.

    Args:
        instance_connect_endpoint_id: Instance connect endpoint id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceConnectEndpointId"] = instance_connect_endpoint_id
    try:
        resp = await client.call("DeleteInstanceConnectEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete instance connect endpoint") from exc
    return DeleteInstanceConnectEndpointResult(
        instance_connect_endpoint=resp.get("InstanceConnectEndpoint"),
    )


async def delete_instance_event_window(
    instance_event_window_id: str,
    *,
    force_delete: bool | None = None,
    region_name: str | None = None,
) -> DeleteInstanceEventWindowResult:
    """Delete instance event window.

    Args:
        instance_event_window_id: Instance event window id.
        force_delete: Force delete.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceEventWindowId"] = instance_event_window_id
    if force_delete is not None:
        kwargs["ForceDelete"] = force_delete
    try:
        resp = await client.call("DeleteInstanceEventWindow", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete instance event window") from exc
    return DeleteInstanceEventWindowResult(
        instance_event_window_state=resp.get("InstanceEventWindowState"),
    )


async def delete_internet_gateway(
    internet_gateway_id: str,
    region_name: str | None = None,
) -> None:
    """Delete internet gateway.

    Args:
        internet_gateway_id: Internet gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InternetGatewayId"] = internet_gateway_id
    try:
        await client.call("DeleteInternetGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete internet gateway") from exc
    return None


async def delete_ipam(
    ipam_id: str,
    *,
    cascade: bool | None = None,
    region_name: str | None = None,
) -> DeleteIpamResult:
    """Delete ipam.

    Args:
        ipam_id: Ipam id.
        cascade: Cascade.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamId"] = ipam_id
    if cascade is not None:
        kwargs["Cascade"] = cascade
    try:
        resp = await client.call("DeleteIpam", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete ipam") from exc
    return DeleteIpamResult(
        ipam=resp.get("Ipam"),
    )


async def delete_ipam_external_resource_verification_token(
    ipam_external_resource_verification_token_id: str,
    region_name: str | None = None,
) -> DeleteIpamExternalResourceVerificationTokenResult:
    """Delete ipam external resource verification token.

    Args:
        ipam_external_resource_verification_token_id: Ipam external resource verification token id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamExternalResourceVerificationTokenId"] = ipam_external_resource_verification_token_id
    try:
        resp = await client.call("DeleteIpamExternalResourceVerificationToken", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to delete ipam external resource verification token"
        ) from exc
    return DeleteIpamExternalResourceVerificationTokenResult(
        ipam_external_resource_verification_token=resp.get("IpamExternalResourceVerificationToken"),
    )


async def delete_ipam_pool(
    ipam_pool_id: str,
    *,
    cascade: bool | None = None,
    region_name: str | None = None,
) -> DeleteIpamPoolResult:
    """Delete ipam pool.

    Args:
        ipam_pool_id: Ipam pool id.
        cascade: Cascade.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPoolId"] = ipam_pool_id
    if cascade is not None:
        kwargs["Cascade"] = cascade
    try:
        resp = await client.call("DeleteIpamPool", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete ipam pool") from exc
    return DeleteIpamPoolResult(
        ipam_pool=resp.get("IpamPool"),
    )


async def delete_ipam_prefix_list_resolver(
    ipam_prefix_list_resolver_id: str,
    region_name: str | None = None,
) -> DeleteIpamPrefixListResolverResult:
    """Delete ipam prefix list resolver.

    Args:
        ipam_prefix_list_resolver_id: Ipam prefix list resolver id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPrefixListResolverId"] = ipam_prefix_list_resolver_id
    try:
        resp = await client.call("DeleteIpamPrefixListResolver", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete ipam prefix list resolver") from exc
    return DeleteIpamPrefixListResolverResult(
        ipam_prefix_list_resolver=resp.get("IpamPrefixListResolver"),
    )


async def delete_ipam_prefix_list_resolver_target(
    ipam_prefix_list_resolver_target_id: str,
    region_name: str | None = None,
) -> DeleteIpamPrefixListResolverTargetResult:
    """Delete ipam prefix list resolver target.

    Args:
        ipam_prefix_list_resolver_target_id: Ipam prefix list resolver target id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPrefixListResolverTargetId"] = ipam_prefix_list_resolver_target_id
    try:
        resp = await client.call("DeleteIpamPrefixListResolverTarget", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete ipam prefix list resolver target") from exc
    return DeleteIpamPrefixListResolverTargetResult(
        ipam_prefix_list_resolver_target=resp.get("IpamPrefixListResolverTarget"),
    )


async def delete_ipam_resource_discovery(
    ipam_resource_discovery_id: str,
    region_name: str | None = None,
) -> DeleteIpamResourceDiscoveryResult:
    """Delete ipam resource discovery.

    Args:
        ipam_resource_discovery_id: Ipam resource discovery id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamResourceDiscoveryId"] = ipam_resource_discovery_id
    try:
        resp = await client.call("DeleteIpamResourceDiscovery", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete ipam resource discovery") from exc
    return DeleteIpamResourceDiscoveryResult(
        ipam_resource_discovery=resp.get("IpamResourceDiscovery"),
    )


async def delete_ipam_scope(
    ipam_scope_id: str,
    region_name: str | None = None,
) -> DeleteIpamScopeResult:
    """Delete ipam scope.

    Args:
        ipam_scope_id: Ipam scope id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamScopeId"] = ipam_scope_id
    try:
        resp = await client.call("DeleteIpamScope", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete ipam scope") from exc
    return DeleteIpamScopeResult(
        ipam_scope=resp.get("IpamScope"),
    )


async def delete_key_pair(
    *,
    key_name: str | None = None,
    key_pair_id: str | None = None,
    region_name: str | None = None,
) -> DeleteKeyPairResult:
    """Delete key pair.

    Args:
        key_name: Key name.
        key_pair_id: Key pair id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if key_name is not None:
        kwargs["KeyName"] = key_name
    if key_pair_id is not None:
        kwargs["KeyPairId"] = key_pair_id
    try:
        resp = await client.call("DeleteKeyPair", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete key pair") from exc
    return DeleteKeyPairResult(
        return_value=resp.get("Return"),
        key_pair_id=resp.get("KeyPairId"),
    )


async def delete_launch_template(
    *,
    launch_template_id: str | None = None,
    launch_template_name: str | None = None,
    region_name: str | None = None,
) -> DeleteLaunchTemplateResult:
    """Delete launch template.

    Args:
        launch_template_id: Launch template id.
        launch_template_name: Launch template name.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if launch_template_id is not None:
        kwargs["LaunchTemplateId"] = launch_template_id
    if launch_template_name is not None:
        kwargs["LaunchTemplateName"] = launch_template_name
    try:
        resp = await client.call("DeleteLaunchTemplate", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete launch template") from exc
    return DeleteLaunchTemplateResult(
        launch_template=resp.get("LaunchTemplate"),
    )


async def delete_launch_template_versions(
    versions: list[str],
    *,
    launch_template_id: str | None = None,
    launch_template_name: str | None = None,
    region_name: str | None = None,
) -> DeleteLaunchTemplateVersionsResult:
    """Delete launch template versions.

    Args:
        versions: Versions.
        launch_template_id: Launch template id.
        launch_template_name: Launch template name.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Versions"] = versions
    if launch_template_id is not None:
        kwargs["LaunchTemplateId"] = launch_template_id
    if launch_template_name is not None:
        kwargs["LaunchTemplateName"] = launch_template_name
    try:
        resp = await client.call("DeleteLaunchTemplateVersions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete launch template versions") from exc
    return DeleteLaunchTemplateVersionsResult(
        successfully_deleted_launch_template_versions=resp.get(
            "SuccessfullyDeletedLaunchTemplateVersions"
        ),
        unsuccessfully_deleted_launch_template_versions=resp.get(
            "UnsuccessfullyDeletedLaunchTemplateVersions"
        ),
    )


async def delete_local_gateway_route(
    local_gateway_route_table_id: str,
    *,
    destination_cidr_block: str | None = None,
    destination_prefix_list_id: str | None = None,
    region_name: str | None = None,
) -> DeleteLocalGatewayRouteResult:
    """Delete local gateway route.

    Args:
        local_gateway_route_table_id: Local gateway route table id.
        destination_cidr_block: Destination cidr block.
        destination_prefix_list_id: Destination prefix list id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayRouteTableId"] = local_gateway_route_table_id
    if destination_cidr_block is not None:
        kwargs["DestinationCidrBlock"] = destination_cidr_block
    if destination_prefix_list_id is not None:
        kwargs["DestinationPrefixListId"] = destination_prefix_list_id
    try:
        resp = await client.call("DeleteLocalGatewayRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete local gateway route") from exc
    return DeleteLocalGatewayRouteResult(
        route=resp.get("Route"),
    )


async def delete_local_gateway_route_table(
    local_gateway_route_table_id: str,
    region_name: str | None = None,
) -> DeleteLocalGatewayRouteTableResult:
    """Delete local gateway route table.

    Args:
        local_gateway_route_table_id: Local gateway route table id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayRouteTableId"] = local_gateway_route_table_id
    try:
        resp = await client.call("DeleteLocalGatewayRouteTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete local gateway route table") from exc
    return DeleteLocalGatewayRouteTableResult(
        local_gateway_route_table=resp.get("LocalGatewayRouteTable"),
    )


async def delete_local_gateway_route_table_virtual_interface_group_association(
    local_gateway_route_table_virtual_interface_group_association_id: str,
    region_name: str | None = None,
) -> DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult:
    """Delete local gateway route table virtual interface group association.

    Args:
        local_gateway_route_table_virtual_interface_group_association_id: Local gateway route table virtual interface group association id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayRouteTableVirtualInterfaceGroupAssociationId"] = (
        local_gateway_route_table_virtual_interface_group_association_id
    )
    try:
        resp = await client.call(
            "DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociation", **kwargs
        )
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to delete local gateway route table virtual interface group association"
        ) from exc
    return DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult(
        local_gateway_route_table_virtual_interface_group_association=resp.get(
            "LocalGatewayRouteTableVirtualInterfaceGroupAssociation"
        ),
    )


async def delete_local_gateway_route_table_vpc_association(
    local_gateway_route_table_vpc_association_id: str,
    region_name: str | None = None,
) -> DeleteLocalGatewayRouteTableVpcAssociationResult:
    """Delete local gateway route table vpc association.

    Args:
        local_gateway_route_table_vpc_association_id: Local gateway route table vpc association id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayRouteTableVpcAssociationId"] = local_gateway_route_table_vpc_association_id
    try:
        resp = await client.call("DeleteLocalGatewayRouteTableVpcAssociation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to delete local gateway route table vpc association"
        ) from exc
    return DeleteLocalGatewayRouteTableVpcAssociationResult(
        local_gateway_route_table_vpc_association=resp.get("LocalGatewayRouteTableVpcAssociation"),
    )


async def delete_local_gateway_virtual_interface(
    local_gateway_virtual_interface_id: str,
    region_name: str | None = None,
) -> DeleteLocalGatewayVirtualInterfaceResult:
    """Delete local gateway virtual interface.

    Args:
        local_gateway_virtual_interface_id: Local gateway virtual interface id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayVirtualInterfaceId"] = local_gateway_virtual_interface_id
    try:
        resp = await client.call("DeleteLocalGatewayVirtualInterface", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete local gateway virtual interface") from exc
    return DeleteLocalGatewayVirtualInterfaceResult(
        local_gateway_virtual_interface=resp.get("LocalGatewayVirtualInterface"),
    )


async def delete_local_gateway_virtual_interface_group(
    local_gateway_virtual_interface_group_id: str,
    region_name: str | None = None,
) -> DeleteLocalGatewayVirtualInterfaceGroupResult:
    """Delete local gateway virtual interface group.

    Args:
        local_gateway_virtual_interface_group_id: Local gateway virtual interface group id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayVirtualInterfaceGroupId"] = local_gateway_virtual_interface_group_id
    try:
        resp = await client.call("DeleteLocalGatewayVirtualInterfaceGroup", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete local gateway virtual interface group") from exc
    return DeleteLocalGatewayVirtualInterfaceGroupResult(
        local_gateway_virtual_interface_group=resp.get("LocalGatewayVirtualInterfaceGroup"),
    )


async def delete_managed_prefix_list(
    prefix_list_id: str,
    region_name: str | None = None,
) -> DeleteManagedPrefixListResult:
    """Delete managed prefix list.

    Args:
        prefix_list_id: Prefix list id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PrefixListId"] = prefix_list_id
    try:
        resp = await client.call("DeleteManagedPrefixList", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete managed prefix list") from exc
    return DeleteManagedPrefixListResult(
        prefix_list=resp.get("PrefixList"),
    )


async def delete_nat_gateway(
    nat_gateway_id: str,
    region_name: str | None = None,
) -> DeleteNatGatewayResult:
    """Delete nat gateway.

    Args:
        nat_gateway_id: Nat gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NatGatewayId"] = nat_gateway_id
    try:
        resp = await client.call("DeleteNatGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete nat gateway") from exc
    return DeleteNatGatewayResult(
        nat_gateway_id=resp.get("NatGatewayId"),
    )


async def delete_network_acl(
    network_acl_id: str,
    region_name: str | None = None,
) -> None:
    """Delete network acl.

    Args:
        network_acl_id: Network acl id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkAclId"] = network_acl_id
    try:
        await client.call("DeleteNetworkAcl", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete network acl") from exc
    return None


async def delete_network_acl_entry(
    network_acl_id: str,
    rule_number: int,
    egress: bool,
    region_name: str | None = None,
) -> None:
    """Delete network acl entry.

    Args:
        network_acl_id: Network acl id.
        rule_number: Rule number.
        egress: Egress.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkAclId"] = network_acl_id
    kwargs["RuleNumber"] = rule_number
    kwargs["Egress"] = egress
    try:
        await client.call("DeleteNetworkAclEntry", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete network acl entry") from exc
    return None


async def delete_network_insights_access_scope(
    network_insights_access_scope_id: str,
    region_name: str | None = None,
) -> DeleteNetworkInsightsAccessScopeResult:
    """Delete network insights access scope.

    Args:
        network_insights_access_scope_id: Network insights access scope id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInsightsAccessScopeId"] = network_insights_access_scope_id
    try:
        resp = await client.call("DeleteNetworkInsightsAccessScope", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete network insights access scope") from exc
    return DeleteNetworkInsightsAccessScopeResult(
        network_insights_access_scope_id=resp.get("NetworkInsightsAccessScopeId"),
    )


async def delete_network_insights_access_scope_analysis(
    network_insights_access_scope_analysis_id: str,
    region_name: str | None = None,
) -> DeleteNetworkInsightsAccessScopeAnalysisResult:
    """Delete network insights access scope analysis.

    Args:
        network_insights_access_scope_analysis_id: Network insights access scope analysis id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInsightsAccessScopeAnalysisId"] = network_insights_access_scope_analysis_id
    try:
        resp = await client.call("DeleteNetworkInsightsAccessScopeAnalysis", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to delete network insights access scope analysis"
        ) from exc
    return DeleteNetworkInsightsAccessScopeAnalysisResult(
        network_insights_access_scope_analysis_id=resp.get("NetworkInsightsAccessScopeAnalysisId"),
    )


async def delete_network_insights_analysis(
    network_insights_analysis_id: str,
    region_name: str | None = None,
) -> DeleteNetworkInsightsAnalysisResult:
    """Delete network insights analysis.

    Args:
        network_insights_analysis_id: Network insights analysis id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInsightsAnalysisId"] = network_insights_analysis_id
    try:
        resp = await client.call("DeleteNetworkInsightsAnalysis", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete network insights analysis") from exc
    return DeleteNetworkInsightsAnalysisResult(
        network_insights_analysis_id=resp.get("NetworkInsightsAnalysisId"),
    )


async def delete_network_insights_path(
    network_insights_path_id: str,
    region_name: str | None = None,
) -> DeleteNetworkInsightsPathResult:
    """Delete network insights path.

    Args:
        network_insights_path_id: Network insights path id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInsightsPathId"] = network_insights_path_id
    try:
        resp = await client.call("DeleteNetworkInsightsPath", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete network insights path") from exc
    return DeleteNetworkInsightsPathResult(
        network_insights_path_id=resp.get("NetworkInsightsPathId"),
    )


async def delete_network_interface(
    network_interface_id: str,
    region_name: str | None = None,
) -> None:
    """Delete network interface.

    Args:
        network_interface_id: Network interface id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    try:
        await client.call("DeleteNetworkInterface", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete network interface") from exc
    return None


async def delete_network_interface_permission(
    network_interface_permission_id: str,
    *,
    force: bool | None = None,
    region_name: str | None = None,
) -> DeleteNetworkInterfacePermissionResult:
    """Delete network interface permission.

    Args:
        network_interface_permission_id: Network interface permission id.
        force: Force.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfacePermissionId"] = network_interface_permission_id
    if force is not None:
        kwargs["Force"] = force
    try:
        resp = await client.call("DeleteNetworkInterfacePermission", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete network interface permission") from exc
    return DeleteNetworkInterfacePermissionResult(
        return_value=resp.get("Return"),
    )


async def delete_placement_group(
    group_name: str,
    region_name: str | None = None,
) -> None:
    """Delete placement group.

    Args:
        group_name: Group name.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["GroupName"] = group_name
    try:
        await client.call("DeletePlacementGroup", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete placement group") from exc
    return None


async def delete_public_ipv4_pool(
    pool_id: str,
    *,
    network_border_group: str | None = None,
    region_name: str | None = None,
) -> DeletePublicIpv4PoolResult:
    """Delete public ipv4 pool.

    Args:
        pool_id: Pool id.
        network_border_group: Network border group.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PoolId"] = pool_id
    if network_border_group is not None:
        kwargs["NetworkBorderGroup"] = network_border_group
    try:
        resp = await client.call("DeletePublicIpv4Pool", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete public ipv4 pool") from exc
    return DeletePublicIpv4PoolResult(
        return_value=resp.get("ReturnValue"),
    )


async def delete_queued_reserved_instances(
    reserved_instances_ids: list[str],
    region_name: str | None = None,
) -> DeleteQueuedReservedInstancesResult:
    """Delete queued reserved instances.

    Args:
        reserved_instances_ids: Reserved instances ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ReservedInstancesIds"] = reserved_instances_ids
    try:
        resp = await client.call("DeleteQueuedReservedInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete queued reserved instances") from exc
    return DeleteQueuedReservedInstancesResult(
        successful_queued_purchase_deletions=resp.get("SuccessfulQueuedPurchaseDeletions"),
        failed_queued_purchase_deletions=resp.get("FailedQueuedPurchaseDeletions"),
    )


async def delete_route(
    route_table_id: str,
    *,
    destination_prefix_list_id: str | None = None,
    destination_cidr_block: str | None = None,
    destination_ipv6_cidr_block: str | None = None,
    region_name: str | None = None,
) -> None:
    """Delete route.

    Args:
        route_table_id: Route table id.
        destination_prefix_list_id: Destination prefix list id.
        destination_cidr_block: Destination cidr block.
        destination_ipv6_cidr_block: Destination ipv6 cidr block.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteTableId"] = route_table_id
    if destination_prefix_list_id is not None:
        kwargs["DestinationPrefixListId"] = destination_prefix_list_id
    if destination_cidr_block is not None:
        kwargs["DestinationCidrBlock"] = destination_cidr_block
    if destination_ipv6_cidr_block is not None:
        kwargs["DestinationIpv6CidrBlock"] = destination_ipv6_cidr_block
    try:
        await client.call("DeleteRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete route") from exc
    return None


async def delete_route_server(
    route_server_id: str,
    region_name: str | None = None,
) -> DeleteRouteServerResult:
    """Delete route server.

    Args:
        route_server_id: Route server id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerId"] = route_server_id
    try:
        resp = await client.call("DeleteRouteServer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete route server") from exc
    return DeleteRouteServerResult(
        route_server=resp.get("RouteServer"),
    )


async def delete_route_server_endpoint(
    route_server_endpoint_id: str,
    region_name: str | None = None,
) -> DeleteRouteServerEndpointResult:
    """Delete route server endpoint.

    Args:
        route_server_endpoint_id: Route server endpoint id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerEndpointId"] = route_server_endpoint_id
    try:
        resp = await client.call("DeleteRouteServerEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete route server endpoint") from exc
    return DeleteRouteServerEndpointResult(
        route_server_endpoint=resp.get("RouteServerEndpoint"),
    )


async def delete_route_server_peer(
    route_server_peer_id: str,
    region_name: str | None = None,
) -> DeleteRouteServerPeerResult:
    """Delete route server peer.

    Args:
        route_server_peer_id: Route server peer id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerPeerId"] = route_server_peer_id
    try:
        resp = await client.call("DeleteRouteServerPeer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete route server peer") from exc
    return DeleteRouteServerPeerResult(
        route_server_peer=resp.get("RouteServerPeer"),
    )


async def delete_route_table(
    route_table_id: str,
    region_name: str | None = None,
) -> None:
    """Delete route table.

    Args:
        route_table_id: Route table id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteTableId"] = route_table_id
    try:
        await client.call("DeleteRouteTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete route table") from exc
    return None


async def delete_security_group(
    *,
    group_id: str | None = None,
    group_name: str | None = None,
    region_name: str | None = None,
) -> DeleteSecurityGroupResult:
    """Delete security group.

    Args:
        group_id: Group id.
        group_name: Group name.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if group_id is not None:
        kwargs["GroupId"] = group_id
    if group_name is not None:
        kwargs["GroupName"] = group_name
    try:
        resp = await client.call("DeleteSecurityGroup", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete security group") from exc
    return DeleteSecurityGroupResult(
        return_value=resp.get("Return"),
        group_id=resp.get("GroupId"),
    )


async def delete_snapshot(
    snapshot_id: str,
    region_name: str | None = None,
) -> None:
    """Delete snapshot.

    Args:
        snapshot_id: Snapshot id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SnapshotId"] = snapshot_id
    try:
        await client.call("DeleteSnapshot", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete snapshot") from exc
    return None


async def delete_spot_datafeed_subscription(
    region_name: str | None = None,
) -> None:
    """Delete spot datafeed subscription.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        await client.call("DeleteSpotDatafeedSubscription", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete spot datafeed subscription") from exc
    return None


async def delete_subnet(
    subnet_id: str,
    region_name: str | None = None,
) -> None:
    """Delete subnet.

    Args:
        subnet_id: Subnet id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SubnetId"] = subnet_id
    try:
        await client.call("DeleteSubnet", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete subnet") from exc
    return None


async def delete_subnet_cidr_reservation(
    subnet_cidr_reservation_id: str,
    region_name: str | None = None,
) -> DeleteSubnetCidrReservationResult:
    """Delete subnet cidr reservation.

    Args:
        subnet_cidr_reservation_id: Subnet cidr reservation id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SubnetCidrReservationId"] = subnet_cidr_reservation_id
    try:
        resp = await client.call("DeleteSubnetCidrReservation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete subnet cidr reservation") from exc
    return DeleteSubnetCidrReservationResult(
        deleted_subnet_cidr_reservation=resp.get("DeletedSubnetCidrReservation"),
    )


async def delete_tags(
    resources: list[str],
    *,
    tags: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> None:
    """Delete tags.

    Args:
        resources: Resources.
        tags: Tags.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Resources"] = resources
    if tags is not None:
        kwargs["Tags"] = tags
    try:
        await client.call("DeleteTags", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete tags") from exc
    return None


async def delete_traffic_mirror_filter(
    traffic_mirror_filter_id: str,
    region_name: str | None = None,
) -> DeleteTrafficMirrorFilterResult:
    """Delete traffic mirror filter.

    Args:
        traffic_mirror_filter_id: Traffic mirror filter id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TrafficMirrorFilterId"] = traffic_mirror_filter_id
    try:
        resp = await client.call("DeleteTrafficMirrorFilter", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete traffic mirror filter") from exc
    return DeleteTrafficMirrorFilterResult(
        traffic_mirror_filter_id=resp.get("TrafficMirrorFilterId"),
    )


async def delete_traffic_mirror_filter_rule(
    traffic_mirror_filter_rule_id: str,
    region_name: str | None = None,
) -> DeleteTrafficMirrorFilterRuleResult:
    """Delete traffic mirror filter rule.

    Args:
        traffic_mirror_filter_rule_id: Traffic mirror filter rule id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TrafficMirrorFilterRuleId"] = traffic_mirror_filter_rule_id
    try:
        resp = await client.call("DeleteTrafficMirrorFilterRule", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete traffic mirror filter rule") from exc
    return DeleteTrafficMirrorFilterRuleResult(
        traffic_mirror_filter_rule_id=resp.get("TrafficMirrorFilterRuleId"),
    )


async def delete_traffic_mirror_session(
    traffic_mirror_session_id: str,
    region_name: str | None = None,
) -> DeleteTrafficMirrorSessionResult:
    """Delete traffic mirror session.

    Args:
        traffic_mirror_session_id: Traffic mirror session id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TrafficMirrorSessionId"] = traffic_mirror_session_id
    try:
        resp = await client.call("DeleteTrafficMirrorSession", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete traffic mirror session") from exc
    return DeleteTrafficMirrorSessionResult(
        traffic_mirror_session_id=resp.get("TrafficMirrorSessionId"),
    )


async def delete_traffic_mirror_target(
    traffic_mirror_target_id: str,
    region_name: str | None = None,
) -> DeleteTrafficMirrorTargetResult:
    """Delete traffic mirror target.

    Args:
        traffic_mirror_target_id: Traffic mirror target id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TrafficMirrorTargetId"] = traffic_mirror_target_id
    try:
        resp = await client.call("DeleteTrafficMirrorTarget", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete traffic mirror target") from exc
    return DeleteTrafficMirrorTargetResult(
        traffic_mirror_target_id=resp.get("TrafficMirrorTargetId"),
    )


async def delete_transit_gateway(
    transit_gateway_id: str,
    region_name: str | None = None,
) -> DeleteTransitGatewayResult:
    """Delete transit gateway.

    Args:
        transit_gateway_id: Transit gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayId"] = transit_gateway_id
    try:
        resp = await client.call("DeleteTransitGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete transit gateway") from exc
    return DeleteTransitGatewayResult(
        transit_gateway=resp.get("TransitGateway"),
    )


async def delete_transit_gateway_connect(
    transit_gateway_attachment_id: str,
    region_name: str | None = None,
) -> DeleteTransitGatewayConnectResult:
    """Delete transit gateway connect.

    Args:
        transit_gateway_attachment_id: Transit gateway attachment id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    try:
        resp = await client.call("DeleteTransitGatewayConnect", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete transit gateway connect") from exc
    return DeleteTransitGatewayConnectResult(
        transit_gateway_connect=resp.get("TransitGatewayConnect"),
    )


async def delete_transit_gateway_connect_peer(
    transit_gateway_connect_peer_id: str,
    region_name: str | None = None,
) -> DeleteTransitGatewayConnectPeerResult:
    """Delete transit gateway connect peer.

    Args:
        transit_gateway_connect_peer_id: Transit gateway connect peer id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayConnectPeerId"] = transit_gateway_connect_peer_id
    try:
        resp = await client.call("DeleteTransitGatewayConnectPeer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete transit gateway connect peer") from exc
    return DeleteTransitGatewayConnectPeerResult(
        transit_gateway_connect_peer=resp.get("TransitGatewayConnectPeer"),
    )


async def delete_transit_gateway_multicast_domain(
    transit_gateway_multicast_domain_id: str,
    region_name: str | None = None,
) -> DeleteTransitGatewayMulticastDomainResult:
    """Delete transit gateway multicast domain.

    Args:
        transit_gateway_multicast_domain_id: Transit gateway multicast domain id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayMulticastDomainId"] = transit_gateway_multicast_domain_id
    try:
        resp = await client.call("DeleteTransitGatewayMulticastDomain", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete transit gateway multicast domain") from exc
    return DeleteTransitGatewayMulticastDomainResult(
        transit_gateway_multicast_domain=resp.get("TransitGatewayMulticastDomain"),
    )


async def delete_transit_gateway_peering_attachment(
    transit_gateway_attachment_id: str,
    region_name: str | None = None,
) -> DeleteTransitGatewayPeeringAttachmentResult:
    """Delete transit gateway peering attachment.

    Args:
        transit_gateway_attachment_id: Transit gateway attachment id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    try:
        resp = await client.call("DeleteTransitGatewayPeeringAttachment", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete transit gateway peering attachment") from exc
    return DeleteTransitGatewayPeeringAttachmentResult(
        transit_gateway_peering_attachment=resp.get("TransitGatewayPeeringAttachment"),
    )


async def delete_transit_gateway_policy_table(
    transit_gateway_policy_table_id: str,
    region_name: str | None = None,
) -> DeleteTransitGatewayPolicyTableResult:
    """Delete transit gateway policy table.

    Args:
        transit_gateway_policy_table_id: Transit gateway policy table id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayPolicyTableId"] = transit_gateway_policy_table_id
    try:
        resp = await client.call("DeleteTransitGatewayPolicyTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete transit gateway policy table") from exc
    return DeleteTransitGatewayPolicyTableResult(
        transit_gateway_policy_table=resp.get("TransitGatewayPolicyTable"),
    )


async def delete_transit_gateway_prefix_list_reference(
    transit_gateway_route_table_id: str,
    prefix_list_id: str,
    region_name: str | None = None,
) -> DeleteTransitGatewayPrefixListReferenceResult:
    """Delete transit gateway prefix list reference.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        prefix_list_id: Prefix list id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    kwargs["PrefixListId"] = prefix_list_id
    try:
        resp = await client.call("DeleteTransitGatewayPrefixListReference", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete transit gateway prefix list reference") from exc
    return DeleteTransitGatewayPrefixListReferenceResult(
        transit_gateway_prefix_list_reference=resp.get("TransitGatewayPrefixListReference"),
    )


async def delete_transit_gateway_route(
    transit_gateway_route_table_id: str,
    destination_cidr_block: str,
    region_name: str | None = None,
) -> DeleteTransitGatewayRouteResult:
    """Delete transit gateway route.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        destination_cidr_block: Destination cidr block.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    kwargs["DestinationCidrBlock"] = destination_cidr_block
    try:
        resp = await client.call("DeleteTransitGatewayRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete transit gateway route") from exc
    return DeleteTransitGatewayRouteResult(
        route=resp.get("Route"),
    )


async def delete_transit_gateway_route_table(
    transit_gateway_route_table_id: str,
    region_name: str | None = None,
) -> DeleteTransitGatewayRouteTableResult:
    """Delete transit gateway route table.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    try:
        resp = await client.call("DeleteTransitGatewayRouteTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete transit gateway route table") from exc
    return DeleteTransitGatewayRouteTableResult(
        transit_gateway_route_table=resp.get("TransitGatewayRouteTable"),
    )


async def delete_transit_gateway_route_table_announcement(
    transit_gateway_route_table_announcement_id: str,
    region_name: str | None = None,
) -> DeleteTransitGatewayRouteTableAnnouncementResult:
    """Delete transit gateway route table announcement.

    Args:
        transit_gateway_route_table_announcement_id: Transit gateway route table announcement id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableAnnouncementId"] = transit_gateway_route_table_announcement_id
    try:
        resp = await client.call("DeleteTransitGatewayRouteTableAnnouncement", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to delete transit gateway route table announcement"
        ) from exc
    return DeleteTransitGatewayRouteTableAnnouncementResult(
        transit_gateway_route_table_announcement=resp.get("TransitGatewayRouteTableAnnouncement"),
    )


async def delete_transit_gateway_vpc_attachment(
    transit_gateway_attachment_id: str,
    region_name: str | None = None,
) -> DeleteTransitGatewayVpcAttachmentResult:
    """Delete transit gateway vpc attachment.

    Args:
        transit_gateway_attachment_id: Transit gateway attachment id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    try:
        resp = await client.call("DeleteTransitGatewayVpcAttachment", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete transit gateway vpc attachment") from exc
    return DeleteTransitGatewayVpcAttachmentResult(
        transit_gateway_vpc_attachment=resp.get("TransitGatewayVpcAttachment"),
    )


async def delete_verified_access_endpoint(
    verified_access_endpoint_id: str,
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> DeleteVerifiedAccessEndpointResult:
    """Delete verified access endpoint.

    Args:
        verified_access_endpoint_id: Verified access endpoint id.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessEndpointId"] = verified_access_endpoint_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("DeleteVerifiedAccessEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete verified access endpoint") from exc
    return DeleteVerifiedAccessEndpointResult(
        verified_access_endpoint=resp.get("VerifiedAccessEndpoint"),
    )


async def delete_verified_access_group(
    verified_access_group_id: str,
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> DeleteVerifiedAccessGroupResult:
    """Delete verified access group.

    Args:
        verified_access_group_id: Verified access group id.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessGroupId"] = verified_access_group_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("DeleteVerifiedAccessGroup", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete verified access group") from exc
    return DeleteVerifiedAccessGroupResult(
        verified_access_group=resp.get("VerifiedAccessGroup"),
    )


async def delete_verified_access_instance(
    verified_access_instance_id: str,
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> DeleteVerifiedAccessInstanceResult:
    """Delete verified access instance.

    Args:
        verified_access_instance_id: Verified access instance id.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessInstanceId"] = verified_access_instance_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("DeleteVerifiedAccessInstance", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete verified access instance") from exc
    return DeleteVerifiedAccessInstanceResult(
        verified_access_instance=resp.get("VerifiedAccessInstance"),
    )


async def delete_verified_access_trust_provider(
    verified_access_trust_provider_id: str,
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> DeleteVerifiedAccessTrustProviderResult:
    """Delete verified access trust provider.

    Args:
        verified_access_trust_provider_id: Verified access trust provider id.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessTrustProviderId"] = verified_access_trust_provider_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("DeleteVerifiedAccessTrustProvider", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete verified access trust provider") from exc
    return DeleteVerifiedAccessTrustProviderResult(
        verified_access_trust_provider=resp.get("VerifiedAccessTrustProvider"),
    )


async def delete_volume(
    volume_id: str,
    region_name: str | None = None,
) -> None:
    """Delete volume.

    Args:
        volume_id: Volume id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VolumeId"] = volume_id
    try:
        await client.call("DeleteVolume", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete volume") from exc
    return None


async def delete_vpc(
    vpc_id: str,
    region_name: str | None = None,
) -> None:
    """Delete vpc.

    Args:
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    try:
        await client.call("DeleteVpc", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete vpc") from exc
    return None


async def delete_vpc_block_public_access_exclusion(
    exclusion_id: str,
    region_name: str | None = None,
) -> DeleteVpcBlockPublicAccessExclusionResult:
    """Delete vpc block public access exclusion.

    Args:
        exclusion_id: Exclusion id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ExclusionId"] = exclusion_id
    try:
        resp = await client.call("DeleteVpcBlockPublicAccessExclusion", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete vpc block public access exclusion") from exc
    return DeleteVpcBlockPublicAccessExclusionResult(
        vpc_block_public_access_exclusion=resp.get("VpcBlockPublicAccessExclusion"),
    )


async def delete_vpc_endpoint_connection_notifications(
    connection_notification_ids: list[str],
    region_name: str | None = None,
) -> DeleteVpcEndpointConnectionNotificationsResult:
    """Delete vpc endpoint connection notifications.

    Args:
        connection_notification_ids: Connection notification ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ConnectionNotificationIds"] = connection_notification_ids
    try:
        resp = await client.call("DeleteVpcEndpointConnectionNotifications", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete vpc endpoint connection notifications") from exc
    return DeleteVpcEndpointConnectionNotificationsResult(
        unsuccessful=resp.get("Unsuccessful"),
    )


async def delete_vpc_endpoint_service_configurations(
    service_ids: list[str],
    region_name: str | None = None,
) -> DeleteVpcEndpointServiceConfigurationsResult:
    """Delete vpc endpoint service configurations.

    Args:
        service_ids: Service ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ServiceIds"] = service_ids
    try:
        resp = await client.call("DeleteVpcEndpointServiceConfigurations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete vpc endpoint service configurations") from exc
    return DeleteVpcEndpointServiceConfigurationsResult(
        unsuccessful=resp.get("Unsuccessful"),
    )


async def delete_vpc_endpoints(
    vpc_endpoint_ids: list[str],
    region_name: str | None = None,
) -> DeleteVpcEndpointsResult:
    """Delete vpc endpoints.

    Args:
        vpc_endpoint_ids: Vpc endpoint ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcEndpointIds"] = vpc_endpoint_ids
    try:
        resp = await client.call("DeleteVpcEndpoints", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete vpc endpoints") from exc
    return DeleteVpcEndpointsResult(
        unsuccessful=resp.get("Unsuccessful"),
    )


async def delete_vpc_peering_connection(
    vpc_peering_connection_id: str,
    region_name: str | None = None,
) -> DeleteVpcPeeringConnectionResult:
    """Delete vpc peering connection.

    Args:
        vpc_peering_connection_id: Vpc peering connection id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcPeeringConnectionId"] = vpc_peering_connection_id
    try:
        resp = await client.call("DeleteVpcPeeringConnection", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete vpc peering connection") from exc
    return DeleteVpcPeeringConnectionResult(
        return_value=resp.get("Return"),
    )


async def delete_vpn_connection(
    vpn_connection_id: str,
    region_name: str | None = None,
) -> None:
    """Delete vpn connection.

    Args:
        vpn_connection_id: Vpn connection id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpnConnectionId"] = vpn_connection_id
    try:
        await client.call("DeleteVpnConnection", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete vpn connection") from exc
    return None


async def delete_vpn_connection_route(
    destination_cidr_block: str,
    vpn_connection_id: str,
    region_name: str | None = None,
) -> None:
    """Delete vpn connection route.

    Args:
        destination_cidr_block: Destination cidr block.
        vpn_connection_id: Vpn connection id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["DestinationCidrBlock"] = destination_cidr_block
    kwargs["VpnConnectionId"] = vpn_connection_id
    try:
        await client.call("DeleteVpnConnectionRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete vpn connection route") from exc
    return None


async def delete_vpn_gateway(
    vpn_gateway_id: str,
    region_name: str | None = None,
) -> None:
    """Delete vpn gateway.

    Args:
        vpn_gateway_id: Vpn gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpnGatewayId"] = vpn_gateway_id
    try:
        await client.call("DeleteVpnGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to delete vpn gateway") from exc
    return None


async def deprovision_byoip_cidr(
    cidr: str,
    region_name: str | None = None,
) -> DeprovisionByoipCidrResult:
    """Deprovision byoip cidr.

    Args:
        cidr: Cidr.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Cidr"] = cidr
    try:
        resp = await client.call("DeprovisionByoipCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to deprovision byoip cidr") from exc
    return DeprovisionByoipCidrResult(
        byoip_cidr=resp.get("ByoipCidr"),
    )


async def deprovision_ipam_byoasn(
    ipam_id: str,
    asn: str,
    region_name: str | None = None,
) -> DeprovisionIpamByoasnResult:
    """Deprovision ipam byoasn.

    Args:
        ipam_id: Ipam id.
        asn: Asn.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamId"] = ipam_id
    kwargs["Asn"] = asn
    try:
        resp = await client.call("DeprovisionIpamByoasn", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to deprovision ipam byoasn") from exc
    return DeprovisionIpamByoasnResult(
        byoasn=resp.get("Byoasn"),
    )


async def deprovision_ipam_pool_cidr(
    ipam_pool_id: str,
    *,
    cidr: str | None = None,
    region_name: str | None = None,
) -> DeprovisionIpamPoolCidrResult:
    """Deprovision ipam pool cidr.

    Args:
        ipam_pool_id: Ipam pool id.
        cidr: Cidr.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPoolId"] = ipam_pool_id
    if cidr is not None:
        kwargs["Cidr"] = cidr
    try:
        resp = await client.call("DeprovisionIpamPoolCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to deprovision ipam pool cidr") from exc
    return DeprovisionIpamPoolCidrResult(
        ipam_pool_cidr=resp.get("IpamPoolCidr"),
    )


async def deprovision_public_ipv4_pool_cidr(
    pool_id: str,
    cidr: str,
    region_name: str | None = None,
) -> DeprovisionPublicIpv4PoolCidrResult:
    """Deprovision public ipv4 pool cidr.

    Args:
        pool_id: Pool id.
        cidr: Cidr.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PoolId"] = pool_id
    kwargs["Cidr"] = cidr
    try:
        resp = await client.call("DeprovisionPublicIpv4PoolCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to deprovision public ipv4 pool cidr") from exc
    return DeprovisionPublicIpv4PoolCidrResult(
        pool_id=resp.get("PoolId"),
        deprovisioned_addresses=resp.get("DeprovisionedAddresses"),
    )


async def deregister_image(
    image_id: str,
    *,
    delete_associated_snapshots: bool | None = None,
    region_name: str | None = None,
) -> DeregisterImageResult:
    """Deregister image.

    Args:
        image_id: Image id.
        delete_associated_snapshots: Delete associated snapshots.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    if delete_associated_snapshots is not None:
        kwargs["DeleteAssociatedSnapshots"] = delete_associated_snapshots
    try:
        resp = await client.call("DeregisterImage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to deregister image") from exc
    return DeregisterImageResult(
        return_value=resp.get("Return"),
        delete_snapshot_results=resp.get("DeleteSnapshotResults"),
    )


async def deregister_instance_event_notification_attributes(
    instance_tag_attribute: dict[str, Any],
    region_name: str | None = None,
) -> DeregisterInstanceEventNotificationAttributesResult:
    """Deregister instance event notification attributes.

    Args:
        instance_tag_attribute: Instance tag attribute.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceTagAttribute"] = instance_tag_attribute
    try:
        resp = await client.call("DeregisterInstanceEventNotificationAttributes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to deregister instance event notification attributes"
        ) from exc
    return DeregisterInstanceEventNotificationAttributesResult(
        instance_tag_attribute=resp.get("InstanceTagAttribute"),
    )


async def deregister_transit_gateway_multicast_group_members(
    *,
    transit_gateway_multicast_domain_id: str | None = None,
    group_ip_address: str | None = None,
    network_interface_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DeregisterTransitGatewayMulticastGroupMembersResult:
    """Deregister transit gateway multicast group members.

    Args:
        transit_gateway_multicast_domain_id: Transit gateway multicast domain id.
        group_ip_address: Group ip address.
        network_interface_ids: Network interface ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_multicast_domain_id is not None:
        kwargs["TransitGatewayMulticastDomainId"] = transit_gateway_multicast_domain_id
    if group_ip_address is not None:
        kwargs["GroupIpAddress"] = group_ip_address
    if network_interface_ids is not None:
        kwargs["NetworkInterfaceIds"] = network_interface_ids
    try:
        resp = await client.call("DeregisterTransitGatewayMulticastGroupMembers", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to deregister transit gateway multicast group members"
        ) from exc
    return DeregisterTransitGatewayMulticastGroupMembersResult(
        deregistered_multicast_group_members=resp.get("DeregisteredMulticastGroupMembers"),
    )


async def deregister_transit_gateway_multicast_group_sources(
    *,
    transit_gateway_multicast_domain_id: str | None = None,
    group_ip_address: str | None = None,
    network_interface_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DeregisterTransitGatewayMulticastGroupSourcesResult:
    """Deregister transit gateway multicast group sources.

    Args:
        transit_gateway_multicast_domain_id: Transit gateway multicast domain id.
        group_ip_address: Group ip address.
        network_interface_ids: Network interface ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_multicast_domain_id is not None:
        kwargs["TransitGatewayMulticastDomainId"] = transit_gateway_multicast_domain_id
    if group_ip_address is not None:
        kwargs["GroupIpAddress"] = group_ip_address
    if network_interface_ids is not None:
        kwargs["NetworkInterfaceIds"] = network_interface_ids
    try:
        resp = await client.call("DeregisterTransitGatewayMulticastGroupSources", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to deregister transit gateway multicast group sources"
        ) from exc
    return DeregisterTransitGatewayMulticastGroupSourcesResult(
        deregistered_multicast_group_sources=resp.get("DeregisteredMulticastGroupSources"),
    )


async def describe_account_attributes(
    *,
    attribute_names: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeAccountAttributesResult:
    """Describe account attributes.

    Args:
        attribute_names: Attribute names.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if attribute_names is not None:
        kwargs["AttributeNames"] = attribute_names
    try:
        resp = await client.call("DescribeAccountAttributes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe account attributes") from exc
    return DescribeAccountAttributesResult(
        account_attributes=resp.get("AccountAttributes"),
    )


async def describe_address_transfers(
    *,
    allocation_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeAddressTransfersResult:
    """Describe address transfers.

    Args:
        allocation_ids: Allocation ids.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if allocation_ids is not None:
        kwargs["AllocationIds"] = allocation_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeAddressTransfers", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe address transfers") from exc
    return DescribeAddressTransfersResult(
        address_transfers=resp.get("AddressTransfers"),
        next_token=resp.get("NextToken"),
    )


async def describe_addresses(
    *,
    public_ips: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    allocation_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeAddressesResult:
    """Describe addresses.

    Args:
        public_ips: Public ips.
        filters: Filters.
        allocation_ids: Allocation ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if public_ips is not None:
        kwargs["PublicIps"] = public_ips
    if filters is not None:
        kwargs["Filters"] = filters
    if allocation_ids is not None:
        kwargs["AllocationIds"] = allocation_ids
    try:
        resp = await client.call("DescribeAddresses", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe addresses") from exc
    return DescribeAddressesResult(
        addresses=resp.get("Addresses"),
    )


async def describe_addresses_attribute(
    *,
    allocation_ids: list[str] | None = None,
    attribute: str | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeAddressesAttributeResult:
    """Describe addresses attribute.

    Args:
        allocation_ids: Allocation ids.
        attribute: Attribute.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if allocation_ids is not None:
        kwargs["AllocationIds"] = allocation_ids
    if attribute is not None:
        kwargs["Attribute"] = attribute
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeAddressesAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe addresses attribute") from exc
    return DescribeAddressesAttributeResult(
        addresses=resp.get("Addresses"),
        next_token=resp.get("NextToken"),
    )


async def describe_aggregate_id_format(
    region_name: str | None = None,
) -> DescribeAggregateIdFormatResult:
    """Describe aggregate id format.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("DescribeAggregateIdFormat", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe aggregate id format") from exc
    return DescribeAggregateIdFormatResult(
        use_long_ids_aggregated=resp.get("UseLongIdsAggregated"),
        statuses=resp.get("Statuses"),
    )


async def describe_availability_zones(
    *,
    zone_names: list[str] | None = None,
    zone_ids: list[str] | None = None,
    all_availability_zones: bool | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeAvailabilityZonesResult:
    """Describe availability zones.

    Args:
        zone_names: Zone names.
        zone_ids: Zone ids.
        all_availability_zones: All availability zones.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if zone_names is not None:
        kwargs["ZoneNames"] = zone_names
    if zone_ids is not None:
        kwargs["ZoneIds"] = zone_ids
    if all_availability_zones is not None:
        kwargs["AllAvailabilityZones"] = all_availability_zones
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeAvailabilityZones", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe availability zones") from exc
    return DescribeAvailabilityZonesResult(
        availability_zones=resp.get("AvailabilityZones"),
    )


async def describe_aws_network_performance_metric_subscriptions(
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeAwsNetworkPerformanceMetricSubscriptionsResult:
    """Describe aws network performance metric subscriptions.

    Args:
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeAwsNetworkPerformanceMetricSubscriptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe aws network performance metric subscriptions"
        ) from exc
    return DescribeAwsNetworkPerformanceMetricSubscriptionsResult(
        next_token=resp.get("NextToken"),
        subscriptions=resp.get("Subscriptions"),
    )


async def describe_bundle_tasks(
    *,
    bundle_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeBundleTasksResult:
    """Describe bundle tasks.

    Args:
        bundle_ids: Bundle ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if bundle_ids is not None:
        kwargs["BundleIds"] = bundle_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeBundleTasks", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe bundle tasks") from exc
    return DescribeBundleTasksResult(
        bundle_tasks=resp.get("BundleTasks"),
    )


async def describe_byoip_cidrs(
    max_results: int,
    *,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeByoipCidrsResult:
    """Describe byoip cidrs.

    Args:
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeByoipCidrs", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe byoip cidrs") from exc
    return DescribeByoipCidrsResult(
        byoip_cidrs=resp.get("ByoipCidrs"),
        next_token=resp.get("NextToken"),
    )


async def describe_capacity_block_extension_history(
    *,
    capacity_reservation_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeCapacityBlockExtensionHistoryResult:
    """Describe capacity block extension history.

    Args:
        capacity_reservation_ids: Capacity reservation ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if capacity_reservation_ids is not None:
        kwargs["CapacityReservationIds"] = capacity_reservation_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeCapacityBlockExtensionHistory", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe capacity block extension history") from exc
    return DescribeCapacityBlockExtensionHistoryResult(
        capacity_block_extensions=resp.get("CapacityBlockExtensions"),
        next_token=resp.get("NextToken"),
    )


async def describe_capacity_block_extension_offerings(
    capacity_block_extension_duration_hours: int,
    capacity_reservation_id: str,
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeCapacityBlockExtensionOfferingsResult:
    """Describe capacity block extension offerings.

    Args:
        capacity_block_extension_duration_hours: Capacity block extension duration hours.
        capacity_reservation_id: Capacity reservation id.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityBlockExtensionDurationHours"] = capacity_block_extension_duration_hours
    kwargs["CapacityReservationId"] = capacity_reservation_id
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeCapacityBlockExtensionOfferings", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe capacity block extension offerings") from exc
    return DescribeCapacityBlockExtensionOfferingsResult(
        capacity_block_extension_offerings=resp.get("CapacityBlockExtensionOfferings"),
        next_token=resp.get("NextToken"),
    )


async def describe_capacity_block_offerings(
    capacity_duration_hours: int,
    *,
    instance_type: str | None = None,
    instance_count: int | None = None,
    start_date_range: str | None = None,
    end_date_range: str | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    ultraserver_type: str | None = None,
    ultraserver_count: int | None = None,
    region_name: str | None = None,
) -> DescribeCapacityBlockOfferingsResult:
    """Describe capacity block offerings.

    Args:
        capacity_duration_hours: Capacity duration hours.
        instance_type: Instance type.
        instance_count: Instance count.
        start_date_range: Start date range.
        end_date_range: End date range.
        next_token: Next token.
        max_results: Max results.
        ultraserver_type: Ultraserver type.
        ultraserver_count: Ultraserver count.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityDurationHours"] = capacity_duration_hours
    if instance_type is not None:
        kwargs["InstanceType"] = instance_type
    if instance_count is not None:
        kwargs["InstanceCount"] = instance_count
    if start_date_range is not None:
        kwargs["StartDateRange"] = start_date_range
    if end_date_range is not None:
        kwargs["EndDateRange"] = end_date_range
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if ultraserver_type is not None:
        kwargs["UltraserverType"] = ultraserver_type
    if ultraserver_count is not None:
        kwargs["UltraserverCount"] = ultraserver_count
    try:
        resp = await client.call("DescribeCapacityBlockOfferings", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe capacity block offerings") from exc
    return DescribeCapacityBlockOfferingsResult(
        capacity_block_offerings=resp.get("CapacityBlockOfferings"),
        next_token=resp.get("NextToken"),
    )


async def describe_capacity_block_status(
    *,
    capacity_block_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeCapacityBlockStatusResult:
    """Describe capacity block status.

    Args:
        capacity_block_ids: Capacity block ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if capacity_block_ids is not None:
        kwargs["CapacityBlockIds"] = capacity_block_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeCapacityBlockStatus", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe capacity block status") from exc
    return DescribeCapacityBlockStatusResult(
        capacity_block_statuses=resp.get("CapacityBlockStatuses"),
        next_token=resp.get("NextToken"),
    )


async def describe_capacity_blocks(
    *,
    capacity_block_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeCapacityBlocksResult:
    """Describe capacity blocks.

    Args:
        capacity_block_ids: Capacity block ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if capacity_block_ids is not None:
        kwargs["CapacityBlockIds"] = capacity_block_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeCapacityBlocks", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe capacity blocks") from exc
    return DescribeCapacityBlocksResult(
        capacity_blocks=resp.get("CapacityBlocks"),
        next_token=resp.get("NextToken"),
    )


async def describe_capacity_manager_data_exports(
    *,
    capacity_manager_data_export_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeCapacityManagerDataExportsResult:
    """Describe capacity manager data exports.

    Args:
        capacity_manager_data_export_ids: Capacity manager data export ids.
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if capacity_manager_data_export_ids is not None:
        kwargs["CapacityManagerDataExportIds"] = capacity_manager_data_export_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeCapacityManagerDataExports", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe capacity manager data exports") from exc
    return DescribeCapacityManagerDataExportsResult(
        capacity_manager_data_exports=resp.get("CapacityManagerDataExports"),
        next_token=resp.get("NextToken"),
    )


async def describe_capacity_reservation_billing_requests(
    role: str,
    *,
    capacity_reservation_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeCapacityReservationBillingRequestsResult:
    """Describe capacity reservation billing requests.

    Args:
        role: Role.
        capacity_reservation_ids: Capacity reservation ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Role"] = role
    if capacity_reservation_ids is not None:
        kwargs["CapacityReservationIds"] = capacity_reservation_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeCapacityReservationBillingRequests", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe capacity reservation billing requests"
        ) from exc
    return DescribeCapacityReservationBillingRequestsResult(
        next_token=resp.get("NextToken"),
        capacity_reservation_billing_requests=resp.get("CapacityReservationBillingRequests"),
    )


async def describe_capacity_reservation_fleets(
    *,
    capacity_reservation_fleet_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeCapacityReservationFleetsResult:
    """Describe capacity reservation fleets.

    Args:
        capacity_reservation_fleet_ids: Capacity reservation fleet ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if capacity_reservation_fleet_ids is not None:
        kwargs["CapacityReservationFleetIds"] = capacity_reservation_fleet_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeCapacityReservationFleets", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe capacity reservation fleets") from exc
    return DescribeCapacityReservationFleetsResult(
        capacity_reservation_fleets=resp.get("CapacityReservationFleets"),
        next_token=resp.get("NextToken"),
    )


async def describe_capacity_reservation_topology(
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    capacity_reservation_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeCapacityReservationTopologyResult:
    """Describe capacity reservation topology.

    Args:
        next_token: Next token.
        max_results: Max results.
        capacity_reservation_ids: Capacity reservation ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if capacity_reservation_ids is not None:
        kwargs["CapacityReservationIds"] = capacity_reservation_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeCapacityReservationTopology", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe capacity reservation topology") from exc
    return DescribeCapacityReservationTopologyResult(
        next_token=resp.get("NextToken"),
        capacity_reservations=resp.get("CapacityReservations"),
    )


async def describe_capacity_reservations(
    *,
    capacity_reservation_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeCapacityReservationsResult:
    """Describe capacity reservations.

    Args:
        capacity_reservation_ids: Capacity reservation ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if capacity_reservation_ids is not None:
        kwargs["CapacityReservationIds"] = capacity_reservation_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeCapacityReservations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe capacity reservations") from exc
    return DescribeCapacityReservationsResult(
        next_token=resp.get("NextToken"),
        capacity_reservations=resp.get("CapacityReservations"),
    )


async def describe_carrier_gateways(
    *,
    carrier_gateway_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeCarrierGatewaysResult:
    """Describe carrier gateways.

    Args:
        carrier_gateway_ids: Carrier gateway ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if carrier_gateway_ids is not None:
        kwargs["CarrierGatewayIds"] = carrier_gateway_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeCarrierGateways", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe carrier gateways") from exc
    return DescribeCarrierGatewaysResult(
        carrier_gateways=resp.get("CarrierGateways"),
        next_token=resp.get("NextToken"),
    )


async def describe_classic_link_instances(
    *,
    instance_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeClassicLinkInstancesResult:
    """Describe classic link instances.

    Args:
        instance_ids: Instance ids.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if instance_ids is not None:
        kwargs["InstanceIds"] = instance_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeClassicLinkInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe classic link instances") from exc
    return DescribeClassicLinkInstancesResult(
        instances=resp.get("Instances"),
        next_token=resp.get("NextToken"),
    )


async def describe_client_vpn_authorization_rules(
    client_vpn_endpoint_id: str,
    *,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeClientVpnAuthorizationRulesResult:
    """Describe client vpn authorization rules.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        next_token: Next token.
        filters: Filters.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeClientVpnAuthorizationRules", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe client vpn authorization rules") from exc
    return DescribeClientVpnAuthorizationRulesResult(
        authorization_rules=resp.get("AuthorizationRules"),
        next_token=resp.get("NextToken"),
    )


async def describe_client_vpn_connections(
    client_vpn_endpoint_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeClientVpnConnectionsResult:
    """Describe client vpn connections.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeClientVpnConnections", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe client vpn connections") from exc
    return DescribeClientVpnConnectionsResult(
        connections=resp.get("Connections"),
        next_token=resp.get("NextToken"),
    )


async def describe_client_vpn_endpoints(
    *,
    client_vpn_endpoint_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeClientVpnEndpointsResult:
    """Describe client vpn endpoints.

    Args:
        client_vpn_endpoint_ids: Client vpn endpoint ids.
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if client_vpn_endpoint_ids is not None:
        kwargs["ClientVpnEndpointIds"] = client_vpn_endpoint_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeClientVpnEndpoints", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe client vpn endpoints") from exc
    return DescribeClientVpnEndpointsResult(
        client_vpn_endpoints=resp.get("ClientVpnEndpoints"),
        next_token=resp.get("NextToken"),
    )


async def describe_client_vpn_routes(
    client_vpn_endpoint_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeClientVpnRoutesResult:
    """Describe client vpn routes.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeClientVpnRoutes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe client vpn routes") from exc
    return DescribeClientVpnRoutesResult(
        routes=resp.get("Routes"),
        next_token=resp.get("NextToken"),
    )


async def describe_client_vpn_target_networks(
    client_vpn_endpoint_id: str,
    *,
    association_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeClientVpnTargetNetworksResult:
    """Describe client vpn target networks.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        association_ids: Association ids.
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    if association_ids is not None:
        kwargs["AssociationIds"] = association_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeClientVpnTargetNetworks", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe client vpn target networks") from exc
    return DescribeClientVpnTargetNetworksResult(
        client_vpn_target_networks=resp.get("ClientVpnTargetNetworks"),
        next_token=resp.get("NextToken"),
    )


async def describe_coip_pools(
    *,
    pool_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeCoipPoolsResult:
    """Describe coip pools.

    Args:
        pool_ids: Pool ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if pool_ids is not None:
        kwargs["PoolIds"] = pool_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeCoipPools", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe coip pools") from exc
    return DescribeCoipPoolsResult(
        coip_pools=resp.get("CoipPools"),
        next_token=resp.get("NextToken"),
    )


async def describe_conversion_tasks(
    *,
    conversion_task_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeConversionTasksResult:
    """Describe conversion tasks.

    Args:
        conversion_task_ids: Conversion task ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if conversion_task_ids is not None:
        kwargs["ConversionTaskIds"] = conversion_task_ids
    try:
        resp = await client.call("DescribeConversionTasks", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe conversion tasks") from exc
    return DescribeConversionTasksResult(
        conversion_tasks=resp.get("ConversionTasks"),
    )


async def describe_customer_gateways(
    *,
    customer_gateway_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeCustomerGatewaysResult:
    """Describe customer gateways.

    Args:
        customer_gateway_ids: Customer gateway ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if customer_gateway_ids is not None:
        kwargs["CustomerGatewayIds"] = customer_gateway_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeCustomerGateways", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe customer gateways") from exc
    return DescribeCustomerGatewaysResult(
        customer_gateways=resp.get("CustomerGateways"),
    )


async def describe_declarative_policies_reports(
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    report_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeDeclarativePoliciesReportsResult:
    """Describe declarative policies reports.

    Args:
        next_token: Next token.
        max_results: Max results.
        report_ids: Report ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if report_ids is not None:
        kwargs["ReportIds"] = report_ids
    try:
        resp = await client.call("DescribeDeclarativePoliciesReports", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe declarative policies reports") from exc
    return DescribeDeclarativePoliciesReportsResult(
        next_token=resp.get("NextToken"),
        reports=resp.get("Reports"),
    )


async def describe_dhcp_options(
    *,
    dhcp_options_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeDhcpOptionsResult:
    """Describe dhcp options.

    Args:
        dhcp_options_ids: Dhcp options ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if dhcp_options_ids is not None:
        kwargs["DhcpOptionsIds"] = dhcp_options_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeDhcpOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe dhcp options") from exc
    return DescribeDhcpOptionsResult(
        next_token=resp.get("NextToken"),
        dhcp_options=resp.get("DhcpOptions"),
    )


async def describe_egress_only_internet_gateways(
    *,
    egress_only_internet_gateway_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeEgressOnlyInternetGatewaysResult:
    """Describe egress only internet gateways.

    Args:
        egress_only_internet_gateway_ids: Egress only internet gateway ids.
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if egress_only_internet_gateway_ids is not None:
        kwargs["EgressOnlyInternetGatewayIds"] = egress_only_internet_gateway_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeEgressOnlyInternetGateways", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe egress only internet gateways") from exc
    return DescribeEgressOnlyInternetGatewaysResult(
        egress_only_internet_gateways=resp.get("EgressOnlyInternetGateways"),
        next_token=resp.get("NextToken"),
    )


async def describe_elastic_gpus(
    *,
    elastic_gpu_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeElasticGpusResult:
    """Describe elastic gpus.

    Args:
        elastic_gpu_ids: Elastic gpu ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if elastic_gpu_ids is not None:
        kwargs["ElasticGpuIds"] = elastic_gpu_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeElasticGpus", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe elastic gpus") from exc
    return DescribeElasticGpusResult(
        elastic_gpu_set=resp.get("ElasticGpuSet"),
        max_results=resp.get("MaxResults"),
        next_token=resp.get("NextToken"),
    )


async def describe_export_image_tasks(
    *,
    filters: list[dict[str, Any]] | None = None,
    export_image_task_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeExportImageTasksResult:
    """Describe export image tasks.

    Args:
        filters: Filters.
        export_image_task_ids: Export image task ids.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if export_image_task_ids is not None:
        kwargs["ExportImageTaskIds"] = export_image_task_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeExportImageTasks", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe export image tasks") from exc
    return DescribeExportImageTasksResult(
        export_image_tasks=resp.get("ExportImageTasks"),
        next_token=resp.get("NextToken"),
    )


async def describe_export_tasks(
    *,
    filters: list[dict[str, Any]] | None = None,
    export_task_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeExportTasksResult:
    """Describe export tasks.

    Args:
        filters: Filters.
        export_task_ids: Export task ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if export_task_ids is not None:
        kwargs["ExportTaskIds"] = export_task_ids
    try:
        resp = await client.call("DescribeExportTasks", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe export tasks") from exc
    return DescribeExportTasksResult(
        export_tasks=resp.get("ExportTasks"),
    )


async def describe_fast_launch_images(
    *,
    image_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeFastLaunchImagesResult:
    """Describe fast launch images.

    Args:
        image_ids: Image ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if image_ids is not None:
        kwargs["ImageIds"] = image_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeFastLaunchImages", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe fast launch images") from exc
    return DescribeFastLaunchImagesResult(
        fast_launch_images=resp.get("FastLaunchImages"),
        next_token=resp.get("NextToken"),
    )


async def describe_fast_snapshot_restores(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeFastSnapshotRestoresResult:
    """Describe fast snapshot restores.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeFastSnapshotRestores", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe fast snapshot restores") from exc
    return DescribeFastSnapshotRestoresResult(
        fast_snapshot_restores=resp.get("FastSnapshotRestores"),
        next_token=resp.get("NextToken"),
    )


async def describe_fleet_history(
    fleet_id: str,
    start_time: str,
    *,
    event_type: str | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeFleetHistoryResult:
    """Describe fleet history.

    Args:
        fleet_id: Fleet id.
        start_time: Start time.
        event_type: Event type.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["FleetId"] = fleet_id
    kwargs["StartTime"] = start_time
    if event_type is not None:
        kwargs["EventType"] = event_type
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeFleetHistory", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe fleet history") from exc
    return DescribeFleetHistoryResult(
        history_records=resp.get("HistoryRecords"),
        last_evaluated_time=resp.get("LastEvaluatedTime"),
        next_token=resp.get("NextToken"),
        fleet_id=resp.get("FleetId"),
        start_time=resp.get("StartTime"),
    )


async def describe_fleet_instances(
    fleet_id: str,
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeFleetInstancesResult:
    """Describe fleet instances.

    Args:
        fleet_id: Fleet id.
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["FleetId"] = fleet_id
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeFleetInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe fleet instances") from exc
    return DescribeFleetInstancesResult(
        active_instances=resp.get("ActiveInstances"),
        next_token=resp.get("NextToken"),
        fleet_id=resp.get("FleetId"),
    )


async def describe_fleets(
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    fleet_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeFleetsResult:
    """Describe fleets.

    Args:
        max_results: Max results.
        next_token: Next token.
        fleet_ids: Fleet ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if fleet_ids is not None:
        kwargs["FleetIds"] = fleet_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeFleets", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe fleets") from exc
    return DescribeFleetsResult(
        next_token=resp.get("NextToken"),
        fleets=resp.get("Fleets"),
    )


async def describe_flow_logs(
    *,
    filter: list[dict[str, Any]] | None = None,
    flow_log_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeFlowLogsResult:
    """Describe flow logs.

    Args:
        filter: Filter.
        flow_log_ids: Flow log ids.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filter is not None:
        kwargs["Filter"] = filter
    if flow_log_ids is not None:
        kwargs["FlowLogIds"] = flow_log_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeFlowLogs", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe flow logs") from exc
    return DescribeFlowLogsResult(
        flow_logs=resp.get("FlowLogs"),
        next_token=resp.get("NextToken"),
    )


async def describe_fpga_image_attribute(
    fpga_image_id: str,
    attribute: str,
    region_name: str | None = None,
) -> DescribeFpgaImageAttributeResult:
    """Describe fpga image attribute.

    Args:
        fpga_image_id: Fpga image id.
        attribute: Attribute.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["FpgaImageId"] = fpga_image_id
    kwargs["Attribute"] = attribute
    try:
        resp = await client.call("DescribeFpgaImageAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe fpga image attribute") from exc
    return DescribeFpgaImageAttributeResult(
        fpga_image_attribute=resp.get("FpgaImageAttribute"),
    )


async def describe_fpga_images(
    *,
    fpga_image_ids: list[str] | None = None,
    owners: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeFpgaImagesResult:
    """Describe fpga images.

    Args:
        fpga_image_ids: Fpga image ids.
        owners: Owners.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if fpga_image_ids is not None:
        kwargs["FpgaImageIds"] = fpga_image_ids
    if owners is not None:
        kwargs["Owners"] = owners
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeFpgaImages", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe fpga images") from exc
    return DescribeFpgaImagesResult(
        fpga_images=resp.get("FpgaImages"),
        next_token=resp.get("NextToken"),
    )


async def describe_host_reservation_offerings(
    *,
    filter: list[dict[str, Any]] | None = None,
    max_duration: int | None = None,
    max_results: int | None = None,
    min_duration: int | None = None,
    next_token: str | None = None,
    offering_id: str | None = None,
    region_name: str | None = None,
) -> DescribeHostReservationOfferingsResult:
    """Describe host reservation offerings.

    Args:
        filter: Filter.
        max_duration: Max duration.
        max_results: Max results.
        min_duration: Min duration.
        next_token: Next token.
        offering_id: Offering id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filter is not None:
        kwargs["Filter"] = filter
    if max_duration is not None:
        kwargs["MaxDuration"] = max_duration
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if min_duration is not None:
        kwargs["MinDuration"] = min_duration
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if offering_id is not None:
        kwargs["OfferingId"] = offering_id
    try:
        resp = await client.call("DescribeHostReservationOfferings", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe host reservation offerings") from exc
    return DescribeHostReservationOfferingsResult(
        next_token=resp.get("NextToken"),
        offering_set=resp.get("OfferingSet"),
    )


async def describe_host_reservations(
    *,
    filter: list[dict[str, Any]] | None = None,
    host_reservation_id_set: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeHostReservationsResult:
    """Describe host reservations.

    Args:
        filter: Filter.
        host_reservation_id_set: Host reservation id set.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filter is not None:
        kwargs["Filter"] = filter
    if host_reservation_id_set is not None:
        kwargs["HostReservationIdSet"] = host_reservation_id_set
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeHostReservations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe host reservations") from exc
    return DescribeHostReservationsResult(
        host_reservation_set=resp.get("HostReservationSet"),
        next_token=resp.get("NextToken"),
    )


async def describe_hosts(
    *,
    host_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filter: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeHostsResult:
    """Describe hosts.

    Args:
        host_ids: Host ids.
        next_token: Next token.
        max_results: Max results.
        filter: Filter.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if host_ids is not None:
        kwargs["HostIds"] = host_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filter is not None:
        kwargs["Filter"] = filter
    try:
        resp = await client.call("DescribeHosts", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe hosts") from exc
    return DescribeHostsResult(
        hosts=resp.get("Hosts"),
        next_token=resp.get("NextToken"),
    )


async def describe_iam_instance_profile_associations(
    *,
    association_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeIamInstanceProfileAssociationsResult:
    """Describe iam instance profile associations.

    Args:
        association_ids: Association ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if association_ids is not None:
        kwargs["AssociationIds"] = association_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeIamInstanceProfileAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe iam instance profile associations") from exc
    return DescribeIamInstanceProfileAssociationsResult(
        iam_instance_profile_associations=resp.get("IamInstanceProfileAssociations"),
        next_token=resp.get("NextToken"),
    )


async def describe_id_format(
    *,
    resource: str | None = None,
    region_name: str | None = None,
) -> DescribeIdFormatResult:
    """Describe id format.

    Args:
        resource: Resource.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if resource is not None:
        kwargs["Resource"] = resource
    try:
        resp = await client.call("DescribeIdFormat", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe id format") from exc
    return DescribeIdFormatResult(
        statuses=resp.get("Statuses"),
    )


async def describe_identity_id_format(
    principal_arn: str,
    *,
    resource: str | None = None,
    region_name: str | None = None,
) -> DescribeIdentityIdFormatResult:
    """Describe identity id format.

    Args:
        principal_arn: Principal arn.
        resource: Resource.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PrincipalArn"] = principal_arn
    if resource is not None:
        kwargs["Resource"] = resource
    try:
        resp = await client.call("DescribeIdentityIdFormat", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe identity id format") from exc
    return DescribeIdentityIdFormatResult(
        statuses=resp.get("Statuses"),
    )


async def describe_image_attribute(
    attribute: str,
    image_id: str,
    region_name: str | None = None,
) -> DescribeImageAttributeResult:
    """Describe image attribute.

    Args:
        attribute: Attribute.
        image_id: Image id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Attribute"] = attribute
    kwargs["ImageId"] = image_id
    try:
        resp = await client.call("DescribeImageAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe image attribute") from exc
    return DescribeImageAttributeResult(
        description=resp.get("Description"),
        kernel_id=resp.get("KernelId"),
        ramdisk_id=resp.get("RamdiskId"),
        sriov_net_support=resp.get("SriovNetSupport"),
        boot_mode=resp.get("BootMode"),
        tpm_support=resp.get("TpmSupport"),
        uefi_data=resp.get("UefiData"),
        last_launched_time=resp.get("LastLaunchedTime"),
        imds_support=resp.get("ImdsSupport"),
        deregistration_protection=resp.get("DeregistrationProtection"),
        image_id=resp.get("ImageId"),
        launch_permissions=resp.get("LaunchPermissions"),
        product_codes=resp.get("ProductCodes"),
        block_device_mappings=resp.get("BlockDeviceMappings"),
    )


async def describe_image_references(
    image_ids: list[str],
    *,
    include_all_resource_types: bool | None = None,
    resource_types: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeImageReferencesResult:
    """Describe image references.

    Args:
        image_ids: Image ids.
        include_all_resource_types: Include all resource types.
        resource_types: Resource types.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageIds"] = image_ids
    if include_all_resource_types is not None:
        kwargs["IncludeAllResourceTypes"] = include_all_resource_types
    if resource_types is not None:
        kwargs["ResourceTypes"] = resource_types
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeImageReferences", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe image references") from exc
    return DescribeImageReferencesResult(
        next_token=resp.get("NextToken"),
        image_references=resp.get("ImageReferences"),
    )


async def describe_image_usage_report_entries(
    *,
    image_ids: list[str] | None = None,
    report_ids: list[str] | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeImageUsageReportEntriesResult:
    """Describe image usage report entries.

    Args:
        image_ids: Image ids.
        report_ids: Report ids.
        next_token: Next token.
        filters: Filters.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if image_ids is not None:
        kwargs["ImageIds"] = image_ids
    if report_ids is not None:
        kwargs["ReportIds"] = report_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeImageUsageReportEntries", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe image usage report entries") from exc
    return DescribeImageUsageReportEntriesResult(
        next_token=resp.get("NextToken"),
        image_usage_report_entries=resp.get("ImageUsageReportEntries"),
    )


async def describe_image_usage_reports(
    *,
    image_ids: list[str] | None = None,
    report_ids: list[str] | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeImageUsageReportsResult:
    """Describe image usage reports.

    Args:
        image_ids: Image ids.
        report_ids: Report ids.
        next_token: Next token.
        filters: Filters.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if image_ids is not None:
        kwargs["ImageIds"] = image_ids
    if report_ids is not None:
        kwargs["ReportIds"] = report_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeImageUsageReports", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe image usage reports") from exc
    return DescribeImageUsageReportsResult(
        next_token=resp.get("NextToken"),
        image_usage_reports=resp.get("ImageUsageReports"),
    )


async def describe_import_image_tasks(
    *,
    filters: list[dict[str, Any]] | None = None,
    import_task_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeImportImageTasksResult:
    """Describe import image tasks.

    Args:
        filters: Filters.
        import_task_ids: Import task ids.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if import_task_ids is not None:
        kwargs["ImportTaskIds"] = import_task_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeImportImageTasks", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe import image tasks") from exc
    return DescribeImportImageTasksResult(
        import_image_tasks=resp.get("ImportImageTasks"),
        next_token=resp.get("NextToken"),
    )


async def describe_import_snapshot_tasks(
    *,
    filters: list[dict[str, Any]] | None = None,
    import_task_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeImportSnapshotTasksResult:
    """Describe import snapshot tasks.

    Args:
        filters: Filters.
        import_task_ids: Import task ids.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if import_task_ids is not None:
        kwargs["ImportTaskIds"] = import_task_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeImportSnapshotTasks", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe import snapshot tasks") from exc
    return DescribeImportSnapshotTasksResult(
        import_snapshot_tasks=resp.get("ImportSnapshotTasks"),
        next_token=resp.get("NextToken"),
    )


async def describe_instance_attribute(
    instance_id: str,
    attribute: str,
    region_name: str | None = None,
) -> DescribeInstanceAttributeResult:
    """Describe instance attribute.

    Args:
        instance_id: Instance id.
        attribute: Attribute.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["Attribute"] = attribute
    try:
        resp = await client.call("DescribeInstanceAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe instance attribute") from exc
    return DescribeInstanceAttributeResult(
        block_device_mappings=resp.get("BlockDeviceMappings"),
        disable_api_termination=resp.get("DisableApiTermination"),
        ena_support=resp.get("EnaSupport"),
        enclave_options=resp.get("EnclaveOptions"),
        ebs_optimized=resp.get("EbsOptimized"),
        instance_id=resp.get("InstanceId"),
        instance_initiated_shutdown_behavior=resp.get("InstanceInitiatedShutdownBehavior"),
        instance_type=resp.get("InstanceType"),
        kernel_id=resp.get("KernelId"),
        product_codes=resp.get("ProductCodes"),
        ramdisk_id=resp.get("RamdiskId"),
        root_device_name=resp.get("RootDeviceName"),
        source_dest_check=resp.get("SourceDestCheck"),
        sriov_net_support=resp.get("SriovNetSupport"),
        user_data=resp.get("UserData"),
        disable_api_stop=resp.get("DisableApiStop"),
        groups=resp.get("Groups"),
    )


async def describe_instance_connect_endpoints(
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    instance_connect_endpoint_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeInstanceConnectEndpointsResult:
    """Describe instance connect endpoints.

    Args:
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        instance_connect_endpoint_ids: Instance connect endpoint ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    if instance_connect_endpoint_ids is not None:
        kwargs["InstanceConnectEndpointIds"] = instance_connect_endpoint_ids
    try:
        resp = await client.call("DescribeInstanceConnectEndpoints", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe instance connect endpoints") from exc
    return DescribeInstanceConnectEndpointsResult(
        instance_connect_endpoints=resp.get("InstanceConnectEndpoints"),
        next_token=resp.get("NextToken"),
    )


async def describe_instance_credit_specifications(
    *,
    filters: list[dict[str, Any]] | None = None,
    instance_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeInstanceCreditSpecificationsResult:
    """Describe instance credit specifications.

    Args:
        filters: Filters.
        instance_ids: Instance ids.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if instance_ids is not None:
        kwargs["InstanceIds"] = instance_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeInstanceCreditSpecifications", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe instance credit specifications") from exc
    return DescribeInstanceCreditSpecificationsResult(
        instance_credit_specifications=resp.get("InstanceCreditSpecifications"),
        next_token=resp.get("NextToken"),
    )


async def describe_instance_event_notification_attributes(
    region_name: str | None = None,
) -> DescribeInstanceEventNotificationAttributesResult:
    """Describe instance event notification attributes.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("DescribeInstanceEventNotificationAttributes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe instance event notification attributes"
        ) from exc
    return DescribeInstanceEventNotificationAttributesResult(
        instance_tag_attribute=resp.get("InstanceTagAttribute"),
    )


async def describe_instance_event_windows(
    *,
    instance_event_window_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeInstanceEventWindowsResult:
    """Describe instance event windows.

    Args:
        instance_event_window_ids: Instance event window ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if instance_event_window_ids is not None:
        kwargs["InstanceEventWindowIds"] = instance_event_window_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeInstanceEventWindows", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe instance event windows") from exc
    return DescribeInstanceEventWindowsResult(
        instance_event_windows=resp.get("InstanceEventWindows"),
        next_token=resp.get("NextToken"),
    )


async def describe_instance_image_metadata(
    *,
    filters: list[dict[str, Any]] | None = None,
    instance_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeInstanceImageMetadataResult:
    """Describe instance image metadata.

    Args:
        filters: Filters.
        instance_ids: Instance ids.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if instance_ids is not None:
        kwargs["InstanceIds"] = instance_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeInstanceImageMetadata", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe instance image metadata") from exc
    return DescribeInstanceImageMetadataResult(
        instance_image_metadata=resp.get("InstanceImageMetadata"),
        next_token=resp.get("NextToken"),
    )


async def describe_instance_status(
    *,
    instance_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    include_all_instances: bool | None = None,
    region_name: str | None = None,
) -> DescribeInstanceStatusResult:
    """Describe instance status.

    Args:
        instance_ids: Instance ids.
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        include_all_instances: Include all instances.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if instance_ids is not None:
        kwargs["InstanceIds"] = instance_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    if include_all_instances is not None:
        kwargs["IncludeAllInstances"] = include_all_instances
    try:
        resp = await client.call("DescribeInstanceStatus", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe instance status") from exc
    return DescribeInstanceStatusResult(
        instance_statuses=resp.get("InstanceStatuses"),
        next_token=resp.get("NextToken"),
    )


async def describe_instance_topology(
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    instance_ids: list[str] | None = None,
    group_names: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeInstanceTopologyResult:
    """Describe instance topology.

    Args:
        next_token: Next token.
        max_results: Max results.
        instance_ids: Instance ids.
        group_names: Group names.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if instance_ids is not None:
        kwargs["InstanceIds"] = instance_ids
    if group_names is not None:
        kwargs["GroupNames"] = group_names
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeInstanceTopology", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe instance topology") from exc
    return DescribeInstanceTopologyResult(
        instances=resp.get("Instances"),
        next_token=resp.get("NextToken"),
    )


async def describe_instance_type_offerings(
    *,
    location_type: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeInstanceTypeOfferingsResult:
    """Describe instance type offerings.

    Args:
        location_type: Location type.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if location_type is not None:
        kwargs["LocationType"] = location_type
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeInstanceTypeOfferings", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe instance type offerings") from exc
    return DescribeInstanceTypeOfferingsResult(
        instance_type_offerings=resp.get("InstanceTypeOfferings"),
        next_token=resp.get("NextToken"),
    )


async def describe_instance_types(
    *,
    instance_types: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeInstanceTypesResult:
    """Describe instance types.

    Args:
        instance_types: Instance types.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if instance_types is not None:
        kwargs["InstanceTypes"] = instance_types
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeInstanceTypes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe instance types") from exc
    return DescribeInstanceTypesResult(
        instance_types=resp.get("InstanceTypes"),
        next_token=resp.get("NextToken"),
    )


async def describe_internet_gateways(
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    internet_gateway_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeInternetGatewaysResult:
    """Describe internet gateways.

    Args:
        next_token: Next token.
        max_results: Max results.
        internet_gateway_ids: Internet gateway ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if internet_gateway_ids is not None:
        kwargs["InternetGatewayIds"] = internet_gateway_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeInternetGateways", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe internet gateways") from exc
    return DescribeInternetGatewaysResult(
        internet_gateways=resp.get("InternetGateways"),
        next_token=resp.get("NextToken"),
    )


async def describe_ipam_byoasn(
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeIpamByoasnResult:
    """Describe ipam byoasn.

    Args:
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeIpamByoasn", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe ipam byoasn") from exc
    return DescribeIpamByoasnResult(
        byoasns=resp.get("Byoasns"),
        next_token=resp.get("NextToken"),
    )


async def describe_ipam_external_resource_verification_tokens(
    *,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    ipam_external_resource_verification_token_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeIpamExternalResourceVerificationTokensResult:
    """Describe ipam external resource verification tokens.

    Args:
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        ipam_external_resource_verification_token_ids: Ipam external resource verification token ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if ipam_external_resource_verification_token_ids is not None:
        kwargs["IpamExternalResourceVerificationTokenIds"] = (
            ipam_external_resource_verification_token_ids
        )
    try:
        resp = await client.call("DescribeIpamExternalResourceVerificationTokens", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe ipam external resource verification tokens"
        ) from exc
    return DescribeIpamExternalResourceVerificationTokensResult(
        next_token=resp.get("NextToken"),
        ipam_external_resource_verification_tokens=resp.get(
            "IpamExternalResourceVerificationTokens"
        ),
    )


async def describe_ipam_pools(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    ipam_pool_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeIpamPoolsResult:
    """Describe ipam pools.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        ipam_pool_ids: Ipam pool ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if ipam_pool_ids is not None:
        kwargs["IpamPoolIds"] = ipam_pool_ids
    try:
        resp = await client.call("DescribeIpamPools", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe ipam pools") from exc
    return DescribeIpamPoolsResult(
        next_token=resp.get("NextToken"),
        ipam_pools=resp.get("IpamPools"),
    )


async def describe_ipam_prefix_list_resolver_targets(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    ipam_prefix_list_resolver_target_ids: list[str] | None = None,
    ipam_prefix_list_resolver_id: str | None = None,
    region_name: str | None = None,
) -> DescribeIpamPrefixListResolverTargetsResult:
    """Describe ipam prefix list resolver targets.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        ipam_prefix_list_resolver_target_ids: Ipam prefix list resolver target ids.
        ipam_prefix_list_resolver_id: Ipam prefix list resolver id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if ipam_prefix_list_resolver_target_ids is not None:
        kwargs["IpamPrefixListResolverTargetIds"] = ipam_prefix_list_resolver_target_ids
    if ipam_prefix_list_resolver_id is not None:
        kwargs["IpamPrefixListResolverId"] = ipam_prefix_list_resolver_id
    try:
        resp = await client.call("DescribeIpamPrefixListResolverTargets", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe ipam prefix list resolver targets") from exc
    return DescribeIpamPrefixListResolverTargetsResult(
        next_token=resp.get("NextToken"),
        ipam_prefix_list_resolver_targets=resp.get("IpamPrefixListResolverTargets"),
    )


async def describe_ipam_prefix_list_resolvers(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    ipam_prefix_list_resolver_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeIpamPrefixListResolversResult:
    """Describe ipam prefix list resolvers.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        ipam_prefix_list_resolver_ids: Ipam prefix list resolver ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if ipam_prefix_list_resolver_ids is not None:
        kwargs["IpamPrefixListResolverIds"] = ipam_prefix_list_resolver_ids
    try:
        resp = await client.call("DescribeIpamPrefixListResolvers", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe ipam prefix list resolvers") from exc
    return DescribeIpamPrefixListResolversResult(
        next_token=resp.get("NextToken"),
        ipam_prefix_list_resolvers=resp.get("IpamPrefixListResolvers"),
    )


async def describe_ipam_resource_discoveries(
    *,
    ipam_resource_discovery_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeIpamResourceDiscoveriesResult:
    """Describe ipam resource discoveries.

    Args:
        ipam_resource_discovery_ids: Ipam resource discovery ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if ipam_resource_discovery_ids is not None:
        kwargs["IpamResourceDiscoveryIds"] = ipam_resource_discovery_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeIpamResourceDiscoveries", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe ipam resource discoveries") from exc
    return DescribeIpamResourceDiscoveriesResult(
        ipam_resource_discoveries=resp.get("IpamResourceDiscoveries"),
        next_token=resp.get("NextToken"),
    )


async def describe_ipam_resource_discovery_associations(
    *,
    ipam_resource_discovery_association_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeIpamResourceDiscoveryAssociationsResult:
    """Describe ipam resource discovery associations.

    Args:
        ipam_resource_discovery_association_ids: Ipam resource discovery association ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if ipam_resource_discovery_association_ids is not None:
        kwargs["IpamResourceDiscoveryAssociationIds"] = ipam_resource_discovery_association_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeIpamResourceDiscoveryAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe ipam resource discovery associations"
        ) from exc
    return DescribeIpamResourceDiscoveryAssociationsResult(
        ipam_resource_discovery_associations=resp.get("IpamResourceDiscoveryAssociations"),
        next_token=resp.get("NextToken"),
    )


async def describe_ipam_scopes(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    ipam_scope_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeIpamScopesResult:
    """Describe ipam scopes.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        ipam_scope_ids: Ipam scope ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if ipam_scope_ids is not None:
        kwargs["IpamScopeIds"] = ipam_scope_ids
    try:
        resp = await client.call("DescribeIpamScopes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe ipam scopes") from exc
    return DescribeIpamScopesResult(
        next_token=resp.get("NextToken"),
        ipam_scopes=resp.get("IpamScopes"),
    )


async def describe_ipams(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    ipam_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeIpamsResult:
    """Describe ipams.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        ipam_ids: Ipam ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if ipam_ids is not None:
        kwargs["IpamIds"] = ipam_ids
    try:
        resp = await client.call("DescribeIpams", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe ipams") from exc
    return DescribeIpamsResult(
        next_token=resp.get("NextToken"),
        ipams=resp.get("Ipams"),
    )


async def describe_ipv6_pools(
    *,
    pool_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeIpv6PoolsResult:
    """Describe ipv6 pools.

    Args:
        pool_ids: Pool ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if pool_ids is not None:
        kwargs["PoolIds"] = pool_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeIpv6Pools", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe ipv6 pools") from exc
    return DescribeIpv6PoolsResult(
        ipv6_pools=resp.get("Ipv6Pools"),
        next_token=resp.get("NextToken"),
    )


async def describe_key_pairs(
    *,
    key_names: list[str] | None = None,
    key_pair_ids: list[str] | None = None,
    include_public_key: bool | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeKeyPairsResult:
    """Describe key pairs.

    Args:
        key_names: Key names.
        key_pair_ids: Key pair ids.
        include_public_key: Include public key.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if key_names is not None:
        kwargs["KeyNames"] = key_names
    if key_pair_ids is not None:
        kwargs["KeyPairIds"] = key_pair_ids
    if include_public_key is not None:
        kwargs["IncludePublicKey"] = include_public_key
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeKeyPairs", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe key pairs") from exc
    return DescribeKeyPairsResult(
        key_pairs=resp.get("KeyPairs"),
    )


async def describe_launch_template_versions(
    *,
    launch_template_id: str | None = None,
    launch_template_name: str | None = None,
    versions: list[str] | None = None,
    min_version: str | None = None,
    max_version: str | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    resolve_alias: bool | None = None,
    region_name: str | None = None,
) -> DescribeLaunchTemplateVersionsResult:
    """Describe launch template versions.

    Args:
        launch_template_id: Launch template id.
        launch_template_name: Launch template name.
        versions: Versions.
        min_version: Min version.
        max_version: Max version.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        resolve_alias: Resolve alias.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if launch_template_id is not None:
        kwargs["LaunchTemplateId"] = launch_template_id
    if launch_template_name is not None:
        kwargs["LaunchTemplateName"] = launch_template_name
    if versions is not None:
        kwargs["Versions"] = versions
    if min_version is not None:
        kwargs["MinVersion"] = min_version
    if max_version is not None:
        kwargs["MaxVersion"] = max_version
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    if resolve_alias is not None:
        kwargs["ResolveAlias"] = resolve_alias
    try:
        resp = await client.call("DescribeLaunchTemplateVersions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe launch template versions") from exc
    return DescribeLaunchTemplateVersionsResult(
        launch_template_versions=resp.get("LaunchTemplateVersions"),
        next_token=resp.get("NextToken"),
    )


async def describe_launch_templates(
    *,
    launch_template_ids: list[str] | None = None,
    launch_template_names: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeLaunchTemplatesResult:
    """Describe launch templates.

    Args:
        launch_template_ids: Launch template ids.
        launch_template_names: Launch template names.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if launch_template_ids is not None:
        kwargs["LaunchTemplateIds"] = launch_template_ids
    if launch_template_names is not None:
        kwargs["LaunchTemplateNames"] = launch_template_names
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeLaunchTemplates", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe launch templates") from exc
    return DescribeLaunchTemplatesResult(
        launch_templates=resp.get("LaunchTemplates"),
        next_token=resp.get("NextToken"),
    )


async def describe_local_gateway_route_table_virtual_interface_group_associations(
    *,
    local_gateway_route_table_virtual_interface_group_association_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult:
    """Describe local gateway route table virtual interface group associations.

    Args:
        local_gateway_route_table_virtual_interface_group_association_ids: Local gateway route table virtual interface group association ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if local_gateway_route_table_virtual_interface_group_association_ids is not None:
        kwargs["LocalGatewayRouteTableVirtualInterfaceGroupAssociationIds"] = (
            local_gateway_route_table_virtual_interface_group_association_ids
        )
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call(
            "DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociations", **kwargs
        )
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe local gateway route table virtual interface group associations"
        ) from exc
    return DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult(
        local_gateway_route_table_virtual_interface_group_associations=resp.get(
            "LocalGatewayRouteTableVirtualInterfaceGroupAssociations"
        ),
        next_token=resp.get("NextToken"),
    )


async def describe_local_gateway_route_table_vpc_associations(
    *,
    local_gateway_route_table_vpc_association_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeLocalGatewayRouteTableVpcAssociationsResult:
    """Describe local gateway route table vpc associations.

    Args:
        local_gateway_route_table_vpc_association_ids: Local gateway route table vpc association ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if local_gateway_route_table_vpc_association_ids is not None:
        kwargs["LocalGatewayRouteTableVpcAssociationIds"] = (
            local_gateway_route_table_vpc_association_ids
        )
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeLocalGatewayRouteTableVpcAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe local gateway route table vpc associations"
        ) from exc
    return DescribeLocalGatewayRouteTableVpcAssociationsResult(
        local_gateway_route_table_vpc_associations=resp.get(
            "LocalGatewayRouteTableVpcAssociations"
        ),
        next_token=resp.get("NextToken"),
    )


async def describe_local_gateway_route_tables(
    *,
    local_gateway_route_table_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeLocalGatewayRouteTablesResult:
    """Describe local gateway route tables.

    Args:
        local_gateway_route_table_ids: Local gateway route table ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if local_gateway_route_table_ids is not None:
        kwargs["LocalGatewayRouteTableIds"] = local_gateway_route_table_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeLocalGatewayRouteTables", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe local gateway route tables") from exc
    return DescribeLocalGatewayRouteTablesResult(
        local_gateway_route_tables=resp.get("LocalGatewayRouteTables"),
        next_token=resp.get("NextToken"),
    )


async def describe_local_gateway_virtual_interface_groups(
    *,
    local_gateway_virtual_interface_group_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeLocalGatewayVirtualInterfaceGroupsResult:
    """Describe local gateway virtual interface groups.

    Args:
        local_gateway_virtual_interface_group_ids: Local gateway virtual interface group ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if local_gateway_virtual_interface_group_ids is not None:
        kwargs["LocalGatewayVirtualInterfaceGroupIds"] = local_gateway_virtual_interface_group_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeLocalGatewayVirtualInterfaceGroups", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe local gateway virtual interface groups"
        ) from exc
    return DescribeLocalGatewayVirtualInterfaceGroupsResult(
        local_gateway_virtual_interface_groups=resp.get("LocalGatewayVirtualInterfaceGroups"),
        next_token=resp.get("NextToken"),
    )


async def describe_local_gateway_virtual_interfaces(
    *,
    local_gateway_virtual_interface_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeLocalGatewayVirtualInterfacesResult:
    """Describe local gateway virtual interfaces.

    Args:
        local_gateway_virtual_interface_ids: Local gateway virtual interface ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if local_gateway_virtual_interface_ids is not None:
        kwargs["LocalGatewayVirtualInterfaceIds"] = local_gateway_virtual_interface_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeLocalGatewayVirtualInterfaces", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe local gateway virtual interfaces") from exc
    return DescribeLocalGatewayVirtualInterfacesResult(
        local_gateway_virtual_interfaces=resp.get("LocalGatewayVirtualInterfaces"),
        next_token=resp.get("NextToken"),
    )


async def describe_local_gateways(
    *,
    local_gateway_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeLocalGatewaysResult:
    """Describe local gateways.

    Args:
        local_gateway_ids: Local gateway ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if local_gateway_ids is not None:
        kwargs["LocalGatewayIds"] = local_gateway_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeLocalGateways", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe local gateways") from exc
    return DescribeLocalGatewaysResult(
        local_gateways=resp.get("LocalGateways"),
        next_token=resp.get("NextToken"),
    )


async def describe_locked_snapshots(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    snapshot_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeLockedSnapshotsResult:
    """Describe locked snapshots.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        snapshot_ids: Snapshot ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if snapshot_ids is not None:
        kwargs["SnapshotIds"] = snapshot_ids
    try:
        resp = await client.call("DescribeLockedSnapshots", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe locked snapshots") from exc
    return DescribeLockedSnapshotsResult(
        snapshots=resp.get("Snapshots"),
        next_token=resp.get("NextToken"),
    )


async def describe_mac_hosts(
    *,
    filters: list[dict[str, Any]] | None = None,
    host_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeMacHostsResult:
    """Describe mac hosts.

    Args:
        filters: Filters.
        host_ids: Host ids.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if host_ids is not None:
        kwargs["HostIds"] = host_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeMacHosts", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe mac hosts") from exc
    return DescribeMacHostsResult(
        mac_hosts=resp.get("MacHosts"),
        next_token=resp.get("NextToken"),
    )


async def describe_mac_modification_tasks(
    *,
    filters: list[dict[str, Any]] | None = None,
    mac_modification_task_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeMacModificationTasksResult:
    """Describe mac modification tasks.

    Args:
        filters: Filters.
        mac_modification_task_ids: Mac modification task ids.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if mac_modification_task_ids is not None:
        kwargs["MacModificationTaskIds"] = mac_modification_task_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeMacModificationTasks", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe mac modification tasks") from exc
    return DescribeMacModificationTasksResult(
        mac_modification_tasks=resp.get("MacModificationTasks"),
        next_token=resp.get("NextToken"),
    )


async def describe_managed_prefix_lists(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    prefix_list_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeManagedPrefixListsResult:
    """Describe managed prefix lists.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        prefix_list_ids: Prefix list ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if prefix_list_ids is not None:
        kwargs["PrefixListIds"] = prefix_list_ids
    try:
        resp = await client.call("DescribeManagedPrefixLists", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe managed prefix lists") from exc
    return DescribeManagedPrefixListsResult(
        next_token=resp.get("NextToken"),
        prefix_lists=resp.get("PrefixLists"),
    )


async def describe_moving_addresses(
    *,
    public_ips: list[str] | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeMovingAddressesResult:
    """Describe moving addresses.

    Args:
        public_ips: Public ips.
        next_token: Next token.
        filters: Filters.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if public_ips is not None:
        kwargs["PublicIps"] = public_ips
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeMovingAddresses", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe moving addresses") from exc
    return DescribeMovingAddressesResult(
        moving_address_statuses=resp.get("MovingAddressStatuses"),
        next_token=resp.get("NextToken"),
    )


async def describe_nat_gateways(
    *,
    filter: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    nat_gateway_ids: list[str] | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeNatGatewaysResult:
    """Describe nat gateways.

    Args:
        filter: Filter.
        max_results: Max results.
        nat_gateway_ids: Nat gateway ids.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filter is not None:
        kwargs["Filter"] = filter
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if nat_gateway_ids is not None:
        kwargs["NatGatewayIds"] = nat_gateway_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeNatGateways", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe nat gateways") from exc
    return DescribeNatGatewaysResult(
        nat_gateways=resp.get("NatGateways"),
        next_token=resp.get("NextToken"),
    )


async def describe_network_acls(
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    network_acl_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeNetworkAclsResult:
    """Describe network acls.

    Args:
        next_token: Next token.
        max_results: Max results.
        network_acl_ids: Network acl ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if network_acl_ids is not None:
        kwargs["NetworkAclIds"] = network_acl_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeNetworkAcls", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe network acls") from exc
    return DescribeNetworkAclsResult(
        network_acls=resp.get("NetworkAcls"),
        next_token=resp.get("NextToken"),
    )


async def describe_network_insights_access_scope_analyses(
    *,
    network_insights_access_scope_analysis_ids: list[str] | None = None,
    network_insights_access_scope_id: str | None = None,
    analysis_start_time_begin: str | None = None,
    analysis_start_time_end: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeNetworkInsightsAccessScopeAnalysesResult:
    """Describe network insights access scope analyses.

    Args:
        network_insights_access_scope_analysis_ids: Network insights access scope analysis ids.
        network_insights_access_scope_id: Network insights access scope id.
        analysis_start_time_begin: Analysis start time begin.
        analysis_start_time_end: Analysis start time end.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if network_insights_access_scope_analysis_ids is not None:
        kwargs["NetworkInsightsAccessScopeAnalysisIds"] = network_insights_access_scope_analysis_ids
    if network_insights_access_scope_id is not None:
        kwargs["NetworkInsightsAccessScopeId"] = network_insights_access_scope_id
    if analysis_start_time_begin is not None:
        kwargs["AnalysisStartTimeBegin"] = analysis_start_time_begin
    if analysis_start_time_end is not None:
        kwargs["AnalysisStartTimeEnd"] = analysis_start_time_end
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeNetworkInsightsAccessScopeAnalyses", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe network insights access scope analyses"
        ) from exc
    return DescribeNetworkInsightsAccessScopeAnalysesResult(
        network_insights_access_scope_analyses=resp.get("NetworkInsightsAccessScopeAnalyses"),
        next_token=resp.get("NextToken"),
    )


async def describe_network_insights_access_scopes(
    *,
    network_insights_access_scope_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeNetworkInsightsAccessScopesResult:
    """Describe network insights access scopes.

    Args:
        network_insights_access_scope_ids: Network insights access scope ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if network_insights_access_scope_ids is not None:
        kwargs["NetworkInsightsAccessScopeIds"] = network_insights_access_scope_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeNetworkInsightsAccessScopes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe network insights access scopes") from exc
    return DescribeNetworkInsightsAccessScopesResult(
        network_insights_access_scopes=resp.get("NetworkInsightsAccessScopes"),
        next_token=resp.get("NextToken"),
    )


async def describe_network_insights_analyses(
    *,
    network_insights_analysis_ids: list[str] | None = None,
    network_insights_path_id: str | None = None,
    analysis_start_time: str | None = None,
    analysis_end_time: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeNetworkInsightsAnalysesResult:
    """Describe network insights analyses.

    Args:
        network_insights_analysis_ids: Network insights analysis ids.
        network_insights_path_id: Network insights path id.
        analysis_start_time: Analysis start time.
        analysis_end_time: Analysis end time.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if network_insights_analysis_ids is not None:
        kwargs["NetworkInsightsAnalysisIds"] = network_insights_analysis_ids
    if network_insights_path_id is not None:
        kwargs["NetworkInsightsPathId"] = network_insights_path_id
    if analysis_start_time is not None:
        kwargs["AnalysisStartTime"] = analysis_start_time
    if analysis_end_time is not None:
        kwargs["AnalysisEndTime"] = analysis_end_time
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeNetworkInsightsAnalyses", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe network insights analyses") from exc
    return DescribeNetworkInsightsAnalysesResult(
        network_insights_analyses=resp.get("NetworkInsightsAnalyses"),
        next_token=resp.get("NextToken"),
    )


async def describe_network_insights_paths(
    *,
    network_insights_path_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeNetworkInsightsPathsResult:
    """Describe network insights paths.

    Args:
        network_insights_path_ids: Network insights path ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if network_insights_path_ids is not None:
        kwargs["NetworkInsightsPathIds"] = network_insights_path_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeNetworkInsightsPaths", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe network insights paths") from exc
    return DescribeNetworkInsightsPathsResult(
        network_insights_paths=resp.get("NetworkInsightsPaths"),
        next_token=resp.get("NextToken"),
    )


async def describe_network_interface_attribute(
    network_interface_id: str,
    *,
    attribute: str | None = None,
    region_name: str | None = None,
) -> DescribeNetworkInterfaceAttributeResult:
    """Describe network interface attribute.

    Args:
        network_interface_id: Network interface id.
        attribute: Attribute.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    if attribute is not None:
        kwargs["Attribute"] = attribute
    try:
        resp = await client.call("DescribeNetworkInterfaceAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe network interface attribute") from exc
    return DescribeNetworkInterfaceAttributeResult(
        attachment=resp.get("Attachment"),
        description=resp.get("Description"),
        groups=resp.get("Groups"),
        network_interface_id=resp.get("NetworkInterfaceId"),
        source_dest_check=resp.get("SourceDestCheck"),
        associate_public_ip_address=resp.get("AssociatePublicIpAddress"),
    )


async def describe_network_interface_permissions(
    *,
    network_interface_permission_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeNetworkInterfacePermissionsResult:
    """Describe network interface permissions.

    Args:
        network_interface_permission_ids: Network interface permission ids.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if network_interface_permission_ids is not None:
        kwargs["NetworkInterfacePermissionIds"] = network_interface_permission_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeNetworkInterfacePermissions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe network interface permissions") from exc
    return DescribeNetworkInterfacePermissionsResult(
        network_interface_permissions=resp.get("NetworkInterfacePermissions"),
        next_token=resp.get("NextToken"),
    )


async def describe_network_interfaces(
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    network_interface_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeNetworkInterfacesResult:
    """Describe network interfaces.

    Args:
        next_token: Next token.
        max_results: Max results.
        network_interface_ids: Network interface ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if network_interface_ids is not None:
        kwargs["NetworkInterfaceIds"] = network_interface_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeNetworkInterfaces", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe network interfaces") from exc
    return DescribeNetworkInterfacesResult(
        network_interfaces=resp.get("NetworkInterfaces"),
        next_token=resp.get("NextToken"),
    )


async def describe_outpost_lags(
    *,
    outpost_lag_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeOutpostLagsResult:
    """Describe outpost lags.

    Args:
        outpost_lag_ids: Outpost lag ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if outpost_lag_ids is not None:
        kwargs["OutpostLagIds"] = outpost_lag_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeOutpostLags", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe outpost lags") from exc
    return DescribeOutpostLagsResult(
        outpost_lags=resp.get("OutpostLags"),
        next_token=resp.get("NextToken"),
    )


async def describe_placement_groups(
    *,
    group_ids: list[str] | None = None,
    group_names: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribePlacementGroupsResult:
    """Describe placement groups.

    Args:
        group_ids: Group ids.
        group_names: Group names.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if group_ids is not None:
        kwargs["GroupIds"] = group_ids
    if group_names is not None:
        kwargs["GroupNames"] = group_names
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribePlacementGroups", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe placement groups") from exc
    return DescribePlacementGroupsResult(
        placement_groups=resp.get("PlacementGroups"),
    )


async def describe_prefix_lists(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    prefix_list_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribePrefixListsResult:
    """Describe prefix lists.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        prefix_list_ids: Prefix list ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if prefix_list_ids is not None:
        kwargs["PrefixListIds"] = prefix_list_ids
    try:
        resp = await client.call("DescribePrefixLists", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe prefix lists") from exc
    return DescribePrefixListsResult(
        next_token=resp.get("NextToken"),
        prefix_lists=resp.get("PrefixLists"),
    )


async def describe_principal_id_format(
    *,
    resources: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribePrincipalIdFormatResult:
    """Describe principal id format.

    Args:
        resources: Resources.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if resources is not None:
        kwargs["Resources"] = resources
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribePrincipalIdFormat", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe principal id format") from exc
    return DescribePrincipalIdFormatResult(
        principals=resp.get("Principals"),
        next_token=resp.get("NextToken"),
    )


async def describe_public_ipv4_pools(
    *,
    pool_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribePublicIpv4PoolsResult:
    """Describe public ipv4 pools.

    Args:
        pool_ids: Pool ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if pool_ids is not None:
        kwargs["PoolIds"] = pool_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribePublicIpv4Pools", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe public ipv4 pools") from exc
    return DescribePublicIpv4PoolsResult(
        public_ipv4_pools=resp.get("PublicIpv4Pools"),
        next_token=resp.get("NextToken"),
    )


async def describe_regions(
    *,
    region_names: list[str] | None = None,
    all_regions: bool | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeRegionsResult:
    """Describe regions.

    Args:
        region_names: Region names.
        all_regions: All regions.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if region_names is not None:
        kwargs["RegionNames"] = region_names
    if all_regions is not None:
        kwargs["AllRegions"] = all_regions
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeRegions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe regions") from exc
    return DescribeRegionsResult(
        regions=resp.get("Regions"),
    )


async def describe_replace_root_volume_tasks(
    *,
    replace_root_volume_task_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeReplaceRootVolumeTasksResult:
    """Describe replace root volume tasks.

    Args:
        replace_root_volume_task_ids: Replace root volume task ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if replace_root_volume_task_ids is not None:
        kwargs["ReplaceRootVolumeTaskIds"] = replace_root_volume_task_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeReplaceRootVolumeTasks", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe replace root volume tasks") from exc
    return DescribeReplaceRootVolumeTasksResult(
        replace_root_volume_tasks=resp.get("ReplaceRootVolumeTasks"),
        next_token=resp.get("NextToken"),
    )


async def describe_reserved_instances(
    *,
    offering_class: str | None = None,
    reserved_instances_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    offering_type: str | None = None,
    region_name: str | None = None,
) -> DescribeReservedInstancesResult:
    """Describe reserved instances.

    Args:
        offering_class: Offering class.
        reserved_instances_ids: Reserved instances ids.
        filters: Filters.
        offering_type: Offering type.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if offering_class is not None:
        kwargs["OfferingClass"] = offering_class
    if reserved_instances_ids is not None:
        kwargs["ReservedInstancesIds"] = reserved_instances_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if offering_type is not None:
        kwargs["OfferingType"] = offering_type
    try:
        resp = await client.call("DescribeReservedInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe reserved instances") from exc
    return DescribeReservedInstancesResult(
        reserved_instances=resp.get("ReservedInstances"),
    )


async def describe_reserved_instances_listings(
    *,
    reserved_instances_id: str | None = None,
    reserved_instances_listing_id: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeReservedInstancesListingsResult:
    """Describe reserved instances listings.

    Args:
        reserved_instances_id: Reserved instances id.
        reserved_instances_listing_id: Reserved instances listing id.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if reserved_instances_id is not None:
        kwargs["ReservedInstancesId"] = reserved_instances_id
    if reserved_instances_listing_id is not None:
        kwargs["ReservedInstancesListingId"] = reserved_instances_listing_id
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeReservedInstancesListings", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe reserved instances listings") from exc
    return DescribeReservedInstancesListingsResult(
        reserved_instances_listings=resp.get("ReservedInstancesListings"),
    )


async def describe_reserved_instances_modifications(
    *,
    reserved_instances_modification_ids: list[str] | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeReservedInstancesModificationsResult:
    """Describe reserved instances modifications.

    Args:
        reserved_instances_modification_ids: Reserved instances modification ids.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if reserved_instances_modification_ids is not None:
        kwargs["ReservedInstancesModificationIds"] = reserved_instances_modification_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeReservedInstancesModifications", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe reserved instances modifications") from exc
    return DescribeReservedInstancesModificationsResult(
        next_token=resp.get("NextToken"),
        reserved_instances_modifications=resp.get("ReservedInstancesModifications"),
    )


async def describe_reserved_instances_offerings(
    *,
    availability_zone: str | None = None,
    include_marketplace: bool | None = None,
    instance_type: str | None = None,
    max_duration: int | None = None,
    max_instance_count: int | None = None,
    min_duration: int | None = None,
    offering_class: str | None = None,
    product_description: str | None = None,
    reserved_instances_offering_ids: list[str] | None = None,
    availability_zone_id: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    instance_tenancy: str | None = None,
    offering_type: str | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeReservedInstancesOfferingsResult:
    """Describe reserved instances offerings.

    Args:
        availability_zone: Availability zone.
        include_marketplace: Include marketplace.
        instance_type: Instance type.
        max_duration: Max duration.
        max_instance_count: Max instance count.
        min_duration: Min duration.
        offering_class: Offering class.
        product_description: Product description.
        reserved_instances_offering_ids: Reserved instances offering ids.
        availability_zone_id: Availability zone id.
        filters: Filters.
        instance_tenancy: Instance tenancy.
        offering_type: Offering type.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if availability_zone is not None:
        kwargs["AvailabilityZone"] = availability_zone
    if include_marketplace is not None:
        kwargs["IncludeMarketplace"] = include_marketplace
    if instance_type is not None:
        kwargs["InstanceType"] = instance_type
    if max_duration is not None:
        kwargs["MaxDuration"] = max_duration
    if max_instance_count is not None:
        kwargs["MaxInstanceCount"] = max_instance_count
    if min_duration is not None:
        kwargs["MinDuration"] = min_duration
    if offering_class is not None:
        kwargs["OfferingClass"] = offering_class
    if product_description is not None:
        kwargs["ProductDescription"] = product_description
    if reserved_instances_offering_ids is not None:
        kwargs["ReservedInstancesOfferingIds"] = reserved_instances_offering_ids
    if availability_zone_id is not None:
        kwargs["AvailabilityZoneId"] = availability_zone_id
    if filters is not None:
        kwargs["Filters"] = filters
    if instance_tenancy is not None:
        kwargs["InstanceTenancy"] = instance_tenancy
    if offering_type is not None:
        kwargs["OfferingType"] = offering_type
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeReservedInstancesOfferings", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe reserved instances offerings") from exc
    return DescribeReservedInstancesOfferingsResult(
        next_token=resp.get("NextToken"),
        reserved_instances_offerings=resp.get("ReservedInstancesOfferings"),
    )


async def describe_route_server_endpoints(
    *,
    route_server_endpoint_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeRouteServerEndpointsResult:
    """Describe route server endpoints.

    Args:
        route_server_endpoint_ids: Route server endpoint ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if route_server_endpoint_ids is not None:
        kwargs["RouteServerEndpointIds"] = route_server_endpoint_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeRouteServerEndpoints", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe route server endpoints") from exc
    return DescribeRouteServerEndpointsResult(
        route_server_endpoints=resp.get("RouteServerEndpoints"),
        next_token=resp.get("NextToken"),
    )


async def describe_route_server_peers(
    *,
    route_server_peer_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeRouteServerPeersResult:
    """Describe route server peers.

    Args:
        route_server_peer_ids: Route server peer ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if route_server_peer_ids is not None:
        kwargs["RouteServerPeerIds"] = route_server_peer_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeRouteServerPeers", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe route server peers") from exc
    return DescribeRouteServerPeersResult(
        route_server_peers=resp.get("RouteServerPeers"),
        next_token=resp.get("NextToken"),
    )


async def describe_route_servers(
    *,
    route_server_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeRouteServersResult:
    """Describe route servers.

    Args:
        route_server_ids: Route server ids.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if route_server_ids is not None:
        kwargs["RouteServerIds"] = route_server_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeRouteServers", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe route servers") from exc
    return DescribeRouteServersResult(
        route_servers=resp.get("RouteServers"),
        next_token=resp.get("NextToken"),
    )


async def describe_route_tables(
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    route_table_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeRouteTablesResult:
    """Describe route tables.

    Args:
        next_token: Next token.
        max_results: Max results.
        route_table_ids: Route table ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if route_table_ids is not None:
        kwargs["RouteTableIds"] = route_table_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeRouteTables", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe route tables") from exc
    return DescribeRouteTablesResult(
        route_tables=resp.get("RouteTables"),
        next_token=resp.get("NextToken"),
    )


async def describe_scheduled_instance_availability(
    first_slot_start_time_range: dict[str, Any],
    recurrence: dict[str, Any],
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    max_slot_duration_in_hours: int | None = None,
    min_slot_duration_in_hours: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeScheduledInstanceAvailabilityResult:
    """Describe scheduled instance availability.

    Args:
        first_slot_start_time_range: First slot start time range.
        recurrence: Recurrence.
        filters: Filters.
        max_results: Max results.
        max_slot_duration_in_hours: Max slot duration in hours.
        min_slot_duration_in_hours: Min slot duration in hours.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["FirstSlotStartTimeRange"] = first_slot_start_time_range
    kwargs["Recurrence"] = recurrence
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if max_slot_duration_in_hours is not None:
        kwargs["MaxSlotDurationInHours"] = max_slot_duration_in_hours
    if min_slot_duration_in_hours is not None:
        kwargs["MinSlotDurationInHours"] = min_slot_duration_in_hours
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeScheduledInstanceAvailability", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe scheduled instance availability") from exc
    return DescribeScheduledInstanceAvailabilityResult(
        next_token=resp.get("NextToken"),
        scheduled_instance_availability_set=resp.get("ScheduledInstanceAvailabilitySet"),
    )


async def describe_scheduled_instances(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    scheduled_instance_ids: list[str] | None = None,
    slot_start_time_range: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> DescribeScheduledInstancesResult:
    """Describe scheduled instances.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        scheduled_instance_ids: Scheduled instance ids.
        slot_start_time_range: Slot start time range.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if scheduled_instance_ids is not None:
        kwargs["ScheduledInstanceIds"] = scheduled_instance_ids
    if slot_start_time_range is not None:
        kwargs["SlotStartTimeRange"] = slot_start_time_range
    try:
        resp = await client.call("DescribeScheduledInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe scheduled instances") from exc
    return DescribeScheduledInstancesResult(
        next_token=resp.get("NextToken"),
        scheduled_instance_set=resp.get("ScheduledInstanceSet"),
    )


async def describe_security_group_references(
    group_id: list[str],
    region_name: str | None = None,
) -> DescribeSecurityGroupReferencesResult:
    """Describe security group references.

    Args:
        group_id: Group id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["GroupId"] = group_id
    try:
        resp = await client.call("DescribeSecurityGroupReferences", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe security group references") from exc
    return DescribeSecurityGroupReferencesResult(
        security_group_reference_set=resp.get("SecurityGroupReferenceSet"),
    )


async def describe_security_group_rules(
    *,
    filters: list[dict[str, Any]] | None = None,
    security_group_rule_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeSecurityGroupRulesResult:
    """Describe security group rules.

    Args:
        filters: Filters.
        security_group_rule_ids: Security group rule ids.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if security_group_rule_ids is not None:
        kwargs["SecurityGroupRuleIds"] = security_group_rule_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeSecurityGroupRules", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe security group rules") from exc
    return DescribeSecurityGroupRulesResult(
        security_group_rules=resp.get("SecurityGroupRules"),
        next_token=resp.get("NextToken"),
    )


async def describe_security_group_vpc_associations(
    *,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeSecurityGroupVpcAssociationsResult:
    """Describe security group vpc associations.

    Args:
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeSecurityGroupVpcAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe security group vpc associations") from exc
    return DescribeSecurityGroupVpcAssociationsResult(
        security_group_vpc_associations=resp.get("SecurityGroupVpcAssociations"),
        next_token=resp.get("NextToken"),
    )


async def describe_service_link_virtual_interfaces(
    *,
    service_link_virtual_interface_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeServiceLinkVirtualInterfacesResult:
    """Describe service link virtual interfaces.

    Args:
        service_link_virtual_interface_ids: Service link virtual interface ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if service_link_virtual_interface_ids is not None:
        kwargs["ServiceLinkVirtualInterfaceIds"] = service_link_virtual_interface_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeServiceLinkVirtualInterfaces", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe service link virtual interfaces") from exc
    return DescribeServiceLinkVirtualInterfacesResult(
        service_link_virtual_interfaces=resp.get("ServiceLinkVirtualInterfaces"),
        next_token=resp.get("NextToken"),
    )


async def describe_snapshot_attribute(
    attribute: str,
    snapshot_id: str,
    region_name: str | None = None,
) -> DescribeSnapshotAttributeResult:
    """Describe snapshot attribute.

    Args:
        attribute: Attribute.
        snapshot_id: Snapshot id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Attribute"] = attribute
    kwargs["SnapshotId"] = snapshot_id
    try:
        resp = await client.call("DescribeSnapshotAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe snapshot attribute") from exc
    return DescribeSnapshotAttributeResult(
        product_codes=resp.get("ProductCodes"),
        snapshot_id=resp.get("SnapshotId"),
        create_volume_permissions=resp.get("CreateVolumePermissions"),
    )


async def describe_snapshot_tier_status(
    *,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeSnapshotTierStatusResult:
    """Describe snapshot tier status.

    Args:
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeSnapshotTierStatus", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe snapshot tier status") from exc
    return DescribeSnapshotTierStatusResult(
        snapshot_tier_statuses=resp.get("SnapshotTierStatuses"),
        next_token=resp.get("NextToken"),
    )


async def describe_snapshots(
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    owner_ids: list[str] | None = None,
    restorable_by_user_ids: list[str] | None = None,
    snapshot_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeSnapshotsResult:
    """Describe snapshots.

    Args:
        max_results: Max results.
        next_token: Next token.
        owner_ids: Owner ids.
        restorable_by_user_ids: Restorable by user ids.
        snapshot_ids: Snapshot ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if owner_ids is not None:
        kwargs["OwnerIds"] = owner_ids
    if restorable_by_user_ids is not None:
        kwargs["RestorableByUserIds"] = restorable_by_user_ids
    if snapshot_ids is not None:
        kwargs["SnapshotIds"] = snapshot_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeSnapshots", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe snapshots") from exc
    return DescribeSnapshotsResult(
        next_token=resp.get("NextToken"),
        snapshots=resp.get("Snapshots"),
    )


async def describe_spot_datafeed_subscription(
    region_name: str | None = None,
) -> DescribeSpotDatafeedSubscriptionResult:
    """Describe spot datafeed subscription.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("DescribeSpotDatafeedSubscription", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe spot datafeed subscription") from exc
    return DescribeSpotDatafeedSubscriptionResult(
        spot_datafeed_subscription=resp.get("SpotDatafeedSubscription"),
    )


async def describe_spot_fleet_instances(
    spot_fleet_request_id: str,
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeSpotFleetInstancesResult:
    """Describe spot fleet instances.

    Args:
        spot_fleet_request_id: Spot fleet request id.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SpotFleetRequestId"] = spot_fleet_request_id
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeSpotFleetInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe spot fleet instances") from exc
    return DescribeSpotFleetInstancesResult(
        active_instances=resp.get("ActiveInstances"),
        next_token=resp.get("NextToken"),
        spot_fleet_request_id=resp.get("SpotFleetRequestId"),
    )


async def describe_spot_fleet_request_history(
    spot_fleet_request_id: str,
    start_time: str,
    *,
    event_type: str | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeSpotFleetRequestHistoryResult:
    """Describe spot fleet request history.

    Args:
        spot_fleet_request_id: Spot fleet request id.
        start_time: Start time.
        event_type: Event type.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SpotFleetRequestId"] = spot_fleet_request_id
    kwargs["StartTime"] = start_time
    if event_type is not None:
        kwargs["EventType"] = event_type
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeSpotFleetRequestHistory", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe spot fleet request history") from exc
    return DescribeSpotFleetRequestHistoryResult(
        history_records=resp.get("HistoryRecords"),
        last_evaluated_time=resp.get("LastEvaluatedTime"),
        next_token=resp.get("NextToken"),
        spot_fleet_request_id=resp.get("SpotFleetRequestId"),
        start_time=resp.get("StartTime"),
    )


async def describe_spot_fleet_requests(
    *,
    spot_fleet_request_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeSpotFleetRequestsResult:
    """Describe spot fleet requests.

    Args:
        spot_fleet_request_ids: Spot fleet request ids.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if spot_fleet_request_ids is not None:
        kwargs["SpotFleetRequestIds"] = spot_fleet_request_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeSpotFleetRequests", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe spot fleet requests") from exc
    return DescribeSpotFleetRequestsResult(
        next_token=resp.get("NextToken"),
        spot_fleet_request_configs=resp.get("SpotFleetRequestConfigs"),
    )


async def describe_spot_instance_requests(
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    spot_instance_request_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeSpotInstanceRequestsResult:
    """Describe spot instance requests.

    Args:
        next_token: Next token.
        max_results: Max results.
        spot_instance_request_ids: Spot instance request ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if spot_instance_request_ids is not None:
        kwargs["SpotInstanceRequestIds"] = spot_instance_request_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeSpotInstanceRequests", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe spot instance requests") from exc
    return DescribeSpotInstanceRequestsResult(
        spot_instance_requests=resp.get("SpotInstanceRequests"),
        next_token=resp.get("NextToken"),
    )


async def describe_spot_price_history(
    *,
    availability_zone_id: str | None = None,
    start_time: str | None = None,
    end_time: str | None = None,
    instance_types: list[str] | None = None,
    product_descriptions: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    availability_zone: str | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeSpotPriceHistoryResult:
    """Describe spot price history.

    Args:
        availability_zone_id: Availability zone id.
        start_time: Start time.
        end_time: End time.
        instance_types: Instance types.
        product_descriptions: Product descriptions.
        filters: Filters.
        availability_zone: Availability zone.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if availability_zone_id is not None:
        kwargs["AvailabilityZoneId"] = availability_zone_id
    if start_time is not None:
        kwargs["StartTime"] = start_time
    if end_time is not None:
        kwargs["EndTime"] = end_time
    if instance_types is not None:
        kwargs["InstanceTypes"] = instance_types
    if product_descriptions is not None:
        kwargs["ProductDescriptions"] = product_descriptions
    if filters is not None:
        kwargs["Filters"] = filters
    if availability_zone is not None:
        kwargs["AvailabilityZone"] = availability_zone
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeSpotPriceHistory", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe spot price history") from exc
    return DescribeSpotPriceHistoryResult(
        next_token=resp.get("NextToken"),
        spot_price_history=resp.get("SpotPriceHistory"),
    )


async def describe_stale_security_groups(
    vpc_id: str,
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeStaleSecurityGroupsResult:
    """Describe stale security groups.

    Args:
        vpc_id: Vpc id.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeStaleSecurityGroups", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe stale security groups") from exc
    return DescribeStaleSecurityGroupsResult(
        next_token=resp.get("NextToken"),
        stale_security_group_set=resp.get("StaleSecurityGroupSet"),
    )


async def describe_store_image_tasks(
    *,
    image_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeStoreImageTasksResult:
    """Describe store image tasks.

    Args:
        image_ids: Image ids.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if image_ids is not None:
        kwargs["ImageIds"] = image_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeStoreImageTasks", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe store image tasks") from exc
    return DescribeStoreImageTasksResult(
        store_image_task_results=resp.get("StoreImageTaskResults"),
        next_token=resp.get("NextToken"),
    )


async def describe_subnets(
    *,
    filters: list[dict[str, Any]] | None = None,
    subnet_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeSubnetsResult:
    """Describe subnets.

    Args:
        filters: Filters.
        subnet_ids: Subnet ids.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if subnet_ids is not None:
        kwargs["SubnetIds"] = subnet_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeSubnets", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe subnets") from exc
    return DescribeSubnetsResult(
        next_token=resp.get("NextToken"),
        subnets=resp.get("Subnets"),
    )


async def describe_tags(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTagsResult:
    """Describe tags.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTags", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe tags") from exc
    return DescribeTagsResult(
        next_token=resp.get("NextToken"),
        tags=resp.get("Tags"),
    )


async def describe_traffic_mirror_filter_rules(
    *,
    traffic_mirror_filter_rule_ids: list[str] | None = None,
    traffic_mirror_filter_id: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTrafficMirrorFilterRulesResult:
    """Describe traffic mirror filter rules.

    Args:
        traffic_mirror_filter_rule_ids: Traffic mirror filter rule ids.
        traffic_mirror_filter_id: Traffic mirror filter id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if traffic_mirror_filter_rule_ids is not None:
        kwargs["TrafficMirrorFilterRuleIds"] = traffic_mirror_filter_rule_ids
    if traffic_mirror_filter_id is not None:
        kwargs["TrafficMirrorFilterId"] = traffic_mirror_filter_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTrafficMirrorFilterRules", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe traffic mirror filter rules") from exc
    return DescribeTrafficMirrorFilterRulesResult(
        traffic_mirror_filter_rules=resp.get("TrafficMirrorFilterRules"),
        next_token=resp.get("NextToken"),
    )


async def describe_traffic_mirror_filters(
    *,
    traffic_mirror_filter_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTrafficMirrorFiltersResult:
    """Describe traffic mirror filters.

    Args:
        traffic_mirror_filter_ids: Traffic mirror filter ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if traffic_mirror_filter_ids is not None:
        kwargs["TrafficMirrorFilterIds"] = traffic_mirror_filter_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTrafficMirrorFilters", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe traffic mirror filters") from exc
    return DescribeTrafficMirrorFiltersResult(
        traffic_mirror_filters=resp.get("TrafficMirrorFilters"),
        next_token=resp.get("NextToken"),
    )


async def describe_traffic_mirror_sessions(
    *,
    traffic_mirror_session_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTrafficMirrorSessionsResult:
    """Describe traffic mirror sessions.

    Args:
        traffic_mirror_session_ids: Traffic mirror session ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if traffic_mirror_session_ids is not None:
        kwargs["TrafficMirrorSessionIds"] = traffic_mirror_session_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTrafficMirrorSessions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe traffic mirror sessions") from exc
    return DescribeTrafficMirrorSessionsResult(
        traffic_mirror_sessions=resp.get("TrafficMirrorSessions"),
        next_token=resp.get("NextToken"),
    )


async def describe_traffic_mirror_targets(
    *,
    traffic_mirror_target_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTrafficMirrorTargetsResult:
    """Describe traffic mirror targets.

    Args:
        traffic_mirror_target_ids: Traffic mirror target ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if traffic_mirror_target_ids is not None:
        kwargs["TrafficMirrorTargetIds"] = traffic_mirror_target_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTrafficMirrorTargets", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe traffic mirror targets") from exc
    return DescribeTrafficMirrorTargetsResult(
        traffic_mirror_targets=resp.get("TrafficMirrorTargets"),
        next_token=resp.get("NextToken"),
    )


async def describe_transit_gateway_attachments(
    *,
    transit_gateway_attachment_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTransitGatewayAttachmentsResult:
    """Describe transit gateway attachments.

    Args:
        transit_gateway_attachment_ids: Transit gateway attachment ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_attachment_ids is not None:
        kwargs["TransitGatewayAttachmentIds"] = transit_gateway_attachment_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTransitGatewayAttachments", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe transit gateway attachments") from exc
    return DescribeTransitGatewayAttachmentsResult(
        transit_gateway_attachments=resp.get("TransitGatewayAttachments"),
        next_token=resp.get("NextToken"),
    )


async def describe_transit_gateway_connect_peers(
    *,
    transit_gateway_connect_peer_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTransitGatewayConnectPeersResult:
    """Describe transit gateway connect peers.

    Args:
        transit_gateway_connect_peer_ids: Transit gateway connect peer ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_connect_peer_ids is not None:
        kwargs["TransitGatewayConnectPeerIds"] = transit_gateway_connect_peer_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTransitGatewayConnectPeers", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe transit gateway connect peers") from exc
    return DescribeTransitGatewayConnectPeersResult(
        transit_gateway_connect_peers=resp.get("TransitGatewayConnectPeers"),
        next_token=resp.get("NextToken"),
    )


async def describe_transit_gateway_connects(
    *,
    transit_gateway_attachment_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTransitGatewayConnectsResult:
    """Describe transit gateway connects.

    Args:
        transit_gateway_attachment_ids: Transit gateway attachment ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_attachment_ids is not None:
        kwargs["TransitGatewayAttachmentIds"] = transit_gateway_attachment_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTransitGatewayConnects", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe transit gateway connects") from exc
    return DescribeTransitGatewayConnectsResult(
        transit_gateway_connects=resp.get("TransitGatewayConnects"),
        next_token=resp.get("NextToken"),
    )


async def describe_transit_gateway_multicast_domains(
    *,
    transit_gateway_multicast_domain_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTransitGatewayMulticastDomainsResult:
    """Describe transit gateway multicast domains.

    Args:
        transit_gateway_multicast_domain_ids: Transit gateway multicast domain ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_multicast_domain_ids is not None:
        kwargs["TransitGatewayMulticastDomainIds"] = transit_gateway_multicast_domain_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTransitGatewayMulticastDomains", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe transit gateway multicast domains") from exc
    return DescribeTransitGatewayMulticastDomainsResult(
        transit_gateway_multicast_domains=resp.get("TransitGatewayMulticastDomains"),
        next_token=resp.get("NextToken"),
    )


async def describe_transit_gateway_peering_attachments(
    *,
    transit_gateway_attachment_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTransitGatewayPeeringAttachmentsResult:
    """Describe transit gateway peering attachments.

    Args:
        transit_gateway_attachment_ids: Transit gateway attachment ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_attachment_ids is not None:
        kwargs["TransitGatewayAttachmentIds"] = transit_gateway_attachment_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTransitGatewayPeeringAttachments", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe transit gateway peering attachments") from exc
    return DescribeTransitGatewayPeeringAttachmentsResult(
        transit_gateway_peering_attachments=resp.get("TransitGatewayPeeringAttachments"),
        next_token=resp.get("NextToken"),
    )


async def describe_transit_gateway_policy_tables(
    *,
    transit_gateway_policy_table_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTransitGatewayPolicyTablesResult:
    """Describe transit gateway policy tables.

    Args:
        transit_gateway_policy_table_ids: Transit gateway policy table ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_policy_table_ids is not None:
        kwargs["TransitGatewayPolicyTableIds"] = transit_gateway_policy_table_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTransitGatewayPolicyTables", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe transit gateway policy tables") from exc
    return DescribeTransitGatewayPolicyTablesResult(
        transit_gateway_policy_tables=resp.get("TransitGatewayPolicyTables"),
        next_token=resp.get("NextToken"),
    )


async def describe_transit_gateway_route_table_announcements(
    *,
    transit_gateway_route_table_announcement_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTransitGatewayRouteTableAnnouncementsResult:
    """Describe transit gateway route table announcements.

    Args:
        transit_gateway_route_table_announcement_ids: Transit gateway route table announcement ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_route_table_announcement_ids is not None:
        kwargs["TransitGatewayRouteTableAnnouncementIds"] = (
            transit_gateway_route_table_announcement_ids
        )
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTransitGatewayRouteTableAnnouncements", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe transit gateway route table announcements"
        ) from exc
    return DescribeTransitGatewayRouteTableAnnouncementsResult(
        transit_gateway_route_table_announcements=resp.get("TransitGatewayRouteTableAnnouncements"),
        next_token=resp.get("NextToken"),
    )


async def describe_transit_gateway_route_tables(
    *,
    transit_gateway_route_table_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTransitGatewayRouteTablesResult:
    """Describe transit gateway route tables.

    Args:
        transit_gateway_route_table_ids: Transit gateway route table ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_route_table_ids is not None:
        kwargs["TransitGatewayRouteTableIds"] = transit_gateway_route_table_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTransitGatewayRouteTables", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe transit gateway route tables") from exc
    return DescribeTransitGatewayRouteTablesResult(
        transit_gateway_route_tables=resp.get("TransitGatewayRouteTables"),
        next_token=resp.get("NextToken"),
    )


async def describe_transit_gateway_vpc_attachments(
    *,
    transit_gateway_attachment_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTransitGatewayVpcAttachmentsResult:
    """Describe transit gateway vpc attachments.

    Args:
        transit_gateway_attachment_ids: Transit gateway attachment ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_attachment_ids is not None:
        kwargs["TransitGatewayAttachmentIds"] = transit_gateway_attachment_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTransitGatewayVpcAttachments", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe transit gateway vpc attachments") from exc
    return DescribeTransitGatewayVpcAttachmentsResult(
        transit_gateway_vpc_attachments=resp.get("TransitGatewayVpcAttachments"),
        next_token=resp.get("NextToken"),
    )


async def describe_transit_gateways(
    *,
    transit_gateway_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeTransitGatewaysResult:
    """Describe transit gateways.

    Args:
        transit_gateway_ids: Transit gateway ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_ids is not None:
        kwargs["TransitGatewayIds"] = transit_gateway_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeTransitGateways", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe transit gateways") from exc
    return DescribeTransitGatewaysResult(
        transit_gateways=resp.get("TransitGateways"),
        next_token=resp.get("NextToken"),
    )


async def describe_trunk_interface_associations(
    *,
    association_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeTrunkInterfaceAssociationsResult:
    """Describe trunk interface associations.

    Args:
        association_ids: Association ids.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if association_ids is not None:
        kwargs["AssociationIds"] = association_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeTrunkInterfaceAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe trunk interface associations") from exc
    return DescribeTrunkInterfaceAssociationsResult(
        interface_associations=resp.get("InterfaceAssociations"),
        next_token=resp.get("NextToken"),
    )


async def describe_verified_access_endpoints(
    *,
    verified_access_endpoint_ids: list[str] | None = None,
    verified_access_instance_id: str | None = None,
    verified_access_group_id: str | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeVerifiedAccessEndpointsResult:
    """Describe verified access endpoints.

    Args:
        verified_access_endpoint_ids: Verified access endpoint ids.
        verified_access_instance_id: Verified access instance id.
        verified_access_group_id: Verified access group id.
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if verified_access_endpoint_ids is not None:
        kwargs["VerifiedAccessEndpointIds"] = verified_access_endpoint_ids
    if verified_access_instance_id is not None:
        kwargs["VerifiedAccessInstanceId"] = verified_access_instance_id
    if verified_access_group_id is not None:
        kwargs["VerifiedAccessGroupId"] = verified_access_group_id
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeVerifiedAccessEndpoints", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe verified access endpoints") from exc
    return DescribeVerifiedAccessEndpointsResult(
        verified_access_endpoints=resp.get("VerifiedAccessEndpoints"),
        next_token=resp.get("NextToken"),
    )


async def describe_verified_access_groups(
    *,
    verified_access_group_ids: list[str] | None = None,
    verified_access_instance_id: str | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeVerifiedAccessGroupsResult:
    """Describe verified access groups.

    Args:
        verified_access_group_ids: Verified access group ids.
        verified_access_instance_id: Verified access instance id.
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if verified_access_group_ids is not None:
        kwargs["VerifiedAccessGroupIds"] = verified_access_group_ids
    if verified_access_instance_id is not None:
        kwargs["VerifiedAccessInstanceId"] = verified_access_instance_id
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeVerifiedAccessGroups", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe verified access groups") from exc
    return DescribeVerifiedAccessGroupsResult(
        verified_access_groups=resp.get("VerifiedAccessGroups"),
        next_token=resp.get("NextToken"),
    )


async def describe_verified_access_instance_logging_configurations(
    *,
    verified_access_instance_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeVerifiedAccessInstanceLoggingConfigurationsResult:
    """Describe verified access instance logging configurations.

    Args:
        verified_access_instance_ids: Verified access instance ids.
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if verified_access_instance_ids is not None:
        kwargs["VerifiedAccessInstanceIds"] = verified_access_instance_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeVerifiedAccessInstanceLoggingConfigurations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe verified access instance logging configurations"
        ) from exc
    return DescribeVerifiedAccessInstanceLoggingConfigurationsResult(
        logging_configurations=resp.get("LoggingConfigurations"),
        next_token=resp.get("NextToken"),
    )


async def describe_verified_access_instances(
    *,
    verified_access_instance_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeVerifiedAccessInstancesResult:
    """Describe verified access instances.

    Args:
        verified_access_instance_ids: Verified access instance ids.
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if verified_access_instance_ids is not None:
        kwargs["VerifiedAccessInstanceIds"] = verified_access_instance_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeVerifiedAccessInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe verified access instances") from exc
    return DescribeVerifiedAccessInstancesResult(
        verified_access_instances=resp.get("VerifiedAccessInstances"),
        next_token=resp.get("NextToken"),
    )


async def describe_verified_access_trust_providers(
    *,
    verified_access_trust_provider_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeVerifiedAccessTrustProvidersResult:
    """Describe verified access trust providers.

    Args:
        verified_access_trust_provider_ids: Verified access trust provider ids.
        max_results: Max results.
        next_token: Next token.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if verified_access_trust_provider_ids is not None:
        kwargs["VerifiedAccessTrustProviderIds"] = verified_access_trust_provider_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeVerifiedAccessTrustProviders", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe verified access trust providers") from exc
    return DescribeVerifiedAccessTrustProvidersResult(
        verified_access_trust_providers=resp.get("VerifiedAccessTrustProviders"),
        next_token=resp.get("NextToken"),
    )


async def describe_volume_attribute(
    attribute: str,
    volume_id: str,
    region_name: str | None = None,
) -> DescribeVolumeAttributeResult:
    """Describe volume attribute.

    Args:
        attribute: Attribute.
        volume_id: Volume id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Attribute"] = attribute
    kwargs["VolumeId"] = volume_id
    try:
        resp = await client.call("DescribeVolumeAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe volume attribute") from exc
    return DescribeVolumeAttributeResult(
        auto_enable_io=resp.get("AutoEnableIO"),
        product_codes=resp.get("ProductCodes"),
        volume_id=resp.get("VolumeId"),
    )


async def describe_volume_status(
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    volume_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeVolumeStatusResult:
    """Describe volume status.

    Args:
        max_results: Max results.
        next_token: Next token.
        volume_ids: Volume ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if volume_ids is not None:
        kwargs["VolumeIds"] = volume_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeVolumeStatus", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe volume status") from exc
    return DescribeVolumeStatusResult(
        next_token=resp.get("NextToken"),
        volume_statuses=resp.get("VolumeStatuses"),
    )


async def describe_volumes(
    *,
    volume_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeVolumesResult:
    """Describe volumes.

    Args:
        volume_ids: Volume ids.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if volume_ids is not None:
        kwargs["VolumeIds"] = volume_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeVolumes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe volumes") from exc
    return DescribeVolumesResult(
        next_token=resp.get("NextToken"),
        volumes=resp.get("Volumes"),
    )


async def describe_volumes_modifications(
    *,
    volume_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeVolumesModificationsResult:
    """Describe volumes modifications.

    Args:
        volume_ids: Volume ids.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if volume_ids is not None:
        kwargs["VolumeIds"] = volume_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeVolumesModifications", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe volumes modifications") from exc
    return DescribeVolumesModificationsResult(
        next_token=resp.get("NextToken"),
        volumes_modifications=resp.get("VolumesModifications"),
    )


async def describe_vpc_attribute(
    attribute: str,
    vpc_id: str,
    region_name: str | None = None,
) -> DescribeVpcAttributeResult:
    """Describe vpc attribute.

    Args:
        attribute: Attribute.
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Attribute"] = attribute
    kwargs["VpcId"] = vpc_id
    try:
        resp = await client.call("DescribeVpcAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc attribute") from exc
    return DescribeVpcAttributeResult(
        enable_dns_hostnames=resp.get("EnableDnsHostnames"),
        enable_dns_support=resp.get("EnableDnsSupport"),
        enable_network_address_usage_metrics=resp.get("EnableNetworkAddressUsageMetrics"),
        vpc_id=resp.get("VpcId"),
    )


async def describe_vpc_block_public_access_exclusions(
    *,
    filters: list[dict[str, Any]] | None = None,
    exclusion_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeVpcBlockPublicAccessExclusionsResult:
    """Describe vpc block public access exclusions.

    Args:
        filters: Filters.
        exclusion_ids: Exclusion ids.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if exclusion_ids is not None:
        kwargs["ExclusionIds"] = exclusion_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeVpcBlockPublicAccessExclusions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc block public access exclusions") from exc
    return DescribeVpcBlockPublicAccessExclusionsResult(
        vpc_block_public_access_exclusions=resp.get("VpcBlockPublicAccessExclusions"),
        next_token=resp.get("NextToken"),
    )


async def describe_vpc_block_public_access_options(
    region_name: str | None = None,
) -> DescribeVpcBlockPublicAccessOptionsResult:
    """Describe vpc block public access options.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("DescribeVpcBlockPublicAccessOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc block public access options") from exc
    return DescribeVpcBlockPublicAccessOptionsResult(
        vpc_block_public_access_options=resp.get("VpcBlockPublicAccessOptions"),
    )


async def describe_vpc_classic_link(
    *,
    vpc_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeVpcClassicLinkResult:
    """Describe vpc classic link.

    Args:
        vpc_ids: Vpc ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if vpc_ids is not None:
        kwargs["VpcIds"] = vpc_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeVpcClassicLink", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc classic link") from exc
    return DescribeVpcClassicLinkResult(
        vpcs=resp.get("Vpcs"),
    )


async def describe_vpc_classic_link_dns_support(
    *,
    vpc_ids: list[str] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeVpcClassicLinkDnsSupportResult:
    """Describe vpc classic link dns support.

    Args:
        vpc_ids: Vpc ids.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if vpc_ids is not None:
        kwargs["VpcIds"] = vpc_ids
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeVpcClassicLinkDnsSupport", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc classic link dns support") from exc
    return DescribeVpcClassicLinkDnsSupportResult(
        next_token=resp.get("NextToken"),
        vpcs=resp.get("Vpcs"),
    )


async def describe_vpc_endpoint_associations(
    *,
    vpc_endpoint_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeVpcEndpointAssociationsResult:
    """Describe vpc endpoint associations.

    Args:
        vpc_endpoint_ids: Vpc endpoint ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if vpc_endpoint_ids is not None:
        kwargs["VpcEndpointIds"] = vpc_endpoint_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeVpcEndpointAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc endpoint associations") from exc
    return DescribeVpcEndpointAssociationsResult(
        vpc_endpoint_associations=resp.get("VpcEndpointAssociations"),
        next_token=resp.get("NextToken"),
    )


async def describe_vpc_endpoint_connection_notifications(
    *,
    connection_notification_id: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeVpcEndpointConnectionNotificationsResult:
    """Describe vpc endpoint connection notifications.

    Args:
        connection_notification_id: Connection notification id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if connection_notification_id is not None:
        kwargs["ConnectionNotificationId"] = connection_notification_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeVpcEndpointConnectionNotifications", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to describe vpc endpoint connection notifications"
        ) from exc
    return DescribeVpcEndpointConnectionNotificationsResult(
        connection_notification_set=resp.get("ConnectionNotificationSet"),
        next_token=resp.get("NextToken"),
    )


async def describe_vpc_endpoint_connections(
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeVpcEndpointConnectionsResult:
    """Describe vpc endpoint connections.

    Args:
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeVpcEndpointConnections", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc endpoint connections") from exc
    return DescribeVpcEndpointConnectionsResult(
        vpc_endpoint_connections=resp.get("VpcEndpointConnections"),
        next_token=resp.get("NextToken"),
    )


async def describe_vpc_endpoint_service_configurations(
    *,
    service_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeVpcEndpointServiceConfigurationsResult:
    """Describe vpc endpoint service configurations.

    Args:
        service_ids: Service ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if service_ids is not None:
        kwargs["ServiceIds"] = service_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeVpcEndpointServiceConfigurations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc endpoint service configurations") from exc
    return DescribeVpcEndpointServiceConfigurationsResult(
        service_configurations=resp.get("ServiceConfigurations"),
        next_token=resp.get("NextToken"),
    )


async def describe_vpc_endpoint_service_permissions(
    service_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeVpcEndpointServicePermissionsResult:
    """Describe vpc endpoint service permissions.

    Args:
        service_id: Service id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ServiceId"] = service_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeVpcEndpointServicePermissions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc endpoint service permissions") from exc
    return DescribeVpcEndpointServicePermissionsResult(
        allowed_principals=resp.get("AllowedPrincipals"),
        next_token=resp.get("NextToken"),
    )


async def describe_vpc_endpoint_services(
    *,
    service_names: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    service_regions: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeVpcEndpointServicesResult:
    """Describe vpc endpoint services.

    Args:
        service_names: Service names.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        service_regions: Service regions.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if service_names is not None:
        kwargs["ServiceNames"] = service_names
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if service_regions is not None:
        kwargs["ServiceRegions"] = service_regions
    try:
        resp = await client.call("DescribeVpcEndpointServices", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc endpoint services") from exc
    return DescribeVpcEndpointServicesResult(
        service_names=resp.get("ServiceNames"),
        service_details=resp.get("ServiceDetails"),
        next_token=resp.get("NextToken"),
    )


async def describe_vpc_endpoints(
    *,
    vpc_endpoint_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> DescribeVpcEndpointsResult:
    """Describe vpc endpoints.

    Args:
        vpc_endpoint_ids: Vpc endpoint ids.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if vpc_endpoint_ids is not None:
        kwargs["VpcEndpointIds"] = vpc_endpoint_ids
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("DescribeVpcEndpoints", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc endpoints") from exc
    return DescribeVpcEndpointsResult(
        vpc_endpoints=resp.get("VpcEndpoints"),
        next_token=resp.get("NextToken"),
    )


async def describe_vpc_peering_connections(
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    vpc_peering_connection_ids: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> DescribeVpcPeeringConnectionsResult:
    """Describe vpc peering connections.

    Args:
        next_token: Next token.
        max_results: Max results.
        vpc_peering_connection_ids: Vpc peering connection ids.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if vpc_peering_connection_ids is not None:
        kwargs["VpcPeeringConnectionIds"] = vpc_peering_connection_ids
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("DescribeVpcPeeringConnections", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpc peering connections") from exc
    return DescribeVpcPeeringConnectionsResult(
        vpc_peering_connections=resp.get("VpcPeeringConnections"),
        next_token=resp.get("NextToken"),
    )


async def describe_vpcs(
    *,
    filters: list[dict[str, Any]] | None = None,
    vpc_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> DescribeVpcsResult:
    """Describe vpcs.

    Args:
        filters: Filters.
        vpc_ids: Vpc ids.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if vpc_ids is not None:
        kwargs["VpcIds"] = vpc_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("DescribeVpcs", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpcs") from exc
    return DescribeVpcsResult(
        next_token=resp.get("NextToken"),
        vpcs=resp.get("Vpcs"),
    )


async def describe_vpn_connections(
    *,
    filters: list[dict[str, Any]] | None = None,
    vpn_connection_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeVpnConnectionsResult:
    """Describe vpn connections.

    Args:
        filters: Filters.
        vpn_connection_ids: Vpn connection ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if vpn_connection_ids is not None:
        kwargs["VpnConnectionIds"] = vpn_connection_ids
    try:
        resp = await client.call("DescribeVpnConnections", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpn connections") from exc
    return DescribeVpnConnectionsResult(
        vpn_connections=resp.get("VpnConnections"),
    )


async def describe_vpn_gateways(
    *,
    filters: list[dict[str, Any]] | None = None,
    vpn_gateway_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DescribeVpnGatewaysResult:
    """Describe vpn gateways.

    Args:
        filters: Filters.
        vpn_gateway_ids: Vpn gateway ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if filters is not None:
        kwargs["Filters"] = filters
    if vpn_gateway_ids is not None:
        kwargs["VpnGatewayIds"] = vpn_gateway_ids
    try:
        resp = await client.call("DescribeVpnGateways", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to describe vpn gateways") from exc
    return DescribeVpnGatewaysResult(
        vpn_gateways=resp.get("VpnGateways"),
    )


async def detach_classic_link_vpc(
    instance_id: str,
    vpc_id: str,
    region_name: str | None = None,
) -> DetachClassicLinkVpcResult:
    """Detach classic link vpc.

    Args:
        instance_id: Instance id.
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["VpcId"] = vpc_id
    try:
        resp = await client.call("DetachClassicLinkVpc", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to detach classic link vpc") from exc
    return DetachClassicLinkVpcResult(
        return_value=resp.get("Return"),
    )


async def detach_internet_gateway(
    internet_gateway_id: str,
    vpc_id: str,
    region_name: str | None = None,
) -> None:
    """Detach internet gateway.

    Args:
        internet_gateway_id: Internet gateway id.
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InternetGatewayId"] = internet_gateway_id
    kwargs["VpcId"] = vpc_id
    try:
        await client.call("DetachInternetGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to detach internet gateway") from exc
    return None


async def detach_network_interface(
    attachment_id: str,
    *,
    force: bool | None = None,
    region_name: str | None = None,
) -> None:
    """Detach network interface.

    Args:
        attachment_id: Attachment id.
        force: Force.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AttachmentId"] = attachment_id
    if force is not None:
        kwargs["Force"] = force
    try:
        await client.call("DetachNetworkInterface", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to detach network interface") from exc
    return None


async def detach_verified_access_trust_provider(
    verified_access_instance_id: str,
    verified_access_trust_provider_id: str,
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> DetachVerifiedAccessTrustProviderResult:
    """Detach verified access trust provider.

    Args:
        verified_access_instance_id: Verified access instance id.
        verified_access_trust_provider_id: Verified access trust provider id.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessInstanceId"] = verified_access_instance_id
    kwargs["VerifiedAccessTrustProviderId"] = verified_access_trust_provider_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("DetachVerifiedAccessTrustProvider", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to detach verified access trust provider") from exc
    return DetachVerifiedAccessTrustProviderResult(
        verified_access_trust_provider=resp.get("VerifiedAccessTrustProvider"),
        verified_access_instance=resp.get("VerifiedAccessInstance"),
    )


async def detach_volume(
    volume_id: str,
    *,
    device: str | None = None,
    force: bool | None = None,
    instance_id: str | None = None,
    region_name: str | None = None,
) -> DetachVolumeResult:
    """Detach volume.

    Args:
        volume_id: Volume id.
        device: Device.
        force: Force.
        instance_id: Instance id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VolumeId"] = volume_id
    if device is not None:
        kwargs["Device"] = device
    if force is not None:
        kwargs["Force"] = force
    if instance_id is not None:
        kwargs["InstanceId"] = instance_id
    try:
        resp = await client.call("DetachVolume", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to detach volume") from exc
    return DetachVolumeResult(
        delete_on_termination=resp.get("DeleteOnTermination"),
        associated_resource=resp.get("AssociatedResource"),
        instance_owning_service=resp.get("InstanceOwningService"),
        volume_id=resp.get("VolumeId"),
        instance_id=resp.get("InstanceId"),
        device=resp.get("Device"),
        state=resp.get("State"),
        attach_time=resp.get("AttachTime"),
    )


async def detach_vpn_gateway(
    vpc_id: str,
    vpn_gateway_id: str,
    region_name: str | None = None,
) -> None:
    """Detach vpn gateway.

    Args:
        vpc_id: Vpc id.
        vpn_gateway_id: Vpn gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    kwargs["VpnGatewayId"] = vpn_gateway_id
    try:
        await client.call("DetachVpnGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to detach vpn gateway") from exc
    return None


async def disable_address_transfer(
    allocation_id: str,
    region_name: str | None = None,
) -> DisableAddressTransferResult:
    """Disable address transfer.

    Args:
        allocation_id: Allocation id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AllocationId"] = allocation_id
    try:
        resp = await client.call("DisableAddressTransfer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable address transfer") from exc
    return DisableAddressTransferResult(
        address_transfer=resp.get("AddressTransfer"),
    )


async def disable_allowed_images_settings(
    region_name: str | None = None,
) -> DisableAllowedImagesSettingsResult:
    """Disable allowed images settings.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("DisableAllowedImagesSettings", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable allowed images settings") from exc
    return DisableAllowedImagesSettingsResult(
        allowed_images_settings_state=resp.get("AllowedImagesSettingsState"),
    )


async def disable_aws_network_performance_metric_subscription(
    *,
    source: str | None = None,
    destination: str | None = None,
    metric: str | None = None,
    statistic: str | None = None,
    region_name: str | None = None,
) -> DisableAwsNetworkPerformanceMetricSubscriptionResult:
    """Disable aws network performance metric subscription.

    Args:
        source: Source.
        destination: Destination.
        metric: Metric.
        statistic: Statistic.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if source is not None:
        kwargs["Source"] = source
    if destination is not None:
        kwargs["Destination"] = destination
    if metric is not None:
        kwargs["Metric"] = metric
    if statistic is not None:
        kwargs["Statistic"] = statistic
    try:
        resp = await client.call("DisableAwsNetworkPerformanceMetricSubscription", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to disable aws network performance metric subscription"
        ) from exc
    return DisableAwsNetworkPerformanceMetricSubscriptionResult(
        output=resp.get("Output"),
    )


async def disable_capacity_manager(
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> DisableCapacityManagerResult:
    """Disable capacity manager.

    Args:
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("DisableCapacityManager", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable capacity manager") from exc
    return DisableCapacityManagerResult(
        capacity_manager_status=resp.get("CapacityManagerStatus"),
        organizations_access=resp.get("OrganizationsAccess"),
    )


async def disable_ebs_encryption_by_default(
    region_name: str | None = None,
) -> DisableEbsEncryptionByDefaultResult:
    """Disable ebs encryption by default.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("DisableEbsEncryptionByDefault", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable ebs encryption by default") from exc
    return DisableEbsEncryptionByDefaultResult(
        ebs_encryption_by_default=resp.get("EbsEncryptionByDefault"),
    )


async def disable_fast_launch(
    image_id: str,
    *,
    force: bool | None = None,
    region_name: str | None = None,
) -> DisableFastLaunchResult:
    """Disable fast launch.

    Args:
        image_id: Image id.
        force: Force.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    if force is not None:
        kwargs["Force"] = force
    try:
        resp = await client.call("DisableFastLaunch", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable fast launch") from exc
    return DisableFastLaunchResult(
        image_id=resp.get("ImageId"),
        resource_type=resp.get("ResourceType"),
        snapshot_configuration=resp.get("SnapshotConfiguration"),
        launch_template=resp.get("LaunchTemplate"),
        max_parallel_launches=resp.get("MaxParallelLaunches"),
        owner_id=resp.get("OwnerId"),
        state=resp.get("State"),
        state_transition_reason=resp.get("StateTransitionReason"),
        state_transition_time=resp.get("StateTransitionTime"),
    )


async def disable_fast_snapshot_restores(
    source_snapshot_ids: list[str],
    *,
    availability_zones: list[str] | None = None,
    availability_zone_ids: list[str] | None = None,
    region_name: str | None = None,
) -> DisableFastSnapshotRestoresResult:
    """Disable fast snapshot restores.

    Args:
        source_snapshot_ids: Source snapshot ids.
        availability_zones: Availability zones.
        availability_zone_ids: Availability zone ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SourceSnapshotIds"] = source_snapshot_ids
    if availability_zones is not None:
        kwargs["AvailabilityZones"] = availability_zones
    if availability_zone_ids is not None:
        kwargs["AvailabilityZoneIds"] = availability_zone_ids
    try:
        resp = await client.call("DisableFastSnapshotRestores", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable fast snapshot restores") from exc
    return DisableFastSnapshotRestoresResult(
        successful=resp.get("Successful"),
        unsuccessful=resp.get("Unsuccessful"),
    )


async def disable_image(
    image_id: str,
    region_name: str | None = None,
) -> DisableImageResult:
    """Disable image.

    Args:
        image_id: Image id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    try:
        resp = await client.call("DisableImage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable image") from exc
    return DisableImageResult(
        return_value=resp.get("Return"),
    )


async def disable_image_block_public_access(
    region_name: str | None = None,
) -> DisableImageBlockPublicAccessResult:
    """Disable image block public access.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("DisableImageBlockPublicAccess", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable image block public access") from exc
    return DisableImageBlockPublicAccessResult(
        image_block_public_access_state=resp.get("ImageBlockPublicAccessState"),
    )


async def disable_image_deprecation(
    image_id: str,
    region_name: str | None = None,
) -> DisableImageDeprecationResult:
    """Disable image deprecation.

    Args:
        image_id: Image id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    try:
        resp = await client.call("DisableImageDeprecation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable image deprecation") from exc
    return DisableImageDeprecationResult(
        return_value=resp.get("Return"),
    )


async def disable_image_deregistration_protection(
    image_id: str,
    region_name: str | None = None,
) -> DisableImageDeregistrationProtectionResult:
    """Disable image deregistration protection.

    Args:
        image_id: Image id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    try:
        resp = await client.call("DisableImageDeregistrationProtection", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable image deregistration protection") from exc
    return DisableImageDeregistrationProtectionResult(
        return_value=resp.get("Return"),
    )


async def disable_ipam_organization_admin_account(
    delegated_admin_account_id: str,
    region_name: str | None = None,
) -> DisableIpamOrganizationAdminAccountResult:
    """Disable ipam organization admin account.

    Args:
        delegated_admin_account_id: Delegated admin account id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["DelegatedAdminAccountId"] = delegated_admin_account_id
    try:
        resp = await client.call("DisableIpamOrganizationAdminAccount", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable ipam organization admin account") from exc
    return DisableIpamOrganizationAdminAccountResult(
        success=resp.get("Success"),
    )


async def disable_route_server_propagation(
    route_server_id: str,
    route_table_id: str,
    region_name: str | None = None,
) -> DisableRouteServerPropagationResult:
    """Disable route server propagation.

    Args:
        route_server_id: Route server id.
        route_table_id: Route table id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerId"] = route_server_id
    kwargs["RouteTableId"] = route_table_id
    try:
        resp = await client.call("DisableRouteServerPropagation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable route server propagation") from exc
    return DisableRouteServerPropagationResult(
        route_server_propagation=resp.get("RouteServerPropagation"),
    )


async def disable_serial_console_access(
    region_name: str | None = None,
) -> DisableSerialConsoleAccessResult:
    """Disable serial console access.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("DisableSerialConsoleAccess", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable serial console access") from exc
    return DisableSerialConsoleAccessResult(
        serial_console_access_enabled=resp.get("SerialConsoleAccessEnabled"),
    )


async def disable_snapshot_block_public_access(
    region_name: str | None = None,
) -> DisableSnapshotBlockPublicAccessResult:
    """Disable snapshot block public access.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("DisableSnapshotBlockPublicAccess", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable snapshot block public access") from exc
    return DisableSnapshotBlockPublicAccessResult(
        state=resp.get("State"),
    )


async def disable_transit_gateway_route_table_propagation(
    transit_gateway_route_table_id: str,
    *,
    transit_gateway_attachment_id: str | None = None,
    transit_gateway_route_table_announcement_id: str | None = None,
    region_name: str | None = None,
) -> DisableTransitGatewayRouteTablePropagationResult:
    """Disable transit gateway route table propagation.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        transit_gateway_route_table_announcement_id: Transit gateway route table announcement id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    if transit_gateway_attachment_id is not None:
        kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    if transit_gateway_route_table_announcement_id is not None:
        kwargs["TransitGatewayRouteTableAnnouncementId"] = (
            transit_gateway_route_table_announcement_id
        )
    try:
        resp = await client.call("DisableTransitGatewayRouteTablePropagation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to disable transit gateway route table propagation"
        ) from exc
    return DisableTransitGatewayRouteTablePropagationResult(
        propagation=resp.get("Propagation"),
    )


async def disable_vgw_route_propagation(
    gateway_id: str,
    route_table_id: str,
    region_name: str | None = None,
) -> None:
    """Disable vgw route propagation.

    Args:
        gateway_id: Gateway id.
        route_table_id: Route table id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["GatewayId"] = gateway_id
    kwargs["RouteTableId"] = route_table_id
    try:
        await client.call("DisableVgwRoutePropagation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable vgw route propagation") from exc
    return None


async def disable_vpc_classic_link(
    vpc_id: str,
    region_name: str | None = None,
) -> DisableVpcClassicLinkResult:
    """Disable vpc classic link.

    Args:
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    try:
        resp = await client.call("DisableVpcClassicLink", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable vpc classic link") from exc
    return DisableVpcClassicLinkResult(
        return_value=resp.get("Return"),
    )


async def disable_vpc_classic_link_dns_support(
    *,
    vpc_id: str | None = None,
    region_name: str | None = None,
) -> DisableVpcClassicLinkDnsSupportResult:
    """Disable vpc classic link dns support.

    Args:
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if vpc_id is not None:
        kwargs["VpcId"] = vpc_id
    try:
        resp = await client.call("DisableVpcClassicLinkDnsSupport", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disable vpc classic link dns support") from exc
    return DisableVpcClassicLinkDnsSupportResult(
        return_value=resp.get("Return"),
    )


async def disassociate_address(
    *,
    association_id: str | None = None,
    public_ip: str | None = None,
    region_name: str | None = None,
) -> None:
    """Disassociate address.

    Args:
        association_id: Association id.
        public_ip: Public ip.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if association_id is not None:
        kwargs["AssociationId"] = association_id
    if public_ip is not None:
        kwargs["PublicIp"] = public_ip
    try:
        await client.call("DisassociateAddress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate address") from exc
    return None


async def disassociate_capacity_reservation_billing_owner(
    capacity_reservation_id: str,
    unused_reservation_billing_owner_id: str,
    region_name: str | None = None,
) -> DisassociateCapacityReservationBillingOwnerResult:
    """Disassociate capacity reservation billing owner.

    Args:
        capacity_reservation_id: Capacity reservation id.
        unused_reservation_billing_owner_id: Unused reservation billing owner id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityReservationId"] = capacity_reservation_id
    kwargs["UnusedReservationBillingOwnerId"] = unused_reservation_billing_owner_id
    try:
        resp = await client.call("DisassociateCapacityReservationBillingOwner", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to disassociate capacity reservation billing owner"
        ) from exc
    return DisassociateCapacityReservationBillingOwnerResult(
        return_value=resp.get("Return"),
    )


async def disassociate_client_vpn_target_network(
    client_vpn_endpoint_id: str,
    association_id: str,
    region_name: str | None = None,
) -> DisassociateClientVpnTargetNetworkResult:
    """Disassociate client vpn target network.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        association_id: Association id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    kwargs["AssociationId"] = association_id
    try:
        resp = await client.call("DisassociateClientVpnTargetNetwork", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate client vpn target network") from exc
    return DisassociateClientVpnTargetNetworkResult(
        association_id=resp.get("AssociationId"),
        status=resp.get("Status"),
    )


async def disassociate_enclave_certificate_iam_role(
    certificate_arn: str,
    role_arn: str,
    region_name: str | None = None,
) -> DisassociateEnclaveCertificateIamRoleResult:
    """Disassociate enclave certificate iam role.

    Args:
        certificate_arn: Certificate arn.
        role_arn: Role arn.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CertificateArn"] = certificate_arn
    kwargs["RoleArn"] = role_arn
    try:
        resp = await client.call("DisassociateEnclaveCertificateIamRole", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate enclave certificate iam role") from exc
    return DisassociateEnclaveCertificateIamRoleResult(
        return_value=resp.get("Return"),
    )


async def disassociate_iam_instance_profile(
    association_id: str,
    region_name: str | None = None,
) -> DisassociateIamInstanceProfileResult:
    """Disassociate iam instance profile.

    Args:
        association_id: Association id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AssociationId"] = association_id
    try:
        resp = await client.call("DisassociateIamInstanceProfile", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate iam instance profile") from exc
    return DisassociateIamInstanceProfileResult(
        iam_instance_profile_association=resp.get("IamInstanceProfileAssociation"),
    )


async def disassociate_instance_event_window(
    instance_event_window_id: str,
    association_target: dict[str, Any],
    region_name: str | None = None,
) -> DisassociateInstanceEventWindowResult:
    """Disassociate instance event window.

    Args:
        instance_event_window_id: Instance event window id.
        association_target: Association target.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceEventWindowId"] = instance_event_window_id
    kwargs["AssociationTarget"] = association_target
    try:
        resp = await client.call("DisassociateInstanceEventWindow", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate instance event window") from exc
    return DisassociateInstanceEventWindowResult(
        instance_event_window=resp.get("InstanceEventWindow"),
    )


async def disassociate_ipam_byoasn(
    asn: str,
    cidr: str,
    region_name: str | None = None,
) -> DisassociateIpamByoasnResult:
    """Disassociate ipam byoasn.

    Args:
        asn: Asn.
        cidr: Cidr.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Asn"] = asn
    kwargs["Cidr"] = cidr
    try:
        resp = await client.call("DisassociateIpamByoasn", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate ipam byoasn") from exc
    return DisassociateIpamByoasnResult(
        asn_association=resp.get("AsnAssociation"),
    )


async def disassociate_ipam_resource_discovery(
    ipam_resource_discovery_association_id: str,
    region_name: str | None = None,
) -> DisassociateIpamResourceDiscoveryResult:
    """Disassociate ipam resource discovery.

    Args:
        ipam_resource_discovery_association_id: Ipam resource discovery association id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamResourceDiscoveryAssociationId"] = ipam_resource_discovery_association_id
    try:
        resp = await client.call("DisassociateIpamResourceDiscovery", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate ipam resource discovery") from exc
    return DisassociateIpamResourceDiscoveryResult(
        ipam_resource_discovery_association=resp.get("IpamResourceDiscoveryAssociation"),
    )


async def disassociate_nat_gateway_address(
    nat_gateway_id: str,
    association_ids: list[str],
    *,
    max_drain_duration_seconds: int | None = None,
    region_name: str | None = None,
) -> DisassociateNatGatewayAddressResult:
    """Disassociate nat gateway address.

    Args:
        nat_gateway_id: Nat gateway id.
        association_ids: Association ids.
        max_drain_duration_seconds: Max drain duration seconds.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NatGatewayId"] = nat_gateway_id
    kwargs["AssociationIds"] = association_ids
    if max_drain_duration_seconds is not None:
        kwargs["MaxDrainDurationSeconds"] = max_drain_duration_seconds
    try:
        resp = await client.call("DisassociateNatGatewayAddress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate nat gateway address") from exc
    return DisassociateNatGatewayAddressResult(
        nat_gateway_id=resp.get("NatGatewayId"),
        nat_gateway_addresses=resp.get("NatGatewayAddresses"),
    )


async def disassociate_route_server(
    route_server_id: str,
    vpc_id: str,
    region_name: str | None = None,
) -> DisassociateRouteServerResult:
    """Disassociate route server.

    Args:
        route_server_id: Route server id.
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerId"] = route_server_id
    kwargs["VpcId"] = vpc_id
    try:
        resp = await client.call("DisassociateRouteServer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate route server") from exc
    return DisassociateRouteServerResult(
        route_server_association=resp.get("RouteServerAssociation"),
    )


async def disassociate_route_table(
    association_id: str,
    region_name: str | None = None,
) -> None:
    """Disassociate route table.

    Args:
        association_id: Association id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AssociationId"] = association_id
    try:
        await client.call("DisassociateRouteTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate route table") from exc
    return None


async def disassociate_security_group_vpc(
    group_id: str,
    vpc_id: str,
    region_name: str | None = None,
) -> DisassociateSecurityGroupVpcResult:
    """Disassociate security group vpc.

    Args:
        group_id: Group id.
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["GroupId"] = group_id
    kwargs["VpcId"] = vpc_id
    try:
        resp = await client.call("DisassociateSecurityGroupVpc", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate security group vpc") from exc
    return DisassociateSecurityGroupVpcResult(
        state=resp.get("State"),
    )


async def disassociate_subnet_cidr_block(
    association_id: str,
    region_name: str | None = None,
) -> DisassociateSubnetCidrBlockResult:
    """Disassociate subnet cidr block.

    Args:
        association_id: Association id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AssociationId"] = association_id
    try:
        resp = await client.call("DisassociateSubnetCidrBlock", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate subnet cidr block") from exc
    return DisassociateSubnetCidrBlockResult(
        ipv6_cidr_block_association=resp.get("Ipv6CidrBlockAssociation"),
        subnet_id=resp.get("SubnetId"),
    )


async def disassociate_transit_gateway_multicast_domain(
    transit_gateway_multicast_domain_id: str,
    transit_gateway_attachment_id: str,
    subnet_ids: list[str],
    region_name: str | None = None,
) -> DisassociateTransitGatewayMulticastDomainResult:
    """Disassociate transit gateway multicast domain.

    Args:
        transit_gateway_multicast_domain_id: Transit gateway multicast domain id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        subnet_ids: Subnet ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayMulticastDomainId"] = transit_gateway_multicast_domain_id
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    kwargs["SubnetIds"] = subnet_ids
    try:
        resp = await client.call("DisassociateTransitGatewayMulticastDomain", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to disassociate transit gateway multicast domain"
        ) from exc
    return DisassociateTransitGatewayMulticastDomainResult(
        associations=resp.get("Associations"),
    )


async def disassociate_transit_gateway_policy_table(
    transit_gateway_policy_table_id: str,
    transit_gateway_attachment_id: str,
    region_name: str | None = None,
) -> DisassociateTransitGatewayPolicyTableResult:
    """Disassociate transit gateway policy table.

    Args:
        transit_gateway_policy_table_id: Transit gateway policy table id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayPolicyTableId"] = transit_gateway_policy_table_id
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    try:
        resp = await client.call("DisassociateTransitGatewayPolicyTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate transit gateway policy table") from exc
    return DisassociateTransitGatewayPolicyTableResult(
        association=resp.get("Association"),
    )


async def disassociate_transit_gateway_route_table(
    transit_gateway_route_table_id: str,
    transit_gateway_attachment_id: str,
    region_name: str | None = None,
) -> DisassociateTransitGatewayRouteTableResult:
    """Disassociate transit gateway route table.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    try:
        resp = await client.call("DisassociateTransitGatewayRouteTable", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate transit gateway route table") from exc
    return DisassociateTransitGatewayRouteTableResult(
        association=resp.get("Association"),
    )


async def disassociate_trunk_interface(
    association_id: str,
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> DisassociateTrunkInterfaceResult:
    """Disassociate trunk interface.

    Args:
        association_id: Association id.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AssociationId"] = association_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("DisassociateTrunkInterface", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate trunk interface") from exc
    return DisassociateTrunkInterfaceResult(
        return_value=resp.get("Return"),
        client_token=resp.get("ClientToken"),
    )


async def disassociate_vpc_cidr_block(
    association_id: str,
    region_name: str | None = None,
) -> DisassociateVpcCidrBlockResult:
    """Disassociate vpc cidr block.

    Args:
        association_id: Association id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AssociationId"] = association_id
    try:
        resp = await client.call("DisassociateVpcCidrBlock", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to disassociate vpc cidr block") from exc
    return DisassociateVpcCidrBlockResult(
        ipv6_cidr_block_association=resp.get("Ipv6CidrBlockAssociation"),
        cidr_block_association=resp.get("CidrBlockAssociation"),
        vpc_id=resp.get("VpcId"),
    )


async def enable_address_transfer(
    allocation_id: str,
    transfer_account_id: str,
    region_name: str | None = None,
) -> EnableAddressTransferResult:
    """Enable address transfer.

    Args:
        allocation_id: Allocation id.
        transfer_account_id: Transfer account id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AllocationId"] = allocation_id
    kwargs["TransferAccountId"] = transfer_account_id
    try:
        resp = await client.call("EnableAddressTransfer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable address transfer") from exc
    return EnableAddressTransferResult(
        address_transfer=resp.get("AddressTransfer"),
    )


async def enable_allowed_images_settings(
    allowed_images_settings_state: str,
    region_name: str | None = None,
) -> EnableAllowedImagesSettingsResult:
    """Enable allowed images settings.

    Args:
        allowed_images_settings_state: Allowed images settings state.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AllowedImagesSettingsState"] = allowed_images_settings_state
    try:
        resp = await client.call("EnableAllowedImagesSettings", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable allowed images settings") from exc
    return EnableAllowedImagesSettingsResult(
        allowed_images_settings_state=resp.get("AllowedImagesSettingsState"),
    )


async def enable_aws_network_performance_metric_subscription(
    *,
    source: str | None = None,
    destination: str | None = None,
    metric: str | None = None,
    statistic: str | None = None,
    region_name: str | None = None,
) -> EnableAwsNetworkPerformanceMetricSubscriptionResult:
    """Enable aws network performance metric subscription.

    Args:
        source: Source.
        destination: Destination.
        metric: Metric.
        statistic: Statistic.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if source is not None:
        kwargs["Source"] = source
    if destination is not None:
        kwargs["Destination"] = destination
    if metric is not None:
        kwargs["Metric"] = metric
    if statistic is not None:
        kwargs["Statistic"] = statistic
    try:
        resp = await client.call("EnableAwsNetworkPerformanceMetricSubscription", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to enable aws network performance metric subscription"
        ) from exc
    return EnableAwsNetworkPerformanceMetricSubscriptionResult(
        output=resp.get("Output"),
    )


async def enable_capacity_manager(
    *,
    organizations_access: bool | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> EnableCapacityManagerResult:
    """Enable capacity manager.

    Args:
        organizations_access: Organizations access.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if organizations_access is not None:
        kwargs["OrganizationsAccess"] = organizations_access
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("EnableCapacityManager", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable capacity manager") from exc
    return EnableCapacityManagerResult(
        capacity_manager_status=resp.get("CapacityManagerStatus"),
        organizations_access=resp.get("OrganizationsAccess"),
    )


async def enable_ebs_encryption_by_default(
    region_name: str | None = None,
) -> EnableEbsEncryptionByDefaultResult:
    """Enable ebs encryption by default.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("EnableEbsEncryptionByDefault", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable ebs encryption by default") from exc
    return EnableEbsEncryptionByDefaultResult(
        ebs_encryption_by_default=resp.get("EbsEncryptionByDefault"),
    )


async def enable_fast_launch(
    image_id: str,
    *,
    resource_type: str | None = None,
    snapshot_configuration: dict[str, Any] | None = None,
    launch_template: dict[str, Any] | None = None,
    max_parallel_launches: int | None = None,
    region_name: str | None = None,
) -> EnableFastLaunchResult:
    """Enable fast launch.

    Args:
        image_id: Image id.
        resource_type: Resource type.
        snapshot_configuration: Snapshot configuration.
        launch_template: Launch template.
        max_parallel_launches: Max parallel launches.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    if resource_type is not None:
        kwargs["ResourceType"] = resource_type
    if snapshot_configuration is not None:
        kwargs["SnapshotConfiguration"] = snapshot_configuration
    if launch_template is not None:
        kwargs["LaunchTemplate"] = launch_template
    if max_parallel_launches is not None:
        kwargs["MaxParallelLaunches"] = max_parallel_launches
    try:
        resp = await client.call("EnableFastLaunch", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable fast launch") from exc
    return EnableFastLaunchResult(
        image_id=resp.get("ImageId"),
        resource_type=resp.get("ResourceType"),
        snapshot_configuration=resp.get("SnapshotConfiguration"),
        launch_template=resp.get("LaunchTemplate"),
        max_parallel_launches=resp.get("MaxParallelLaunches"),
        owner_id=resp.get("OwnerId"),
        state=resp.get("State"),
        state_transition_reason=resp.get("StateTransitionReason"),
        state_transition_time=resp.get("StateTransitionTime"),
    )


async def enable_fast_snapshot_restores(
    source_snapshot_ids: list[str],
    *,
    availability_zones: list[str] | None = None,
    availability_zone_ids: list[str] | None = None,
    region_name: str | None = None,
) -> EnableFastSnapshotRestoresResult:
    """Enable fast snapshot restores.

    Args:
        source_snapshot_ids: Source snapshot ids.
        availability_zones: Availability zones.
        availability_zone_ids: Availability zone ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SourceSnapshotIds"] = source_snapshot_ids
    if availability_zones is not None:
        kwargs["AvailabilityZones"] = availability_zones
    if availability_zone_ids is not None:
        kwargs["AvailabilityZoneIds"] = availability_zone_ids
    try:
        resp = await client.call("EnableFastSnapshotRestores", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable fast snapshot restores") from exc
    return EnableFastSnapshotRestoresResult(
        successful=resp.get("Successful"),
        unsuccessful=resp.get("Unsuccessful"),
    )


async def enable_image(
    image_id: str,
    region_name: str | None = None,
) -> EnableImageResult:
    """Enable image.

    Args:
        image_id: Image id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    try:
        resp = await client.call("EnableImage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable image") from exc
    return EnableImageResult(
        return_value=resp.get("Return"),
    )


async def enable_image_block_public_access(
    image_block_public_access_state: str,
    region_name: str | None = None,
) -> EnableImageBlockPublicAccessResult:
    """Enable image block public access.

    Args:
        image_block_public_access_state: Image block public access state.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageBlockPublicAccessState"] = image_block_public_access_state
    try:
        resp = await client.call("EnableImageBlockPublicAccess", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable image block public access") from exc
    return EnableImageBlockPublicAccessResult(
        image_block_public_access_state=resp.get("ImageBlockPublicAccessState"),
    )


async def enable_image_deprecation(
    image_id: str,
    deprecate_at: str,
    region_name: str | None = None,
) -> EnableImageDeprecationResult:
    """Enable image deprecation.

    Args:
        image_id: Image id.
        deprecate_at: Deprecate at.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    kwargs["DeprecateAt"] = deprecate_at
    try:
        resp = await client.call("EnableImageDeprecation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable image deprecation") from exc
    return EnableImageDeprecationResult(
        return_value=resp.get("Return"),
    )


async def enable_image_deregistration_protection(
    image_id: str,
    *,
    with_cooldown: bool | None = None,
    region_name: str | None = None,
) -> EnableImageDeregistrationProtectionResult:
    """Enable image deregistration protection.

    Args:
        image_id: Image id.
        with_cooldown: With cooldown.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    if with_cooldown is not None:
        kwargs["WithCooldown"] = with_cooldown
    try:
        resp = await client.call("EnableImageDeregistrationProtection", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable image deregistration protection") from exc
    return EnableImageDeregistrationProtectionResult(
        return_value=resp.get("Return"),
    )


async def enable_ipam_organization_admin_account(
    delegated_admin_account_id: str,
    region_name: str | None = None,
) -> EnableIpamOrganizationAdminAccountResult:
    """Enable ipam organization admin account.

    Args:
        delegated_admin_account_id: Delegated admin account id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["DelegatedAdminAccountId"] = delegated_admin_account_id
    try:
        resp = await client.call("EnableIpamOrganizationAdminAccount", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable ipam organization admin account") from exc
    return EnableIpamOrganizationAdminAccountResult(
        success=resp.get("Success"),
    )


async def enable_reachability_analyzer_organization_sharing(
    region_name: str | None = None,
) -> EnableReachabilityAnalyzerOrganizationSharingResult:
    """Enable reachability analyzer organization sharing.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("EnableReachabilityAnalyzerOrganizationSharing", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to enable reachability analyzer organization sharing"
        ) from exc
    return EnableReachabilityAnalyzerOrganizationSharingResult(
        return_value=resp.get("ReturnValue"),
    )


async def enable_route_server_propagation(
    route_server_id: str,
    route_table_id: str,
    region_name: str | None = None,
) -> EnableRouteServerPropagationResult:
    """Enable route server propagation.

    Args:
        route_server_id: Route server id.
        route_table_id: Route table id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerId"] = route_server_id
    kwargs["RouteTableId"] = route_table_id
    try:
        resp = await client.call("EnableRouteServerPropagation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable route server propagation") from exc
    return EnableRouteServerPropagationResult(
        route_server_propagation=resp.get("RouteServerPropagation"),
    )


async def enable_serial_console_access(
    region_name: str | None = None,
) -> EnableSerialConsoleAccessResult:
    """Enable serial console access.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("EnableSerialConsoleAccess", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable serial console access") from exc
    return EnableSerialConsoleAccessResult(
        serial_console_access_enabled=resp.get("SerialConsoleAccessEnabled"),
    )


async def enable_snapshot_block_public_access(
    state: str,
    region_name: str | None = None,
) -> EnableSnapshotBlockPublicAccessResult:
    """Enable snapshot block public access.

    Args:
        state: State.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["State"] = state
    try:
        resp = await client.call("EnableSnapshotBlockPublicAccess", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable snapshot block public access") from exc
    return EnableSnapshotBlockPublicAccessResult(
        state=resp.get("State"),
    )


async def enable_transit_gateway_route_table_propagation(
    transit_gateway_route_table_id: str,
    *,
    transit_gateway_attachment_id: str | None = None,
    transit_gateway_route_table_announcement_id: str | None = None,
    region_name: str | None = None,
) -> EnableTransitGatewayRouteTablePropagationResult:
    """Enable transit gateway route table propagation.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        transit_gateway_route_table_announcement_id: Transit gateway route table announcement id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    if transit_gateway_attachment_id is not None:
        kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    if transit_gateway_route_table_announcement_id is not None:
        kwargs["TransitGatewayRouteTableAnnouncementId"] = (
            transit_gateway_route_table_announcement_id
        )
    try:
        resp = await client.call("EnableTransitGatewayRouteTablePropagation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to enable transit gateway route table propagation"
        ) from exc
    return EnableTransitGatewayRouteTablePropagationResult(
        propagation=resp.get("Propagation"),
    )


async def enable_vgw_route_propagation(
    gateway_id: str,
    route_table_id: str,
    region_name: str | None = None,
) -> None:
    """Enable vgw route propagation.

    Args:
        gateway_id: Gateway id.
        route_table_id: Route table id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["GatewayId"] = gateway_id
    kwargs["RouteTableId"] = route_table_id
    try:
        await client.call("EnableVgwRoutePropagation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable vgw route propagation") from exc
    return None


async def enable_volume_io(
    volume_id: str,
    region_name: str | None = None,
) -> None:
    """Enable volume io.

    Args:
        volume_id: Volume id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VolumeId"] = volume_id
    try:
        await client.call("EnableVolumeIO", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable volume io") from exc
    return None


async def enable_vpc_classic_link(
    vpc_id: str,
    region_name: str | None = None,
) -> EnableVpcClassicLinkResult:
    """Enable vpc classic link.

    Args:
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    try:
        resp = await client.call("EnableVpcClassicLink", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable vpc classic link") from exc
    return EnableVpcClassicLinkResult(
        return_value=resp.get("Return"),
    )


async def enable_vpc_classic_link_dns_support(
    *,
    vpc_id: str | None = None,
    region_name: str | None = None,
) -> EnableVpcClassicLinkDnsSupportResult:
    """Enable vpc classic link dns support.

    Args:
        vpc_id: Vpc id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if vpc_id is not None:
        kwargs["VpcId"] = vpc_id
    try:
        resp = await client.call("EnableVpcClassicLinkDnsSupport", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to enable vpc classic link dns support") from exc
    return EnableVpcClassicLinkDnsSupportResult(
        return_value=resp.get("Return"),
    )


async def export_client_vpn_client_certificate_revocation_list(
    client_vpn_endpoint_id: str,
    region_name: str | None = None,
) -> ExportClientVpnClientCertificateRevocationListResult:
    """Export client vpn client certificate revocation list.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    try:
        resp = await client.call("ExportClientVpnClientCertificateRevocationList", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to export client vpn client certificate revocation list"
        ) from exc
    return ExportClientVpnClientCertificateRevocationListResult(
        certificate_revocation_list=resp.get("CertificateRevocationList"),
        status=resp.get("Status"),
    )


async def export_client_vpn_client_configuration(
    client_vpn_endpoint_id: str,
    region_name: str | None = None,
) -> ExportClientVpnClientConfigurationResult:
    """Export client vpn client configuration.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    try:
        resp = await client.call("ExportClientVpnClientConfiguration", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to export client vpn client configuration") from exc
    return ExportClientVpnClientConfigurationResult(
        client_configuration=resp.get("ClientConfiguration"),
    )


async def export_image(
    disk_image_format: str,
    image_id: str,
    s3_export_location: dict[str, Any],
    *,
    client_token: str | None = None,
    description: str | None = None,
    role_name: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> ExportImageResult:
    """Export image.

    Args:
        disk_image_format: Disk image format.
        image_id: Image id.
        s3_export_location: S3 export location.
        client_token: Client token.
        description: Description.
        role_name: Role name.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["DiskImageFormat"] = disk_image_format
    kwargs["ImageId"] = image_id
    kwargs["S3ExportLocation"] = s3_export_location
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if description is not None:
        kwargs["Description"] = description
    if role_name is not None:
        kwargs["RoleName"] = role_name
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("ExportImage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to export image") from exc
    return ExportImageResult(
        description=resp.get("Description"),
        disk_image_format=resp.get("DiskImageFormat"),
        export_image_task_id=resp.get("ExportImageTaskId"),
        image_id=resp.get("ImageId"),
        role_name=resp.get("RoleName"),
        progress=resp.get("Progress"),
        s3_export_location=resp.get("S3ExportLocation"),
        status=resp.get("Status"),
        status_message=resp.get("StatusMessage"),
        tags=resp.get("Tags"),
    )


async def export_transit_gateway_routes(
    transit_gateway_route_table_id: str,
    s3_bucket: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> ExportTransitGatewayRoutesResult:
    """Export transit gateway routes.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        s3_bucket: S3 bucket.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    kwargs["S3Bucket"] = s3_bucket
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("ExportTransitGatewayRoutes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to export transit gateway routes") from exc
    return ExportTransitGatewayRoutesResult(
        s3_location=resp.get("S3Location"),
    )


async def export_verified_access_instance_client_configuration(
    verified_access_instance_id: str,
    region_name: str | None = None,
) -> ExportVerifiedAccessInstanceClientConfigurationResult:
    """Export verified access instance client configuration.

    Args:
        verified_access_instance_id: Verified access instance id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessInstanceId"] = verified_access_instance_id
    try:
        resp = await client.call("ExportVerifiedAccessInstanceClientConfiguration", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to export verified access instance client configuration"
        ) from exc
    return ExportVerifiedAccessInstanceClientConfigurationResult(
        version=resp.get("Version"),
        verified_access_instance_id=resp.get("VerifiedAccessInstanceId"),
        region=resp.get("Region"),
        device_trust_providers=resp.get("DeviceTrustProviders"),
        user_trust_provider=resp.get("UserTrustProvider"),
        open_vpn_configurations=resp.get("OpenVpnConfigurations"),
    )


async def get_active_vpn_tunnel_status(
    vpn_connection_id: str,
    vpn_tunnel_outside_ip_address: str,
    region_name: str | None = None,
) -> GetActiveVpnTunnelStatusResult:
    """Get active vpn tunnel status.

    Args:
        vpn_connection_id: Vpn connection id.
        vpn_tunnel_outside_ip_address: Vpn tunnel outside ip address.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpnConnectionId"] = vpn_connection_id
    kwargs["VpnTunnelOutsideIpAddress"] = vpn_tunnel_outside_ip_address
    try:
        resp = await client.call("GetActiveVpnTunnelStatus", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get active vpn tunnel status") from exc
    return GetActiveVpnTunnelStatusResult(
        active_vpn_tunnel_status=resp.get("ActiveVpnTunnelStatus"),
    )


async def get_allowed_images_settings(
    region_name: str | None = None,
) -> GetAllowedImagesSettingsResult:
    """Get allowed images settings.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("GetAllowedImagesSettings", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get allowed images settings") from exc
    return GetAllowedImagesSettingsResult(
        state=resp.get("State"),
        image_criteria=resp.get("ImageCriteria"),
        managed_by=resp.get("ManagedBy"),
    )


async def get_associated_enclave_certificate_iam_roles(
    certificate_arn: str,
    region_name: str | None = None,
) -> GetAssociatedEnclaveCertificateIamRolesResult:
    """Get associated enclave certificate iam roles.

    Args:
        certificate_arn: Certificate arn.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CertificateArn"] = certificate_arn
    try:
        resp = await client.call("GetAssociatedEnclaveCertificateIamRoles", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get associated enclave certificate iam roles") from exc
    return GetAssociatedEnclaveCertificateIamRolesResult(
        associated_roles=resp.get("AssociatedRoles"),
    )


async def get_associated_ipv6_pool_cidrs(
    pool_id: str,
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> GetAssociatedIpv6PoolCidrsResult:
    """Get associated ipv6 pool cidrs.

    Args:
        pool_id: Pool id.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PoolId"] = pool_id
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("GetAssociatedIpv6PoolCidrs", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get associated ipv6 pool cidrs") from exc
    return GetAssociatedIpv6PoolCidrsResult(
        ipv6_cidr_associations=resp.get("Ipv6CidrAssociations"),
        next_token=resp.get("NextToken"),
    )


async def get_aws_network_performance_data(
    *,
    data_queries: list[dict[str, Any]] | None = None,
    start_time: str | None = None,
    end_time: str | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetAwsNetworkPerformanceDataResult:
    """Get aws network performance data.

    Args:
        data_queries: Data queries.
        start_time: Start time.
        end_time: End time.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if data_queries is not None:
        kwargs["DataQueries"] = data_queries
    if start_time is not None:
        kwargs["StartTime"] = start_time
    if end_time is not None:
        kwargs["EndTime"] = end_time
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetAwsNetworkPerformanceData", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get aws network performance data") from exc
    return GetAwsNetworkPerformanceDataResult(
        data_responses=resp.get("DataResponses"),
        next_token=resp.get("NextToken"),
    )


async def get_capacity_manager_attributes(
    region_name: str | None = None,
) -> GetCapacityManagerAttributesResult:
    """Get capacity manager attributes.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("GetCapacityManagerAttributes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get capacity manager attributes") from exc
    return GetCapacityManagerAttributesResult(
        capacity_manager_status=resp.get("CapacityManagerStatus"),
        organizations_access=resp.get("OrganizationsAccess"),
        data_export_count=resp.get("DataExportCount"),
        ingestion_status=resp.get("IngestionStatus"),
        ingestion_status_message=resp.get("IngestionStatusMessage"),
        earliest_datapoint_timestamp=resp.get("EarliestDatapointTimestamp"),
        latest_datapoint_timestamp=resp.get("LatestDatapointTimestamp"),
    )


async def get_capacity_manager_metric_data(
    metric_names: list[str],
    start_time: str,
    end_time: str,
    period: int,
    *,
    group_by: list[str] | None = None,
    filter_by: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetCapacityManagerMetricDataResult:
    """Get capacity manager metric data.

    Args:
        metric_names: Metric names.
        start_time: Start time.
        end_time: End time.
        period: Period.
        group_by: Group by.
        filter_by: Filter by.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["MetricNames"] = metric_names
    kwargs["StartTime"] = start_time
    kwargs["EndTime"] = end_time
    kwargs["Period"] = period
    if group_by is not None:
        kwargs["GroupBy"] = group_by
    if filter_by is not None:
        kwargs["FilterBy"] = filter_by
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetCapacityManagerMetricData", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get capacity manager metric data") from exc
    return GetCapacityManagerMetricDataResult(
        metric_data_results=resp.get("MetricDataResults"),
        next_token=resp.get("NextToken"),
    )


async def get_capacity_manager_metric_dimensions(
    group_by: list[str],
    start_time: str,
    end_time: str,
    metric_names: list[str],
    *,
    filter_by: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetCapacityManagerMetricDimensionsResult:
    """Get capacity manager metric dimensions.

    Args:
        group_by: Group by.
        start_time: Start time.
        end_time: End time.
        metric_names: Metric names.
        filter_by: Filter by.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["GroupBy"] = group_by
    kwargs["StartTime"] = start_time
    kwargs["EndTime"] = end_time
    kwargs["MetricNames"] = metric_names
    if filter_by is not None:
        kwargs["FilterBy"] = filter_by
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetCapacityManagerMetricDimensions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get capacity manager metric dimensions") from exc
    return GetCapacityManagerMetricDimensionsResult(
        metric_dimension_results=resp.get("MetricDimensionResults"),
        next_token=resp.get("NextToken"),
    )


async def get_capacity_reservation_usage(
    capacity_reservation_id: str,
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> GetCapacityReservationUsageResult:
    """Get capacity reservation usage.

    Args:
        capacity_reservation_id: Capacity reservation id.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityReservationId"] = capacity_reservation_id
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("GetCapacityReservationUsage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get capacity reservation usage") from exc
    return GetCapacityReservationUsageResult(
        next_token=resp.get("NextToken"),
        capacity_reservation_id=resp.get("CapacityReservationId"),
        instance_type=resp.get("InstanceType"),
        total_instance_count=resp.get("TotalInstanceCount"),
        available_instance_count=resp.get("AvailableInstanceCount"),
        state=resp.get("State"),
        instance_usages=resp.get("InstanceUsages"),
    )


async def get_coip_pool_usage(
    pool_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetCoipPoolUsageResult:
    """Get coip pool usage.

    Args:
        pool_id: Pool id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PoolId"] = pool_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetCoipPoolUsage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get coip pool usage") from exc
    return GetCoipPoolUsageResult(
        coip_pool_id=resp.get("CoipPoolId"),
        coip_address_usages=resp.get("CoipAddressUsages"),
        local_gateway_route_table_id=resp.get("LocalGatewayRouteTableId"),
        next_token=resp.get("NextToken"),
    )


async def get_console_output(
    instance_id: str,
    *,
    latest: bool | None = None,
    region_name: str | None = None,
) -> GetConsoleOutputResult:
    """Get console output.

    Args:
        instance_id: Instance id.
        latest: Latest.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    if latest is not None:
        kwargs["Latest"] = latest
    try:
        resp = await client.call("GetConsoleOutput", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get console output") from exc
    return GetConsoleOutputResult(
        instance_id=resp.get("InstanceId"),
        timestamp=resp.get("Timestamp"),
        output=resp.get("Output"),
    )


async def get_console_screenshot(
    instance_id: str,
    *,
    wake_up: bool | None = None,
    region_name: str | None = None,
) -> GetConsoleScreenshotResult:
    """Get console screenshot.

    Args:
        instance_id: Instance id.
        wake_up: Wake up.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    if wake_up is not None:
        kwargs["WakeUp"] = wake_up
    try:
        resp = await client.call("GetConsoleScreenshot", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get console screenshot") from exc
    return GetConsoleScreenshotResult(
        image_data=resp.get("ImageData"),
        instance_id=resp.get("InstanceId"),
    )


async def get_declarative_policies_report_summary(
    report_id: str,
    region_name: str | None = None,
) -> GetDeclarativePoliciesReportSummaryResult:
    """Get declarative policies report summary.

    Args:
        report_id: Report id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ReportId"] = report_id
    try:
        resp = await client.call("GetDeclarativePoliciesReportSummary", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get declarative policies report summary") from exc
    return GetDeclarativePoliciesReportSummaryResult(
        report_id=resp.get("ReportId"),
        s3_bucket=resp.get("S3Bucket"),
        s3_prefix=resp.get("S3Prefix"),
        target_id=resp.get("TargetId"),
        start_time=resp.get("StartTime"),
        end_time=resp.get("EndTime"),
        number_of_accounts=resp.get("NumberOfAccounts"),
        number_of_failed_accounts=resp.get("NumberOfFailedAccounts"),
        attribute_summaries=resp.get("AttributeSummaries"),
    )


async def get_default_credit_specification(
    instance_family: str,
    region_name: str | None = None,
) -> GetDefaultCreditSpecificationResult:
    """Get default credit specification.

    Args:
        instance_family: Instance family.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceFamily"] = instance_family
    try:
        resp = await client.call("GetDefaultCreditSpecification", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get default credit specification") from exc
    return GetDefaultCreditSpecificationResult(
        instance_family_credit_specification=resp.get("InstanceFamilyCreditSpecification"),
    )


async def get_ebs_default_kms_key_id(
    region_name: str | None = None,
) -> GetEbsDefaultKmsKeyIdResult:
    """Get ebs default kms key id.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("GetEbsDefaultKmsKeyId", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get ebs default kms key id") from exc
    return GetEbsDefaultKmsKeyIdResult(
        kms_key_id=resp.get("KmsKeyId"),
    )


async def get_ebs_encryption_by_default(
    region_name: str | None = None,
) -> GetEbsEncryptionByDefaultResult:
    """Get ebs encryption by default.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("GetEbsEncryptionByDefault", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get ebs encryption by default") from exc
    return GetEbsEncryptionByDefaultResult(
        ebs_encryption_by_default=resp.get("EbsEncryptionByDefault"),
        sse_type=resp.get("SseType"),
    )


async def get_flow_logs_integration_template(
    flow_log_id: str,
    config_delivery_s3_destination_arn: str,
    integrate_services: dict[str, Any],
    region_name: str | None = None,
) -> GetFlowLogsIntegrationTemplateResult:
    """Get flow logs integration template.

    Args:
        flow_log_id: Flow log id.
        config_delivery_s3_destination_arn: Config delivery s3 destination arn.
        integrate_services: Integrate services.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["FlowLogId"] = flow_log_id
    kwargs["ConfigDeliveryS3DestinationArn"] = config_delivery_s3_destination_arn
    kwargs["IntegrateServices"] = integrate_services
    try:
        resp = await client.call("GetFlowLogsIntegrationTemplate", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get flow logs integration template") from exc
    return GetFlowLogsIntegrationTemplateResult(
        result=resp.get("Result"),
    )


async def get_groups_for_capacity_reservation(
    capacity_reservation_id: str,
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> GetGroupsForCapacityReservationResult:
    """Get groups for capacity reservation.

    Args:
        capacity_reservation_id: Capacity reservation id.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityReservationId"] = capacity_reservation_id
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("GetGroupsForCapacityReservation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get groups for capacity reservation") from exc
    return GetGroupsForCapacityReservationResult(
        next_token=resp.get("NextToken"),
        capacity_reservation_groups=resp.get("CapacityReservationGroups"),
    )


async def get_host_reservation_purchase_preview(
    host_id_set: list[str],
    offering_id: str,
    region_name: str | None = None,
) -> GetHostReservationPurchasePreviewResult:
    """Get host reservation purchase preview.

    Args:
        host_id_set: Host id set.
        offering_id: Offering id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["HostIdSet"] = host_id_set
    kwargs["OfferingId"] = offering_id
    try:
        resp = await client.call("GetHostReservationPurchasePreview", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get host reservation purchase preview") from exc
    return GetHostReservationPurchasePreviewResult(
        currency_code=resp.get("CurrencyCode"),
        purchase=resp.get("Purchase"),
        total_hourly_price=resp.get("TotalHourlyPrice"),
        total_upfront_price=resp.get("TotalUpfrontPrice"),
    )


async def get_image_ancestry(
    image_id: str,
    region_name: str | None = None,
) -> GetImageAncestryResult:
    """Get image ancestry.

    Args:
        image_id: Image id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    try:
        resp = await client.call("GetImageAncestry", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get image ancestry") from exc
    return GetImageAncestryResult(
        image_ancestry_entries=resp.get("ImageAncestryEntries"),
    )


async def get_image_block_public_access_state(
    region_name: str | None = None,
) -> GetImageBlockPublicAccessStateResult:
    """Get image block public access state.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("GetImageBlockPublicAccessState", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get image block public access state") from exc
    return GetImageBlockPublicAccessStateResult(
        image_block_public_access_state=resp.get("ImageBlockPublicAccessState"),
        managed_by=resp.get("ManagedBy"),
    )


async def get_instance_metadata_defaults(
    region_name: str | None = None,
) -> GetInstanceMetadataDefaultsResult:
    """Get instance metadata defaults.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("GetInstanceMetadataDefaults", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get instance metadata defaults") from exc
    return GetInstanceMetadataDefaultsResult(
        account_level=resp.get("AccountLevel"),
    )


async def get_instance_tpm_ek_pub(
    instance_id: str,
    key_type: str,
    key_format: str,
    region_name: str | None = None,
) -> GetInstanceTpmEkPubResult:
    """Get instance tpm ek pub.

    Args:
        instance_id: Instance id.
        key_type: Key type.
        key_format: Key format.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["KeyType"] = key_type
    kwargs["KeyFormat"] = key_format
    try:
        resp = await client.call("GetInstanceTpmEkPub", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get instance tpm ek pub") from exc
    return GetInstanceTpmEkPubResult(
        instance_id=resp.get("InstanceId"),
        key_type=resp.get("KeyType"),
        key_format=resp.get("KeyFormat"),
        key_value=resp.get("KeyValue"),
    )


async def get_instance_types_from_instance_requirements(
    architecture_types: list[str],
    virtualization_types: list[str],
    instance_requirements: dict[str, Any],
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    context: str | None = None,
    region_name: str | None = None,
) -> GetInstanceTypesFromInstanceRequirementsResult:
    """Get instance types from instance requirements.

    Args:
        architecture_types: Architecture types.
        virtualization_types: Virtualization types.
        instance_requirements: Instance requirements.
        max_results: Max results.
        next_token: Next token.
        context: Context.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ArchitectureTypes"] = architecture_types
    kwargs["VirtualizationTypes"] = virtualization_types
    kwargs["InstanceRequirements"] = instance_requirements
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if context is not None:
        kwargs["Context"] = context
    try:
        resp = await client.call("GetInstanceTypesFromInstanceRequirements", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to get instance types from instance requirements"
        ) from exc
    return GetInstanceTypesFromInstanceRequirementsResult(
        instance_types=resp.get("InstanceTypes"),
        next_token=resp.get("NextToken"),
    )


async def get_instance_uefi_data(
    instance_id: str,
    region_name: str | None = None,
) -> GetInstanceUefiDataResult:
    """Get instance uefi data.

    Args:
        instance_id: Instance id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    try:
        resp = await client.call("GetInstanceUefiData", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get instance uefi data") from exc
    return GetInstanceUefiDataResult(
        instance_id=resp.get("InstanceId"),
        uefi_data=resp.get("UefiData"),
    )


async def get_ipam_address_history(
    cidr: str,
    ipam_scope_id: str,
    *,
    vpc_id: str | None = None,
    start_time: str | None = None,
    end_time: str | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetIpamAddressHistoryResult:
    """Get ipam address history.

    Args:
        cidr: Cidr.
        ipam_scope_id: Ipam scope id.
        vpc_id: Vpc id.
        start_time: Start time.
        end_time: End time.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Cidr"] = cidr
    kwargs["IpamScopeId"] = ipam_scope_id
    if vpc_id is not None:
        kwargs["VpcId"] = vpc_id
    if start_time is not None:
        kwargs["StartTime"] = start_time
    if end_time is not None:
        kwargs["EndTime"] = end_time
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetIpamAddressHistory", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get ipam address history") from exc
    return GetIpamAddressHistoryResult(
        history_records=resp.get("HistoryRecords"),
        next_token=resp.get("NextToken"),
    )


async def get_ipam_discovered_accounts(
    ipam_resource_discovery_id: str,
    discovery_region: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> GetIpamDiscoveredAccountsResult:
    """Get ipam discovered accounts.

    Args:
        ipam_resource_discovery_id: Ipam resource discovery id.
        discovery_region: Discovery region.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamResourceDiscoveryId"] = ipam_resource_discovery_id
    kwargs["DiscoveryRegion"] = discovery_region
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("GetIpamDiscoveredAccounts", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get ipam discovered accounts") from exc
    return GetIpamDiscoveredAccountsResult(
        ipam_discovered_accounts=resp.get("IpamDiscoveredAccounts"),
        next_token=resp.get("NextToken"),
    )


async def get_ipam_discovered_public_addresses(
    ipam_resource_discovery_id: str,
    address_region: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> GetIpamDiscoveredPublicAddressesResult:
    """Get ipam discovered public addresses.

    Args:
        ipam_resource_discovery_id: Ipam resource discovery id.
        address_region: Address region.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamResourceDiscoveryId"] = ipam_resource_discovery_id
    kwargs["AddressRegion"] = address_region
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("GetIpamDiscoveredPublicAddresses", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get ipam discovered public addresses") from exc
    return GetIpamDiscoveredPublicAddressesResult(
        ipam_discovered_public_addresses=resp.get("IpamDiscoveredPublicAddresses"),
        oldest_sample_time=resp.get("OldestSampleTime"),
        next_token=resp.get("NextToken"),
    )


async def get_ipam_discovered_resource_cidrs(
    ipam_resource_discovery_id: str,
    resource_region: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> GetIpamDiscoveredResourceCidrsResult:
    """Get ipam discovered resource cidrs.

    Args:
        ipam_resource_discovery_id: Ipam resource discovery id.
        resource_region: Resource region.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamResourceDiscoveryId"] = ipam_resource_discovery_id
    kwargs["ResourceRegion"] = resource_region
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("GetIpamDiscoveredResourceCidrs", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get ipam discovered resource cidrs") from exc
    return GetIpamDiscoveredResourceCidrsResult(
        ipam_discovered_resource_cidrs=resp.get("IpamDiscoveredResourceCidrs"),
        next_token=resp.get("NextToken"),
    )


async def get_ipam_pool_allocations(
    ipam_pool_id: str,
    *,
    ipam_pool_allocation_id: str | None = None,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetIpamPoolAllocationsResult:
    """Get ipam pool allocations.

    Args:
        ipam_pool_id: Ipam pool id.
        ipam_pool_allocation_id: Ipam pool allocation id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPoolId"] = ipam_pool_id
    if ipam_pool_allocation_id is not None:
        kwargs["IpamPoolAllocationId"] = ipam_pool_allocation_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetIpamPoolAllocations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get ipam pool allocations") from exc
    return GetIpamPoolAllocationsResult(
        ipam_pool_allocations=resp.get("IpamPoolAllocations"),
        next_token=resp.get("NextToken"),
    )


async def get_ipam_pool_cidrs(
    ipam_pool_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetIpamPoolCidrsResult:
    """Get ipam pool cidrs.

    Args:
        ipam_pool_id: Ipam pool id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPoolId"] = ipam_pool_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetIpamPoolCidrs", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get ipam pool cidrs") from exc
    return GetIpamPoolCidrsResult(
        ipam_pool_cidrs=resp.get("IpamPoolCidrs"),
        next_token=resp.get("NextToken"),
    )


async def get_ipam_prefix_list_resolver_rules(
    ipam_prefix_list_resolver_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetIpamPrefixListResolverRulesResult:
    """Get ipam prefix list resolver rules.

    Args:
        ipam_prefix_list_resolver_id: Ipam prefix list resolver id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPrefixListResolverId"] = ipam_prefix_list_resolver_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetIpamPrefixListResolverRules", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get ipam prefix list resolver rules") from exc
    return GetIpamPrefixListResolverRulesResult(
        rules=resp.get("Rules"),
        next_token=resp.get("NextToken"),
    )


async def get_ipam_prefix_list_resolver_version_entries(
    ipam_prefix_list_resolver_id: str,
    ipam_prefix_list_resolver_version: int,
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetIpamPrefixListResolverVersionEntriesResult:
    """Get ipam prefix list resolver version entries.

    Args:
        ipam_prefix_list_resolver_id: Ipam prefix list resolver id.
        ipam_prefix_list_resolver_version: Ipam prefix list resolver version.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPrefixListResolverId"] = ipam_prefix_list_resolver_id
    kwargs["IpamPrefixListResolverVersion"] = ipam_prefix_list_resolver_version
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetIpamPrefixListResolverVersionEntries", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to get ipam prefix list resolver version entries"
        ) from exc
    return GetIpamPrefixListResolverVersionEntriesResult(
        entries=resp.get("Entries"),
        next_token=resp.get("NextToken"),
    )


async def get_ipam_prefix_list_resolver_versions(
    ipam_prefix_list_resolver_id: str,
    *,
    ipam_prefix_list_resolver_versions: list[int] | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetIpamPrefixListResolverVersionsResult:
    """Get ipam prefix list resolver versions.

    Args:
        ipam_prefix_list_resolver_id: Ipam prefix list resolver id.
        ipam_prefix_list_resolver_versions: Ipam prefix list resolver versions.
        max_results: Max results.
        filters: Filters.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPrefixListResolverId"] = ipam_prefix_list_resolver_id
    if ipam_prefix_list_resolver_versions is not None:
        kwargs["IpamPrefixListResolverVersions"] = ipam_prefix_list_resolver_versions
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetIpamPrefixListResolverVersions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get ipam prefix list resolver versions") from exc
    return GetIpamPrefixListResolverVersionsResult(
        ipam_prefix_list_resolver_versions=resp.get("IpamPrefixListResolverVersions"),
        next_token=resp.get("NextToken"),
    )


async def get_ipam_resource_cidrs(
    ipam_scope_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    ipam_pool_id: str | None = None,
    resource_id: str | None = None,
    resource_type: str | None = None,
    resource_tag: dict[str, Any] | None = None,
    resource_owner: str | None = None,
    region_name: str | None = None,
) -> GetIpamResourceCidrsResult:
    """Get ipam resource cidrs.

    Args:
        ipam_scope_id: Ipam scope id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        ipam_pool_id: Ipam pool id.
        resource_id: Resource id.
        resource_type: Resource type.
        resource_tag: Resource tag.
        resource_owner: Resource owner.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamScopeId"] = ipam_scope_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if ipam_pool_id is not None:
        kwargs["IpamPoolId"] = ipam_pool_id
    if resource_id is not None:
        kwargs["ResourceId"] = resource_id
    if resource_type is not None:
        kwargs["ResourceType"] = resource_type
    if resource_tag is not None:
        kwargs["ResourceTag"] = resource_tag
    if resource_owner is not None:
        kwargs["ResourceOwner"] = resource_owner
    try:
        resp = await client.call("GetIpamResourceCidrs", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get ipam resource cidrs") from exc
    return GetIpamResourceCidrsResult(
        next_token=resp.get("NextToken"),
        ipam_resource_cidrs=resp.get("IpamResourceCidrs"),
    )


async def get_launch_template_data(
    instance_id: str,
    region_name: str | None = None,
) -> GetLaunchTemplateDataResult:
    """Get launch template data.

    Args:
        instance_id: Instance id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    try:
        resp = await client.call("GetLaunchTemplateData", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get launch template data") from exc
    return GetLaunchTemplateDataResult(
        launch_template_data=resp.get("LaunchTemplateData"),
    )


async def get_managed_prefix_list_associations(
    prefix_list_id: str,
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetManagedPrefixListAssociationsResult:
    """Get managed prefix list associations.

    Args:
        prefix_list_id: Prefix list id.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PrefixListId"] = prefix_list_id
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetManagedPrefixListAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get managed prefix list associations") from exc
    return GetManagedPrefixListAssociationsResult(
        prefix_list_associations=resp.get("PrefixListAssociations"),
        next_token=resp.get("NextToken"),
    )


async def get_managed_prefix_list_entries(
    prefix_list_id: str,
    *,
    target_version: int | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetManagedPrefixListEntriesResult:
    """Get managed prefix list entries.

    Args:
        prefix_list_id: Prefix list id.
        target_version: Target version.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PrefixListId"] = prefix_list_id
    if target_version is not None:
        kwargs["TargetVersion"] = target_version
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetManagedPrefixListEntries", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get managed prefix list entries") from exc
    return GetManagedPrefixListEntriesResult(
        entries=resp.get("Entries"),
        next_token=resp.get("NextToken"),
    )


async def get_network_insights_access_scope_analysis_findings(
    network_insights_access_scope_analysis_id: str,
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetNetworkInsightsAccessScopeAnalysisFindingsResult:
    """Get network insights access scope analysis findings.

    Args:
        network_insights_access_scope_analysis_id: Network insights access scope analysis id.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInsightsAccessScopeAnalysisId"] = network_insights_access_scope_analysis_id
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetNetworkInsightsAccessScopeAnalysisFindings", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to get network insights access scope analysis findings"
        ) from exc
    return GetNetworkInsightsAccessScopeAnalysisFindingsResult(
        network_insights_access_scope_analysis_id=resp.get("NetworkInsightsAccessScopeAnalysisId"),
        analysis_status=resp.get("AnalysisStatus"),
        analysis_findings=resp.get("AnalysisFindings"),
        next_token=resp.get("NextToken"),
    )


async def get_network_insights_access_scope_content(
    network_insights_access_scope_id: str,
    region_name: str | None = None,
) -> GetNetworkInsightsAccessScopeContentResult:
    """Get network insights access scope content.

    Args:
        network_insights_access_scope_id: Network insights access scope id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInsightsAccessScopeId"] = network_insights_access_scope_id
    try:
        resp = await client.call("GetNetworkInsightsAccessScopeContent", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get network insights access scope content") from exc
    return GetNetworkInsightsAccessScopeContentResult(
        network_insights_access_scope_content=resp.get("NetworkInsightsAccessScopeContent"),
    )


async def get_password_data(
    instance_id: str,
    region_name: str | None = None,
) -> GetPasswordDataResult:
    """Get password data.

    Args:
        instance_id: Instance id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    try:
        resp = await client.call("GetPasswordData", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get password data") from exc
    return GetPasswordDataResult(
        instance_id=resp.get("InstanceId"),
        timestamp=resp.get("Timestamp"),
        password_data=resp.get("PasswordData"),
    )


async def get_reserved_instances_exchange_quote(
    reserved_instance_ids: list[str],
    *,
    target_configurations: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> GetReservedInstancesExchangeQuoteResult:
    """Get reserved instances exchange quote.

    Args:
        reserved_instance_ids: Reserved instance ids.
        target_configurations: Target configurations.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ReservedInstanceIds"] = reserved_instance_ids
    if target_configurations is not None:
        kwargs["TargetConfigurations"] = target_configurations
    try:
        resp = await client.call("GetReservedInstancesExchangeQuote", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get reserved instances exchange quote") from exc
    return GetReservedInstancesExchangeQuoteResult(
        currency_code=resp.get("CurrencyCode"),
        is_valid_exchange=resp.get("IsValidExchange"),
        output_reserved_instances_will_expire_at=resp.get("OutputReservedInstancesWillExpireAt"),
        payment_due=resp.get("PaymentDue"),
        reserved_instance_value_rollup=resp.get("ReservedInstanceValueRollup"),
        reserved_instance_value_set=resp.get("ReservedInstanceValueSet"),
        target_configuration_value_rollup=resp.get("TargetConfigurationValueRollup"),
        target_configuration_value_set=resp.get("TargetConfigurationValueSet"),
        validation_failure_reason=resp.get("ValidationFailureReason"),
    )


async def get_route_server_associations(
    route_server_id: str,
    region_name: str | None = None,
) -> GetRouteServerAssociationsResult:
    """Get route server associations.

    Args:
        route_server_id: Route server id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerId"] = route_server_id
    try:
        resp = await client.call("GetRouteServerAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get route server associations") from exc
    return GetRouteServerAssociationsResult(
        route_server_associations=resp.get("RouteServerAssociations"),
    )


async def get_route_server_propagations(
    route_server_id: str,
    *,
    route_table_id: str | None = None,
    region_name: str | None = None,
) -> GetRouteServerPropagationsResult:
    """Get route server propagations.

    Args:
        route_server_id: Route server id.
        route_table_id: Route table id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerId"] = route_server_id
    if route_table_id is not None:
        kwargs["RouteTableId"] = route_table_id
    try:
        resp = await client.call("GetRouteServerPropagations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get route server propagations") from exc
    return GetRouteServerPropagationsResult(
        route_server_propagations=resp.get("RouteServerPropagations"),
    )


async def get_route_server_routing_database(
    route_server_id: str,
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> GetRouteServerRoutingDatabaseResult:
    """Get route server routing database.

    Args:
        route_server_id: Route server id.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerId"] = route_server_id
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("GetRouteServerRoutingDatabase", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get route server routing database") from exc
    return GetRouteServerRoutingDatabaseResult(
        are_routes_persisted=resp.get("AreRoutesPersisted"),
        routes=resp.get("Routes"),
        next_token=resp.get("NextToken"),
    )


async def get_security_groups_for_vpc(
    vpc_id: str,
    *,
    next_token: str | None = None,
    max_results: int | None = None,
    filters: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> GetSecurityGroupsForVpcResult:
    """Get security groups for vpc.

    Args:
        vpc_id: Vpc id.
        next_token: Next token.
        max_results: Max results.
        filters: Filters.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if filters is not None:
        kwargs["Filters"] = filters
    try:
        resp = await client.call("GetSecurityGroupsForVpc", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get security groups for vpc") from exc
    return GetSecurityGroupsForVpcResult(
        next_token=resp.get("NextToken"),
        security_group_for_vpcs=resp.get("SecurityGroupForVpcs"),
    )


async def get_serial_console_access_status(
    region_name: str | None = None,
) -> GetSerialConsoleAccessStatusResult:
    """Get serial console access status.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("GetSerialConsoleAccessStatus", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get serial console access status") from exc
    return GetSerialConsoleAccessStatusResult(
        serial_console_access_enabled=resp.get("SerialConsoleAccessEnabled"),
        managed_by=resp.get("ManagedBy"),
    )


async def get_snapshot_block_public_access_state(
    region_name: str | None = None,
) -> GetSnapshotBlockPublicAccessStateResult:
    """Get snapshot block public access state.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("GetSnapshotBlockPublicAccessState", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get snapshot block public access state") from exc
    return GetSnapshotBlockPublicAccessStateResult(
        state=resp.get("State"),
        managed_by=resp.get("ManagedBy"),
    )


async def get_spot_placement_scores(
    target_capacity: int,
    *,
    instance_types: list[str] | None = None,
    target_capacity_unit_type: str | None = None,
    single_availability_zone: bool | None = None,
    region_names: list[str] | None = None,
    instance_requirements_with_metadata: dict[str, Any] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetSpotPlacementScoresResult:
    """Get spot placement scores.

    Args:
        target_capacity: Target capacity.
        instance_types: Instance types.
        target_capacity_unit_type: Target capacity unit type.
        single_availability_zone: Single availability zone.
        region_names: Region names.
        instance_requirements_with_metadata: Instance requirements with metadata.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TargetCapacity"] = target_capacity
    if instance_types is not None:
        kwargs["InstanceTypes"] = instance_types
    if target_capacity_unit_type is not None:
        kwargs["TargetCapacityUnitType"] = target_capacity_unit_type
    if single_availability_zone is not None:
        kwargs["SingleAvailabilityZone"] = single_availability_zone
    if region_names is not None:
        kwargs["RegionNames"] = region_names
    if instance_requirements_with_metadata is not None:
        kwargs["InstanceRequirementsWithMetadata"] = instance_requirements_with_metadata
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetSpotPlacementScores", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get spot placement scores") from exc
    return GetSpotPlacementScoresResult(
        spot_placement_scores=resp.get("SpotPlacementScores"),
        next_token=resp.get("NextToken"),
    )


async def get_subnet_cidr_reservations(
    subnet_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> GetSubnetCidrReservationsResult:
    """Get subnet cidr reservations.

    Args:
        subnet_id: Subnet id.
        filters: Filters.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SubnetId"] = subnet_id
    if filters is not None:
        kwargs["Filters"] = filters
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("GetSubnetCidrReservations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get subnet cidr reservations") from exc
    return GetSubnetCidrReservationsResult(
        subnet_ipv4_cidr_reservations=resp.get("SubnetIpv4CidrReservations"),
        subnet_ipv6_cidr_reservations=resp.get("SubnetIpv6CidrReservations"),
        next_token=resp.get("NextToken"),
    )


async def get_transit_gateway_attachment_propagations(
    transit_gateway_attachment_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetTransitGatewayAttachmentPropagationsResult:
    """Get transit gateway attachment propagations.

    Args:
        transit_gateway_attachment_id: Transit gateway attachment id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetTransitGatewayAttachmentPropagations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get transit gateway attachment propagations") from exc
    return GetTransitGatewayAttachmentPropagationsResult(
        transit_gateway_attachment_propagations=resp.get("TransitGatewayAttachmentPropagations"),
        next_token=resp.get("NextToken"),
    )


async def get_transit_gateway_multicast_domain_associations(
    transit_gateway_multicast_domain_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetTransitGatewayMulticastDomainAssociationsResult:
    """Get transit gateway multicast domain associations.

    Args:
        transit_gateway_multicast_domain_id: Transit gateway multicast domain id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayMulticastDomainId"] = transit_gateway_multicast_domain_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetTransitGatewayMulticastDomainAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to get transit gateway multicast domain associations"
        ) from exc
    return GetTransitGatewayMulticastDomainAssociationsResult(
        multicast_domain_associations=resp.get("MulticastDomainAssociations"),
        next_token=resp.get("NextToken"),
    )


async def get_transit_gateway_policy_table_associations(
    transit_gateway_policy_table_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetTransitGatewayPolicyTableAssociationsResult:
    """Get transit gateway policy table associations.

    Args:
        transit_gateway_policy_table_id: Transit gateway policy table id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayPolicyTableId"] = transit_gateway_policy_table_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetTransitGatewayPolicyTableAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to get transit gateway policy table associations"
        ) from exc
    return GetTransitGatewayPolicyTableAssociationsResult(
        associations=resp.get("Associations"),
        next_token=resp.get("NextToken"),
    )


async def get_transit_gateway_policy_table_entries(
    transit_gateway_policy_table_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetTransitGatewayPolicyTableEntriesResult:
    """Get transit gateway policy table entries.

    Args:
        transit_gateway_policy_table_id: Transit gateway policy table id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayPolicyTableId"] = transit_gateway_policy_table_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetTransitGatewayPolicyTableEntries", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get transit gateway policy table entries") from exc
    return GetTransitGatewayPolicyTableEntriesResult(
        transit_gateway_policy_table_entries=resp.get("TransitGatewayPolicyTableEntries"),
    )


async def get_transit_gateway_prefix_list_references(
    transit_gateway_route_table_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetTransitGatewayPrefixListReferencesResult:
    """Get transit gateway prefix list references.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetTransitGatewayPrefixListReferences", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get transit gateway prefix list references") from exc
    return GetTransitGatewayPrefixListReferencesResult(
        transit_gateway_prefix_list_references=resp.get("TransitGatewayPrefixListReferences"),
        next_token=resp.get("NextToken"),
    )


async def get_transit_gateway_route_table_associations(
    transit_gateway_route_table_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetTransitGatewayRouteTableAssociationsResult:
    """Get transit gateway route table associations.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetTransitGatewayRouteTableAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get transit gateway route table associations") from exc
    return GetTransitGatewayRouteTableAssociationsResult(
        associations=resp.get("Associations"),
        next_token=resp.get("NextToken"),
    )


async def get_transit_gateway_route_table_propagations(
    transit_gateway_route_table_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetTransitGatewayRouteTablePropagationsResult:
    """Get transit gateway route table propagations.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetTransitGatewayRouteTablePropagations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get transit gateway route table propagations") from exc
    return GetTransitGatewayRouteTablePropagationsResult(
        transit_gateway_route_table_propagations=resp.get("TransitGatewayRouteTablePropagations"),
        next_token=resp.get("NextToken"),
    )


async def get_verified_access_endpoint_policy(
    verified_access_endpoint_id: str,
    region_name: str | None = None,
) -> GetVerifiedAccessEndpointPolicyResult:
    """Get verified access endpoint policy.

    Args:
        verified_access_endpoint_id: Verified access endpoint id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessEndpointId"] = verified_access_endpoint_id
    try:
        resp = await client.call("GetVerifiedAccessEndpointPolicy", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get verified access endpoint policy") from exc
    return GetVerifiedAccessEndpointPolicyResult(
        policy_enabled=resp.get("PolicyEnabled"),
        policy_document=resp.get("PolicyDocument"),
    )


async def get_verified_access_endpoint_targets(
    verified_access_endpoint_id: str,
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetVerifiedAccessEndpointTargetsResult:
    """Get verified access endpoint targets.

    Args:
        verified_access_endpoint_id: Verified access endpoint id.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessEndpointId"] = verified_access_endpoint_id
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetVerifiedAccessEndpointTargets", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get verified access endpoint targets") from exc
    return GetVerifiedAccessEndpointTargetsResult(
        verified_access_endpoint_targets=resp.get("VerifiedAccessEndpointTargets"),
        next_token=resp.get("NextToken"),
    )


async def get_verified_access_group_policy(
    verified_access_group_id: str,
    region_name: str | None = None,
) -> GetVerifiedAccessGroupPolicyResult:
    """Get verified access group policy.

    Args:
        verified_access_group_id: Verified access group id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessGroupId"] = verified_access_group_id
    try:
        resp = await client.call("GetVerifiedAccessGroupPolicy", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get verified access group policy") from exc
    return GetVerifiedAccessGroupPolicyResult(
        policy_enabled=resp.get("PolicyEnabled"),
        policy_document=resp.get("PolicyDocument"),
    )


async def get_vpn_connection_device_sample_configuration(
    vpn_connection_id: str,
    vpn_connection_device_type_id: str,
    *,
    internet_key_exchange_version: str | None = None,
    sample_type: str | None = None,
    region_name: str | None = None,
) -> GetVpnConnectionDeviceSampleConfigurationResult:
    """Get vpn connection device sample configuration.

    Args:
        vpn_connection_id: Vpn connection id.
        vpn_connection_device_type_id: Vpn connection device type id.
        internet_key_exchange_version: Internet key exchange version.
        sample_type: Sample type.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpnConnectionId"] = vpn_connection_id
    kwargs["VpnConnectionDeviceTypeId"] = vpn_connection_device_type_id
    if internet_key_exchange_version is not None:
        kwargs["InternetKeyExchangeVersion"] = internet_key_exchange_version
    if sample_type is not None:
        kwargs["SampleType"] = sample_type
    try:
        resp = await client.call("GetVpnConnectionDeviceSampleConfiguration", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to get vpn connection device sample configuration"
        ) from exc
    return GetVpnConnectionDeviceSampleConfigurationResult(
        vpn_connection_device_sample_configuration=resp.get(
            "VpnConnectionDeviceSampleConfiguration"
        ),
    )


async def get_vpn_connection_device_types(
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> GetVpnConnectionDeviceTypesResult:
    """Get vpn connection device types.

    Args:
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("GetVpnConnectionDeviceTypes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get vpn connection device types") from exc
    return GetVpnConnectionDeviceTypesResult(
        vpn_connection_device_types=resp.get("VpnConnectionDeviceTypes"),
        next_token=resp.get("NextToken"),
    )


async def get_vpn_tunnel_replacement_status(
    vpn_connection_id: str,
    vpn_tunnel_outside_ip_address: str,
    region_name: str | None = None,
) -> GetVpnTunnelReplacementStatusResult:
    """Get vpn tunnel replacement status.

    Args:
        vpn_connection_id: Vpn connection id.
        vpn_tunnel_outside_ip_address: Vpn tunnel outside ip address.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpnConnectionId"] = vpn_connection_id
    kwargs["VpnTunnelOutsideIpAddress"] = vpn_tunnel_outside_ip_address
    try:
        resp = await client.call("GetVpnTunnelReplacementStatus", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to get vpn tunnel replacement status") from exc
    return GetVpnTunnelReplacementStatusResult(
        vpn_connection_id=resp.get("VpnConnectionId"),
        transit_gateway_id=resp.get("TransitGatewayId"),
        customer_gateway_id=resp.get("CustomerGatewayId"),
        vpn_gateway_id=resp.get("VpnGatewayId"),
        vpn_tunnel_outside_ip_address=resp.get("VpnTunnelOutsideIpAddress"),
        maintenance_details=resp.get("MaintenanceDetails"),
    )


async def import_client_vpn_client_certificate_revocation_list(
    client_vpn_endpoint_id: str,
    certificate_revocation_list: str,
    region_name: str | None = None,
) -> ImportClientVpnClientCertificateRevocationListResult:
    """Import client vpn client certificate revocation list.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        certificate_revocation_list: Certificate revocation list.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    kwargs["CertificateRevocationList"] = certificate_revocation_list
    try:
        resp = await client.call("ImportClientVpnClientCertificateRevocationList", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to import client vpn client certificate revocation list"
        ) from exc
    return ImportClientVpnClientCertificateRevocationListResult(
        return_value=resp.get("Return"),
    )


async def import_image(
    *,
    architecture: str | None = None,
    client_data: dict[str, Any] | None = None,
    client_token: str | None = None,
    description: str | None = None,
    disk_containers: list[dict[str, Any]] | None = None,
    encrypted: bool | None = None,
    hypervisor: str | None = None,
    kms_key_id: str | None = None,
    license_type: str | None = None,
    platform: str | None = None,
    role_name: str | None = None,
    license_specifications: list[dict[str, Any]] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    usage_operation: str | None = None,
    boot_mode: str | None = None,
    region_name: str | None = None,
) -> ImportImageResult:
    """Import image.

    Args:
        architecture: Architecture.
        client_data: Client data.
        client_token: Client token.
        description: Description.
        disk_containers: Disk containers.
        encrypted: Encrypted.
        hypervisor: Hypervisor.
        kms_key_id: Kms key id.
        license_type: License type.
        platform: Platform.
        role_name: Role name.
        license_specifications: License specifications.
        tag_specifications: Tag specifications.
        usage_operation: Usage operation.
        boot_mode: Boot mode.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if architecture is not None:
        kwargs["Architecture"] = architecture
    if client_data is not None:
        kwargs["ClientData"] = client_data
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if description is not None:
        kwargs["Description"] = description
    if disk_containers is not None:
        kwargs["DiskContainers"] = disk_containers
    if encrypted is not None:
        kwargs["Encrypted"] = encrypted
    if hypervisor is not None:
        kwargs["Hypervisor"] = hypervisor
    if kms_key_id is not None:
        kwargs["KmsKeyId"] = kms_key_id
    if license_type is not None:
        kwargs["LicenseType"] = license_type
    if platform is not None:
        kwargs["Platform"] = platform
    if role_name is not None:
        kwargs["RoleName"] = role_name
    if license_specifications is not None:
        kwargs["LicenseSpecifications"] = license_specifications
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if usage_operation is not None:
        kwargs["UsageOperation"] = usage_operation
    if boot_mode is not None:
        kwargs["BootMode"] = boot_mode
    try:
        resp = await client.call("ImportImage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to import image") from exc
    return ImportImageResult(
        architecture=resp.get("Architecture"),
        description=resp.get("Description"),
        encrypted=resp.get("Encrypted"),
        hypervisor=resp.get("Hypervisor"),
        image_id=resp.get("ImageId"),
        import_task_id=resp.get("ImportTaskId"),
        kms_key_id=resp.get("KmsKeyId"),
        license_type=resp.get("LicenseType"),
        platform=resp.get("Platform"),
        progress=resp.get("Progress"),
        snapshot_details=resp.get("SnapshotDetails"),
        status=resp.get("Status"),
        status_message=resp.get("StatusMessage"),
        license_specifications=resp.get("LicenseSpecifications"),
        tags=resp.get("Tags"),
        usage_operation=resp.get("UsageOperation"),
    )


async def import_instance(
    platform: str,
    *,
    description: str | None = None,
    launch_specification: dict[str, Any] | None = None,
    disk_images: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> ImportInstanceResult:
    """Import instance.

    Args:
        platform: Platform.
        description: Description.
        launch_specification: Launch specification.
        disk_images: Disk images.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Platform"] = platform
    if description is not None:
        kwargs["Description"] = description
    if launch_specification is not None:
        kwargs["LaunchSpecification"] = launch_specification
    if disk_images is not None:
        kwargs["DiskImages"] = disk_images
    try:
        resp = await client.call("ImportInstance", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to import instance") from exc
    return ImportInstanceResult(
        conversion_task=resp.get("ConversionTask"),
    )


async def import_key_pair(
    key_name: str,
    public_key_material: bytes,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> ImportKeyPairResult:
    """Import key pair.

    Args:
        key_name: Key name.
        public_key_material: Public key material.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["KeyName"] = key_name
    kwargs["PublicKeyMaterial"] = public_key_material
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("ImportKeyPair", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to import key pair") from exc
    return ImportKeyPairResult(
        key_fingerprint=resp.get("KeyFingerprint"),
        key_name=resp.get("KeyName"),
        key_pair_id=resp.get("KeyPairId"),
        tags=resp.get("Tags"),
    )


async def import_snapshot(
    *,
    client_data: dict[str, Any] | None = None,
    client_token: str | None = None,
    description: str | None = None,
    disk_container: dict[str, Any] | None = None,
    encrypted: bool | None = None,
    kms_key_id: str | None = None,
    role_name: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> ImportSnapshotResult:
    """Import snapshot.

    Args:
        client_data: Client data.
        client_token: Client token.
        description: Description.
        disk_container: Disk container.
        encrypted: Encrypted.
        kms_key_id: Kms key id.
        role_name: Role name.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if client_data is not None:
        kwargs["ClientData"] = client_data
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if description is not None:
        kwargs["Description"] = description
    if disk_container is not None:
        kwargs["DiskContainer"] = disk_container
    if encrypted is not None:
        kwargs["Encrypted"] = encrypted
    if kms_key_id is not None:
        kwargs["KmsKeyId"] = kms_key_id
    if role_name is not None:
        kwargs["RoleName"] = role_name
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("ImportSnapshot", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to import snapshot") from exc
    return ImportSnapshotResult(
        description=resp.get("Description"),
        import_task_id=resp.get("ImportTaskId"),
        snapshot_task_detail=resp.get("SnapshotTaskDetail"),
        tags=resp.get("Tags"),
    )


async def import_volume(
    image: dict[str, Any],
    volume: dict[str, Any],
    *,
    availability_zone_id: str | None = None,
    availability_zone: str | None = None,
    description: str | None = None,
    region_name: str | None = None,
) -> ImportVolumeResult:
    """Import volume.

    Args:
        image: Image.
        volume: Volume.
        availability_zone_id: Availability zone id.
        availability_zone: Availability zone.
        description: Description.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Image"] = image
    kwargs["Volume"] = volume
    if availability_zone_id is not None:
        kwargs["AvailabilityZoneId"] = availability_zone_id
    if availability_zone is not None:
        kwargs["AvailabilityZone"] = availability_zone
    if description is not None:
        kwargs["Description"] = description
    try:
        resp = await client.call("ImportVolume", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to import volume") from exc
    return ImportVolumeResult(
        conversion_task=resp.get("ConversionTask"),
    )


async def list_images_in_recycle_bin(
    *,
    image_ids: list[str] | None = None,
    next_token: str | None = None,
    max_results: int | None = None,
    region_name: str | None = None,
) -> ListImagesInRecycleBinResult:
    """List images in recycle bin.

    Args:
        image_ids: Image ids.
        next_token: Next token.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if image_ids is not None:
        kwargs["ImageIds"] = image_ids
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("ListImagesInRecycleBin", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to list images in recycle bin") from exc
    return ListImagesInRecycleBinResult(
        images=resp.get("Images"),
        next_token=resp.get("NextToken"),
    )


async def list_snapshots_in_recycle_bin(
    *,
    max_results: int | None = None,
    next_token: str | None = None,
    snapshot_ids: list[str] | None = None,
    region_name: str | None = None,
) -> ListSnapshotsInRecycleBinResult:
    """List snapshots in recycle bin.

    Args:
        max_results: Max results.
        next_token: Next token.
        snapshot_ids: Snapshot ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    if snapshot_ids is not None:
        kwargs["SnapshotIds"] = snapshot_ids
    try:
        resp = await client.call("ListSnapshotsInRecycleBin", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to list snapshots in recycle bin") from exc
    return ListSnapshotsInRecycleBinResult(
        snapshots=resp.get("Snapshots"),
        next_token=resp.get("NextToken"),
    )


async def lock_snapshot(
    snapshot_id: str,
    lock_mode: str,
    *,
    cool_off_period: int | None = None,
    lock_duration: int | None = None,
    expiration_date: str | None = None,
    region_name: str | None = None,
) -> LockSnapshotResult:
    """Lock snapshot.

    Args:
        snapshot_id: Snapshot id.
        lock_mode: Lock mode.
        cool_off_period: Cool off period.
        lock_duration: Lock duration.
        expiration_date: Expiration date.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SnapshotId"] = snapshot_id
    kwargs["LockMode"] = lock_mode
    if cool_off_period is not None:
        kwargs["CoolOffPeriod"] = cool_off_period
    if lock_duration is not None:
        kwargs["LockDuration"] = lock_duration
    if expiration_date is not None:
        kwargs["ExpirationDate"] = expiration_date
    try:
        resp = await client.call("LockSnapshot", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to lock snapshot") from exc
    return LockSnapshotResult(
        snapshot_id=resp.get("SnapshotId"),
        lock_state=resp.get("LockState"),
        lock_duration=resp.get("LockDuration"),
        cool_off_period=resp.get("CoolOffPeriod"),
        cool_off_period_expires_on=resp.get("CoolOffPeriodExpiresOn"),
        lock_created_on=resp.get("LockCreatedOn"),
        lock_expires_on=resp.get("LockExpiresOn"),
        lock_duration_start_time=resp.get("LockDurationStartTime"),
    )


async def modify_address_attribute(
    allocation_id: str,
    *,
    domain_name: str | None = None,
    region_name: str | None = None,
) -> ModifyAddressAttributeResult:
    """Modify address attribute.

    Args:
        allocation_id: Allocation id.
        domain_name: Domain name.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AllocationId"] = allocation_id
    if domain_name is not None:
        kwargs["DomainName"] = domain_name
    try:
        resp = await client.call("ModifyAddressAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify address attribute") from exc
    return ModifyAddressAttributeResult(
        address=resp.get("Address"),
    )


async def modify_availability_zone_group(
    group_name: str,
    opt_in_status: str,
    region_name: str | None = None,
) -> ModifyAvailabilityZoneGroupResult:
    """Modify availability zone group.

    Args:
        group_name: Group name.
        opt_in_status: Opt in status.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["GroupName"] = group_name
    kwargs["OptInStatus"] = opt_in_status
    try:
        resp = await client.call("ModifyAvailabilityZoneGroup", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify availability zone group") from exc
    return ModifyAvailabilityZoneGroupResult(
        return_value=resp.get("Return"),
    )


async def modify_capacity_reservation(
    capacity_reservation_id: str,
    *,
    instance_count: int | None = None,
    end_date: str | None = None,
    end_date_type: str | None = None,
    accept: bool | None = None,
    additional_info: str | None = None,
    instance_match_criteria: str | None = None,
    region_name: str | None = None,
) -> ModifyCapacityReservationResult:
    """Modify capacity reservation.

    Args:
        capacity_reservation_id: Capacity reservation id.
        instance_count: Instance count.
        end_date: End date.
        end_date_type: End date type.
        accept: Accept.
        additional_info: Additional info.
        instance_match_criteria: Instance match criteria.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityReservationId"] = capacity_reservation_id
    if instance_count is not None:
        kwargs["InstanceCount"] = instance_count
    if end_date is not None:
        kwargs["EndDate"] = end_date
    if end_date_type is not None:
        kwargs["EndDateType"] = end_date_type
    if accept is not None:
        kwargs["Accept"] = accept
    if additional_info is not None:
        kwargs["AdditionalInfo"] = additional_info
    if instance_match_criteria is not None:
        kwargs["InstanceMatchCriteria"] = instance_match_criteria
    try:
        resp = await client.call("ModifyCapacityReservation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify capacity reservation") from exc
    return ModifyCapacityReservationResult(
        return_value=resp.get("Return"),
    )


async def modify_capacity_reservation_fleet(
    capacity_reservation_fleet_id: str,
    *,
    total_target_capacity: int | None = None,
    end_date: str | None = None,
    remove_end_date: bool | None = None,
    region_name: str | None = None,
) -> ModifyCapacityReservationFleetResult:
    """Modify capacity reservation fleet.

    Args:
        capacity_reservation_fleet_id: Capacity reservation fleet id.
        total_target_capacity: Total target capacity.
        end_date: End date.
        remove_end_date: Remove end date.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityReservationFleetId"] = capacity_reservation_fleet_id
    if total_target_capacity is not None:
        kwargs["TotalTargetCapacity"] = total_target_capacity
    if end_date is not None:
        kwargs["EndDate"] = end_date
    if remove_end_date is not None:
        kwargs["RemoveEndDate"] = remove_end_date
    try:
        resp = await client.call("ModifyCapacityReservationFleet", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify capacity reservation fleet") from exc
    return ModifyCapacityReservationFleetResult(
        return_value=resp.get("Return"),
    )


async def modify_client_vpn_endpoint(
    client_vpn_endpoint_id: str,
    *,
    server_certificate_arn: str | None = None,
    connection_log_options: dict[str, Any] | None = None,
    dns_servers: dict[str, Any] | None = None,
    vpn_port: int | None = None,
    description: str | None = None,
    split_tunnel: bool | None = None,
    security_group_ids: list[str] | None = None,
    vpc_id: str | None = None,
    self_service_portal: str | None = None,
    client_connect_options: dict[str, Any] | None = None,
    session_timeout_hours: int | None = None,
    client_login_banner_options: dict[str, Any] | None = None,
    client_route_enforcement_options: dict[str, Any] | None = None,
    disconnect_on_session_timeout: bool | None = None,
    region_name: str | None = None,
) -> ModifyClientVpnEndpointResult:
    """Modify client vpn endpoint.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        server_certificate_arn: Server certificate arn.
        connection_log_options: Connection log options.
        dns_servers: Dns servers.
        vpn_port: Vpn port.
        description: Description.
        split_tunnel: Split tunnel.
        security_group_ids: Security group ids.
        vpc_id: Vpc id.
        self_service_portal: Self service portal.
        client_connect_options: Client connect options.
        session_timeout_hours: Session timeout hours.
        client_login_banner_options: Client login banner options.
        client_route_enforcement_options: Client route enforcement options.
        disconnect_on_session_timeout: Disconnect on session timeout.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    if server_certificate_arn is not None:
        kwargs["ServerCertificateArn"] = server_certificate_arn
    if connection_log_options is not None:
        kwargs["ConnectionLogOptions"] = connection_log_options
    if dns_servers is not None:
        kwargs["DnsServers"] = dns_servers
    if vpn_port is not None:
        kwargs["VpnPort"] = vpn_port
    if description is not None:
        kwargs["Description"] = description
    if split_tunnel is not None:
        kwargs["SplitTunnel"] = split_tunnel
    if security_group_ids is not None:
        kwargs["SecurityGroupIds"] = security_group_ids
    if vpc_id is not None:
        kwargs["VpcId"] = vpc_id
    if self_service_portal is not None:
        kwargs["SelfServicePortal"] = self_service_portal
    if client_connect_options is not None:
        kwargs["ClientConnectOptions"] = client_connect_options
    if session_timeout_hours is not None:
        kwargs["SessionTimeoutHours"] = session_timeout_hours
    if client_login_banner_options is not None:
        kwargs["ClientLoginBannerOptions"] = client_login_banner_options
    if client_route_enforcement_options is not None:
        kwargs["ClientRouteEnforcementOptions"] = client_route_enforcement_options
    if disconnect_on_session_timeout is not None:
        kwargs["DisconnectOnSessionTimeout"] = disconnect_on_session_timeout
    try:
        resp = await client.call("ModifyClientVpnEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify client vpn endpoint") from exc
    return ModifyClientVpnEndpointResult(
        return_value=resp.get("Return"),
    )


async def modify_default_credit_specification(
    instance_family: str,
    cpu_credits: str,
    region_name: str | None = None,
) -> ModifyDefaultCreditSpecificationResult:
    """Modify default credit specification.

    Args:
        instance_family: Instance family.
        cpu_credits: Cpu credits.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceFamily"] = instance_family
    kwargs["CpuCredits"] = cpu_credits
    try:
        resp = await client.call("ModifyDefaultCreditSpecification", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify default credit specification") from exc
    return ModifyDefaultCreditSpecificationResult(
        instance_family_credit_specification=resp.get("InstanceFamilyCreditSpecification"),
    )


async def modify_ebs_default_kms_key_id(
    kms_key_id: str,
    region_name: str | None = None,
) -> ModifyEbsDefaultKmsKeyIdResult:
    """Modify ebs default kms key id.

    Args:
        kms_key_id: Kms key id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["KmsKeyId"] = kms_key_id
    try:
        resp = await client.call("ModifyEbsDefaultKmsKeyId", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify ebs default kms key id") from exc
    return ModifyEbsDefaultKmsKeyIdResult(
        kms_key_id=resp.get("KmsKeyId"),
    )


async def modify_fleet(
    fleet_id: str,
    *,
    excess_capacity_termination_policy: str | None = None,
    launch_template_configs: list[dict[str, Any]] | None = None,
    target_capacity_specification: dict[str, Any] | None = None,
    context: str | None = None,
    region_name: str | None = None,
) -> ModifyFleetResult:
    """Modify fleet.

    Args:
        fleet_id: Fleet id.
        excess_capacity_termination_policy: Excess capacity termination policy.
        launch_template_configs: Launch template configs.
        target_capacity_specification: Target capacity specification.
        context: Context.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["FleetId"] = fleet_id
    if excess_capacity_termination_policy is not None:
        kwargs["ExcessCapacityTerminationPolicy"] = excess_capacity_termination_policy
    if launch_template_configs is not None:
        kwargs["LaunchTemplateConfigs"] = launch_template_configs
    if target_capacity_specification is not None:
        kwargs["TargetCapacitySpecification"] = target_capacity_specification
    if context is not None:
        kwargs["Context"] = context
    try:
        resp = await client.call("ModifyFleet", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify fleet") from exc
    return ModifyFleetResult(
        return_value=resp.get("Return"),
    )


async def modify_fpga_image_attribute(
    fpga_image_id: str,
    *,
    attribute: str | None = None,
    operation_type: str | None = None,
    user_ids: list[str] | None = None,
    user_groups: list[str] | None = None,
    product_codes: list[str] | None = None,
    load_permission: dict[str, Any] | None = None,
    description: str | None = None,
    name: str | None = None,
    region_name: str | None = None,
) -> ModifyFpgaImageAttributeResult:
    """Modify fpga image attribute.

    Args:
        fpga_image_id: Fpga image id.
        attribute: Attribute.
        operation_type: Operation type.
        user_ids: User ids.
        user_groups: User groups.
        product_codes: Product codes.
        load_permission: Load permission.
        description: Description.
        name: Name.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["FpgaImageId"] = fpga_image_id
    if attribute is not None:
        kwargs["Attribute"] = attribute
    if operation_type is not None:
        kwargs["OperationType"] = operation_type
    if user_ids is not None:
        kwargs["UserIds"] = user_ids
    if user_groups is not None:
        kwargs["UserGroups"] = user_groups
    if product_codes is not None:
        kwargs["ProductCodes"] = product_codes
    if load_permission is not None:
        kwargs["LoadPermission"] = load_permission
    if description is not None:
        kwargs["Description"] = description
    if name is not None:
        kwargs["Name"] = name
    try:
        resp = await client.call("ModifyFpgaImageAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify fpga image attribute") from exc
    return ModifyFpgaImageAttributeResult(
        fpga_image_attribute=resp.get("FpgaImageAttribute"),
    )


async def modify_hosts(
    host_ids: list[str],
    *,
    host_recovery: str | None = None,
    instance_type: str | None = None,
    instance_family: str | None = None,
    host_maintenance: str | None = None,
    auto_placement: str | None = None,
    region_name: str | None = None,
) -> ModifyHostsResult:
    """Modify hosts.

    Args:
        host_ids: Host ids.
        host_recovery: Host recovery.
        instance_type: Instance type.
        instance_family: Instance family.
        host_maintenance: Host maintenance.
        auto_placement: Auto placement.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["HostIds"] = host_ids
    if host_recovery is not None:
        kwargs["HostRecovery"] = host_recovery
    if instance_type is not None:
        kwargs["InstanceType"] = instance_type
    if instance_family is not None:
        kwargs["InstanceFamily"] = instance_family
    if host_maintenance is not None:
        kwargs["HostMaintenance"] = host_maintenance
    if auto_placement is not None:
        kwargs["AutoPlacement"] = auto_placement
    try:
        resp = await client.call("ModifyHosts", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify hosts") from exc
    return ModifyHostsResult(
        successful=resp.get("Successful"),
        unsuccessful=resp.get("Unsuccessful"),
    )


async def modify_id_format(
    resource: str,
    use_long_ids: bool,
    region_name: str | None = None,
) -> None:
    """Modify id format.

    Args:
        resource: Resource.
        use_long_ids: Use long ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Resource"] = resource
    kwargs["UseLongIds"] = use_long_ids
    try:
        await client.call("ModifyIdFormat", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify id format") from exc
    return None


async def modify_identity_id_format(
    resource: str,
    use_long_ids: bool,
    principal_arn: str,
    region_name: str | None = None,
) -> None:
    """Modify identity id format.

    Args:
        resource: Resource.
        use_long_ids: Use long ids.
        principal_arn: Principal arn.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Resource"] = resource
    kwargs["UseLongIds"] = use_long_ids
    kwargs["PrincipalArn"] = principal_arn
    try:
        await client.call("ModifyIdentityIdFormat", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify identity id format") from exc
    return None


async def modify_image_attribute(
    image_id: str,
    *,
    attribute: str | None = None,
    description: dict[str, Any] | None = None,
    launch_permission: dict[str, Any] | None = None,
    operation_type: str | None = None,
    product_codes: list[str] | None = None,
    user_groups: list[str] | None = None,
    user_ids: list[str] | None = None,
    value: str | None = None,
    organization_arns: list[str] | None = None,
    organizational_unit_arns: list[str] | None = None,
    imds_support: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> None:
    """Modify image attribute.

    Args:
        image_id: Image id.
        attribute: Attribute.
        description: Description.
        launch_permission: Launch permission.
        operation_type: Operation type.
        product_codes: Product codes.
        user_groups: User groups.
        user_ids: User ids.
        value: Value.
        organization_arns: Organization arns.
        organizational_unit_arns: Organizational unit arns.
        imds_support: Imds support.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    if attribute is not None:
        kwargs["Attribute"] = attribute
    if description is not None:
        kwargs["Description"] = description
    if launch_permission is not None:
        kwargs["LaunchPermission"] = launch_permission
    if operation_type is not None:
        kwargs["OperationType"] = operation_type
    if product_codes is not None:
        kwargs["ProductCodes"] = product_codes
    if user_groups is not None:
        kwargs["UserGroups"] = user_groups
    if user_ids is not None:
        kwargs["UserIds"] = user_ids
    if value is not None:
        kwargs["Value"] = value
    if organization_arns is not None:
        kwargs["OrganizationArns"] = organization_arns
    if organizational_unit_arns is not None:
        kwargs["OrganizationalUnitArns"] = organizational_unit_arns
    if imds_support is not None:
        kwargs["ImdsSupport"] = imds_support
    try:
        await client.call("ModifyImageAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify image attribute") from exc
    return None


async def modify_instance_attribute(
    instance_id: str,
    *,
    source_dest_check: dict[str, Any] | None = None,
    disable_api_stop: dict[str, Any] | None = None,
    attribute: str | None = None,
    value: str | None = None,
    block_device_mappings: list[dict[str, Any]] | None = None,
    disable_api_termination: dict[str, Any] | None = None,
    instance_type: dict[str, Any] | None = None,
    kernel: dict[str, Any] | None = None,
    ramdisk: dict[str, Any] | None = None,
    user_data: dict[str, Any] | None = None,
    instance_initiated_shutdown_behavior: dict[str, Any] | None = None,
    groups: list[str] | None = None,
    ebs_optimized: dict[str, Any] | None = None,
    sriov_net_support: dict[str, Any] | None = None,
    ena_support: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> None:
    """Modify instance attribute.

    Args:
        instance_id: Instance id.
        source_dest_check: Source dest check.
        disable_api_stop: Disable api stop.
        attribute: Attribute.
        value: Value.
        block_device_mappings: Block device mappings.
        disable_api_termination: Disable api termination.
        instance_type: Instance type.
        kernel: Kernel.
        ramdisk: Ramdisk.
        user_data: User data.
        instance_initiated_shutdown_behavior: Instance initiated shutdown behavior.
        groups: Groups.
        ebs_optimized: Ebs optimized.
        sriov_net_support: Sriov net support.
        ena_support: Ena support.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    if source_dest_check is not None:
        kwargs["SourceDestCheck"] = source_dest_check
    if disable_api_stop is not None:
        kwargs["DisableApiStop"] = disable_api_stop
    if attribute is not None:
        kwargs["Attribute"] = attribute
    if value is not None:
        kwargs["Value"] = value
    if block_device_mappings is not None:
        kwargs["BlockDeviceMappings"] = block_device_mappings
    if disable_api_termination is not None:
        kwargs["DisableApiTermination"] = disable_api_termination
    if instance_type is not None:
        kwargs["InstanceType"] = instance_type
    if kernel is not None:
        kwargs["Kernel"] = kernel
    if ramdisk is not None:
        kwargs["Ramdisk"] = ramdisk
    if user_data is not None:
        kwargs["UserData"] = user_data
    if instance_initiated_shutdown_behavior is not None:
        kwargs["InstanceInitiatedShutdownBehavior"] = instance_initiated_shutdown_behavior
    if groups is not None:
        kwargs["Groups"] = groups
    if ebs_optimized is not None:
        kwargs["EbsOptimized"] = ebs_optimized
    if sriov_net_support is not None:
        kwargs["SriovNetSupport"] = sriov_net_support
    if ena_support is not None:
        kwargs["EnaSupport"] = ena_support
    try:
        await client.call("ModifyInstanceAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify instance attribute") from exc
    return None


async def modify_instance_capacity_reservation_attributes(
    instance_id: str,
    capacity_reservation_specification: dict[str, Any],
    region_name: str | None = None,
) -> ModifyInstanceCapacityReservationAttributesResult:
    """Modify instance capacity reservation attributes.

    Args:
        instance_id: Instance id.
        capacity_reservation_specification: Capacity reservation specification.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["CapacityReservationSpecification"] = capacity_reservation_specification
    try:
        resp = await client.call("ModifyInstanceCapacityReservationAttributes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to modify instance capacity reservation attributes"
        ) from exc
    return ModifyInstanceCapacityReservationAttributesResult(
        return_value=resp.get("Return"),
    )


async def modify_instance_connect_endpoint(
    instance_connect_endpoint_id: str,
    *,
    ip_address_type: str | None = None,
    security_group_ids: list[str] | None = None,
    preserve_client_ip: bool | None = None,
    region_name: str | None = None,
) -> ModifyInstanceConnectEndpointResult:
    """Modify instance connect endpoint.

    Args:
        instance_connect_endpoint_id: Instance connect endpoint id.
        ip_address_type: Ip address type.
        security_group_ids: Security group ids.
        preserve_client_ip: Preserve client ip.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceConnectEndpointId"] = instance_connect_endpoint_id
    if ip_address_type is not None:
        kwargs["IpAddressType"] = ip_address_type
    if security_group_ids is not None:
        kwargs["SecurityGroupIds"] = security_group_ids
    if preserve_client_ip is not None:
        kwargs["PreserveClientIp"] = preserve_client_ip
    try:
        resp = await client.call("ModifyInstanceConnectEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify instance connect endpoint") from exc
    return ModifyInstanceConnectEndpointResult(
        return_value=resp.get("Return"),
    )


async def modify_instance_cpu_options(
    instance_id: str,
    core_count: int,
    threads_per_core: int,
    region_name: str | None = None,
) -> ModifyInstanceCpuOptionsResult:
    """Modify instance cpu options.

    Args:
        instance_id: Instance id.
        core_count: Core count.
        threads_per_core: Threads per core.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["CoreCount"] = core_count
    kwargs["ThreadsPerCore"] = threads_per_core
    try:
        resp = await client.call("ModifyInstanceCpuOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify instance cpu options") from exc
    return ModifyInstanceCpuOptionsResult(
        instance_id=resp.get("InstanceId"),
        core_count=resp.get("CoreCount"),
        threads_per_core=resp.get("ThreadsPerCore"),
    )


async def modify_instance_credit_specification(
    instance_credit_specifications: list[dict[str, Any]],
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> ModifyInstanceCreditSpecificationResult:
    """Modify instance credit specification.

    Args:
        instance_credit_specifications: Instance credit specifications.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceCreditSpecifications"] = instance_credit_specifications
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("ModifyInstanceCreditSpecification", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify instance credit specification") from exc
    return ModifyInstanceCreditSpecificationResult(
        successful_instance_credit_specifications=resp.get(
            "SuccessfulInstanceCreditSpecifications"
        ),
        unsuccessful_instance_credit_specifications=resp.get(
            "UnsuccessfulInstanceCreditSpecifications"
        ),
    )


async def modify_instance_event_start_time(
    instance_id: str,
    instance_event_id: str,
    not_before: str,
    region_name: str | None = None,
) -> ModifyInstanceEventStartTimeResult:
    """Modify instance event start time.

    Args:
        instance_id: Instance id.
        instance_event_id: Instance event id.
        not_before: Not before.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["InstanceEventId"] = instance_event_id
    kwargs["NotBefore"] = not_before
    try:
        resp = await client.call("ModifyInstanceEventStartTime", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify instance event start time") from exc
    return ModifyInstanceEventStartTimeResult(
        event=resp.get("Event"),
    )


async def modify_instance_event_window(
    instance_event_window_id: str,
    *,
    name: str | None = None,
    time_ranges: list[dict[str, Any]] | None = None,
    cron_expression: str | None = None,
    region_name: str | None = None,
) -> ModifyInstanceEventWindowResult:
    """Modify instance event window.

    Args:
        instance_event_window_id: Instance event window id.
        name: Name.
        time_ranges: Time ranges.
        cron_expression: Cron expression.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceEventWindowId"] = instance_event_window_id
    if name is not None:
        kwargs["Name"] = name
    if time_ranges is not None:
        kwargs["TimeRanges"] = time_ranges
    if cron_expression is not None:
        kwargs["CronExpression"] = cron_expression
    try:
        resp = await client.call("ModifyInstanceEventWindow", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify instance event window") from exc
    return ModifyInstanceEventWindowResult(
        instance_event_window=resp.get("InstanceEventWindow"),
    )


async def modify_instance_maintenance_options(
    instance_id: str,
    *,
    auto_recovery: str | None = None,
    reboot_migration: str | None = None,
    region_name: str | None = None,
) -> ModifyInstanceMaintenanceOptionsResult:
    """Modify instance maintenance options.

    Args:
        instance_id: Instance id.
        auto_recovery: Auto recovery.
        reboot_migration: Reboot migration.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    if auto_recovery is not None:
        kwargs["AutoRecovery"] = auto_recovery
    if reboot_migration is not None:
        kwargs["RebootMigration"] = reboot_migration
    try:
        resp = await client.call("ModifyInstanceMaintenanceOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify instance maintenance options") from exc
    return ModifyInstanceMaintenanceOptionsResult(
        instance_id=resp.get("InstanceId"),
        auto_recovery=resp.get("AutoRecovery"),
        reboot_migration=resp.get("RebootMigration"),
    )


async def modify_instance_metadata_defaults(
    *,
    http_tokens: str | None = None,
    http_put_response_hop_limit: int | None = None,
    http_endpoint: str | None = None,
    instance_metadata_tags: str | None = None,
    region_name: str | None = None,
) -> ModifyInstanceMetadataDefaultsResult:
    """Modify instance metadata defaults.

    Args:
        http_tokens: Http tokens.
        http_put_response_hop_limit: Http put response hop limit.
        http_endpoint: Http endpoint.
        instance_metadata_tags: Instance metadata tags.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if http_tokens is not None:
        kwargs["HttpTokens"] = http_tokens
    if http_put_response_hop_limit is not None:
        kwargs["HttpPutResponseHopLimit"] = http_put_response_hop_limit
    if http_endpoint is not None:
        kwargs["HttpEndpoint"] = http_endpoint
    if instance_metadata_tags is not None:
        kwargs["InstanceMetadataTags"] = instance_metadata_tags
    try:
        resp = await client.call("ModifyInstanceMetadataDefaults", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify instance metadata defaults") from exc
    return ModifyInstanceMetadataDefaultsResult(
        return_value=resp.get("Return"),
    )


async def modify_instance_metadata_options(
    instance_id: str,
    *,
    http_tokens: str | None = None,
    http_put_response_hop_limit: int | None = None,
    http_endpoint: str | None = None,
    http_protocol_ipv6: str | None = None,
    instance_metadata_tags: str | None = None,
    region_name: str | None = None,
) -> ModifyInstanceMetadataOptionsResult:
    """Modify instance metadata options.

    Args:
        instance_id: Instance id.
        http_tokens: Http tokens.
        http_put_response_hop_limit: Http put response hop limit.
        http_endpoint: Http endpoint.
        http_protocol_ipv6: Http protocol ipv6.
        instance_metadata_tags: Instance metadata tags.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    if http_tokens is not None:
        kwargs["HttpTokens"] = http_tokens
    if http_put_response_hop_limit is not None:
        kwargs["HttpPutResponseHopLimit"] = http_put_response_hop_limit
    if http_endpoint is not None:
        kwargs["HttpEndpoint"] = http_endpoint
    if http_protocol_ipv6 is not None:
        kwargs["HttpProtocolIpv6"] = http_protocol_ipv6
    if instance_metadata_tags is not None:
        kwargs["InstanceMetadataTags"] = instance_metadata_tags
    try:
        resp = await client.call("ModifyInstanceMetadataOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify instance metadata options") from exc
    return ModifyInstanceMetadataOptionsResult(
        instance_id=resp.get("InstanceId"),
        instance_metadata_options=resp.get("InstanceMetadataOptions"),
    )


async def modify_instance_network_performance_options(
    instance_id: str,
    bandwidth_weighting: str,
    region_name: str | None = None,
) -> ModifyInstanceNetworkPerformanceOptionsResult:
    """Modify instance network performance options.

    Args:
        instance_id: Instance id.
        bandwidth_weighting: Bandwidth weighting.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["BandwidthWeighting"] = bandwidth_weighting
    try:
        resp = await client.call("ModifyInstanceNetworkPerformanceOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify instance network performance options") from exc
    return ModifyInstanceNetworkPerformanceOptionsResult(
        instance_id=resp.get("InstanceId"),
        bandwidth_weighting=resp.get("BandwidthWeighting"),
    )


async def modify_instance_placement(
    instance_id: str,
    *,
    group_name: str | None = None,
    partition_number: int | None = None,
    host_resource_group_arn: str | None = None,
    group_id: str | None = None,
    tenancy: str | None = None,
    affinity: str | None = None,
    host_id: str | None = None,
    region_name: str | None = None,
) -> ModifyInstancePlacementResult:
    """Modify instance placement.

    Args:
        instance_id: Instance id.
        group_name: Group name.
        partition_number: Partition number.
        host_resource_group_arn: Host resource group arn.
        group_id: Group id.
        tenancy: Tenancy.
        affinity: Affinity.
        host_id: Host id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    if group_name is not None:
        kwargs["GroupName"] = group_name
    if partition_number is not None:
        kwargs["PartitionNumber"] = partition_number
    if host_resource_group_arn is not None:
        kwargs["HostResourceGroupArn"] = host_resource_group_arn
    if group_id is not None:
        kwargs["GroupId"] = group_id
    if tenancy is not None:
        kwargs["Tenancy"] = tenancy
    if affinity is not None:
        kwargs["Affinity"] = affinity
    if host_id is not None:
        kwargs["HostId"] = host_id
    try:
        resp = await client.call("ModifyInstancePlacement", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify instance placement") from exc
    return ModifyInstancePlacementResult(
        return_value=resp.get("Return"),
    )


async def modify_ipam(
    ipam_id: str,
    *,
    description: str | None = None,
    add_operating_regions: list[dict[str, Any]] | None = None,
    remove_operating_regions: list[dict[str, Any]] | None = None,
    tier: str | None = None,
    enable_private_gua: bool | None = None,
    metered_account: str | None = None,
    region_name: str | None = None,
) -> ModifyIpamResult:
    """Modify ipam.

    Args:
        ipam_id: Ipam id.
        description: Description.
        add_operating_regions: Add operating regions.
        remove_operating_regions: Remove operating regions.
        tier: Tier.
        enable_private_gua: Enable private gua.
        metered_account: Metered account.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamId"] = ipam_id
    if description is not None:
        kwargs["Description"] = description
    if add_operating_regions is not None:
        kwargs["AddOperatingRegions"] = add_operating_regions
    if remove_operating_regions is not None:
        kwargs["RemoveOperatingRegions"] = remove_operating_regions
    if tier is not None:
        kwargs["Tier"] = tier
    if enable_private_gua is not None:
        kwargs["EnablePrivateGua"] = enable_private_gua
    if metered_account is not None:
        kwargs["MeteredAccount"] = metered_account
    try:
        resp = await client.call("ModifyIpam", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify ipam") from exc
    return ModifyIpamResult(
        ipam=resp.get("Ipam"),
    )


async def modify_ipam_pool(
    ipam_pool_id: str,
    *,
    description: str | None = None,
    auto_import: bool | None = None,
    allocation_min_netmask_length: int | None = None,
    allocation_max_netmask_length: int | None = None,
    allocation_default_netmask_length: int | None = None,
    clear_allocation_default_netmask_length: bool | None = None,
    add_allocation_resource_tags: list[dict[str, Any]] | None = None,
    remove_allocation_resource_tags: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> ModifyIpamPoolResult:
    """Modify ipam pool.

    Args:
        ipam_pool_id: Ipam pool id.
        description: Description.
        auto_import: Auto import.
        allocation_min_netmask_length: Allocation min netmask length.
        allocation_max_netmask_length: Allocation max netmask length.
        allocation_default_netmask_length: Allocation default netmask length.
        clear_allocation_default_netmask_length: Clear allocation default netmask length.
        add_allocation_resource_tags: Add allocation resource tags.
        remove_allocation_resource_tags: Remove allocation resource tags.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPoolId"] = ipam_pool_id
    if description is not None:
        kwargs["Description"] = description
    if auto_import is not None:
        kwargs["AutoImport"] = auto_import
    if allocation_min_netmask_length is not None:
        kwargs["AllocationMinNetmaskLength"] = allocation_min_netmask_length
    if allocation_max_netmask_length is not None:
        kwargs["AllocationMaxNetmaskLength"] = allocation_max_netmask_length
    if allocation_default_netmask_length is not None:
        kwargs["AllocationDefaultNetmaskLength"] = allocation_default_netmask_length
    if clear_allocation_default_netmask_length is not None:
        kwargs["ClearAllocationDefaultNetmaskLength"] = clear_allocation_default_netmask_length
    if add_allocation_resource_tags is not None:
        kwargs["AddAllocationResourceTags"] = add_allocation_resource_tags
    if remove_allocation_resource_tags is not None:
        kwargs["RemoveAllocationResourceTags"] = remove_allocation_resource_tags
    try:
        resp = await client.call("ModifyIpamPool", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify ipam pool") from exc
    return ModifyIpamPoolResult(
        ipam_pool=resp.get("IpamPool"),
    )


async def modify_ipam_prefix_list_resolver(
    ipam_prefix_list_resolver_id: str,
    *,
    description: str | None = None,
    rules: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> ModifyIpamPrefixListResolverResult:
    """Modify ipam prefix list resolver.

    Args:
        ipam_prefix_list_resolver_id: Ipam prefix list resolver id.
        description: Description.
        rules: Rules.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPrefixListResolverId"] = ipam_prefix_list_resolver_id
    if description is not None:
        kwargs["Description"] = description
    if rules is not None:
        kwargs["Rules"] = rules
    try:
        resp = await client.call("ModifyIpamPrefixListResolver", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify ipam prefix list resolver") from exc
    return ModifyIpamPrefixListResolverResult(
        ipam_prefix_list_resolver=resp.get("IpamPrefixListResolver"),
    )


async def modify_ipam_prefix_list_resolver_target(
    ipam_prefix_list_resolver_target_id: str,
    *,
    desired_version: int | None = None,
    track_latest_version: bool | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> ModifyIpamPrefixListResolverTargetResult:
    """Modify ipam prefix list resolver target.

    Args:
        ipam_prefix_list_resolver_target_id: Ipam prefix list resolver target id.
        desired_version: Desired version.
        track_latest_version: Track latest version.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPrefixListResolverTargetId"] = ipam_prefix_list_resolver_target_id
    if desired_version is not None:
        kwargs["DesiredVersion"] = desired_version
    if track_latest_version is not None:
        kwargs["TrackLatestVersion"] = track_latest_version
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("ModifyIpamPrefixListResolverTarget", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify ipam prefix list resolver target") from exc
    return ModifyIpamPrefixListResolverTargetResult(
        ipam_prefix_list_resolver_target=resp.get("IpamPrefixListResolverTarget"),
    )


async def modify_ipam_resource_cidr(
    resource_id: str,
    resource_cidr: str,
    resource_region: str,
    current_ipam_scope_id: str,
    monitored: bool,
    *,
    destination_ipam_scope_id: str | None = None,
    region_name: str | None = None,
) -> ModifyIpamResourceCidrResult:
    """Modify ipam resource cidr.

    Args:
        resource_id: Resource id.
        resource_cidr: Resource cidr.
        resource_region: Resource region.
        current_ipam_scope_id: Current ipam scope id.
        monitored: Monitored.
        destination_ipam_scope_id: Destination ipam scope id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ResourceId"] = resource_id
    kwargs["ResourceCidr"] = resource_cidr
    kwargs["ResourceRegion"] = resource_region
    kwargs["CurrentIpamScopeId"] = current_ipam_scope_id
    kwargs["Monitored"] = monitored
    if destination_ipam_scope_id is not None:
        kwargs["DestinationIpamScopeId"] = destination_ipam_scope_id
    try:
        resp = await client.call("ModifyIpamResourceCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify ipam resource cidr") from exc
    return ModifyIpamResourceCidrResult(
        ipam_resource_cidr=resp.get("IpamResourceCidr"),
    )


async def modify_ipam_resource_discovery(
    ipam_resource_discovery_id: str,
    *,
    description: str | None = None,
    add_operating_regions: list[dict[str, Any]] | None = None,
    remove_operating_regions: list[dict[str, Any]] | None = None,
    add_organizational_unit_exclusions: list[dict[str, Any]] | None = None,
    remove_organizational_unit_exclusions: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> ModifyIpamResourceDiscoveryResult:
    """Modify ipam resource discovery.

    Args:
        ipam_resource_discovery_id: Ipam resource discovery id.
        description: Description.
        add_operating_regions: Add operating regions.
        remove_operating_regions: Remove operating regions.
        add_organizational_unit_exclusions: Add organizational unit exclusions.
        remove_organizational_unit_exclusions: Remove organizational unit exclusions.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamResourceDiscoveryId"] = ipam_resource_discovery_id
    if description is not None:
        kwargs["Description"] = description
    if add_operating_regions is not None:
        kwargs["AddOperatingRegions"] = add_operating_regions
    if remove_operating_regions is not None:
        kwargs["RemoveOperatingRegions"] = remove_operating_regions
    if add_organizational_unit_exclusions is not None:
        kwargs["AddOrganizationalUnitExclusions"] = add_organizational_unit_exclusions
    if remove_organizational_unit_exclusions is not None:
        kwargs["RemoveOrganizationalUnitExclusions"] = remove_organizational_unit_exclusions
    try:
        resp = await client.call("ModifyIpamResourceDiscovery", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify ipam resource discovery") from exc
    return ModifyIpamResourceDiscoveryResult(
        ipam_resource_discovery=resp.get("IpamResourceDiscovery"),
    )


async def modify_ipam_scope(
    ipam_scope_id: str,
    *,
    description: str | None = None,
    external_authority_configuration: dict[str, Any] | None = None,
    remove_external_authority_configuration: bool | None = None,
    region_name: str | None = None,
) -> ModifyIpamScopeResult:
    """Modify ipam scope.

    Args:
        ipam_scope_id: Ipam scope id.
        description: Description.
        external_authority_configuration: External authority configuration.
        remove_external_authority_configuration: Remove external authority configuration.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamScopeId"] = ipam_scope_id
    if description is not None:
        kwargs["Description"] = description
    if external_authority_configuration is not None:
        kwargs["ExternalAuthorityConfiguration"] = external_authority_configuration
    if remove_external_authority_configuration is not None:
        kwargs["RemoveExternalAuthorityConfiguration"] = remove_external_authority_configuration
    try:
        resp = await client.call("ModifyIpamScope", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify ipam scope") from exc
    return ModifyIpamScopeResult(
        ipam_scope=resp.get("IpamScope"),
    )


async def modify_launch_template(
    *,
    client_token: str | None = None,
    launch_template_id: str | None = None,
    launch_template_name: str | None = None,
    default_version: str | None = None,
    region_name: str | None = None,
) -> ModifyLaunchTemplateResult:
    """Modify launch template.

    Args:
        client_token: Client token.
        launch_template_id: Launch template id.
        launch_template_name: Launch template name.
        default_version: Default version.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if launch_template_id is not None:
        kwargs["LaunchTemplateId"] = launch_template_id
    if launch_template_name is not None:
        kwargs["LaunchTemplateName"] = launch_template_name
    if default_version is not None:
        kwargs["DefaultVersion"] = default_version
    try:
        resp = await client.call("ModifyLaunchTemplate", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify launch template") from exc
    return ModifyLaunchTemplateResult(
        launch_template=resp.get("LaunchTemplate"),
    )


async def modify_local_gateway_route(
    local_gateway_route_table_id: str,
    *,
    destination_cidr_block: str | None = None,
    local_gateway_virtual_interface_group_id: str | None = None,
    network_interface_id: str | None = None,
    destination_prefix_list_id: str | None = None,
    region_name: str | None = None,
) -> ModifyLocalGatewayRouteResult:
    """Modify local gateway route.

    Args:
        local_gateway_route_table_id: Local gateway route table id.
        destination_cidr_block: Destination cidr block.
        local_gateway_virtual_interface_group_id: Local gateway virtual interface group id.
        network_interface_id: Network interface id.
        destination_prefix_list_id: Destination prefix list id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayRouteTableId"] = local_gateway_route_table_id
    if destination_cidr_block is not None:
        kwargs["DestinationCidrBlock"] = destination_cidr_block
    if local_gateway_virtual_interface_group_id is not None:
        kwargs["LocalGatewayVirtualInterfaceGroupId"] = local_gateway_virtual_interface_group_id
    if network_interface_id is not None:
        kwargs["NetworkInterfaceId"] = network_interface_id
    if destination_prefix_list_id is not None:
        kwargs["DestinationPrefixListId"] = destination_prefix_list_id
    try:
        resp = await client.call("ModifyLocalGatewayRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify local gateway route") from exc
    return ModifyLocalGatewayRouteResult(
        route=resp.get("Route"),
    )


async def modify_managed_prefix_list(
    prefix_list_id: str,
    *,
    current_version: int | None = None,
    prefix_list_name: str | None = None,
    add_entries: list[dict[str, Any]] | None = None,
    remove_entries: list[dict[str, Any]] | None = None,
    max_entries: int | None = None,
    ipam_prefix_list_resolver_sync_enabled: bool | None = None,
    region_name: str | None = None,
) -> ModifyManagedPrefixListResult:
    """Modify managed prefix list.

    Args:
        prefix_list_id: Prefix list id.
        current_version: Current version.
        prefix_list_name: Prefix list name.
        add_entries: Add entries.
        remove_entries: Remove entries.
        max_entries: Max entries.
        ipam_prefix_list_resolver_sync_enabled: Ipam prefix list resolver sync enabled.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PrefixListId"] = prefix_list_id
    if current_version is not None:
        kwargs["CurrentVersion"] = current_version
    if prefix_list_name is not None:
        kwargs["PrefixListName"] = prefix_list_name
    if add_entries is not None:
        kwargs["AddEntries"] = add_entries
    if remove_entries is not None:
        kwargs["RemoveEntries"] = remove_entries
    if max_entries is not None:
        kwargs["MaxEntries"] = max_entries
    if ipam_prefix_list_resolver_sync_enabled is not None:
        kwargs["IpamPrefixListResolverSyncEnabled"] = ipam_prefix_list_resolver_sync_enabled
    try:
        resp = await client.call("ModifyManagedPrefixList", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify managed prefix list") from exc
    return ModifyManagedPrefixListResult(
        prefix_list=resp.get("PrefixList"),
    )


async def modify_network_interface_attribute(
    network_interface_id: str,
    *,
    ena_srd_specification: dict[str, Any] | None = None,
    enable_primary_ipv6: bool | None = None,
    connection_tracking_specification: dict[str, Any] | None = None,
    associate_public_ip_address: bool | None = None,
    associated_subnet_ids: list[str] | None = None,
    description: dict[str, Any] | None = None,
    source_dest_check: dict[str, Any] | None = None,
    groups: list[str] | None = None,
    attachment: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> None:
    """Modify network interface attribute.

    Args:
        network_interface_id: Network interface id.
        ena_srd_specification: Ena srd specification.
        enable_primary_ipv6: Enable primary ipv6.
        connection_tracking_specification: Connection tracking specification.
        associate_public_ip_address: Associate public ip address.
        associated_subnet_ids: Associated subnet ids.
        description: Description.
        source_dest_check: Source dest check.
        groups: Groups.
        attachment: Attachment.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    if ena_srd_specification is not None:
        kwargs["EnaSrdSpecification"] = ena_srd_specification
    if enable_primary_ipv6 is not None:
        kwargs["EnablePrimaryIpv6"] = enable_primary_ipv6
    if connection_tracking_specification is not None:
        kwargs["ConnectionTrackingSpecification"] = connection_tracking_specification
    if associate_public_ip_address is not None:
        kwargs["AssociatePublicIpAddress"] = associate_public_ip_address
    if associated_subnet_ids is not None:
        kwargs["AssociatedSubnetIds"] = associated_subnet_ids
    if description is not None:
        kwargs["Description"] = description
    if source_dest_check is not None:
        kwargs["SourceDestCheck"] = source_dest_check
    if groups is not None:
        kwargs["Groups"] = groups
    if attachment is not None:
        kwargs["Attachment"] = attachment
    try:
        await client.call("ModifyNetworkInterfaceAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify network interface attribute") from exc
    return None


async def modify_private_dns_name_options(
    instance_id: str,
    *,
    private_dns_hostname_type: str | None = None,
    enable_resource_name_dns_a_record: bool | None = None,
    enable_resource_name_dns_aaaa_record: bool | None = None,
    region_name: str | None = None,
) -> ModifyPrivateDnsNameOptionsResult:
    """Modify private dns name options.

    Args:
        instance_id: Instance id.
        private_dns_hostname_type: Private dns hostname type.
        enable_resource_name_dns_a_record: Enable resource name dns a record.
        enable_resource_name_dns_aaaa_record: Enable resource name dns aaaa record.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    if private_dns_hostname_type is not None:
        kwargs["PrivateDnsHostnameType"] = private_dns_hostname_type
    if enable_resource_name_dns_a_record is not None:
        kwargs["EnableResourceNameDnsARecord"] = enable_resource_name_dns_a_record
    if enable_resource_name_dns_aaaa_record is not None:
        kwargs["EnableResourceNameDnsAAAARecord"] = enable_resource_name_dns_aaaa_record
    try:
        resp = await client.call("ModifyPrivateDnsNameOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify private dns name options") from exc
    return ModifyPrivateDnsNameOptionsResult(
        return_value=resp.get("Return"),
    )


async def modify_public_ip_dns_name_options(
    network_interface_id: str,
    hostname_type: str,
    region_name: str | None = None,
) -> ModifyPublicIpDnsNameOptionsResult:
    """Modify public ip dns name options.

    Args:
        network_interface_id: Network interface id.
        hostname_type: Hostname type.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    kwargs["HostnameType"] = hostname_type
    try:
        resp = await client.call("ModifyPublicIpDnsNameOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify public ip dns name options") from exc
    return ModifyPublicIpDnsNameOptionsResult(
        successful=resp.get("Successful"),
    )


async def modify_reserved_instances(
    reserved_instances_ids: list[str],
    target_configurations: list[dict[str, Any]],
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> ModifyReservedInstancesResult:
    """Modify reserved instances.

    Args:
        reserved_instances_ids: Reserved instances ids.
        target_configurations: Target configurations.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ReservedInstancesIds"] = reserved_instances_ids
    kwargs["TargetConfigurations"] = target_configurations
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("ModifyReservedInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify reserved instances") from exc
    return ModifyReservedInstancesResult(
        reserved_instances_modification_id=resp.get("ReservedInstancesModificationId"),
    )


async def modify_route_server(
    route_server_id: str,
    *,
    persist_routes: str | None = None,
    persist_routes_duration: int | None = None,
    sns_notifications_enabled: bool | None = None,
    region_name: str | None = None,
) -> ModifyRouteServerResult:
    """Modify route server.

    Args:
        route_server_id: Route server id.
        persist_routes: Persist routes.
        persist_routes_duration: Persist routes duration.
        sns_notifications_enabled: Sns notifications enabled.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteServerId"] = route_server_id
    if persist_routes is not None:
        kwargs["PersistRoutes"] = persist_routes
    if persist_routes_duration is not None:
        kwargs["PersistRoutesDuration"] = persist_routes_duration
    if sns_notifications_enabled is not None:
        kwargs["SnsNotificationsEnabled"] = sns_notifications_enabled
    try:
        resp = await client.call("ModifyRouteServer", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify route server") from exc
    return ModifyRouteServerResult(
        route_server=resp.get("RouteServer"),
    )


async def modify_security_group_rules(
    group_id: str,
    security_group_rules: list[dict[str, Any]],
    region_name: str | None = None,
) -> ModifySecurityGroupRulesResult:
    """Modify security group rules.

    Args:
        group_id: Group id.
        security_group_rules: Security group rules.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["GroupId"] = group_id
    kwargs["SecurityGroupRules"] = security_group_rules
    try:
        resp = await client.call("ModifySecurityGroupRules", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify security group rules") from exc
    return ModifySecurityGroupRulesResult(
        return_value=resp.get("Return"),
    )


async def modify_snapshot_attribute(
    snapshot_id: str,
    *,
    attribute: str | None = None,
    create_volume_permission: dict[str, Any] | None = None,
    group_names: list[str] | None = None,
    operation_type: str | None = None,
    user_ids: list[str] | None = None,
    region_name: str | None = None,
) -> None:
    """Modify snapshot attribute.

    Args:
        snapshot_id: Snapshot id.
        attribute: Attribute.
        create_volume_permission: Create volume permission.
        group_names: Group names.
        operation_type: Operation type.
        user_ids: User ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SnapshotId"] = snapshot_id
    if attribute is not None:
        kwargs["Attribute"] = attribute
    if create_volume_permission is not None:
        kwargs["CreateVolumePermission"] = create_volume_permission
    if group_names is not None:
        kwargs["GroupNames"] = group_names
    if operation_type is not None:
        kwargs["OperationType"] = operation_type
    if user_ids is not None:
        kwargs["UserIds"] = user_ids
    try:
        await client.call("ModifySnapshotAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify snapshot attribute") from exc
    return None


async def modify_snapshot_tier(
    snapshot_id: str,
    *,
    storage_tier: str | None = None,
    region_name: str | None = None,
) -> ModifySnapshotTierResult:
    """Modify snapshot tier.

    Args:
        snapshot_id: Snapshot id.
        storage_tier: Storage tier.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SnapshotId"] = snapshot_id
    if storage_tier is not None:
        kwargs["StorageTier"] = storage_tier
    try:
        resp = await client.call("ModifySnapshotTier", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify snapshot tier") from exc
    return ModifySnapshotTierResult(
        snapshot_id=resp.get("SnapshotId"),
        tiering_start_time=resp.get("TieringStartTime"),
    )


async def modify_spot_fleet_request(
    spot_fleet_request_id: str,
    *,
    launch_template_configs: list[dict[str, Any]] | None = None,
    on_demand_target_capacity: int | None = None,
    context: str | None = None,
    target_capacity: int | None = None,
    excess_capacity_termination_policy: str | None = None,
    region_name: str | None = None,
) -> ModifySpotFleetRequestResult:
    """Modify spot fleet request.

    Args:
        spot_fleet_request_id: Spot fleet request id.
        launch_template_configs: Launch template configs.
        on_demand_target_capacity: On demand target capacity.
        context: Context.
        target_capacity: Target capacity.
        excess_capacity_termination_policy: Excess capacity termination policy.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SpotFleetRequestId"] = spot_fleet_request_id
    if launch_template_configs is not None:
        kwargs["LaunchTemplateConfigs"] = launch_template_configs
    if on_demand_target_capacity is not None:
        kwargs["OnDemandTargetCapacity"] = on_demand_target_capacity
    if context is not None:
        kwargs["Context"] = context
    if target_capacity is not None:
        kwargs["TargetCapacity"] = target_capacity
    if excess_capacity_termination_policy is not None:
        kwargs["ExcessCapacityTerminationPolicy"] = excess_capacity_termination_policy
    try:
        resp = await client.call("ModifySpotFleetRequest", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify spot fleet request") from exc
    return ModifySpotFleetRequestResult(
        return_value=resp.get("Return"),
    )


async def modify_subnet_attribute(
    subnet_id: str,
    *,
    assign_ipv6_address_on_creation: dict[str, Any] | None = None,
    map_public_ip_on_launch: dict[str, Any] | None = None,
    map_customer_owned_ip_on_launch: dict[str, Any] | None = None,
    customer_owned_ipv4_pool: str | None = None,
    enable_dns64: dict[str, Any] | None = None,
    private_dns_hostname_type_on_launch: str | None = None,
    enable_resource_name_dns_a_record_on_launch: dict[str, Any] | None = None,
    enable_resource_name_dns_aaaa_record_on_launch: dict[str, Any] | None = None,
    enable_lni_at_device_index: int | None = None,
    disable_lni_at_device_index: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> None:
    """Modify subnet attribute.

    Args:
        subnet_id: Subnet id.
        assign_ipv6_address_on_creation: Assign ipv6 address on creation.
        map_public_ip_on_launch: Map public ip on launch.
        map_customer_owned_ip_on_launch: Map customer owned ip on launch.
        customer_owned_ipv4_pool: Customer owned ipv4 pool.
        enable_dns64: Enable dns64.
        private_dns_hostname_type_on_launch: Private dns hostname type on launch.
        enable_resource_name_dns_a_record_on_launch: Enable resource name dns a record on launch.
        enable_resource_name_dns_aaaa_record_on_launch: Enable resource name dns aaaa record on launch.
        enable_lni_at_device_index: Enable lni at device index.
        disable_lni_at_device_index: Disable lni at device index.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SubnetId"] = subnet_id
    if assign_ipv6_address_on_creation is not None:
        kwargs["AssignIpv6AddressOnCreation"] = assign_ipv6_address_on_creation
    if map_public_ip_on_launch is not None:
        kwargs["MapPublicIpOnLaunch"] = map_public_ip_on_launch
    if map_customer_owned_ip_on_launch is not None:
        kwargs["MapCustomerOwnedIpOnLaunch"] = map_customer_owned_ip_on_launch
    if customer_owned_ipv4_pool is not None:
        kwargs["CustomerOwnedIpv4Pool"] = customer_owned_ipv4_pool
    if enable_dns64 is not None:
        kwargs["EnableDns64"] = enable_dns64
    if private_dns_hostname_type_on_launch is not None:
        kwargs["PrivateDnsHostnameTypeOnLaunch"] = private_dns_hostname_type_on_launch
    if enable_resource_name_dns_a_record_on_launch is not None:
        kwargs["EnableResourceNameDnsARecordOnLaunch"] = enable_resource_name_dns_a_record_on_launch
    if enable_resource_name_dns_aaaa_record_on_launch is not None:
        kwargs["EnableResourceNameDnsAAAARecordOnLaunch"] = (
            enable_resource_name_dns_aaaa_record_on_launch
        )
    if enable_lni_at_device_index is not None:
        kwargs["EnableLniAtDeviceIndex"] = enable_lni_at_device_index
    if disable_lni_at_device_index is not None:
        kwargs["DisableLniAtDeviceIndex"] = disable_lni_at_device_index
    try:
        await client.call("ModifySubnetAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify subnet attribute") from exc
    return None


async def modify_traffic_mirror_filter_network_services(
    traffic_mirror_filter_id: str,
    *,
    add_network_services: list[str] | None = None,
    remove_network_services: list[str] | None = None,
    region_name: str | None = None,
) -> ModifyTrafficMirrorFilterNetworkServicesResult:
    """Modify traffic mirror filter network services.

    Args:
        traffic_mirror_filter_id: Traffic mirror filter id.
        add_network_services: Add network services.
        remove_network_services: Remove network services.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TrafficMirrorFilterId"] = traffic_mirror_filter_id
    if add_network_services is not None:
        kwargs["AddNetworkServices"] = add_network_services
    if remove_network_services is not None:
        kwargs["RemoveNetworkServices"] = remove_network_services
    try:
        resp = await client.call("ModifyTrafficMirrorFilterNetworkServices", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to modify traffic mirror filter network services"
        ) from exc
    return ModifyTrafficMirrorFilterNetworkServicesResult(
        traffic_mirror_filter=resp.get("TrafficMirrorFilter"),
    )


async def modify_traffic_mirror_filter_rule(
    traffic_mirror_filter_rule_id: str,
    *,
    traffic_direction: str | None = None,
    rule_number: int | None = None,
    rule_action: str | None = None,
    destination_port_range: dict[str, Any] | None = None,
    source_port_range: dict[str, Any] | None = None,
    protocol: int | None = None,
    destination_cidr_block: str | None = None,
    source_cidr_block: str | None = None,
    description: str | None = None,
    remove_fields: list[str] | None = None,
    region_name: str | None = None,
) -> ModifyTrafficMirrorFilterRuleResult:
    """Modify traffic mirror filter rule.

    Args:
        traffic_mirror_filter_rule_id: Traffic mirror filter rule id.
        traffic_direction: Traffic direction.
        rule_number: Rule number.
        rule_action: Rule action.
        destination_port_range: Destination port range.
        source_port_range: Source port range.
        protocol: Protocol.
        destination_cidr_block: Destination cidr block.
        source_cidr_block: Source cidr block.
        description: Description.
        remove_fields: Remove fields.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TrafficMirrorFilterRuleId"] = traffic_mirror_filter_rule_id
    if traffic_direction is not None:
        kwargs["TrafficDirection"] = traffic_direction
    if rule_number is not None:
        kwargs["RuleNumber"] = rule_number
    if rule_action is not None:
        kwargs["RuleAction"] = rule_action
    if destination_port_range is not None:
        kwargs["DestinationPortRange"] = destination_port_range
    if source_port_range is not None:
        kwargs["SourcePortRange"] = source_port_range
    if protocol is not None:
        kwargs["Protocol"] = protocol
    if destination_cidr_block is not None:
        kwargs["DestinationCidrBlock"] = destination_cidr_block
    if source_cidr_block is not None:
        kwargs["SourceCidrBlock"] = source_cidr_block
    if description is not None:
        kwargs["Description"] = description
    if remove_fields is not None:
        kwargs["RemoveFields"] = remove_fields
    try:
        resp = await client.call("ModifyTrafficMirrorFilterRule", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify traffic mirror filter rule") from exc
    return ModifyTrafficMirrorFilterRuleResult(
        traffic_mirror_filter_rule=resp.get("TrafficMirrorFilterRule"),
    )


async def modify_traffic_mirror_session(
    traffic_mirror_session_id: str,
    *,
    traffic_mirror_target_id: str | None = None,
    traffic_mirror_filter_id: str | None = None,
    packet_length: int | None = None,
    session_number: int | None = None,
    virtual_network_id: int | None = None,
    description: str | None = None,
    remove_fields: list[str] | None = None,
    region_name: str | None = None,
) -> ModifyTrafficMirrorSessionResult:
    """Modify traffic mirror session.

    Args:
        traffic_mirror_session_id: Traffic mirror session id.
        traffic_mirror_target_id: Traffic mirror target id.
        traffic_mirror_filter_id: Traffic mirror filter id.
        packet_length: Packet length.
        session_number: Session number.
        virtual_network_id: Virtual network id.
        description: Description.
        remove_fields: Remove fields.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TrafficMirrorSessionId"] = traffic_mirror_session_id
    if traffic_mirror_target_id is not None:
        kwargs["TrafficMirrorTargetId"] = traffic_mirror_target_id
    if traffic_mirror_filter_id is not None:
        kwargs["TrafficMirrorFilterId"] = traffic_mirror_filter_id
    if packet_length is not None:
        kwargs["PacketLength"] = packet_length
    if session_number is not None:
        kwargs["SessionNumber"] = session_number
    if virtual_network_id is not None:
        kwargs["VirtualNetworkId"] = virtual_network_id
    if description is not None:
        kwargs["Description"] = description
    if remove_fields is not None:
        kwargs["RemoveFields"] = remove_fields
    try:
        resp = await client.call("ModifyTrafficMirrorSession", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify traffic mirror session") from exc
    return ModifyTrafficMirrorSessionResult(
        traffic_mirror_session=resp.get("TrafficMirrorSession"),
    )


async def modify_transit_gateway(
    transit_gateway_id: str,
    *,
    description: str | None = None,
    options: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> ModifyTransitGatewayResult:
    """Modify transit gateway.

    Args:
        transit_gateway_id: Transit gateway id.
        description: Description.
        options: Options.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayId"] = transit_gateway_id
    if description is not None:
        kwargs["Description"] = description
    if options is not None:
        kwargs["Options"] = options
    try:
        resp = await client.call("ModifyTransitGateway", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify transit gateway") from exc
    return ModifyTransitGatewayResult(
        transit_gateway=resp.get("TransitGateway"),
    )


async def modify_transit_gateway_prefix_list_reference(
    transit_gateway_route_table_id: str,
    prefix_list_id: str,
    *,
    transit_gateway_attachment_id: str | None = None,
    blackhole: bool | None = None,
    region_name: str | None = None,
) -> ModifyTransitGatewayPrefixListReferenceResult:
    """Modify transit gateway prefix list reference.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        prefix_list_id: Prefix list id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        blackhole: Blackhole.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    kwargs["PrefixListId"] = prefix_list_id
    if transit_gateway_attachment_id is not None:
        kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    if blackhole is not None:
        kwargs["Blackhole"] = blackhole
    try:
        resp = await client.call("ModifyTransitGatewayPrefixListReference", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify transit gateway prefix list reference") from exc
    return ModifyTransitGatewayPrefixListReferenceResult(
        transit_gateway_prefix_list_reference=resp.get("TransitGatewayPrefixListReference"),
    )


async def modify_transit_gateway_vpc_attachment(
    transit_gateway_attachment_id: str,
    *,
    add_subnet_ids: list[str] | None = None,
    remove_subnet_ids: list[str] | None = None,
    options: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> ModifyTransitGatewayVpcAttachmentResult:
    """Modify transit gateway vpc attachment.

    Args:
        transit_gateway_attachment_id: Transit gateway attachment id.
        add_subnet_ids: Add subnet ids.
        remove_subnet_ids: Remove subnet ids.
        options: Options.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    if add_subnet_ids is not None:
        kwargs["AddSubnetIds"] = add_subnet_ids
    if remove_subnet_ids is not None:
        kwargs["RemoveSubnetIds"] = remove_subnet_ids
    if options is not None:
        kwargs["Options"] = options
    try:
        resp = await client.call("ModifyTransitGatewayVpcAttachment", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify transit gateway vpc attachment") from exc
    return ModifyTransitGatewayVpcAttachmentResult(
        transit_gateway_vpc_attachment=resp.get("TransitGatewayVpcAttachment"),
    )


async def modify_verified_access_endpoint(
    verified_access_endpoint_id: str,
    *,
    verified_access_group_id: str | None = None,
    load_balancer_options: dict[str, Any] | None = None,
    network_interface_options: dict[str, Any] | None = None,
    description: str | None = None,
    client_token: str | None = None,
    rds_options: dict[str, Any] | None = None,
    cidr_options: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> ModifyVerifiedAccessEndpointResult:
    """Modify verified access endpoint.

    Args:
        verified_access_endpoint_id: Verified access endpoint id.
        verified_access_group_id: Verified access group id.
        load_balancer_options: Load balancer options.
        network_interface_options: Network interface options.
        description: Description.
        client_token: Client token.
        rds_options: Rds options.
        cidr_options: Cidr options.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessEndpointId"] = verified_access_endpoint_id
    if verified_access_group_id is not None:
        kwargs["VerifiedAccessGroupId"] = verified_access_group_id
    if load_balancer_options is not None:
        kwargs["LoadBalancerOptions"] = load_balancer_options
    if network_interface_options is not None:
        kwargs["NetworkInterfaceOptions"] = network_interface_options
    if description is not None:
        kwargs["Description"] = description
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if rds_options is not None:
        kwargs["RdsOptions"] = rds_options
    if cidr_options is not None:
        kwargs["CidrOptions"] = cidr_options
    try:
        resp = await client.call("ModifyVerifiedAccessEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify verified access endpoint") from exc
    return ModifyVerifiedAccessEndpointResult(
        verified_access_endpoint=resp.get("VerifiedAccessEndpoint"),
    )


async def modify_verified_access_endpoint_policy(
    verified_access_endpoint_id: str,
    *,
    policy_enabled: bool | None = None,
    policy_document: str | None = None,
    client_token: str | None = None,
    sse_specification: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> ModifyVerifiedAccessEndpointPolicyResult:
    """Modify verified access endpoint policy.

    Args:
        verified_access_endpoint_id: Verified access endpoint id.
        policy_enabled: Policy enabled.
        policy_document: Policy document.
        client_token: Client token.
        sse_specification: Sse specification.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessEndpointId"] = verified_access_endpoint_id
    if policy_enabled is not None:
        kwargs["PolicyEnabled"] = policy_enabled
    if policy_document is not None:
        kwargs["PolicyDocument"] = policy_document
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if sse_specification is not None:
        kwargs["SseSpecification"] = sse_specification
    try:
        resp = await client.call("ModifyVerifiedAccessEndpointPolicy", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify verified access endpoint policy") from exc
    return ModifyVerifiedAccessEndpointPolicyResult(
        policy_enabled=resp.get("PolicyEnabled"),
        policy_document=resp.get("PolicyDocument"),
        sse_specification=resp.get("SseSpecification"),
    )


async def modify_verified_access_group(
    verified_access_group_id: str,
    *,
    verified_access_instance_id: str | None = None,
    description: str | None = None,
    client_token: str | None = None,
    region_name: str | None = None,
) -> ModifyVerifiedAccessGroupResult:
    """Modify verified access group.

    Args:
        verified_access_group_id: Verified access group id.
        verified_access_instance_id: Verified access instance id.
        description: Description.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessGroupId"] = verified_access_group_id
    if verified_access_instance_id is not None:
        kwargs["VerifiedAccessInstanceId"] = verified_access_instance_id
    if description is not None:
        kwargs["Description"] = description
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("ModifyVerifiedAccessGroup", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify verified access group") from exc
    return ModifyVerifiedAccessGroupResult(
        verified_access_group=resp.get("VerifiedAccessGroup"),
    )


async def modify_verified_access_group_policy(
    verified_access_group_id: str,
    *,
    policy_enabled: bool | None = None,
    policy_document: str | None = None,
    client_token: str | None = None,
    sse_specification: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> ModifyVerifiedAccessGroupPolicyResult:
    """Modify verified access group policy.

    Args:
        verified_access_group_id: Verified access group id.
        policy_enabled: Policy enabled.
        policy_document: Policy document.
        client_token: Client token.
        sse_specification: Sse specification.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessGroupId"] = verified_access_group_id
    if policy_enabled is not None:
        kwargs["PolicyEnabled"] = policy_enabled
    if policy_document is not None:
        kwargs["PolicyDocument"] = policy_document
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if sse_specification is not None:
        kwargs["SseSpecification"] = sse_specification
    try:
        resp = await client.call("ModifyVerifiedAccessGroupPolicy", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify verified access group policy") from exc
    return ModifyVerifiedAccessGroupPolicyResult(
        policy_enabled=resp.get("PolicyEnabled"),
        policy_document=resp.get("PolicyDocument"),
        sse_specification=resp.get("SseSpecification"),
    )


async def modify_verified_access_instance(
    verified_access_instance_id: str,
    *,
    description: str | None = None,
    client_token: str | None = None,
    cidr_endpoints_custom_sub_domain: str | None = None,
    region_name: str | None = None,
) -> ModifyVerifiedAccessInstanceResult:
    """Modify verified access instance.

    Args:
        verified_access_instance_id: Verified access instance id.
        description: Description.
        client_token: Client token.
        cidr_endpoints_custom_sub_domain: Cidr endpoints custom sub domain.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessInstanceId"] = verified_access_instance_id
    if description is not None:
        kwargs["Description"] = description
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if cidr_endpoints_custom_sub_domain is not None:
        kwargs["CidrEndpointsCustomSubDomain"] = cidr_endpoints_custom_sub_domain
    try:
        resp = await client.call("ModifyVerifiedAccessInstance", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify verified access instance") from exc
    return ModifyVerifiedAccessInstanceResult(
        verified_access_instance=resp.get("VerifiedAccessInstance"),
    )


async def modify_verified_access_instance_logging_configuration(
    verified_access_instance_id: str,
    access_logs: dict[str, Any],
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> ModifyVerifiedAccessInstanceLoggingConfigurationResult:
    """Modify verified access instance logging configuration.

    Args:
        verified_access_instance_id: Verified access instance id.
        access_logs: Access logs.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessInstanceId"] = verified_access_instance_id
    kwargs["AccessLogs"] = access_logs
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("ModifyVerifiedAccessInstanceLoggingConfiguration", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to modify verified access instance logging configuration"
        ) from exc
    return ModifyVerifiedAccessInstanceLoggingConfigurationResult(
        logging_configuration=resp.get("LoggingConfiguration"),
    )


async def modify_verified_access_trust_provider(
    verified_access_trust_provider_id: str,
    *,
    oidc_options: dict[str, Any] | None = None,
    device_options: dict[str, Any] | None = None,
    description: str | None = None,
    client_token: str | None = None,
    sse_specification: dict[str, Any] | None = None,
    native_application_oidc_options: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> ModifyVerifiedAccessTrustProviderResult:
    """Modify verified access trust provider.

    Args:
        verified_access_trust_provider_id: Verified access trust provider id.
        oidc_options: Oidc options.
        device_options: Device options.
        description: Description.
        client_token: Client token.
        sse_specification: Sse specification.
        native_application_oidc_options: Native application oidc options.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VerifiedAccessTrustProviderId"] = verified_access_trust_provider_id
    if oidc_options is not None:
        kwargs["OidcOptions"] = oidc_options
    if device_options is not None:
        kwargs["DeviceOptions"] = device_options
    if description is not None:
        kwargs["Description"] = description
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if sse_specification is not None:
        kwargs["SseSpecification"] = sse_specification
    if native_application_oidc_options is not None:
        kwargs["NativeApplicationOidcOptions"] = native_application_oidc_options
    try:
        resp = await client.call("ModifyVerifiedAccessTrustProvider", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify verified access trust provider") from exc
    return ModifyVerifiedAccessTrustProviderResult(
        verified_access_trust_provider=resp.get("VerifiedAccessTrustProvider"),
    )


async def modify_volume(
    volume_id: str,
    *,
    size: int | None = None,
    volume_type: str | None = None,
    iops: int | None = None,
    throughput: int | None = None,
    multi_attach_enabled: bool | None = None,
    region_name: str | None = None,
) -> ModifyVolumeResult:
    """Modify volume.

    Args:
        volume_id: Volume id.
        size: Size.
        volume_type: Volume type.
        iops: Iops.
        throughput: Throughput.
        multi_attach_enabled: Multi attach enabled.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VolumeId"] = volume_id
    if size is not None:
        kwargs["Size"] = size
    if volume_type is not None:
        kwargs["VolumeType"] = volume_type
    if iops is not None:
        kwargs["Iops"] = iops
    if throughput is not None:
        kwargs["Throughput"] = throughput
    if multi_attach_enabled is not None:
        kwargs["MultiAttachEnabled"] = multi_attach_enabled
    try:
        resp = await client.call("ModifyVolume", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify volume") from exc
    return ModifyVolumeResult(
        volume_modification=resp.get("VolumeModification"),
    )


async def modify_volume_attribute(
    volume_id: str,
    *,
    auto_enable_io: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> None:
    """Modify volume attribute.

    Args:
        volume_id: Volume id.
        auto_enable_io: Auto enable io.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VolumeId"] = volume_id
    if auto_enable_io is not None:
        kwargs["AutoEnableIO"] = auto_enable_io
    try:
        await client.call("ModifyVolumeAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify volume attribute") from exc
    return None


async def modify_vpc_attribute(
    vpc_id: str,
    *,
    enable_dns_hostnames: dict[str, Any] | None = None,
    enable_dns_support: dict[str, Any] | None = None,
    enable_network_address_usage_metrics: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> None:
    """Modify vpc attribute.

    Args:
        vpc_id: Vpc id.
        enable_dns_hostnames: Enable dns hostnames.
        enable_dns_support: Enable dns support.
        enable_network_address_usage_metrics: Enable network address usage metrics.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    if enable_dns_hostnames is not None:
        kwargs["EnableDnsHostnames"] = enable_dns_hostnames
    if enable_dns_support is not None:
        kwargs["EnableDnsSupport"] = enable_dns_support
    if enable_network_address_usage_metrics is not None:
        kwargs["EnableNetworkAddressUsageMetrics"] = enable_network_address_usage_metrics
    try:
        await client.call("ModifyVpcAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpc attribute") from exc
    return None


async def modify_vpc_block_public_access_exclusion(
    exclusion_id: str,
    internet_gateway_exclusion_mode: str,
    region_name: str | None = None,
) -> ModifyVpcBlockPublicAccessExclusionResult:
    """Modify vpc block public access exclusion.

    Args:
        exclusion_id: Exclusion id.
        internet_gateway_exclusion_mode: Internet gateway exclusion mode.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ExclusionId"] = exclusion_id
    kwargs["InternetGatewayExclusionMode"] = internet_gateway_exclusion_mode
    try:
        resp = await client.call("ModifyVpcBlockPublicAccessExclusion", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpc block public access exclusion") from exc
    return ModifyVpcBlockPublicAccessExclusionResult(
        vpc_block_public_access_exclusion=resp.get("VpcBlockPublicAccessExclusion"),
    )


async def modify_vpc_block_public_access_options(
    internet_gateway_block_mode: str,
    region_name: str | None = None,
) -> ModifyVpcBlockPublicAccessOptionsResult:
    """Modify vpc block public access options.

    Args:
        internet_gateway_block_mode: Internet gateway block mode.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InternetGatewayBlockMode"] = internet_gateway_block_mode
    try:
        resp = await client.call("ModifyVpcBlockPublicAccessOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpc block public access options") from exc
    return ModifyVpcBlockPublicAccessOptionsResult(
        vpc_block_public_access_options=resp.get("VpcBlockPublicAccessOptions"),
    )


async def modify_vpc_endpoint(
    vpc_endpoint_id: str,
    *,
    reset_policy: bool | None = None,
    policy_document: str | None = None,
    add_route_table_ids: list[str] | None = None,
    remove_route_table_ids: list[str] | None = None,
    add_subnet_ids: list[str] | None = None,
    remove_subnet_ids: list[str] | None = None,
    add_security_group_ids: list[str] | None = None,
    remove_security_group_ids: list[str] | None = None,
    ip_address_type: str | None = None,
    dns_options: dict[str, Any] | None = None,
    private_dns_enabled: bool | None = None,
    subnet_configurations: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> ModifyVpcEndpointResult:
    """Modify vpc endpoint.

    Args:
        vpc_endpoint_id: Vpc endpoint id.
        reset_policy: Reset policy.
        policy_document: Policy document.
        add_route_table_ids: Add route table ids.
        remove_route_table_ids: Remove route table ids.
        add_subnet_ids: Add subnet ids.
        remove_subnet_ids: Remove subnet ids.
        add_security_group_ids: Add security group ids.
        remove_security_group_ids: Remove security group ids.
        ip_address_type: Ip address type.
        dns_options: Dns options.
        private_dns_enabled: Private dns enabled.
        subnet_configurations: Subnet configurations.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcEndpointId"] = vpc_endpoint_id
    if reset_policy is not None:
        kwargs["ResetPolicy"] = reset_policy
    if policy_document is not None:
        kwargs["PolicyDocument"] = policy_document
    if add_route_table_ids is not None:
        kwargs["AddRouteTableIds"] = add_route_table_ids
    if remove_route_table_ids is not None:
        kwargs["RemoveRouteTableIds"] = remove_route_table_ids
    if add_subnet_ids is not None:
        kwargs["AddSubnetIds"] = add_subnet_ids
    if remove_subnet_ids is not None:
        kwargs["RemoveSubnetIds"] = remove_subnet_ids
    if add_security_group_ids is not None:
        kwargs["AddSecurityGroupIds"] = add_security_group_ids
    if remove_security_group_ids is not None:
        kwargs["RemoveSecurityGroupIds"] = remove_security_group_ids
    if ip_address_type is not None:
        kwargs["IpAddressType"] = ip_address_type
    if dns_options is not None:
        kwargs["DnsOptions"] = dns_options
    if private_dns_enabled is not None:
        kwargs["PrivateDnsEnabled"] = private_dns_enabled
    if subnet_configurations is not None:
        kwargs["SubnetConfigurations"] = subnet_configurations
    try:
        resp = await client.call("ModifyVpcEndpoint", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpc endpoint") from exc
    return ModifyVpcEndpointResult(
        return_value=resp.get("Return"),
    )


async def modify_vpc_endpoint_connection_notification(
    connection_notification_id: str,
    *,
    connection_notification_arn: str | None = None,
    connection_events: list[str] | None = None,
    region_name: str | None = None,
) -> ModifyVpcEndpointConnectionNotificationResult:
    """Modify vpc endpoint connection notification.

    Args:
        connection_notification_id: Connection notification id.
        connection_notification_arn: Connection notification arn.
        connection_events: Connection events.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ConnectionNotificationId"] = connection_notification_id
    if connection_notification_arn is not None:
        kwargs["ConnectionNotificationArn"] = connection_notification_arn
    if connection_events is not None:
        kwargs["ConnectionEvents"] = connection_events
    try:
        resp = await client.call("ModifyVpcEndpointConnectionNotification", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpc endpoint connection notification") from exc
    return ModifyVpcEndpointConnectionNotificationResult(
        return_value=resp.get("ReturnValue"),
    )


async def modify_vpc_endpoint_service_configuration(
    service_id: str,
    *,
    private_dns_name: str | None = None,
    remove_private_dns_name: bool | None = None,
    acceptance_required: bool | None = None,
    add_network_load_balancer_arns: list[str] | None = None,
    remove_network_load_balancer_arns: list[str] | None = None,
    add_gateway_load_balancer_arns: list[str] | None = None,
    remove_gateway_load_balancer_arns: list[str] | None = None,
    add_supported_ip_address_types: list[str] | None = None,
    remove_supported_ip_address_types: list[str] | None = None,
    add_supported_regions: list[str] | None = None,
    remove_supported_regions: list[str] | None = None,
    region_name: str | None = None,
) -> ModifyVpcEndpointServiceConfigurationResult:
    """Modify vpc endpoint service configuration.

    Args:
        service_id: Service id.
        private_dns_name: Private dns name.
        remove_private_dns_name: Remove private dns name.
        acceptance_required: Acceptance required.
        add_network_load_balancer_arns: Add network load balancer arns.
        remove_network_load_balancer_arns: Remove network load balancer arns.
        add_gateway_load_balancer_arns: Add gateway load balancer arns.
        remove_gateway_load_balancer_arns: Remove gateway load balancer arns.
        add_supported_ip_address_types: Add supported ip address types.
        remove_supported_ip_address_types: Remove supported ip address types.
        add_supported_regions: Add supported regions.
        remove_supported_regions: Remove supported regions.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ServiceId"] = service_id
    if private_dns_name is not None:
        kwargs["PrivateDnsName"] = private_dns_name
    if remove_private_dns_name is not None:
        kwargs["RemovePrivateDnsName"] = remove_private_dns_name
    if acceptance_required is not None:
        kwargs["AcceptanceRequired"] = acceptance_required
    if add_network_load_balancer_arns is not None:
        kwargs["AddNetworkLoadBalancerArns"] = add_network_load_balancer_arns
    if remove_network_load_balancer_arns is not None:
        kwargs["RemoveNetworkLoadBalancerArns"] = remove_network_load_balancer_arns
    if add_gateway_load_balancer_arns is not None:
        kwargs["AddGatewayLoadBalancerArns"] = add_gateway_load_balancer_arns
    if remove_gateway_load_balancer_arns is not None:
        kwargs["RemoveGatewayLoadBalancerArns"] = remove_gateway_load_balancer_arns
    if add_supported_ip_address_types is not None:
        kwargs["AddSupportedIpAddressTypes"] = add_supported_ip_address_types
    if remove_supported_ip_address_types is not None:
        kwargs["RemoveSupportedIpAddressTypes"] = remove_supported_ip_address_types
    if add_supported_regions is not None:
        kwargs["AddSupportedRegions"] = add_supported_regions
    if remove_supported_regions is not None:
        kwargs["RemoveSupportedRegions"] = remove_supported_regions
    try:
        resp = await client.call("ModifyVpcEndpointServiceConfiguration", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpc endpoint service configuration") from exc
    return ModifyVpcEndpointServiceConfigurationResult(
        return_value=resp.get("Return"),
    )


async def modify_vpc_endpoint_service_payer_responsibility(
    service_id: str,
    payer_responsibility: str,
    region_name: str | None = None,
) -> ModifyVpcEndpointServicePayerResponsibilityResult:
    """Modify vpc endpoint service payer responsibility.

    Args:
        service_id: Service id.
        payer_responsibility: Payer responsibility.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ServiceId"] = service_id
    kwargs["PayerResponsibility"] = payer_responsibility
    try:
        resp = await client.call("ModifyVpcEndpointServicePayerResponsibility", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to modify vpc endpoint service payer responsibility"
        ) from exc
    return ModifyVpcEndpointServicePayerResponsibilityResult(
        return_value=resp.get("ReturnValue"),
    )


async def modify_vpc_endpoint_service_permissions(
    service_id: str,
    *,
    add_allowed_principals: list[str] | None = None,
    remove_allowed_principals: list[str] | None = None,
    region_name: str | None = None,
) -> ModifyVpcEndpointServicePermissionsResult:
    """Modify vpc endpoint service permissions.

    Args:
        service_id: Service id.
        add_allowed_principals: Add allowed principals.
        remove_allowed_principals: Remove allowed principals.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ServiceId"] = service_id
    if add_allowed_principals is not None:
        kwargs["AddAllowedPrincipals"] = add_allowed_principals
    if remove_allowed_principals is not None:
        kwargs["RemoveAllowedPrincipals"] = remove_allowed_principals
    try:
        resp = await client.call("ModifyVpcEndpointServicePermissions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpc endpoint service permissions") from exc
    return ModifyVpcEndpointServicePermissionsResult(
        added_principals=resp.get("AddedPrincipals"),
        return_value=resp.get("ReturnValue"),
    )


async def modify_vpc_peering_connection_options(
    vpc_peering_connection_id: str,
    *,
    accepter_peering_connection_options: dict[str, Any] | None = None,
    requester_peering_connection_options: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> ModifyVpcPeeringConnectionOptionsResult:
    """Modify vpc peering connection options.

    Args:
        vpc_peering_connection_id: Vpc peering connection id.
        accepter_peering_connection_options: Accepter peering connection options.
        requester_peering_connection_options: Requester peering connection options.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcPeeringConnectionId"] = vpc_peering_connection_id
    if accepter_peering_connection_options is not None:
        kwargs["AccepterPeeringConnectionOptions"] = accepter_peering_connection_options
    if requester_peering_connection_options is not None:
        kwargs["RequesterPeeringConnectionOptions"] = requester_peering_connection_options
    try:
        resp = await client.call("ModifyVpcPeeringConnectionOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpc peering connection options") from exc
    return ModifyVpcPeeringConnectionOptionsResult(
        accepter_peering_connection_options=resp.get("AccepterPeeringConnectionOptions"),
        requester_peering_connection_options=resp.get("RequesterPeeringConnectionOptions"),
    )


async def modify_vpc_tenancy(
    vpc_id: str,
    instance_tenancy: str,
    region_name: str | None = None,
) -> ModifyVpcTenancyResult:
    """Modify vpc tenancy.

    Args:
        vpc_id: Vpc id.
        instance_tenancy: Instance tenancy.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcId"] = vpc_id
    kwargs["InstanceTenancy"] = instance_tenancy
    try:
        resp = await client.call("ModifyVpcTenancy", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpc tenancy") from exc
    return ModifyVpcTenancyResult(
        return_value=resp.get("ReturnValue"),
    )


async def modify_vpn_connection(
    vpn_connection_id: str,
    *,
    transit_gateway_id: str | None = None,
    customer_gateway_id: str | None = None,
    vpn_gateway_id: str | None = None,
    region_name: str | None = None,
) -> ModifyVpnConnectionResult:
    """Modify vpn connection.

    Args:
        vpn_connection_id: Vpn connection id.
        transit_gateway_id: Transit gateway id.
        customer_gateway_id: Customer gateway id.
        vpn_gateway_id: Vpn gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpnConnectionId"] = vpn_connection_id
    if transit_gateway_id is not None:
        kwargs["TransitGatewayId"] = transit_gateway_id
    if customer_gateway_id is not None:
        kwargs["CustomerGatewayId"] = customer_gateway_id
    if vpn_gateway_id is not None:
        kwargs["VpnGatewayId"] = vpn_gateway_id
    try:
        resp = await client.call("ModifyVpnConnection", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpn connection") from exc
    return ModifyVpnConnectionResult(
        vpn_connection=resp.get("VpnConnection"),
    )


async def modify_vpn_connection_options(
    vpn_connection_id: str,
    *,
    local_ipv4_network_cidr: str | None = None,
    remote_ipv4_network_cidr: str | None = None,
    local_ipv6_network_cidr: str | None = None,
    remote_ipv6_network_cidr: str | None = None,
    region_name: str | None = None,
) -> ModifyVpnConnectionOptionsResult:
    """Modify vpn connection options.

    Args:
        vpn_connection_id: Vpn connection id.
        local_ipv4_network_cidr: Local ipv4 network cidr.
        remote_ipv4_network_cidr: Remote ipv4 network cidr.
        local_ipv6_network_cidr: Local ipv6 network cidr.
        remote_ipv6_network_cidr: Remote ipv6 network cidr.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpnConnectionId"] = vpn_connection_id
    if local_ipv4_network_cidr is not None:
        kwargs["LocalIpv4NetworkCidr"] = local_ipv4_network_cidr
    if remote_ipv4_network_cidr is not None:
        kwargs["RemoteIpv4NetworkCidr"] = remote_ipv4_network_cidr
    if local_ipv6_network_cidr is not None:
        kwargs["LocalIpv6NetworkCidr"] = local_ipv6_network_cidr
    if remote_ipv6_network_cidr is not None:
        kwargs["RemoteIpv6NetworkCidr"] = remote_ipv6_network_cidr
    try:
        resp = await client.call("ModifyVpnConnectionOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpn connection options") from exc
    return ModifyVpnConnectionOptionsResult(
        vpn_connection=resp.get("VpnConnection"),
    )


async def modify_vpn_tunnel_certificate(
    vpn_connection_id: str,
    vpn_tunnel_outside_ip_address: str,
    region_name: str | None = None,
) -> ModifyVpnTunnelCertificateResult:
    """Modify vpn tunnel certificate.

    Args:
        vpn_connection_id: Vpn connection id.
        vpn_tunnel_outside_ip_address: Vpn tunnel outside ip address.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpnConnectionId"] = vpn_connection_id
    kwargs["VpnTunnelOutsideIpAddress"] = vpn_tunnel_outside_ip_address
    try:
        resp = await client.call("ModifyVpnTunnelCertificate", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpn tunnel certificate") from exc
    return ModifyVpnTunnelCertificateResult(
        vpn_connection=resp.get("VpnConnection"),
    )


async def modify_vpn_tunnel_options(
    vpn_connection_id: str,
    vpn_tunnel_outside_ip_address: str,
    tunnel_options: dict[str, Any],
    *,
    skip_tunnel_replacement: bool | None = None,
    pre_shared_key_storage: str | None = None,
    region_name: str | None = None,
) -> ModifyVpnTunnelOptionsResult:
    """Modify vpn tunnel options.

    Args:
        vpn_connection_id: Vpn connection id.
        vpn_tunnel_outside_ip_address: Vpn tunnel outside ip address.
        tunnel_options: Tunnel options.
        skip_tunnel_replacement: Skip tunnel replacement.
        pre_shared_key_storage: Pre shared key storage.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpnConnectionId"] = vpn_connection_id
    kwargs["VpnTunnelOutsideIpAddress"] = vpn_tunnel_outside_ip_address
    kwargs["TunnelOptions"] = tunnel_options
    if skip_tunnel_replacement is not None:
        kwargs["SkipTunnelReplacement"] = skip_tunnel_replacement
    if pre_shared_key_storage is not None:
        kwargs["PreSharedKeyStorage"] = pre_shared_key_storage
    try:
        resp = await client.call("ModifyVpnTunnelOptions", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to modify vpn tunnel options") from exc
    return ModifyVpnTunnelOptionsResult(
        vpn_connection=resp.get("VpnConnection"),
    )


async def monitor_instances(
    instance_ids: list[str],
    region_name: str | None = None,
) -> MonitorInstancesResult:
    """Monitor instances.

    Args:
        instance_ids: Instance ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceIds"] = instance_ids
    try:
        resp = await client.call("MonitorInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to monitor instances") from exc
    return MonitorInstancesResult(
        instance_monitorings=resp.get("InstanceMonitorings"),
    )


async def move_address_to_vpc(
    public_ip: str,
    region_name: str | None = None,
) -> MoveAddressToVpcResult:
    """Move address to vpc.

    Args:
        public_ip: Public ip.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PublicIp"] = public_ip
    try:
        resp = await client.call("MoveAddressToVpc", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to move address to vpc") from exc
    return MoveAddressToVpcResult(
        allocation_id=resp.get("AllocationId"),
        status=resp.get("Status"),
    )


async def move_byoip_cidr_to_ipam(
    cidr: str,
    ipam_pool_id: str,
    ipam_pool_owner: str,
    region_name: str | None = None,
) -> MoveByoipCidrToIpamResult:
    """Move byoip cidr to ipam.

    Args:
        cidr: Cidr.
        ipam_pool_id: Ipam pool id.
        ipam_pool_owner: Ipam pool owner.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Cidr"] = cidr
    kwargs["IpamPoolId"] = ipam_pool_id
    kwargs["IpamPoolOwner"] = ipam_pool_owner
    try:
        resp = await client.call("MoveByoipCidrToIpam", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to move byoip cidr to ipam") from exc
    return MoveByoipCidrToIpamResult(
        byoip_cidr=resp.get("ByoipCidr"),
    )


async def move_capacity_reservation_instances(
    source_capacity_reservation_id: str,
    destination_capacity_reservation_id: str,
    instance_count: int,
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> MoveCapacityReservationInstancesResult:
    """Move capacity reservation instances.

    Args:
        source_capacity_reservation_id: Source capacity reservation id.
        destination_capacity_reservation_id: Destination capacity reservation id.
        instance_count: Instance count.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SourceCapacityReservationId"] = source_capacity_reservation_id
    kwargs["DestinationCapacityReservationId"] = destination_capacity_reservation_id
    kwargs["InstanceCount"] = instance_count
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("MoveCapacityReservationInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to move capacity reservation instances") from exc
    return MoveCapacityReservationInstancesResult(
        source_capacity_reservation=resp.get("SourceCapacityReservation"),
        destination_capacity_reservation=resp.get("DestinationCapacityReservation"),
        instance_count=resp.get("InstanceCount"),
    )


async def provision_byoip_cidr(
    cidr: str,
    *,
    cidr_authorization_context: dict[str, Any] | None = None,
    publicly_advertisable: bool | None = None,
    description: str | None = None,
    pool_tag_specifications: list[dict[str, Any]] | None = None,
    multi_region: bool | None = None,
    network_border_group: str | None = None,
    region_name: str | None = None,
) -> ProvisionByoipCidrResult:
    """Provision byoip cidr.

    Args:
        cidr: Cidr.
        cidr_authorization_context: Cidr authorization context.
        publicly_advertisable: Publicly advertisable.
        description: Description.
        pool_tag_specifications: Pool tag specifications.
        multi_region: Multi region.
        network_border_group: Network border group.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Cidr"] = cidr
    if cidr_authorization_context is not None:
        kwargs["CidrAuthorizationContext"] = cidr_authorization_context
    if publicly_advertisable is not None:
        kwargs["PubliclyAdvertisable"] = publicly_advertisable
    if description is not None:
        kwargs["Description"] = description
    if pool_tag_specifications is not None:
        kwargs["PoolTagSpecifications"] = pool_tag_specifications
    if multi_region is not None:
        kwargs["MultiRegion"] = multi_region
    if network_border_group is not None:
        kwargs["NetworkBorderGroup"] = network_border_group
    try:
        resp = await client.call("ProvisionByoipCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to provision byoip cidr") from exc
    return ProvisionByoipCidrResult(
        byoip_cidr=resp.get("ByoipCidr"),
    )


async def provision_ipam_byoasn(
    ipam_id: str,
    asn: str,
    asn_authorization_context: dict[str, Any],
    region_name: str | None = None,
) -> ProvisionIpamByoasnResult:
    """Provision ipam byoasn.

    Args:
        ipam_id: Ipam id.
        asn: Asn.
        asn_authorization_context: Asn authorization context.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamId"] = ipam_id
    kwargs["Asn"] = asn
    kwargs["AsnAuthorizationContext"] = asn_authorization_context
    try:
        resp = await client.call("ProvisionIpamByoasn", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to provision ipam byoasn") from exc
    return ProvisionIpamByoasnResult(
        byoasn=resp.get("Byoasn"),
    )


async def provision_ipam_pool_cidr(
    ipam_pool_id: str,
    *,
    cidr: str | None = None,
    cidr_authorization_context: dict[str, Any] | None = None,
    netmask_length: int | None = None,
    client_token: str | None = None,
    verification_method: str | None = None,
    ipam_external_resource_verification_token_id: str | None = None,
    region_name: str | None = None,
) -> ProvisionIpamPoolCidrResult:
    """Provision ipam pool cidr.

    Args:
        ipam_pool_id: Ipam pool id.
        cidr: Cidr.
        cidr_authorization_context: Cidr authorization context.
        netmask_length: Netmask length.
        client_token: Client token.
        verification_method: Verification method.
        ipam_external_resource_verification_token_id: Ipam external resource verification token id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPoolId"] = ipam_pool_id
    if cidr is not None:
        kwargs["Cidr"] = cidr
    if cidr_authorization_context is not None:
        kwargs["CidrAuthorizationContext"] = cidr_authorization_context
    if netmask_length is not None:
        kwargs["NetmaskLength"] = netmask_length
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if verification_method is not None:
        kwargs["VerificationMethod"] = verification_method
    if ipam_external_resource_verification_token_id is not None:
        kwargs["IpamExternalResourceVerificationTokenId"] = (
            ipam_external_resource_verification_token_id
        )
    try:
        resp = await client.call("ProvisionIpamPoolCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to provision ipam pool cidr") from exc
    return ProvisionIpamPoolCidrResult(
        ipam_pool_cidr=resp.get("IpamPoolCidr"),
    )


async def provision_public_ipv4_pool_cidr(
    ipam_pool_id: str,
    pool_id: str,
    netmask_length: int,
    *,
    network_border_group: str | None = None,
    region_name: str | None = None,
) -> ProvisionPublicIpv4PoolCidrResult:
    """Provision public ipv4 pool cidr.

    Args:
        ipam_pool_id: Ipam pool id.
        pool_id: Pool id.
        netmask_length: Netmask length.
        network_border_group: Network border group.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPoolId"] = ipam_pool_id
    kwargs["PoolId"] = pool_id
    kwargs["NetmaskLength"] = netmask_length
    if network_border_group is not None:
        kwargs["NetworkBorderGroup"] = network_border_group
    try:
        resp = await client.call("ProvisionPublicIpv4PoolCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to provision public ipv4 pool cidr") from exc
    return ProvisionPublicIpv4PoolCidrResult(
        pool_id=resp.get("PoolId"),
        pool_address_range=resp.get("PoolAddressRange"),
    )


async def purchase_capacity_block(
    capacity_block_offering_id: str,
    instance_platform: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> PurchaseCapacityBlockResult:
    """Purchase capacity block.

    Args:
        capacity_block_offering_id: Capacity block offering id.
        instance_platform: Instance platform.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityBlockOfferingId"] = capacity_block_offering_id
    kwargs["InstancePlatform"] = instance_platform
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("PurchaseCapacityBlock", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to purchase capacity block") from exc
    return PurchaseCapacityBlockResult(
        capacity_reservation=resp.get("CapacityReservation"),
        capacity_blocks=resp.get("CapacityBlocks"),
    )


async def purchase_capacity_block_extension(
    capacity_block_extension_offering_id: str,
    capacity_reservation_id: str,
    region_name: str | None = None,
) -> PurchaseCapacityBlockExtensionResult:
    """Purchase capacity block extension.

    Args:
        capacity_block_extension_offering_id: Capacity block extension offering id.
        capacity_reservation_id: Capacity reservation id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityBlockExtensionOfferingId"] = capacity_block_extension_offering_id
    kwargs["CapacityReservationId"] = capacity_reservation_id
    try:
        resp = await client.call("PurchaseCapacityBlockExtension", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to purchase capacity block extension") from exc
    return PurchaseCapacityBlockExtensionResult(
        capacity_block_extensions=resp.get("CapacityBlockExtensions"),
    )


async def purchase_host_reservation(
    host_id_set: list[str],
    offering_id: str,
    *,
    client_token: str | None = None,
    currency_code: str | None = None,
    limit_price: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> PurchaseHostReservationResult:
    """Purchase host reservation.

    Args:
        host_id_set: Host id set.
        offering_id: Offering id.
        client_token: Client token.
        currency_code: Currency code.
        limit_price: Limit price.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["HostIdSet"] = host_id_set
    kwargs["OfferingId"] = offering_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if currency_code is not None:
        kwargs["CurrencyCode"] = currency_code
    if limit_price is not None:
        kwargs["LimitPrice"] = limit_price
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("PurchaseHostReservation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to purchase host reservation") from exc
    return PurchaseHostReservationResult(
        client_token=resp.get("ClientToken"),
        currency_code=resp.get("CurrencyCode"),
        purchase=resp.get("Purchase"),
        total_hourly_price=resp.get("TotalHourlyPrice"),
        total_upfront_price=resp.get("TotalUpfrontPrice"),
    )


async def purchase_reserved_instances_offering(
    instance_count: int,
    reserved_instances_offering_id: str,
    *,
    purchase_time: str | None = None,
    limit_price: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> PurchaseReservedInstancesOfferingResult:
    """Purchase reserved instances offering.

    Args:
        instance_count: Instance count.
        reserved_instances_offering_id: Reserved instances offering id.
        purchase_time: Purchase time.
        limit_price: Limit price.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceCount"] = instance_count
    kwargs["ReservedInstancesOfferingId"] = reserved_instances_offering_id
    if purchase_time is not None:
        kwargs["PurchaseTime"] = purchase_time
    if limit_price is not None:
        kwargs["LimitPrice"] = limit_price
    try:
        resp = await client.call("PurchaseReservedInstancesOffering", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to purchase reserved instances offering") from exc
    return PurchaseReservedInstancesOfferingResult(
        reserved_instances_id=resp.get("ReservedInstancesId"),
    )


async def purchase_scheduled_instances(
    purchase_requests: list[dict[str, Any]],
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> PurchaseScheduledInstancesResult:
    """Purchase scheduled instances.

    Args:
        purchase_requests: Purchase requests.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PurchaseRequests"] = purchase_requests
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("PurchaseScheduledInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to purchase scheduled instances") from exc
    return PurchaseScheduledInstancesResult(
        scheduled_instance_set=resp.get("ScheduledInstanceSet"),
    )


async def register_image(
    name: str,
    *,
    image_location: str | None = None,
    billing_products: list[str] | None = None,
    boot_mode: str | None = None,
    tpm_support: str | None = None,
    uefi_data: str | None = None,
    imds_support: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    description: str | None = None,
    architecture: str | None = None,
    kernel_id: str | None = None,
    ramdisk_id: str | None = None,
    root_device_name: str | None = None,
    block_device_mappings: list[dict[str, Any]] | None = None,
    virtualization_type: str | None = None,
    sriov_net_support: str | None = None,
    ena_support: bool | None = None,
    region_name: str | None = None,
) -> RegisterImageResult:
    """Register image.

    Args:
        name: Name.
        image_location: Image location.
        billing_products: Billing products.
        boot_mode: Boot mode.
        tpm_support: Tpm support.
        uefi_data: Uefi data.
        imds_support: Imds support.
        tag_specifications: Tag specifications.
        description: Description.
        architecture: Architecture.
        kernel_id: Kernel id.
        ramdisk_id: Ramdisk id.
        root_device_name: Root device name.
        block_device_mappings: Block device mappings.
        virtualization_type: Virtualization type.
        sriov_net_support: Sriov net support.
        ena_support: Ena support.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Name"] = name
    if image_location is not None:
        kwargs["ImageLocation"] = image_location
    if billing_products is not None:
        kwargs["BillingProducts"] = billing_products
    if boot_mode is not None:
        kwargs["BootMode"] = boot_mode
    if tpm_support is not None:
        kwargs["TpmSupport"] = tpm_support
    if uefi_data is not None:
        kwargs["UefiData"] = uefi_data
    if imds_support is not None:
        kwargs["ImdsSupport"] = imds_support
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if description is not None:
        kwargs["Description"] = description
    if architecture is not None:
        kwargs["Architecture"] = architecture
    if kernel_id is not None:
        kwargs["KernelId"] = kernel_id
    if ramdisk_id is not None:
        kwargs["RamdiskId"] = ramdisk_id
    if root_device_name is not None:
        kwargs["RootDeviceName"] = root_device_name
    if block_device_mappings is not None:
        kwargs["BlockDeviceMappings"] = block_device_mappings
    if virtualization_type is not None:
        kwargs["VirtualizationType"] = virtualization_type
    if sriov_net_support is not None:
        kwargs["SriovNetSupport"] = sriov_net_support
    if ena_support is not None:
        kwargs["EnaSupport"] = ena_support
    try:
        resp = await client.call("RegisterImage", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to register image") from exc
    return RegisterImageResult(
        image_id=resp.get("ImageId"),
    )


async def register_instance_event_notification_attributes(
    instance_tag_attribute: dict[str, Any],
    region_name: str | None = None,
) -> RegisterInstanceEventNotificationAttributesResult:
    """Register instance event notification attributes.

    Args:
        instance_tag_attribute: Instance tag attribute.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceTagAttribute"] = instance_tag_attribute
    try:
        resp = await client.call("RegisterInstanceEventNotificationAttributes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to register instance event notification attributes"
        ) from exc
    return RegisterInstanceEventNotificationAttributesResult(
        instance_tag_attribute=resp.get("InstanceTagAttribute"),
    )


async def register_transit_gateway_multicast_group_members(
    transit_gateway_multicast_domain_id: str,
    network_interface_ids: list[str],
    *,
    group_ip_address: str | None = None,
    region_name: str | None = None,
) -> RegisterTransitGatewayMulticastGroupMembersResult:
    """Register transit gateway multicast group members.

    Args:
        transit_gateway_multicast_domain_id: Transit gateway multicast domain id.
        network_interface_ids: Network interface ids.
        group_ip_address: Group ip address.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayMulticastDomainId"] = transit_gateway_multicast_domain_id
    kwargs["NetworkInterfaceIds"] = network_interface_ids
    if group_ip_address is not None:
        kwargs["GroupIpAddress"] = group_ip_address
    try:
        resp = await client.call("RegisterTransitGatewayMulticastGroupMembers", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to register transit gateway multicast group members"
        ) from exc
    return RegisterTransitGatewayMulticastGroupMembersResult(
        registered_multicast_group_members=resp.get("RegisteredMulticastGroupMembers"),
    )


async def register_transit_gateway_multicast_group_sources(
    transit_gateway_multicast_domain_id: str,
    network_interface_ids: list[str],
    *,
    group_ip_address: str | None = None,
    region_name: str | None = None,
) -> RegisterTransitGatewayMulticastGroupSourcesResult:
    """Register transit gateway multicast group sources.

    Args:
        transit_gateway_multicast_domain_id: Transit gateway multicast domain id.
        network_interface_ids: Network interface ids.
        group_ip_address: Group ip address.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayMulticastDomainId"] = transit_gateway_multicast_domain_id
    kwargs["NetworkInterfaceIds"] = network_interface_ids
    if group_ip_address is not None:
        kwargs["GroupIpAddress"] = group_ip_address
    try:
        resp = await client.call("RegisterTransitGatewayMulticastGroupSources", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to register transit gateway multicast group sources"
        ) from exc
    return RegisterTransitGatewayMulticastGroupSourcesResult(
        registered_multicast_group_sources=resp.get("RegisteredMulticastGroupSources"),
    )


async def reject_capacity_reservation_billing_ownership(
    capacity_reservation_id: str,
    region_name: str | None = None,
) -> RejectCapacityReservationBillingOwnershipResult:
    """Reject capacity reservation billing ownership.

    Args:
        capacity_reservation_id: Capacity reservation id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["CapacityReservationId"] = capacity_reservation_id
    try:
        resp = await client.call("RejectCapacityReservationBillingOwnership", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to reject capacity reservation billing ownership"
        ) from exc
    return RejectCapacityReservationBillingOwnershipResult(
        return_value=resp.get("Return"),
    )


async def reject_transit_gateway_multicast_domain_associations(
    *,
    transit_gateway_multicast_domain_id: str | None = None,
    transit_gateway_attachment_id: str | None = None,
    subnet_ids: list[str] | None = None,
    region_name: str | None = None,
) -> RejectTransitGatewayMulticastDomainAssociationsResult:
    """Reject transit gateway multicast domain associations.

    Args:
        transit_gateway_multicast_domain_id: Transit gateway multicast domain id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        subnet_ids: Subnet ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if transit_gateway_multicast_domain_id is not None:
        kwargs["TransitGatewayMulticastDomainId"] = transit_gateway_multicast_domain_id
    if transit_gateway_attachment_id is not None:
        kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    if subnet_ids is not None:
        kwargs["SubnetIds"] = subnet_ids
    try:
        resp = await client.call("RejectTransitGatewayMulticastDomainAssociations", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to reject transit gateway multicast domain associations"
        ) from exc
    return RejectTransitGatewayMulticastDomainAssociationsResult(
        associations=resp.get("Associations"),
    )


async def reject_transit_gateway_peering_attachment(
    transit_gateway_attachment_id: str,
    region_name: str | None = None,
) -> RejectTransitGatewayPeeringAttachmentResult:
    """Reject transit gateway peering attachment.

    Args:
        transit_gateway_attachment_id: Transit gateway attachment id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    try:
        resp = await client.call("RejectTransitGatewayPeeringAttachment", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to reject transit gateway peering attachment") from exc
    return RejectTransitGatewayPeeringAttachmentResult(
        transit_gateway_peering_attachment=resp.get("TransitGatewayPeeringAttachment"),
    )


async def reject_transit_gateway_vpc_attachment(
    transit_gateway_attachment_id: str,
    region_name: str | None = None,
) -> RejectTransitGatewayVpcAttachmentResult:
    """Reject transit gateway vpc attachment.

    Args:
        transit_gateway_attachment_id: Transit gateway attachment id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    try:
        resp = await client.call("RejectTransitGatewayVpcAttachment", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to reject transit gateway vpc attachment") from exc
    return RejectTransitGatewayVpcAttachmentResult(
        transit_gateway_vpc_attachment=resp.get("TransitGatewayVpcAttachment"),
    )


async def reject_vpc_endpoint_connections(
    service_id: str,
    vpc_endpoint_ids: list[str],
    region_name: str | None = None,
) -> RejectVpcEndpointConnectionsResult:
    """Reject vpc endpoint connections.

    Args:
        service_id: Service id.
        vpc_endpoint_ids: Vpc endpoint ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ServiceId"] = service_id
    kwargs["VpcEndpointIds"] = vpc_endpoint_ids
    try:
        resp = await client.call("RejectVpcEndpointConnections", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to reject vpc endpoint connections") from exc
    return RejectVpcEndpointConnectionsResult(
        unsuccessful=resp.get("Unsuccessful"),
    )


async def reject_vpc_peering_connection(
    vpc_peering_connection_id: str,
    region_name: str | None = None,
) -> RejectVpcPeeringConnectionResult:
    """Reject vpc peering connection.

    Args:
        vpc_peering_connection_id: Vpc peering connection id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpcPeeringConnectionId"] = vpc_peering_connection_id
    try:
        resp = await client.call("RejectVpcPeeringConnection", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to reject vpc peering connection") from exc
    return RejectVpcPeeringConnectionResult(
        return_value=resp.get("Return"),
    )


async def release_address(
    *,
    allocation_id: str | None = None,
    public_ip: str | None = None,
    network_border_group: str | None = None,
    region_name: str | None = None,
) -> None:
    """Release address.

    Args:
        allocation_id: Allocation id.
        public_ip: Public ip.
        network_border_group: Network border group.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if allocation_id is not None:
        kwargs["AllocationId"] = allocation_id
    if public_ip is not None:
        kwargs["PublicIp"] = public_ip
    if network_border_group is not None:
        kwargs["NetworkBorderGroup"] = network_border_group
    try:
        await client.call("ReleaseAddress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to release address") from exc
    return None


async def release_hosts(
    host_ids: list[str],
    region_name: str | None = None,
) -> ReleaseHostsResult:
    """Release hosts.

    Args:
        host_ids: Host ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["HostIds"] = host_ids
    try:
        resp = await client.call("ReleaseHosts", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to release hosts") from exc
    return ReleaseHostsResult(
        successful=resp.get("Successful"),
        unsuccessful=resp.get("Unsuccessful"),
    )


async def release_ipam_pool_allocation(
    ipam_pool_id: str,
    cidr: str,
    ipam_pool_allocation_id: str,
    region_name: str | None = None,
) -> ReleaseIpamPoolAllocationResult:
    """Release ipam pool allocation.

    Args:
        ipam_pool_id: Ipam pool id.
        cidr: Cidr.
        ipam_pool_allocation_id: Ipam pool allocation id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IpamPoolId"] = ipam_pool_id
    kwargs["Cidr"] = cidr
    kwargs["IpamPoolAllocationId"] = ipam_pool_allocation_id
    try:
        resp = await client.call("ReleaseIpamPoolAllocation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to release ipam pool allocation") from exc
    return ReleaseIpamPoolAllocationResult(
        success=resp.get("Success"),
    )


async def replace_iam_instance_profile_association(
    iam_instance_profile: dict[str, Any],
    association_id: str,
    region_name: str | None = None,
) -> ReplaceIamInstanceProfileAssociationResult:
    """Replace iam instance profile association.

    Args:
        iam_instance_profile: Iam instance profile.
        association_id: Association id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["IamInstanceProfile"] = iam_instance_profile
    kwargs["AssociationId"] = association_id
    try:
        resp = await client.call("ReplaceIamInstanceProfileAssociation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to replace iam instance profile association") from exc
    return ReplaceIamInstanceProfileAssociationResult(
        iam_instance_profile_association=resp.get("IamInstanceProfileAssociation"),
    )


async def replace_image_criteria_in_allowed_images_settings(
    *,
    image_criteria: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> ReplaceImageCriteriaInAllowedImagesSettingsResult:
    """Replace image criteria in allowed images settings.

    Args:
        image_criteria: Image criteria.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if image_criteria is not None:
        kwargs["ImageCriteria"] = image_criteria
    try:
        resp = await client.call("ReplaceImageCriteriaInAllowedImagesSettings", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to replace image criteria in allowed images settings"
        ) from exc
    return ReplaceImageCriteriaInAllowedImagesSettingsResult(
        return_value=resp.get("ReturnValue"),
    )


async def replace_network_acl_association(
    association_id: str,
    network_acl_id: str,
    region_name: str | None = None,
) -> ReplaceNetworkAclAssociationResult:
    """Replace network acl association.

    Args:
        association_id: Association id.
        network_acl_id: Network acl id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AssociationId"] = association_id
    kwargs["NetworkAclId"] = network_acl_id
    try:
        resp = await client.call("ReplaceNetworkAclAssociation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to replace network acl association") from exc
    return ReplaceNetworkAclAssociationResult(
        new_association_id=resp.get("NewAssociationId"),
    )


async def replace_network_acl_entry(
    network_acl_id: str,
    rule_number: int,
    protocol: str,
    rule_action: str,
    egress: bool,
    *,
    cidr_block: str | None = None,
    ipv6_cidr_block: str | None = None,
    icmp_type_code: dict[str, Any] | None = None,
    port_range: dict[str, Any] | None = None,
    region_name: str | None = None,
) -> None:
    """Replace network acl entry.

    Args:
        network_acl_id: Network acl id.
        rule_number: Rule number.
        protocol: Protocol.
        rule_action: Rule action.
        egress: Egress.
        cidr_block: Cidr block.
        ipv6_cidr_block: Ipv6 cidr block.
        icmp_type_code: Icmp type code.
        port_range: Port range.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkAclId"] = network_acl_id
    kwargs["RuleNumber"] = rule_number
    kwargs["Protocol"] = protocol
    kwargs["RuleAction"] = rule_action
    kwargs["Egress"] = egress
    if cidr_block is not None:
        kwargs["CidrBlock"] = cidr_block
    if ipv6_cidr_block is not None:
        kwargs["Ipv6CidrBlock"] = ipv6_cidr_block
    if icmp_type_code is not None:
        kwargs["IcmpTypeCode"] = icmp_type_code
    if port_range is not None:
        kwargs["PortRange"] = port_range
    try:
        await client.call("ReplaceNetworkAclEntry", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to replace network acl entry") from exc
    return None


async def replace_route(
    route_table_id: str,
    *,
    destination_prefix_list_id: str | None = None,
    vpc_endpoint_id: str | None = None,
    local_target: bool | None = None,
    transit_gateway_id: str | None = None,
    local_gateway_id: str | None = None,
    carrier_gateway_id: str | None = None,
    core_network_arn: str | None = None,
    odb_network_arn: str | None = None,
    destination_cidr_block: str | None = None,
    gateway_id: str | None = None,
    destination_ipv6_cidr_block: str | None = None,
    egress_only_internet_gateway_id: str | None = None,
    instance_id: str | None = None,
    network_interface_id: str | None = None,
    vpc_peering_connection_id: str | None = None,
    nat_gateway_id: str | None = None,
    region_name: str | None = None,
) -> None:
    """Replace route.

    Args:
        route_table_id: Route table id.
        destination_prefix_list_id: Destination prefix list id.
        vpc_endpoint_id: Vpc endpoint id.
        local_target: Local target.
        transit_gateway_id: Transit gateway id.
        local_gateway_id: Local gateway id.
        carrier_gateway_id: Carrier gateway id.
        core_network_arn: Core network arn.
        odb_network_arn: Odb network arn.
        destination_cidr_block: Destination cidr block.
        gateway_id: Gateway id.
        destination_ipv6_cidr_block: Destination ipv6 cidr block.
        egress_only_internet_gateway_id: Egress only internet gateway id.
        instance_id: Instance id.
        network_interface_id: Network interface id.
        vpc_peering_connection_id: Vpc peering connection id.
        nat_gateway_id: Nat gateway id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["RouteTableId"] = route_table_id
    if destination_prefix_list_id is not None:
        kwargs["DestinationPrefixListId"] = destination_prefix_list_id
    if vpc_endpoint_id is not None:
        kwargs["VpcEndpointId"] = vpc_endpoint_id
    if local_target is not None:
        kwargs["LocalTarget"] = local_target
    if transit_gateway_id is not None:
        kwargs["TransitGatewayId"] = transit_gateway_id
    if local_gateway_id is not None:
        kwargs["LocalGatewayId"] = local_gateway_id
    if carrier_gateway_id is not None:
        kwargs["CarrierGatewayId"] = carrier_gateway_id
    if core_network_arn is not None:
        kwargs["CoreNetworkArn"] = core_network_arn
    if odb_network_arn is not None:
        kwargs["OdbNetworkArn"] = odb_network_arn
    if destination_cidr_block is not None:
        kwargs["DestinationCidrBlock"] = destination_cidr_block
    if gateway_id is not None:
        kwargs["GatewayId"] = gateway_id
    if destination_ipv6_cidr_block is not None:
        kwargs["DestinationIpv6CidrBlock"] = destination_ipv6_cidr_block
    if egress_only_internet_gateway_id is not None:
        kwargs["EgressOnlyInternetGatewayId"] = egress_only_internet_gateway_id
    if instance_id is not None:
        kwargs["InstanceId"] = instance_id
    if network_interface_id is not None:
        kwargs["NetworkInterfaceId"] = network_interface_id
    if vpc_peering_connection_id is not None:
        kwargs["VpcPeeringConnectionId"] = vpc_peering_connection_id
    if nat_gateway_id is not None:
        kwargs["NatGatewayId"] = nat_gateway_id
    try:
        await client.call("ReplaceRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to replace route") from exc
    return None


async def replace_route_table_association(
    association_id: str,
    route_table_id: str,
    region_name: str | None = None,
) -> ReplaceRouteTableAssociationResult:
    """Replace route table association.

    Args:
        association_id: Association id.
        route_table_id: Route table id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AssociationId"] = association_id
    kwargs["RouteTableId"] = route_table_id
    try:
        resp = await client.call("ReplaceRouteTableAssociation", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to replace route table association") from exc
    return ReplaceRouteTableAssociationResult(
        new_association_id=resp.get("NewAssociationId"),
        association_state=resp.get("AssociationState"),
    )


async def replace_transit_gateway_route(
    destination_cidr_block: str,
    transit_gateway_route_table_id: str,
    *,
    transit_gateway_attachment_id: str | None = None,
    blackhole: bool | None = None,
    region_name: str | None = None,
) -> ReplaceTransitGatewayRouteResult:
    """Replace transit gateway route.

    Args:
        destination_cidr_block: Destination cidr block.
        transit_gateway_route_table_id: Transit gateway route table id.
        transit_gateway_attachment_id: Transit gateway attachment id.
        blackhole: Blackhole.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["DestinationCidrBlock"] = destination_cidr_block
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    if transit_gateway_attachment_id is not None:
        kwargs["TransitGatewayAttachmentId"] = transit_gateway_attachment_id
    if blackhole is not None:
        kwargs["Blackhole"] = blackhole
    try:
        resp = await client.call("ReplaceTransitGatewayRoute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to replace transit gateway route") from exc
    return ReplaceTransitGatewayRouteResult(
        route=resp.get("Route"),
    )


async def replace_vpn_tunnel(
    vpn_connection_id: str,
    vpn_tunnel_outside_ip_address: str,
    *,
    apply_pending_maintenance: bool | None = None,
    region_name: str | None = None,
) -> ReplaceVpnTunnelResult:
    """Replace vpn tunnel.

    Args:
        vpn_connection_id: Vpn connection id.
        vpn_tunnel_outside_ip_address: Vpn tunnel outside ip address.
        apply_pending_maintenance: Apply pending maintenance.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["VpnConnectionId"] = vpn_connection_id
    kwargs["VpnTunnelOutsideIpAddress"] = vpn_tunnel_outside_ip_address
    if apply_pending_maintenance is not None:
        kwargs["ApplyPendingMaintenance"] = apply_pending_maintenance
    try:
        resp = await client.call("ReplaceVpnTunnel", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to replace vpn tunnel") from exc
    return ReplaceVpnTunnelResult(
        return_value=resp.get("Return"),
    )


async def report_instance_status(
    instances: list[str],
    status: str,
    reason_codes: list[str],
    *,
    start_time: str | None = None,
    end_time: str | None = None,
    description: str | None = None,
    region_name: str | None = None,
) -> None:
    """Report instance status.

    Args:
        instances: Instances.
        status: Status.
        reason_codes: Reason codes.
        start_time: Start time.
        end_time: End time.
        description: Description.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Instances"] = instances
    kwargs["Status"] = status
    kwargs["ReasonCodes"] = reason_codes
    if start_time is not None:
        kwargs["StartTime"] = start_time
    if end_time is not None:
        kwargs["EndTime"] = end_time
    if description is not None:
        kwargs["Description"] = description
    try:
        await client.call("ReportInstanceStatus", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to report instance status") from exc
    return None


async def request_spot_fleet(
    spot_fleet_request_config: dict[str, Any],
    region_name: str | None = None,
) -> RequestSpotFleetResult:
    """Request spot fleet.

    Args:
        spot_fleet_request_config: Spot fleet request config.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SpotFleetRequestConfig"] = spot_fleet_request_config
    try:
        resp = await client.call("RequestSpotFleet", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to request spot fleet") from exc
    return RequestSpotFleetResult(
        spot_fleet_request_id=resp.get("SpotFleetRequestId"),
    )


async def request_spot_instances(
    *,
    launch_specification: dict[str, Any] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    instance_interruption_behavior: str | None = None,
    spot_price: str | None = None,
    client_token: str | None = None,
    instance_count: int | None = None,
    type_value: str | None = None,
    valid_from: str | None = None,
    valid_until: str | None = None,
    launch_group: str | None = None,
    availability_zone_group: str | None = None,
    block_duration_minutes: int | None = None,
    region_name: str | None = None,
) -> RequestSpotInstancesResult:
    """Request spot instances.

    Args:
        launch_specification: Launch specification.
        tag_specifications: Tag specifications.
        instance_interruption_behavior: Instance interruption behavior.
        spot_price: Spot price.
        client_token: Client token.
        instance_count: Instance count.
        type_value: Type value.
        valid_from: Valid from.
        valid_until: Valid until.
        launch_group: Launch group.
        availability_zone_group: Availability zone group.
        block_duration_minutes: Block duration minutes.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if launch_specification is not None:
        kwargs["LaunchSpecification"] = launch_specification
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if instance_interruption_behavior is not None:
        kwargs["InstanceInterruptionBehavior"] = instance_interruption_behavior
    if spot_price is not None:
        kwargs["SpotPrice"] = spot_price
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if instance_count is not None:
        kwargs["InstanceCount"] = instance_count
    if type_value is not None:
        kwargs["Type"] = type_value
    if valid_from is not None:
        kwargs["ValidFrom"] = valid_from
    if valid_until is not None:
        kwargs["ValidUntil"] = valid_until
    if launch_group is not None:
        kwargs["LaunchGroup"] = launch_group
    if availability_zone_group is not None:
        kwargs["AvailabilityZoneGroup"] = availability_zone_group
    if block_duration_minutes is not None:
        kwargs["BlockDurationMinutes"] = block_duration_minutes
    try:
        resp = await client.call("RequestSpotInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to request spot instances") from exc
    return RequestSpotInstancesResult(
        spot_instance_requests=resp.get("SpotInstanceRequests"),
    )


async def reset_address_attribute(
    allocation_id: str,
    attribute: str,
    region_name: str | None = None,
) -> ResetAddressAttributeResult:
    """Reset address attribute.

    Args:
        allocation_id: Allocation id.
        attribute: Attribute.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["AllocationId"] = allocation_id
    kwargs["Attribute"] = attribute
    try:
        resp = await client.call("ResetAddressAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to reset address attribute") from exc
    return ResetAddressAttributeResult(
        address=resp.get("Address"),
    )


async def reset_ebs_default_kms_key_id(
    region_name: str | None = None,
) -> ResetEbsDefaultKmsKeyIdResult:
    """Reset ebs default kms key id.

    Args:

        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}

    try:
        resp = await client.call("ResetEbsDefaultKmsKeyId", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to reset ebs default kms key id") from exc
    return ResetEbsDefaultKmsKeyIdResult(
        kms_key_id=resp.get("KmsKeyId"),
    )


async def reset_fpga_image_attribute(
    fpga_image_id: str,
    *,
    attribute: str | None = None,
    region_name: str | None = None,
) -> ResetFpgaImageAttributeResult:
    """Reset fpga image attribute.

    Args:
        fpga_image_id: Fpga image id.
        attribute: Attribute.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["FpgaImageId"] = fpga_image_id
    if attribute is not None:
        kwargs["Attribute"] = attribute
    try:
        resp = await client.call("ResetFpgaImageAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to reset fpga image attribute") from exc
    return ResetFpgaImageAttributeResult(
        return_value=resp.get("Return"),
    )


async def reset_image_attribute(
    attribute: str,
    image_id: str,
    region_name: str | None = None,
) -> None:
    """Reset image attribute.

    Args:
        attribute: Attribute.
        image_id: Image id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Attribute"] = attribute
    kwargs["ImageId"] = image_id
    try:
        await client.call("ResetImageAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to reset image attribute") from exc
    return None


async def reset_instance_attribute(
    instance_id: str,
    attribute: str,
    region_name: str | None = None,
) -> None:
    """Reset instance attribute.

    Args:
        instance_id: Instance id.
        attribute: Attribute.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    kwargs["Attribute"] = attribute
    try:
        await client.call("ResetInstanceAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to reset instance attribute") from exc
    return None


async def reset_network_interface_attribute(
    network_interface_id: str,
    *,
    source_dest_check: str | None = None,
    region_name: str | None = None,
) -> None:
    """Reset network interface attribute.

    Args:
        network_interface_id: Network interface id.
        source_dest_check: Source dest check.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    if source_dest_check is not None:
        kwargs["SourceDestCheck"] = source_dest_check
    try:
        await client.call("ResetNetworkInterfaceAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to reset network interface attribute") from exc
    return None


async def reset_snapshot_attribute(
    attribute: str,
    snapshot_id: str,
    region_name: str | None = None,
) -> None:
    """Reset snapshot attribute.

    Args:
        attribute: Attribute.
        snapshot_id: Snapshot id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Attribute"] = attribute
    kwargs["SnapshotId"] = snapshot_id
    try:
        await client.call("ResetSnapshotAttribute", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to reset snapshot attribute") from exc
    return None


async def restore_address_to_classic(
    public_ip: str,
    region_name: str | None = None,
) -> RestoreAddressToClassicResult:
    """Restore address to classic.

    Args:
        public_ip: Public ip.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PublicIp"] = public_ip
    try:
        resp = await client.call("RestoreAddressToClassic", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to restore address to classic") from exc
    return RestoreAddressToClassicResult(
        public_ip=resp.get("PublicIp"),
        status=resp.get("Status"),
    )


async def restore_image_from_recycle_bin(
    image_id: str,
    region_name: str | None = None,
) -> RestoreImageFromRecycleBinResult:
    """Restore image from recycle bin.

    Args:
        image_id: Image id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ImageId"] = image_id
    try:
        resp = await client.call("RestoreImageFromRecycleBin", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to restore image from recycle bin") from exc
    return RestoreImageFromRecycleBinResult(
        return_value=resp.get("Return"),
    )


async def restore_managed_prefix_list_version(
    prefix_list_id: str,
    previous_version: int,
    current_version: int,
    region_name: str | None = None,
) -> RestoreManagedPrefixListVersionResult:
    """Restore managed prefix list version.

    Args:
        prefix_list_id: Prefix list id.
        previous_version: Previous version.
        current_version: Current version.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["PrefixListId"] = prefix_list_id
    kwargs["PreviousVersion"] = previous_version
    kwargs["CurrentVersion"] = current_version
    try:
        resp = await client.call("RestoreManagedPrefixListVersion", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to restore managed prefix list version") from exc
    return RestoreManagedPrefixListVersionResult(
        prefix_list=resp.get("PrefixList"),
    )


async def restore_snapshot_from_recycle_bin(
    snapshot_id: str,
    region_name: str | None = None,
) -> RestoreSnapshotFromRecycleBinResult:
    """Restore snapshot from recycle bin.

    Args:
        snapshot_id: Snapshot id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SnapshotId"] = snapshot_id
    try:
        resp = await client.call("RestoreSnapshotFromRecycleBin", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to restore snapshot from recycle bin") from exc
    return RestoreSnapshotFromRecycleBinResult(
        snapshot_id=resp.get("SnapshotId"),
        outpost_arn=resp.get("OutpostArn"),
        description=resp.get("Description"),
        encrypted=resp.get("Encrypted"),
        owner_id=resp.get("OwnerId"),
        progress=resp.get("Progress"),
        start_time=resp.get("StartTime"),
        state=resp.get("State"),
        volume_id=resp.get("VolumeId"),
        volume_size=resp.get("VolumeSize"),
        sse_type=resp.get("SseType"),
    )


async def restore_snapshot_tier(
    snapshot_id: str,
    *,
    temporary_restore_days: int | None = None,
    permanent_restore: bool | None = None,
    region_name: str | None = None,
) -> RestoreSnapshotTierResult:
    """Restore snapshot tier.

    Args:
        snapshot_id: Snapshot id.
        temporary_restore_days: Temporary restore days.
        permanent_restore: Permanent restore.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SnapshotId"] = snapshot_id
    if temporary_restore_days is not None:
        kwargs["TemporaryRestoreDays"] = temporary_restore_days
    if permanent_restore is not None:
        kwargs["PermanentRestore"] = permanent_restore
    try:
        resp = await client.call("RestoreSnapshotTier", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to restore snapshot tier") from exc
    return RestoreSnapshotTierResult(
        snapshot_id=resp.get("SnapshotId"),
        restore_start_time=resp.get("RestoreStartTime"),
        restore_duration=resp.get("RestoreDuration"),
        is_permanent_restore=resp.get("IsPermanentRestore"),
    )


async def revoke_client_vpn_ingress(
    client_vpn_endpoint_id: str,
    target_network_cidr: str,
    *,
    access_group_id: str | None = None,
    revoke_all_groups: bool | None = None,
    region_name: str | None = None,
) -> RevokeClientVpnIngressResult:
    """Revoke client vpn ingress.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        target_network_cidr: Target network cidr.
        access_group_id: Access group id.
        revoke_all_groups: Revoke all groups.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    kwargs["TargetNetworkCidr"] = target_network_cidr
    if access_group_id is not None:
        kwargs["AccessGroupId"] = access_group_id
    if revoke_all_groups is not None:
        kwargs["RevokeAllGroups"] = revoke_all_groups
    try:
        resp = await client.call("RevokeClientVpnIngress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to revoke client vpn ingress") from exc
    return RevokeClientVpnIngressResult(
        status=resp.get("Status"),
    )


async def revoke_security_group_egress(
    group_id: str,
    *,
    security_group_rule_ids: list[str] | None = None,
    source_security_group_name: str | None = None,
    source_security_group_owner_id: str | None = None,
    ip_protocol: str | None = None,
    from_port: int | None = None,
    to_port: int | None = None,
    cidr_ip: str | None = None,
    ip_permissions: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> RevokeSecurityGroupEgressResult:
    """Revoke security group egress.

    Args:
        group_id: Group id.
        security_group_rule_ids: Security group rule ids.
        source_security_group_name: Source security group name.
        source_security_group_owner_id: Source security group owner id.
        ip_protocol: Ip protocol.
        from_port: From port.
        to_port: To port.
        cidr_ip: Cidr ip.
        ip_permissions: Ip permissions.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["GroupId"] = group_id
    if security_group_rule_ids is not None:
        kwargs["SecurityGroupRuleIds"] = security_group_rule_ids
    if source_security_group_name is not None:
        kwargs["SourceSecurityGroupName"] = source_security_group_name
    if source_security_group_owner_id is not None:
        kwargs["SourceSecurityGroupOwnerId"] = source_security_group_owner_id
    if ip_protocol is not None:
        kwargs["IpProtocol"] = ip_protocol
    if from_port is not None:
        kwargs["FromPort"] = from_port
    if to_port is not None:
        kwargs["ToPort"] = to_port
    if cidr_ip is not None:
        kwargs["CidrIp"] = cidr_ip
    if ip_permissions is not None:
        kwargs["IpPermissions"] = ip_permissions
    try:
        resp = await client.call("RevokeSecurityGroupEgress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to revoke security group egress") from exc
    return RevokeSecurityGroupEgressResult(
        return_value=resp.get("Return"),
        unknown_ip_permissions=resp.get("UnknownIpPermissions"),
        revoked_security_group_rules=resp.get("RevokedSecurityGroupRules"),
    )


async def revoke_security_group_ingress(
    *,
    cidr_ip: str | None = None,
    from_port: int | None = None,
    group_id: str | None = None,
    group_name: str | None = None,
    ip_permissions: list[dict[str, Any]] | None = None,
    ip_protocol: str | None = None,
    source_security_group_name: str | None = None,
    source_security_group_owner_id: str | None = None,
    to_port: int | None = None,
    security_group_rule_ids: list[str] | None = None,
    region_name: str | None = None,
) -> RevokeSecurityGroupIngressResult:
    """Revoke security group ingress.

    Args:
        cidr_ip: Cidr ip.
        from_port: From port.
        group_id: Group id.
        group_name: Group name.
        ip_permissions: Ip permissions.
        ip_protocol: Ip protocol.
        source_security_group_name: Source security group name.
        source_security_group_owner_id: Source security group owner id.
        to_port: To port.
        security_group_rule_ids: Security group rule ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if cidr_ip is not None:
        kwargs["CidrIp"] = cidr_ip
    if from_port is not None:
        kwargs["FromPort"] = from_port
    if group_id is not None:
        kwargs["GroupId"] = group_id
    if group_name is not None:
        kwargs["GroupName"] = group_name
    if ip_permissions is not None:
        kwargs["IpPermissions"] = ip_permissions
    if ip_protocol is not None:
        kwargs["IpProtocol"] = ip_protocol
    if source_security_group_name is not None:
        kwargs["SourceSecurityGroupName"] = source_security_group_name
    if source_security_group_owner_id is not None:
        kwargs["SourceSecurityGroupOwnerId"] = source_security_group_owner_id
    if to_port is not None:
        kwargs["ToPort"] = to_port
    if security_group_rule_ids is not None:
        kwargs["SecurityGroupRuleIds"] = security_group_rule_ids
    try:
        resp = await client.call("RevokeSecurityGroupIngress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to revoke security group ingress") from exc
    return RevokeSecurityGroupIngressResult(
        return_value=resp.get("Return"),
        unknown_ip_permissions=resp.get("UnknownIpPermissions"),
        revoked_security_group_rules=resp.get("RevokedSecurityGroupRules"),
    )


async def run_instances(
    max_count: int,
    min_count: int,
    *,
    block_device_mappings: list[dict[str, Any]] | None = None,
    image_id: str | None = None,
    instance_type: str | None = None,
    ipv6_address_count: int | None = None,
    ipv6_addresses: list[dict[str, Any]] | None = None,
    kernel_id: str | None = None,
    key_name: str | None = None,
    monitoring: dict[str, Any] | None = None,
    placement: dict[str, Any] | None = None,
    ramdisk_id: str | None = None,
    security_group_ids: list[str] | None = None,
    security_groups: list[str] | None = None,
    subnet_id: str | None = None,
    user_data: str | None = None,
    elastic_gpu_specification: list[dict[str, Any]] | None = None,
    elastic_inference_accelerators: list[dict[str, Any]] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    launch_template: dict[str, Any] | None = None,
    instance_market_options: dict[str, Any] | None = None,
    credit_specification: dict[str, Any] | None = None,
    cpu_options: dict[str, Any] | None = None,
    capacity_reservation_specification: dict[str, Any] | None = None,
    hibernation_options: dict[str, Any] | None = None,
    license_specifications: list[dict[str, Any]] | None = None,
    metadata_options: dict[str, Any] | None = None,
    enclave_options: dict[str, Any] | None = None,
    private_dns_name_options: dict[str, Any] | None = None,
    maintenance_options: dict[str, Any] | None = None,
    disable_api_stop: bool | None = None,
    enable_primary_ipv6: bool | None = None,
    network_performance_options: dict[str, Any] | None = None,
    operator: dict[str, Any] | None = None,
    disable_api_termination: bool | None = None,
    instance_initiated_shutdown_behavior: str | None = None,
    private_ip_address: str | None = None,
    client_token: str | None = None,
    additional_info: str | None = None,
    network_interfaces: list[dict[str, Any]] | None = None,
    iam_instance_profile: dict[str, Any] | None = None,
    ebs_optimized: bool | None = None,
    region_name: str | None = None,
) -> RunInstancesResult:
    """Run instances.

    Args:
        max_count: Max count.
        min_count: Min count.
        block_device_mappings: Block device mappings.
        image_id: Image id.
        instance_type: Instance type.
        ipv6_address_count: Ipv6 address count.
        ipv6_addresses: Ipv6 addresses.
        kernel_id: Kernel id.
        key_name: Key name.
        monitoring: Monitoring.
        placement: Placement.
        ramdisk_id: Ramdisk id.
        security_group_ids: Security group ids.
        security_groups: Security groups.
        subnet_id: Subnet id.
        user_data: User data.
        elastic_gpu_specification: Elastic gpu specification.
        elastic_inference_accelerators: Elastic inference accelerators.
        tag_specifications: Tag specifications.
        launch_template: Launch template.
        instance_market_options: Instance market options.
        credit_specification: Credit specification.
        cpu_options: Cpu options.
        capacity_reservation_specification: Capacity reservation specification.
        hibernation_options: Hibernation options.
        license_specifications: License specifications.
        metadata_options: Metadata options.
        enclave_options: Enclave options.
        private_dns_name_options: Private dns name options.
        maintenance_options: Maintenance options.
        disable_api_stop: Disable api stop.
        enable_primary_ipv6: Enable primary ipv6.
        network_performance_options: Network performance options.
        operator: Operator.
        disable_api_termination: Disable api termination.
        instance_initiated_shutdown_behavior: Instance initiated shutdown behavior.
        private_ip_address: Private ip address.
        client_token: Client token.
        additional_info: Additional info.
        network_interfaces: Network interfaces.
        iam_instance_profile: Iam instance profile.
        ebs_optimized: Ebs optimized.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["MaxCount"] = max_count
    kwargs["MinCount"] = min_count
    if block_device_mappings is not None:
        kwargs["BlockDeviceMappings"] = block_device_mappings
    if image_id is not None:
        kwargs["ImageId"] = image_id
    if instance_type is not None:
        kwargs["InstanceType"] = instance_type
    if ipv6_address_count is not None:
        kwargs["Ipv6AddressCount"] = ipv6_address_count
    if ipv6_addresses is not None:
        kwargs["Ipv6Addresses"] = ipv6_addresses
    if kernel_id is not None:
        kwargs["KernelId"] = kernel_id
    if key_name is not None:
        kwargs["KeyName"] = key_name
    if monitoring is not None:
        kwargs["Monitoring"] = monitoring
    if placement is not None:
        kwargs["Placement"] = placement
    if ramdisk_id is not None:
        kwargs["RamdiskId"] = ramdisk_id
    if security_group_ids is not None:
        kwargs["SecurityGroupIds"] = security_group_ids
    if security_groups is not None:
        kwargs["SecurityGroups"] = security_groups
    if subnet_id is not None:
        kwargs["SubnetId"] = subnet_id
    if user_data is not None:
        kwargs["UserData"] = user_data
    if elastic_gpu_specification is not None:
        kwargs["ElasticGpuSpecification"] = elastic_gpu_specification
    if elastic_inference_accelerators is not None:
        kwargs["ElasticInferenceAccelerators"] = elastic_inference_accelerators
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    if launch_template is not None:
        kwargs["LaunchTemplate"] = launch_template
    if instance_market_options is not None:
        kwargs["InstanceMarketOptions"] = instance_market_options
    if credit_specification is not None:
        kwargs["CreditSpecification"] = credit_specification
    if cpu_options is not None:
        kwargs["CpuOptions"] = cpu_options
    if capacity_reservation_specification is not None:
        kwargs["CapacityReservationSpecification"] = capacity_reservation_specification
    if hibernation_options is not None:
        kwargs["HibernationOptions"] = hibernation_options
    if license_specifications is not None:
        kwargs["LicenseSpecifications"] = license_specifications
    if metadata_options is not None:
        kwargs["MetadataOptions"] = metadata_options
    if enclave_options is not None:
        kwargs["EnclaveOptions"] = enclave_options
    if private_dns_name_options is not None:
        kwargs["PrivateDnsNameOptions"] = private_dns_name_options
    if maintenance_options is not None:
        kwargs["MaintenanceOptions"] = maintenance_options
    if disable_api_stop is not None:
        kwargs["DisableApiStop"] = disable_api_stop
    if enable_primary_ipv6 is not None:
        kwargs["EnablePrimaryIpv6"] = enable_primary_ipv6
    if network_performance_options is not None:
        kwargs["NetworkPerformanceOptions"] = network_performance_options
    if operator is not None:
        kwargs["Operator"] = operator
    if disable_api_termination is not None:
        kwargs["DisableApiTermination"] = disable_api_termination
    if instance_initiated_shutdown_behavior is not None:
        kwargs["InstanceInitiatedShutdownBehavior"] = instance_initiated_shutdown_behavior
    if private_ip_address is not None:
        kwargs["PrivateIpAddress"] = private_ip_address
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if additional_info is not None:
        kwargs["AdditionalInfo"] = additional_info
    if network_interfaces is not None:
        kwargs["NetworkInterfaces"] = network_interfaces
    if iam_instance_profile is not None:
        kwargs["IamInstanceProfile"] = iam_instance_profile
    if ebs_optimized is not None:
        kwargs["EbsOptimized"] = ebs_optimized
    try:
        resp = await client.call("RunInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to run instances") from exc
    return RunInstancesResult(
        reservation_id=resp.get("ReservationId"),
        owner_id=resp.get("OwnerId"),
        requester_id=resp.get("RequesterId"),
        groups=resp.get("Groups"),
        instances=resp.get("Instances"),
    )


async def run_scheduled_instances(
    launch_specification: dict[str, Any],
    scheduled_instance_id: str,
    *,
    client_token: str | None = None,
    instance_count: int | None = None,
    region_name: str | None = None,
) -> RunScheduledInstancesResult:
    """Run scheduled instances.

    Args:
        launch_specification: Launch specification.
        scheduled_instance_id: Scheduled instance id.
        client_token: Client token.
        instance_count: Instance count.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LaunchSpecification"] = launch_specification
    kwargs["ScheduledInstanceId"] = scheduled_instance_id
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    if instance_count is not None:
        kwargs["InstanceCount"] = instance_count
    try:
        resp = await client.call("RunScheduledInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to run scheduled instances") from exc
    return RunScheduledInstancesResult(
        instance_id_set=resp.get("InstanceIdSet"),
    )


async def search_local_gateway_routes(
    local_gateway_route_table_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> SearchLocalGatewayRoutesResult:
    """Search local gateway routes.

    Args:
        local_gateway_route_table_id: Local gateway route table id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["LocalGatewayRouteTableId"] = local_gateway_route_table_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("SearchLocalGatewayRoutes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to search local gateway routes") from exc
    return SearchLocalGatewayRoutesResult(
        routes=resp.get("Routes"),
        next_token=resp.get("NextToken"),
    )


async def search_transit_gateway_multicast_groups(
    transit_gateway_multicast_domain_id: str,
    *,
    filters: list[dict[str, Any]] | None = None,
    max_results: int | None = None,
    next_token: str | None = None,
    region_name: str | None = None,
) -> SearchTransitGatewayMulticastGroupsResult:
    """Search transit gateway multicast groups.

    Args:
        transit_gateway_multicast_domain_id: Transit gateway multicast domain id.
        filters: Filters.
        max_results: Max results.
        next_token: Next token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayMulticastDomainId"] = transit_gateway_multicast_domain_id
    if filters is not None:
        kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    if next_token is not None:
        kwargs["NextToken"] = next_token
    try:
        resp = await client.call("SearchTransitGatewayMulticastGroups", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to search transit gateway multicast groups") from exc
    return SearchTransitGatewayMulticastGroupsResult(
        multicast_groups=resp.get("MulticastGroups"),
        next_token=resp.get("NextToken"),
    )


async def search_transit_gateway_routes(
    transit_gateway_route_table_id: str,
    filters: list[dict[str, Any]],
    *,
    max_results: int | None = None,
    region_name: str | None = None,
) -> SearchTransitGatewayRoutesResult:
    """Search transit gateway routes.

    Args:
        transit_gateway_route_table_id: Transit gateway route table id.
        filters: Filters.
        max_results: Max results.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["TransitGatewayRouteTableId"] = transit_gateway_route_table_id
    kwargs["Filters"] = filters
    if max_results is not None:
        kwargs["MaxResults"] = max_results
    try:
        resp = await client.call("SearchTransitGatewayRoutes", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to search transit gateway routes") from exc
    return SearchTransitGatewayRoutesResult(
        routes=resp.get("Routes"),
        additional_routes_available=resp.get("AdditionalRoutesAvailable"),
    )


async def send_diagnostic_interrupt(
    instance_id: str,
    region_name: str | None = None,
) -> None:
    """Send diagnostic interrupt.

    Args:
        instance_id: Instance id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceId"] = instance_id
    try:
        await client.call("SendDiagnosticInterrupt", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to send diagnostic interrupt") from exc
    return None


async def start_declarative_policies_report(
    s3_bucket: str,
    target_id: str,
    *,
    s3_prefix: str | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> StartDeclarativePoliciesReportResult:
    """Start declarative policies report.

    Args:
        s3_bucket: S3 bucket.
        target_id: Target id.
        s3_prefix: S3 prefix.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["S3Bucket"] = s3_bucket
    kwargs["TargetId"] = target_id
    if s3_prefix is not None:
        kwargs["S3Prefix"] = s3_prefix
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("StartDeclarativePoliciesReport", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to start declarative policies report") from exc
    return StartDeclarativePoliciesReportResult(
        report_id=resp.get("ReportId"),
    )


async def start_network_insights_access_scope_analysis(
    network_insights_access_scope_id: str,
    client_token: str,
    *,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> StartNetworkInsightsAccessScopeAnalysisResult:
    """Start network insights access scope analysis.

    Args:
        network_insights_access_scope_id: Network insights access scope id.
        client_token: Client token.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInsightsAccessScopeId"] = network_insights_access_scope_id
    kwargs["ClientToken"] = client_token
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("StartNetworkInsightsAccessScopeAnalysis", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to start network insights access scope analysis") from exc
    return StartNetworkInsightsAccessScopeAnalysisResult(
        network_insights_access_scope_analysis=resp.get("NetworkInsightsAccessScopeAnalysis"),
    )


async def start_network_insights_analysis(
    network_insights_path_id: str,
    client_token: str,
    *,
    additional_accounts: list[str] | None = None,
    filter_in_arns: list[str] | None = None,
    filter_out_arns: list[str] | None = None,
    tag_specifications: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> StartNetworkInsightsAnalysisResult:
    """Start network insights analysis.

    Args:
        network_insights_path_id: Network insights path id.
        client_token: Client token.
        additional_accounts: Additional accounts.
        filter_in_arns: Filter in arns.
        filter_out_arns: Filter out arns.
        tag_specifications: Tag specifications.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInsightsPathId"] = network_insights_path_id
    kwargs["ClientToken"] = client_token
    if additional_accounts is not None:
        kwargs["AdditionalAccounts"] = additional_accounts
    if filter_in_arns is not None:
        kwargs["FilterInArns"] = filter_in_arns
    if filter_out_arns is not None:
        kwargs["FilterOutArns"] = filter_out_arns
    if tag_specifications is not None:
        kwargs["TagSpecifications"] = tag_specifications
    try:
        resp = await client.call("StartNetworkInsightsAnalysis", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to start network insights analysis") from exc
    return StartNetworkInsightsAnalysisResult(
        network_insights_analysis=resp.get("NetworkInsightsAnalysis"),
    )


async def start_vpc_endpoint_service_private_dns_verification(
    service_id: str,
    region_name: str | None = None,
) -> StartVpcEndpointServicePrivateDnsVerificationResult:
    """Start vpc endpoint service private dns verification.

    Args:
        service_id: Service id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ServiceId"] = service_id
    try:
        resp = await client.call("StartVpcEndpointServicePrivateDnsVerification", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to start vpc endpoint service private dns verification"
        ) from exc
    return StartVpcEndpointServicePrivateDnsVerificationResult(
        return_value=resp.get("ReturnValue"),
    )


async def terminate_client_vpn_connections(
    client_vpn_endpoint_id: str,
    *,
    connection_id: str | None = None,
    username: str | None = None,
    region_name: str | None = None,
) -> TerminateClientVpnConnectionsResult:
    """Terminate client vpn connections.

    Args:
        client_vpn_endpoint_id: Client vpn endpoint id.
        connection_id: Connection id.
        username: Username.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["ClientVpnEndpointId"] = client_vpn_endpoint_id
    if connection_id is not None:
        kwargs["ConnectionId"] = connection_id
    if username is not None:
        kwargs["Username"] = username
    try:
        resp = await client.call("TerminateClientVpnConnections", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to terminate client vpn connections") from exc
    return TerminateClientVpnConnectionsResult(
        client_vpn_endpoint_id=resp.get("ClientVpnEndpointId"),
        username=resp.get("Username"),
        connection_statuses=resp.get("ConnectionStatuses"),
    )


async def unassign_ipv6_addresses(
    network_interface_id: str,
    *,
    ipv6_prefixes: list[str] | None = None,
    ipv6_addresses: list[str] | None = None,
    region_name: str | None = None,
) -> UnassignIpv6AddressesResult:
    """Unassign ipv6 addresses.

    Args:
        network_interface_id: Network interface id.
        ipv6_prefixes: Ipv6 prefixes.
        ipv6_addresses: Ipv6 addresses.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    if ipv6_prefixes is not None:
        kwargs["Ipv6Prefixes"] = ipv6_prefixes
    if ipv6_addresses is not None:
        kwargs["Ipv6Addresses"] = ipv6_addresses
    try:
        resp = await client.call("UnassignIpv6Addresses", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to unassign ipv6 addresses") from exc
    return UnassignIpv6AddressesResult(
        network_interface_id=resp.get("NetworkInterfaceId"),
        unassigned_ipv6_addresses=resp.get("UnassignedIpv6Addresses"),
        unassigned_ipv6_prefixes=resp.get("UnassignedIpv6Prefixes"),
    )


async def unassign_private_ip_addresses(
    network_interface_id: str,
    *,
    ipv4_prefixes: list[str] | None = None,
    private_ip_addresses: list[str] | None = None,
    region_name: str | None = None,
) -> None:
    """Unassign private ip addresses.

    Args:
        network_interface_id: Network interface id.
        ipv4_prefixes: Ipv4 prefixes.
        private_ip_addresses: Private ip addresses.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NetworkInterfaceId"] = network_interface_id
    if ipv4_prefixes is not None:
        kwargs["Ipv4Prefixes"] = ipv4_prefixes
    if private_ip_addresses is not None:
        kwargs["PrivateIpAddresses"] = private_ip_addresses
    try:
        await client.call("UnassignPrivateIpAddresses", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to unassign private ip addresses") from exc
    return None


async def unassign_private_nat_gateway_address(
    nat_gateway_id: str,
    private_ip_addresses: list[str],
    *,
    max_drain_duration_seconds: int | None = None,
    region_name: str | None = None,
) -> UnassignPrivateNatGatewayAddressResult:
    """Unassign private nat gateway address.

    Args:
        nat_gateway_id: Nat gateway id.
        private_ip_addresses: Private ip addresses.
        max_drain_duration_seconds: Max drain duration seconds.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["NatGatewayId"] = nat_gateway_id
    kwargs["PrivateIpAddresses"] = private_ip_addresses
    if max_drain_duration_seconds is not None:
        kwargs["MaxDrainDurationSeconds"] = max_drain_duration_seconds
    try:
        resp = await client.call("UnassignPrivateNatGatewayAddress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to unassign private nat gateway address") from exc
    return UnassignPrivateNatGatewayAddressResult(
        nat_gateway_id=resp.get("NatGatewayId"),
        nat_gateway_addresses=resp.get("NatGatewayAddresses"),
    )


async def unlock_snapshot(
    snapshot_id: str,
    region_name: str | None = None,
) -> UnlockSnapshotResult:
    """Unlock snapshot.

    Args:
        snapshot_id: Snapshot id.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["SnapshotId"] = snapshot_id
    try:
        resp = await client.call("UnlockSnapshot", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to unlock snapshot") from exc
    return UnlockSnapshotResult(
        snapshot_id=resp.get("SnapshotId"),
    )


async def unmonitor_instances(
    instance_ids: list[str],
    region_name: str | None = None,
) -> UnmonitorInstancesResult:
    """Unmonitor instances.

    Args:
        instance_ids: Instance ids.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["InstanceIds"] = instance_ids
    try:
        resp = await client.call("UnmonitorInstances", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to unmonitor instances") from exc
    return UnmonitorInstancesResult(
        instance_monitorings=resp.get("InstanceMonitorings"),
    )


async def update_capacity_manager_organizations_access(
    organizations_access: bool,
    *,
    client_token: str | None = None,
    region_name: str | None = None,
) -> UpdateCapacityManagerOrganizationsAccessResult:
    """Update capacity manager organizations access.

    Args:
        organizations_access: Organizations access.
        client_token: Client token.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["OrganizationsAccess"] = organizations_access
    if client_token is not None:
        kwargs["ClientToken"] = client_token
    try:
        resp = await client.call("UpdateCapacityManagerOrganizationsAccess", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to update capacity manager organizations access") from exc
    return UpdateCapacityManagerOrganizationsAccessResult(
        capacity_manager_status=resp.get("CapacityManagerStatus"),
        organizations_access=resp.get("OrganizationsAccess"),
    )


async def update_security_group_rule_descriptions_egress(
    *,
    group_id: str | None = None,
    group_name: str | None = None,
    ip_permissions: list[dict[str, Any]] | None = None,
    security_group_rule_descriptions: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> UpdateSecurityGroupRuleDescriptionsEgressResult:
    """Update security group rule descriptions egress.

    Args:
        group_id: Group id.
        group_name: Group name.
        ip_permissions: Ip permissions.
        security_group_rule_descriptions: Security group rule descriptions.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if group_id is not None:
        kwargs["GroupId"] = group_id
    if group_name is not None:
        kwargs["GroupName"] = group_name
    if ip_permissions is not None:
        kwargs["IpPermissions"] = ip_permissions
    if security_group_rule_descriptions is not None:
        kwargs["SecurityGroupRuleDescriptions"] = security_group_rule_descriptions
    try:
        resp = await client.call("UpdateSecurityGroupRuleDescriptionsEgress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to update security group rule descriptions egress"
        ) from exc
    return UpdateSecurityGroupRuleDescriptionsEgressResult(
        return_value=resp.get("Return"),
    )


async def update_security_group_rule_descriptions_ingress(
    *,
    group_id: str | None = None,
    group_name: str | None = None,
    ip_permissions: list[dict[str, Any]] | None = None,
    security_group_rule_descriptions: list[dict[str, Any]] | None = None,
    region_name: str | None = None,
) -> UpdateSecurityGroupRuleDescriptionsIngressResult:
    """Update security group rule descriptions ingress.

    Args:
        group_id: Group id.
        group_name: Group name.
        ip_permissions: Ip permissions.
        security_group_rule_descriptions: Security group rule descriptions.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    if group_id is not None:
        kwargs["GroupId"] = group_id
    if group_name is not None:
        kwargs["GroupName"] = group_name
    if ip_permissions is not None:
        kwargs["IpPermissions"] = ip_permissions
    if security_group_rule_descriptions is not None:
        kwargs["SecurityGroupRuleDescriptions"] = security_group_rule_descriptions
    try:
        resp = await client.call("UpdateSecurityGroupRuleDescriptionsIngress", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(
            exc, "Failed to update security group rule descriptions ingress"
        ) from exc
    return UpdateSecurityGroupRuleDescriptionsIngressResult(
        return_value=resp.get("Return"),
    )


async def withdraw_byoip_cidr(
    cidr: str,
    region_name: str | None = None,
) -> WithdrawByoipCidrResult:
    """Withdraw byoip cidr.

    Args:
        cidr: Cidr.
        region_name: AWS region override.

    Raises:
        RuntimeError: If the API call fails.
    """
    client = async_client("ec2", region_name)
    kwargs: dict[str, Any] = {}
    kwargs["Cidr"] = cidr
    try:
        resp = await client.call("WithdrawByoipCidr", **kwargs)
    except Exception as exc:
        raise wrap_aws_error(exc, "Failed to withdraw byoip cidr") from exc
    return WithdrawByoipCidrResult(
        byoip_cidr=resp.get("ByoipCidr"),
    )
